# API Specification — LTC E-Learning (REST `/api/v1`)

|          |                                                    |
| -------- | -------------------------------------------------- |
| เวอร์ชัน | 1.4.5 — Wave G P3 gate r5 fix wave (feat/wave-g-p3 · [#93] · R5-M1): **แถว 224 edit GET/POST /admin/assessments (สัญญาโมดัลกติกา)** — registry เป็น observable: โมดัลที่เปิดอยู่สมัครรับการเปลี่ยน (subscription) จึงรับรู้ผล POST ที่มาถึงหลังปิด/เปิดโมดัลใหม่ — ผล "ยังไม่แน่นอน" → โมดัลเริ่ม read-back กับเซิร์ฟเวิร์งเอง (ปิดช่องผู้ใช้เห็นข้อความให้ "ตรวจสอบอีกครั้ง" แต่ไร้ปุ่มและไม่มีใครเริ่มตรวจ) · คำขอที่ถูกปฏิเสธแน่นอน (definitive 4xx) → ปลดล็อกทันทีที่ re-render + **refresh หน้าทุกผล** (เดิม 400 ไม่ refresh ไม่อัปเดต state ใด หน้าค้างล็อก "ยังไม่เสร็จ" จนเกิด render จากเหตุอื่น) — ปิด R5-M1 ของ gate r5<br>1.4.4 — Wave G P3 gate r4 fix wave (feat/wave-g-p3 · [#93] · R4-M1/M2 + R4-m1): **แถว 224 edit GET /admin/assessments (สัญญาโมดัลกติกา)** — read-back ที่อ่านไม่ได้ (unreadable = envelope ผิดรูป/ฟิลด์ผิดชนิด) = **fail-closed คงล็อก คงรายการ registry ห้าม resolve** (R4-M1 — เดิม resolve ทั้ง unreadable แล้วแจ้ง "ครั้งก่อนไม่สำเร็จ" ทั้งที่ยังไม่รู้ความจริง) · คำขอ POST กติกาที่กำลังส่งถูกติดตามนอก lifecycle ของโมดัล (in-flight ใน registry เดียวกัน) — ปิดโมดัลกลางคันแล้วเปิดใหม่ ปุ่มบันทึกของชุดนั้นยังล็อกจนกว่าคำขอจะเสร็จ (R4-M2 — ปิดช่อง "ปิดกลาง POST → เปิดใหม่ → ส่งซ้ำได้") · ผล POST ที่มาช้า (deferred) จัดการ registry ทุกกรณีแม้ epoch ไม่ตรง (uncertain → ลงทะเบียนเสมอ · committed/definitely-not → resolve) — เขียน state ของฟอร์มเท่านั้นที่ติด epoch · read-back ระหว่าง in-flight ถูกห้าม (แข่งกับ POST ที่ยังไม่ตอบ)<br>1.4.3 — Wave G P3 gate r3 fix wave (feat/wave-g-p3 · [#93] · R3-M1/M2 + R3-m1): **แถว 224 edit GET /admin/assessments** — **migration 0052**: audience ของ RPC `admin_latest_assessment_rules` ขยายเป็น 2 สายแรกของ ar_read จริง = staff เต็ม (viewer/exam/registrar/super_admin) + instructor แบบ row-filter (เจ้าของหลักสูตรเท่านั้น) — instructor/registrar กลับมา 200 · แถว 225: instructor POST กลับมา 201 จริง (0051 กรอบ audience จน 503 หลัง INSERT สำเร็จ — IT กลุ่ม 7 พิสูจน์บนสแตกจริง) · **R3-m1**: ตรวจ drift ต่อแถว RPC ก่อนเชื่อ — assessment_id ต้องเป็น UUID + อยู่ในชุดที่ขอ + ไม่ซ้ำ + แถวที่โปรเจกต์ 14 คอลัมน์ต้องผ่าน AssessmentRuleRowSchema strict → ใด ๆ พัง = 503 `admin_assessments_rules_rpc_drift` ก่อน merge · **R3-M1**: query additive `id=<uuid>` ชี้แถวเดียว (ไม่มี/ไม่เข้าถึง = `data:[]`) สำหรับ read-back ของโมดัลกติกา (cache no-store) + โมดัลเก็บสถานะ "ผลยังไม่แน่นอน" ข้ามการปิดโมดัล (registry ระดับ module) ปลดล็อกเฉพาะเมื่อ read-back เสร็จจริง — committed → prefill จากกติกาจริงบนเซิร์ฟเวอร์ · not_committed → บันทึกใหม่ได้ · อ่านไม่ได้ = คงล็อก fail-closed พร้อมปุ่ม "ตรวจสอบอีกครั้ง" (กันสร้าง version ซ้ำ)<br>1.4.2 — Wave G P3 gate r2 fix wave (feat/wave-g-p3 · [#93] · R2-M1/M2/M3 + R2-m1/m2): **แถว 224 edit GET /admin/assessments** — สรุปกติกาล่าสุดอ่านจาก **RPC `admin_latest_assessment_rules` (migration 0051)** แทน embed ตาราง: 0050 เปิด grant `select (selection)` แล้วพบว่า ar_read ให้ผู้เรียนที่ลงทะเรียน active เห็นแถว assessment_rules ด้วย = อ่านขอบเขตคลังทางตรง PostgREST ได้ (เจ้าหน้าที่/ผู้เรียนเป็น role `authenticated` เดียวกัน) → 0051 **revoke คืน** + RPC SECURITY DEFINER คุมบทบาทเอง (aal2 + staff:exam/staff:viewer/super_admin · version สูงสุดต่อ assessment · ผู้เรียนยิงตรง `select=selection` = 42501) · RPC ล้ม/คืนผิดรูป = 503 ERR-SYS-002 fail-closed · โมดัลกติกา: สถานะเป็น state machine เดียว — 201 แต่อ่าน version ไม่ได้ / network ตาย / 5xx หลัง commit = **ผลยังไม่แน่นอน** ล็อกปุ่มกันสร้าง version ซ้ำ + refresh อ่านกลับ · ปิดโมดัลกลาง POST = รีเซ็ตทุกสถานะ (ไม่ค้าง submitting) · IT ทั้งสองไฟล์: ไม่มี TEST_DATABASE_URL ใต้ battery (TEST_REQUIRE_APP=1) = ล้มที่ระดับโมดูล + เคส loader แยก per-row (it.for) ให้หลักฐาน mutation เห็นทุกแถว<br>1.4.1 — Wave G P3 gate r1 fix wave (feat/wave-g-p3 · [#93] · M1-M4/m1-m4): **แถว 224 edit GET /admin/assessments** — embed กติกาล่าสุดใช้เกณฑ์ **version สูงสุด** (แทน effective_from ล่าสุด — ตรงฐาน max+1 ของ RPC · แถว backdated/effective_from ซ้ำไม่หมุนแถวที่ตอบ) + embed ตอนนี้ครบ `requireCourseComplete`/`selection` โดย **migration 0050** grant `select (selection)` ให้ authenticated (pattern 0019 · ไม่มี PII · แถวยังถูก ar_read คุม) — โมดัลกติกา prefill จากกติกาจริงและส่งต่อ selection เดิม (ไม่ส่ง = RPC เขียน '{}' ทับขอบเขตคลัง)<br>1.4.0 — Wave G P3 contract draft rows (feat/wave-g-p3 · [#93] · แผน r5 gate APPROVED — ลงก่อน spawn ตาม D78): **migration 0049** — `exam_review_mode` enum 2 ค่า (`after_final_attempt` default · `never`) บน assessment_rules + แถว 173 edit GET result — เฉลยเปิดตาม **current-rules gating** (`submitted ∧ mode≠never ∧ ไม่มี attempt in_progress ของ (user, assessment) ∧ (ครบครั้งปัจจุบัน หรือ ผ่าน)`) ใน view ทั้งสอง (cr null = ปิดแม้เคยผ่าน fail-closed · D84 invariant คู่กับฝั่งสร้าง) · **start_attempt B3.5** บล็อกผู้ผ่านสอบซ้ำ → รหัสใหม่ **ERR-ASM-007 (422)** (หลัง ASM-001 ก่อน cooldown — ผ่าน+เหลือครั้งแม้ใน cooldown ได้ ASM-007) · §3.8 เพิ่ม 1 เส้น — **POST /admin/assessments/{id}/rules** (เพิ่มกติกา version ใหม่ — RPC `admin_add_assessment_rules` 0049 · version = max+1 server-side · unique_violation retry ×3 → ERR-SYS-002 · audit `ASSESSMENT_CONFIG_CHANGE` ยิงโดย DB trigger ทุก INSERT ของ assessment_rules — ปิด 3 ทาง รวม POST v1 เดิมและ insert ตรงผ่าน ar_write)<br>1.3.1 — Wave G P2 as-built confirm หลัง 3 รอบ gate [#92] (merge `c88dc7a` · gate r1 FAIL 0B/4M/3m → fix wave `44f1e8e` → r2 FAIL 0B/1M/3m → fix wave `c083059` → **r3 PASS 0B/0M/0m**): แถว 220 edit GET — **options เปล่าได้** (ข้อร่างที่ยังไม่มีตัวเลือก — schema edit GET ไม่มี min · PATCH `options:[]` = ไม่แตะตัวเลือกเดิม ข้อร่างแก้โจทย์ต่อได้) · แถว 221 PATCH — **เปลี่ยน type เป็นเลือกตอบเดียว/ถูกผิดของข้อไร้ตัวเลือก = ปฏิเสธ** (UI zod-mirror ฟ้องที่ฟอร์มก่อนส่ง + trigger `trg_questions_type_correctness` 0005 rollback เป็น backstop · ไป multiple_choice ผ่าน — trigger ไม่ตรวจ path นั้น) · **migration 0048** `start_attempt` เพิ่ม pool re-check **หลัง selection** (หน้าต่าง A ตาม D77: retire commit แทรกระหว่างนับ pool กับเลือก = ยก `ERR-ASM-003` "คลังข้อไม่พอ" ไม่สร้าง attempt สั้น · หน้าต่าง B — retire หลังเลือกก่อน snapshot = ยอมรับข้อที่เลือกไว้ตาม snapshot design) — contract ภายนอกเดิมทุกประการ<br>1.3.0 — Wave G P2 contract draft rows (feat/wave-g-p2 · [#92] · plan r4 ผ่าน gate r4 APPROVED — ลงก่อน spawn ตาม D78): §3.8 question-banks เพิ่ม 4 เส้น — **GET /admin/question-banks/{id}** (envelope `{data}` · UUID ผิด = 400 `ERR-VAL-001` · bank ไม่มี/เข้าไม่ถึง = 404 `ERR-NF-001` ต่างจากคลังว่าง 200 `data:[]`) · **GET .../{id}/questions** (cursor `(created_at,id)` DESC · limit default 20/max 100 · limit+1 probe หา hasMore · `{data,page}` · แถวไม่มีเฉลย — QuestionResource options ไม่มี isCorrect) · **GET .../{id}/questions/{qid}** (edit — EditQuestionResource มี isCorrect เฉพาะ `question_bank:update` · user-scoped client กรองทั้ง bankId+qid → ผิดบริบท = 404 ไม่เปิดเผยการมีอยู่ · **`Cache-Control: private, no-store` ทุก response รวมทาง error** ตาม D74) · **PATCH .../{qid}/status** (RPC `admin_set_question_status` 0047 · transition matrix `draft→active · active→retired · retired→active` · version+1 + audit `QB_QUESTION_STATUS` ใน TX เดียว · p_request_id จาก header `x-request-id` ส่ง `text\|null` · staff:exam/super_admin เท่านั้น · revoke UPDATE ตรงบน questions จาก authenticated) · **แก้แถว PATCH .../{qid} เดิม**: ตัดวลี "ข้อที่ใช้แล้วอ่านอย่างเดียว" ที่ขัด as-built — ดีไซน์จริงคือ **snapshot design** (attempt ที่เริ่มแล้วติด snapshot ของ version ที่เลือก) · เติม super_admin ในแถว PATCH ตาม rbac.ts `question_bank:update`<br>1.2.14 — codex gate g-p1-r4 MINOR closure (feat/wave-g-p1 · [#92]): แถว 115 สำนวนตรงพฤติกรรมจริง — “นับ quota ก่อน network แรกของ request” (ไม่ใช่ “network แรก = การนับ” · token หมดจริง SDK refresh เองหลังนับ พลาด = 401) · accessTokenFromAuthCookie ถือค่า cookie ว่าง = ไม่มี ตาม truthiness ของ SDK (base ว่างตกไปอ่าน chunk · chunk ว่างคั่นกลาง = หยุด) +2 unit fixtures<br>1.2.13 — codex gate g-p1-r3 fix wave (feat/wave-g-p1 · [#92]): middleware ข้าม session refresh ของ POST /api/v1/me/password (network แรก = การนับ quota — regression integration จับจริง 429 ไม่มี Set-Cookie auth) · accessTokenFromAuthCookie ปรับ prefix base64- ที่หัว chunk .0 เท่านั้น + อ่าน chunk ต่อเนื่องจนพบช่องว่าง (ตรง cookies.ts/chunker.ts ของ SDK)<br>1.2.12 — codex gate g-p1-r2 fix wave (feat/wave-g-p1 · [#92]): §3.1 แถว 113 audit DONE มี actor จริง (ส่ง context.user_id ให้ RPC ยกเป็น actor แล้ว strip) + dcr16(g) assert actor จริงจาก DB · แถว 115 คีย์รองอ่าน cookie header เอง ไม่ผ่าน SDK getSession<br>1.2.11 — codex gate g-p1-r1 fix wave (feat/wave-g-p1 · [#92]): §3.1 แถว 113 confirm — **หลักฐาน recovery บังคับด้วย amr ของ access token** (B1: method `otp` ตาม probe จริง GoTrue v2.164 · session login ปกติ = 400 `ERR-AUTH-005` ไม่ล้าง cookie) + คีย์รอง mirror IP ของ PWD_RESET (M1) + error ที่ไม่ยืนยันตายจาก getSession = 503 คง cookie (M2) + kind `audit_failed` ทำให้การล้าง cookie ถูก flush จริงแม้ audit ล้มหลัง mutation สำเร็จ (M3) + actor = claim `sub` (M4 · logout-all ใช้ตรงกัน) · แถว 115 me/password — นับ quota ก่อนแตะ GoTrue คีย์รอง sub จาก cookie (MINOR-2) + การ์ด `password-audit-failed` ข้อความตรงความจริง (MINOR-1) · แถว 118 logout-all — commit rotation ก่อนเข้าชั้น audit (MINOR-3) · 1.2.10 — codex gate r2 fix wave (feat/wave-f · migration 0046 · [#91]): แถว 107 login-stash — **ciphertext ผูกเจ้าของแถว+deadline ผ่าน AAD ของ AES-256-GCM** ปิดการ re-host สำเนาของ uuid ที่ถูกขโมย (G1: create รับ `p_expires_at` ที่ RPC ตรวจกรอบ > now และ ≤ now()+305s · take คืน jsonb `{payload, user_id, expires_at}` ให้แอปประกอบ AAD จากแถวจริง) + **consume fail-closed** (G3: RPC คืน boolean + ตรวจ deadline ณ consume — ไม่ยืนยัน consume จริง = ไม่ออก session) · แถว 120 regenerate — invalidate ถือ **advisory lock คีย์เดียวกับ replace** + replace ตรวจ factor-verified ใต้ lock (G4 — ปิด MFA คู่แข่ง regenerate จบด้วยโค้ดตายทั้งชุด) · แถว 129 callback — force-dynamic server shell + client component แยกไฟล์ (G2 — nonce ของ CSP ทำงานเฉพาะ dynamic rendering · `/verify` และ `/my/*` 5 หน้าใส่ force-dynamic ด้วย · `/_not-found` เหลือ static เป็นข้อจำกัดที่ยอมรับ) · 1.2.9 — codex gate r1 fix wave (feat/wave-f · migration 0045 · [#91]): แถว 107 login — pending cookie เก็บ **uuid ของ stash** เท่านั้น (F2/F5: token เข้ารหัส AES-256-GCM เก็บ `mfa_pending_stash` อายุ 300 วิ/single-use ที่ DB · peek/consume split) · แถว 120 regenerate — ชั้น BFF recent-MFA เดิม **+ guard aal2 ที่ตัว RPC 0045** (F1: ปิดช่อง JWT aal1 ยิง PostgREST ตรง · replace/invalidate ทั้งคู่ · consume ไม่ใส่ — เป็นเส้นทาง login) + consume มี **lockout ผิด 5 ครั้ง/ล็อก 15 นาที** คืน `locked` (F4) · **CSP nonce**: middleware ออก nonce ต่อ request — `script-src 'self' 'nonce-…' 'strict-dynamic'` ไม่มี `unsafe-inline/eval` ใน prod (F8 — ตัวออกจาก next.config.ts มาเป็น middleware เพราะ header ซ้ำ = browser ใช้ตัวแรกไม่เดาได้ · style-src ยัง `'unsafe-inline'` โดยเจตนา — inline styles ของ Next/Tailwind) · แถว 129 callback อีเมลเปลี่ยนเคลียร์ token ออกจาก URL fragment ทันทีที่อ่าน (F9) · 1.2.8 — **Wave F Phase 0 as-built** (feat/wave-f · migrations 0040-0044 · [#91]): §3.1 แถว 109-111/119-120 **ย้าย path MFA จาก `/auth/mfa/*` → `/api/v1/me/mfa/*` ตามของจริง** (enroll คืน `{factorId, secret, otpauthUri}` ไม่มีภาพ QR · verify TOTP-only + `factorId` บังคับตอน enrollment · GoTrue v2.164: ตรวจ factor ผ่าน user-level `listFactors()` ได้ปกติ — ทาง admin `POST /admin/users/{id}/factors` ต่างหากที่ตอบ 405 · verify ฆ่า session อื่น — ใช้ session ที่ verify คืน · **amr method = `totp`** ไม่ใช่ `mfa/totp` — recent-MFA ยอมรับสองค่า) + แถว 107 login pin สองขั้นเมื่อมี factor verified (pending cookie `ltc_mfa_pending` 300 วิ → `/login/verify` · โค้ดสำรอง = RPC consume + ephemeral recovery factor aal2) + แถว 106 register pin checkbox consent → `options.data.consents_granted` → trigger 0043 (D-f-3) + §3.2 แถว 129 อีเมลเปลี่ยนสองลิงก์ (D-f-2) + §3.2 แถว 139/§3.8 แถว 213 pin P0002 ทึบ (D-f-6) + §3.8 แถว 208 `isBanned`/`bannedUntil` (D-f-5) + §5 กลุ่ม MFA ตาม path ใหม่ — ทั้งชุดผ่าน dcr13/14/15 + dcr11/12 เคสเพิ่ม · 1.2.7 — codex gate p5-r3 fix wave (feat/wave-e-phase5 · [#90]): `SUPABASE_PUBLIC_URL` ต้องเป็น origin **http(s) ที่มี hostname จริง** (r3 MINOR — `new URL()` รับ scheme ใดก็ได้: `ftp:` ผ่านมาได้ทั้งที่ scheme ผิด และ `mailto:` ทำให้ host-rewrite เป็น no-op หลุดลิงก์ `kong:8000` ออกไปเงียบ ๆ — ตอนนี้ fail แถว `download_url_invalid` ก่อนส่งเหมือน media_asset_missing) · dcr12 เคส g assert ที่ **origin** (protocol+host พร้อมกัน) แล้ว fetch **ลิงก์ดิบจากเมล์** ไม่เขียนกลับก่อนเปิด (r3 NIT) · 1.2.6 — codex gate p5-r2 fix wave (feat/wave-e-phase5 · [#90] · migration 0039 · พิสูจน์จริง DCR-12 เคส g ครบสาย: worker จริง → เมล → **ผู้รับกดลิงก์จริง 200 + JSON ของเจ้าของ** โดยไม่มี apikey ผ่าน Kong route `storage-v1-signed` เปิดเฉพาะ GET `/storage/v1/object/sign/*` (cors-only · storage ตรวจ token เอง — invalid = 4xx จาก storage ไม่ใช่ 401 Kong · การสร้างลายเซ็น POST sign ยังอยู่หลัง key-auth · **host ของลิงก์ = มุมมองผู้รับ** — email worker เขียนทับ protocol+host ของ signed URL ด้วย env `SUPABASE_PUBLIC_URL` ก่อนใส่เมล์ (dev = `http://localhost:8000` แทน `kong:8000` ที่ container เห็นแต่ผู้รับเปิดไม่ได้ · ไม่ตั้ง = ใช้ URL จาก `SUPABASE_URL` ตรง ๆ → prod โดเมนเดียวไม่ต้องตั้ง · ค่าผิดรูปแบบ = fail แถว `download_url_invalid` ไม่ส่งลิงก์ตาย — r2 hostname ruling): **§3.2 แถว 139** worker RPC เปลี่ยนลายเซ็น (0039) — `claim` คืน `{jobId, userId, claimToken}` · `complete`/`fail` **ต้องแนบ `p_claim_token`** ของรอบถือครองปัจจุบัน (เก่า = 400 `(ERR-VAL-001\|stale_lease)` errcode 22023 ผ่าน gateway · worker เก่าหลัง lease ถูกยึดปิดงานไม่ได้ — M1) + lease reclaim สองทาง (เกิน 10 นาที **หรือ claimed_at IS NULL** — B4 backfill แถวค้างก่อนมี lease) + **สัญญา path สองชั้น (B1)**: คีย์อัปโหลด = `{user_id}/{job_id}.json` ในบักเก็ต (storage-js เติม bucketId เอง — ส่ง path เต็มได้ object ซ้อนสอง prefix ที่ email worker หาไม่เจอ) ขณะที่ `media_assets.storage_path` เก็บ path เต็ม `pdpa-exports/{user_id}/{job_id}.json` · 1.2.5 — codex gate p5-r1 (feat/wave-e-phase5 · [#90] · พิสูจน์จริงด้วย DCR-12 integration 7/7): **§3.8 แถว 209** audit `USER_CREATE` เปลี่ยน best-effort (1.2.3) → **durable RPC `admin_audit_user_created` (0038)** หลัง invite+grant — retry จำกัด ค้าง = 503 `ERR-SYS-002\|user_create_audit_failed` (บัญชีถูกสร้างแล้ว ซองบอกให้ตรวจซ้ำ — ไม่ใช่ WARN เงียบ) · **§3.8 แถว 210** PATCH = GoTrue ban/unban ก่อน แล้ว **RPC `admin_set_user_active` (0038)** = `profiles.is_active` + audit `USER_DISABLE`/`USER_UPDATE` **atomic TX เดียว** (retry 3 · guard 4xx ไม่ retry · ค้างหลัง ban = 503 `ERR-SYS-002\|user_active_update_failed` fail-closed) · **§3.2 แถว 139** อีเมล `data_export.ready` เทมเพลตรับ **ตัวระบุเท่านั้น** (`full_name`+`job_id`+`file_media_id`) — **signed URL 604800s ผู้กลาง email worker ลงนามจาก `media_assets` ตอนส่ง** (URL ไม่เก็บใน outbox จึงไม่มีลิงก์ตายค้างตาม retention) + claim ตั้ง lease `claimed_at` — worker ตายเกิน 10 นาที claim ถัดไปยึดงานคืน (ไม่ติด processing ตลอดกาล) · **§3.2 แถว 140** `my_request_account_deletion` คืน `userId` ของผู้ขอ (BFF ผูกเจ้าของอีเมลยืนยันด้วยตัวตนผู้ใช้ ไม่ใช่ id ของคำขอ) · **§3.2 แถว 141** หน้า `/profile/delete/confirm` เพิ่มการ์ดที่สาม `sod_role_changed` (ได้บทบาทพนักงาน/ผู้สอนระหว่างอายุ token — TX กลิ้งทั้งหมด คำขอยัง pending หลังยุติบทบาท token เดิมยืนยันได้) และแท็ก `token_not_found` เปลี่ยน errcode **P0002→22023 เฉพาะการขนส่ง** (P0002 โดน gateway ตัดร่างกาย error — ข้อความ/แท็กคงเดิม — หน้า UI เคยตกการ์ด system_error แทนการ์ดลิงก์ไม่ถูกต้อง · DCR-12 เคส b จับ) · `admin_grant_role`/`admin_revoke_role`/`confirm_account_deletion` ถือ **advisory lock `ltc:account:roles:{user_id}` ร่วมกัน** (ถอนสองบทบาทสุดท้ายขนานกัน = ผ่านรายการเดียว อีกรายการ `last_role` กลิ้งทั้ง TX — บัญชีไร้บทบาทเป็นไปไม่ได้ · DCR-12 เคส c) · 1.2.4 — pin ความหมาย `canResubmit` ของ §3.2 `GET /me/license` ตรง as-built (ยื่นใหม่ได้ = ยังไม่เคยยื่น หรือคำขอล่าสุด `rejected` — `pending`/`approved` = false · เดิมสรุปหลวม "ไม่มี pending" ทำ route คืน true หลังอนุมัติ จน UI เสนอฟอร์มยื่นซ้ำแก่ทนายที่ verify แล้ว — e2e-15 t4 จับ) · 1.2.3 — ปรับให้ตรง as-built ของ lane C admin BFF (Wave E Phase 5 · commit `dda9fc8`): **§3.8 แถว 209/210/212/213** — pin รูปร่าง body/ขาออกจริงของพื้นผิว users/roles: **POST /admin/users** = body strict `{email, displayName, role, reason}` · ชุด role เจ้าหน้าที่ = `instructor\|staff:viewer\|staff:content\|staff:exam\|staff:registrar` เท่านั้น (**ไม่มี super_admin** — zod ปฏิเสธ 400 `fields:["role"]` ก่อนแตะ DB) · กลไก = GoTrue `inviteUserByEmail` → RPC `admin_grant_role` atomic · อีเมลถูกใช้แล้ว = 400 `ERR-VAL-001\|email_already_registered` (GoTrue 422 แปลงที่ BFF) · 201 `{userId, email, displayName, role, granted, invitedAt}` · **PATCH /admin/users/{id}** = body strict `{is_active, reason?}` (reason บังคับเมื่อ disable — cross-field 400 ก่อนแตะ DB/GoTrue) + GoTrue ban/unban เป็นตัวบังคับจริง (`876000h`/`"none"` · ลำดับ ban ก่อน `profiles.is_active` — ล้มหลัง ban = บัญชีค้างถูกปิด = ทิศทาง fail-closed) + guard เป้าหมาย super_admin โดยผู้ไม่ใช่ super_admin (403) + v1 ครอบเฉพาะ enable/disable ผ่าน `user:disable` (super_admin — RBAC §2 · การแก้ข้อมูลสมาชิก first/last_name โดย registrar = Wave F) · 200 `{userId, isActive}` · **DELETE /admin/users/{id}/roles บังคับ body `{role, reason 10-500}`** (RPC กำหนด reason ของ audit — DELETE พร้อม body ตามสัญญา RPC) · ชี้ชัดชั้นที่แท็ก `role_not_grantable` เกิด (BFF zod = 400 ก่อน RPC · แท็กจริงเกิดเฉพาะชั้น RPC เมื่อเรียก PostgREST ตรง) · **audit `USER_CREATE`/`USER_DISABLE`/`USER_UPDATE` = best-effort + WARN tripwire** — `append_audit_event` allowlist (0008 §4/0019-r1/0025) ยังไม่รับ USER_* จาก BFF ทั้ง authenticated/service_role → mutation สำเร็จแต่ audit อาจหล่น ตรวจจับผ่าน log WARN `admin_users_event_audit_rpc_denied` (แบบแผน best-effort ที่วัดผลจริง; แก้เป็น RPC atomic ใน Wave F) · 1.2.2 — ปรับให้ตรง as-built ของ lane B license BFF: **ตัดรหัสตาย ERR-PRF-001/002 ออกจากทะเบียนข้อผิดพลาด** (ตายตั้งแต่แรกร่าง — ของจริงไม่เคย emit: รูปแบบพลาด = 400 `ERR-VAL-001` (zod ที่ BFF) · ยื่นซ้อน = 409 `ERR-VAL-001\|pending_exists` · เลขซ้ำคนอื่น (ตอนตัดสิน) = 409 `ERR-VAL-001\|license_no_conflict` — สองค่าหลังคือ envelope ที่ route ประกอบเองตามแถว 132/229 · DCR-11 assert ครบ) — แก้พร้อมกันที่ `src/lib/errors.ts` + DD + SDS · **§5**: `PUT /me/license` + `PATCH /me` เข้ากลุ่ม **LEARN_WRITE** (mutation ผู้เรียน — กติกา 1 กลุ่มเฉพาะชนะ READ ทั่วไปของ `/me*`) · 1.2.1 — ปรับให้ตรง as-built ของ migration ที่ผ่าน DCR-11 แล้ว (commit `3de40b7` · ไม่เปลี่ยนรูปร่าง response ใด): §3.7 roles — pin แท็กข้อผิดพลาดจริงของ RPC `admin_grant_role`/`admin_revoke_role` (grant `super_admin` → 400 `ERR-VAL-001\|role_not_grantable` · revoke `super_admin`/ค่านอก enum → 400 `ERR-VAL-001\|role_not_manageable` — คนละแท็กกับ grant โดยเจตนา) · **ถอน role สุดท้าย = 400** (แก้จาก 422 — errcode 22023 แปลง 400 ตามแบบแผน BFF · TX rollback แถวเดิมคงใช้งาน) · grant ซ้ำ = idempotent `granted:false` · `GET /admin/dashboard` ชุดสิทธิ์ตรง RBAC §2 `report:view` = staff:viewer/exam/registrar/super_admin (**ตัด staff:content ออก** — RPC ปฏิเสธ `ERR-RBAC-001\|report_view_forbidden` พิสูจน์แล้ว DCR-11 เคส a) · 1.2.0 — DCR-11 (Wave E Phase 5 · D-p5-14): **§3.2 เพิ่ม `GET /me/license`** (สถานะคำขอล่าสุด+ใบปัจจุบัน+canResubmit — IDENT-005) และกำกับ `PUT /me/license` ให้ครบสัญญา multipart (license_no + ไฟล์หลักฐาน jpg/png/pdf ≤10MB → บักเก็ต `license-evidence` ส่วนตัว · มี pending = 409 · แถวคำขอ+audit `LICENSE_BIND` atomic ใน RPC `my_submit_license_application` — 0035) · **PDPA §3.2**: `GET /profile/export` ชี้ job จริง (202 `{jobId,status}` · pending ซ้ำ = 409 · cooldown 24 ชม. · จัดส่งผ่านการแจ้งเตือนพร้อม signed URL อายุ 7 วัน) + **เพิ่ม `GET /profile/delete/confirm` (สาธารณะ ไม่ต้อง session — ยืนยัน token ลบบัญชี single-use 24 ชม.)** + `POST /profile/delete` กำกับ guard บทบาทพนักงาน/ผู้สอนห้ามลบเอง (403) · **§3.7/3.8 ฝั่งเจ้าหน้าที่**: `PATCH /admin/license-applications/{id}` = atomic RPC `admin_decide_license_application` (TX เดียว: ตัดสิน+ใบ verify+มอบ role lawyer+audit+event) · roles grant/revoke = RPC `admin_grant_role`/`admin_revoke_role` (super_admin ห้ามผ่าน endpoint · บัญชีต้องมี role ≥1 · registrar จำกัด lawyer) · `PATCH /admin/courses/{id}` ขยาย action **publish\|unpublish\|return** (return บังคับ comment ≥10) ผ่าน atomic RPC `admin_decide_course` · `GET /admin/users` = RPC `admin_list_users` + `PII_ACCESS` fail-closed · `PATCH /admin/users/{id}` บังคับจริงด้วย GoTrue ban/unban + reason บังคับ · `GET /admin/audit-logs` = RPC `admin_list_audit_logs` (keyset) · `GET /admin/dashboard` = RPC `admin_dashboard_stats` (aggregate สด) — ทั้งชุด aal2 ตรวจใน RPC ตามแบบแผน 0032 B3 · 1.1.3 — codex gate r2 (feat/wave-e-phase3) ชี้แจงสัญญาเดิม ไม่เปลี่ยนรูปร่าง response: `creditReversedTotal` ของ POST /admin/certificates/{id}/revoke เป็น**ผลรวมติดลบ** (แถว reversal = −amount → sum ≤ 0 — AUDIT §2.5 CREDIT_REVERSAL · BLOCKER-1) · 1.1.2 — codex gate r1 (feat/wave-e-phase3): **B2** audit ของกฎเครดิตเปลี่ยนเป็น **atomic RPC** — `CREDIT_RULE_CREATE/UPDATE` บันทึกใน TX เดียวกับ mutation ผ่าน RPC `admin_create_credit_rule`/`admin_update_credit_rule_status` (migration 0032 ฉบับ atomic — allowlist best-effort ฉบับแรกถูกปฏิเสธตาม AUDIT §1.5: event ที่คู่ mutation ห้าม best-effort · ADMIN_EXPORT เป็น read-side จึงใช้เป็นแบบแผนไม่ได้) · RPC ทั้งสอง + `admin_credit_adjust` ตรวจ **aal2 ซ้ำในตัว** (B3 — ปิดช่อง JWT aal1 เรียก PostgREST ตรง) · **B7** ledger GET `PII_ACCESS` เปลี่ยน best-effort → **fail-closed** (retry ครั้งเดียว ยังล้ม = 503 ไม่เปิดเผยข้อมูล) · **B1** response ของ POST /admin/certificates/{id}/revoke เพิ่มฟิลด์ additive `creditReversedRows`/`creditReversedTotal` (สรุป reversal จาก TX เดียวกัน) · **B6** GET /me/transcript `format=pdf` ทำจริง (pdf-lib ฝั่ง BFF — ตรงสัญญา 1.1.0) · 1.1.1 — DCR-9 r1 (lead · ตามแผน C-7): แก้พาธ §3.7 สองแถวให้ตรง implementation — `GET /admin/users/{id}/credits` → **`GET /admin/credits/{userId}`** และ `POST /admin/credit-adjustments` → **`POST /admin/credits/adjustments`** (แผน C-7 กำหนด `admin/credits/*` ไว้ตั้งแต่ต้น — doc 1.1.0 เขียนคลาด) · เพิ่มหมายเหตุ audit ของกฎเครดิต: `CREDIT_RULE_CREATE/UPDATE` เขียนทาง BFF service_role RPC (allowlist เปิดโดย migration 0032 — best-effort ตามแบบแผน ADMIN_EXPORT/0025 · การตัดสินของ lead บันทึกในหัวไฟล์ 0032 — **ถูกปฏิเสธใน 1.1.2**) · ledger GET เพิ่ม audit `PII_ACCESS` best-effort (**เปลี่ยนเป็น fail-closed ใน 1.1.2**) · 1.1.0 — DCR-9 (Wave E Phase 3 · D68): ทำจริงตาม §3.2/§3.7 — **GET /me/credits** (RPC my_credit_summary — รอบสร้างแบบ lazy ตาม default Q1 ผ่าน `credit_cycle_defaults()`) · **GET /me/transcript** เพิ่ม `?format=json\|csv\|pdf` (CSV UTF-8 BOM · PDF ฝั่ง BFF) · กลุ่มเจ้าหน้าที่ §3.7 ย้ายขึ้น `/admin/*` ให้ตรง convention §3.8: **GET/POST /admin/credit-rules · PATCH /admin/credit-rules/{id} · GET /admin/credits/{userId} · POST /admin/credits/adjustments** · กลไก: event `credit.accrual` เขียนใน grading TX ตั้งแต่ 0020 (snapshot กฎ — F15/F17/D11-17) · consumer `credit_accrual_tick` (pg_cron ทุก 1 นาที — AC ≤5 นาที) เป็นผู้ INSERT ledger + สร้างรอบ · citizen ไม่มีรอบ = ไม่มีรายการ ledger (event processed · no_cycle_target) · **เพิกถอนใบ → reversal อัตโนมัติ** ต่อรายการ accrual ที่ยังไม่ถูก reverse (idempotent ต่อ original_entry_id — CRT-006) · re-issue ไม่กระทบ credit · แจ้งเจ้าของเมื่อ adjust/reverse = Phase 4 (NTF) · 1.0.9 — escape pipe ของ ERR-SYS-002 ในตารางครบทุกจุด (GFM แยกเซลล์ตารางบน \| แม้ใน backticks — codex gate r5 MINOR-3) · 1.0.8 — walked-markers (0030 ตาม codex gate r4 MINOR-2): แถว GET bulk `lastError` ปรับข้อความเพดานให้ชี้แจง coalesce ตรงกันทั้งแถว (auto worker ไม่ผ่าน BFF — สัญญาอื่นไม่เปลี่ยน) · 1.0.7 — arrival-first tick (0029 ตาม codex gate r3): auto worker เป็น tick สองเฟส — ผู้ผ่านเงื่อนไขที่ส่งข้อสอบกลาง sweep ถูก fresh lane รับก่อน backlog ได้ใบใน tick ถัดไป (cron ทุก 2 นาที — AC ≤5 นาทีถูกบังคับจริงแม้คิวเดิมยาว) · GET bulk `lastError` ชี้แจง coalesce (ข้อความเพดาน ERR-SYS-002\|bulk_row_limit เฉพาะเมื่อยังไม่เคยมีข้อผิดพลาดจริง) · 1.0.6 — cursor durable (0028 ตาม codex gate r2): worker เดินคิวต่อจาก cursor ที่ commit ค้างข้าม CALL (bulk = คอลัมน์ของ job · auto = ตาราง cert_auto_cursor ต่อ scope) · job สะสมครบ 100,000 แถวถูกปิด `failed` ทันที (ERR-SYS-002\|bulk_row_limit) · 1.0.5 — DCR-8 worker redesign (0027 ตาม codex gate r1): POST /admin/certificates/bulk เป็น insert-only 202 (worker pg_cron ≤1 นาที รันต่อ commit ต่อใบ — ดู GET ตามผล) + สัญญาฟิลด์/สถานะ/ข้อผิดพลาดครบ · 1.0.4 — DCR-7 (Wave E): เพิ่ม GET /admin/certificates + assessmentId ใน course exam summary + bulk job spec · 1.0.3 — DCR-6 (D36): submit ข้อสอบตอบผลตรวจทันที (ตาม RPC จริง — idempotency ระดับ RPC ไม่ใช้แคช BFF) · /admin/certificates/bulk + /admin/exams/monitoring\|statistics เลื่อน Wave E · เฉลยเปิดตาม baseline `after_final_attempt` (ASM-012 config ต่อหลักสูตร = DCR อนาคต) · 1.0.2 — DCR-4/DCR-5 (D28): catalog fields (level/credits/learnerCount/outcomes/instructors/exam) + GET /lessons/{id}/quiz ไม่มีเฉลย · 1.0.1 DCR-3 (D25): duplicate enroll = 200 idempotent ตาม SRS LRN-001 · 1.0.0 ผ่าน CTO gate (codex รอบ 5: PASS — D17) · baseline สำหรับ Wave B |
| วันที่    | 2026-09-14                                         |
| อ้างอิง  | PROJECT-BRIEF.md §5 (โดเมน), §6 (stack), §8 (security) · RBAC-DESIGN.md · AUDIT-LOG-DESIGN.md · DATA-DICTIONARY.md (canonical schema) · SRS.md (Appendix A) |
| ขอบเขต  | Next.js Route Handlers ภายใต้ `/api/v1/*` (BFF) — Server Actions ที่ไม่ใช่ REST อยู่นอกเอกสารนี้ |

---

## 1. หลักการทั่วไป (binding)

1. **JSON เท่านั้น** — request/response เป็น `application/json; charset=utf-8` (ยกเว้นไฟล์ media ที่เสิร์ฟผ่าน Storage/CDN โดยตรง ไม่ผ่าน `/api/v1`)
2. **Authentication ผ่าน session (Supabase Auth)** — httpOnly cookie ที่ออกให้โดย BFF; frontend ไม่ถือ Supabase key ใด ๆ (บังคับตาม BRIEF §8)
   **Data access ของ BFF (D11-1 + D12-18)** — request ธรรมดาทุก endpoint: BFF เรียกฐานข้อมูลด้วย **user JWT (role `authenticated`)** เสมอ → **RLS บังคับจริงทุกแถว** (policies ที่ RBAC-DESIGN.md §3.1); `service_role` (bypass RLS) **ห้ามใช้เป็นเส้นทางหลัก** — ใช้เฉพาะ (a) background jobs เฉพาะกิจ (export, email/queue worker) และ (b) server functions ขอบเขตแคบสำหรับ write ที่ server เป็นผู้ควบคุมค่า (enroll, record_lesson_progress, record_quiz_attempt, start_attempt, save_answer, submit_attempt, ออก/เพิกถอนประกาศนียบัตร, ปรับ credit — รายชื่อ canonical ที่ DATA-DICTIONARY.md); งาน **purge retention ใช้ `purge_role` บทบาทเฉพาะ ไม่ใช่ service_role** (ตาม DD §4.6) — ทุกจุดที่ใช้สิทธิ์พิเศษต้องระบุเหตุผล จำกัดคอลัมน์/เงื่อนไขให้แคบที่สุด และ audit ทุกครั้ง
3. **Authorization ตรวจที่ BFF ทุก request** — middleware `requirePermission()` ตรวจ permission (ไม่ใช่ตรวจ "ชื่อบทบาท" ตรง ๆ) ก่อนเข้า handler — คู่ขนานกับ RLS ที่ DB (defense in depth)
4. **zod validate ทุก input และ output** — query string, path params, body ผ่าน zod; response ขาออก validate ด้วย zod schema ก่อนส่ง (ป้องกัน PII/ฟิลด์รั่ว)
5. **Error envelope มาตรฐาน** — ทุก error ใช้รูปแบบเดียว `{ "error": { "code", "message", "details?" } }` โดย `message` เป็นภาษาไทยเสมอ (i18n-ready ตาม BRIEF §9.3)
6. **Pagination แบบ cursor-based** — ไม่ใช้ offset บนชุดข้อมูลใหญ่ (audit log, notifications, รายงาน)
7. **Versioning ที่ path** — `/api/v1`; breaking change = เปิด `/api/v1` เดิมคู่กับ `/api/v2` พร้อม header `Deprecation` + `Sunset` (ระยะเปลี่ยน ≥ 90 วัน, config)
8. **Idempotency-Key บังคับสำหรับ submit ข้อสอบ** — header `Idempotency-Key: <uuid>` ที่ client สร้าง; BFF เก็บผลลัพธ์ 24 ชม. (config) ต่อ (user, endpoint) — ยิงซ้ำได้โดยไม่สร้าง attempt ใหม่ — **DCR-6: แคช BFF ไม่ต้องใช้จริง — idempotency บังคับที่ระดับ RPC `submit_attempt` (attempt ที่ส่งแล้วคืนผลเดิมพร้อม `already_submitted: true`)**
9. **Rate limit ต่อ endpoint group** — ค่าเดียวต่อ endpoint ใช้ทุก environment (ดู §5) อ้างผ่าน config key ห้าม hardcode (BRIEF §6)
10. **ห้าม log PII ใน request/response** — email, เลขบัตรประชาชน, เลขที่ใบอนุญาต ห้ามปรากฏใน log/error `details` (BRIEF §8) — อ้างด้วย `user_id` เสมอ

### 1.1 Conventions

- **Method ใช้:** `GET` (อ่าน), `POST` (สร้าง/การกระทำ), `PATCH` (แก้บางฟิลด์), `PUT` (แทนที่ทั้งชุด — ใช้เฉพาะ `/me/license`), `DELETE` ใช้น้อยมาก (soft-delete เป็นหลัก)
- **Status codes:** `200` สำเร็จ · `201` สร้างใหม่ · `204` สำเร็จไม่มี body (ใช้เฉพาะ mark-read) · `400` validation · `401` ไม่ได้ login/session หมด · `403` ไม่มีสิทธิ์ · `404` ไม่พบ/ไม่เปิดเผยการมีอยู่ · `409` conflict/idempotency mismatch · `422` business rule · `429` rate limit · `500/503` ระบบ
- **เวลา:** ISO 8601 UTC (`2026-09-08T10:00:00Z`)
- **ID:** **UUID v4** ทุกตาราง (ตัดสินแล้ว M-04) — เหตุผล: เป็นค่า default ของ `gen_random_uuid()` บน PostgreSQL 15/Supabase, ไม่เปิดเผยลำดับการสร้าง (ต่างจาก v7/serial), รองรับทุก client; งานที่ต้องการ sort ใช้ cursor `(created_at, id)` แทน
- **ภาษา error:** ไทยหลัก อังกฤษรอง (เตรียม next-intl-style key)

### 1.2 Pagination (cursor-based)

Request: `?limit=20&cursor=<opaque-base64>`
Response wrapper ทุก list endpoint:

```json
{ "data": [ ... ], "page": { "nextCursor": "b3BhcXVl...", "hasMore": true } }
```

- `limit` สูงสุดต่อ endpoint กำหนดใน zod (default 20, max 100)
- cursor เข้ารหัสจาก (sort_key, id) — ผู้ใช้แกะ/ปลอมไม่ได้ (signed)

### 1.3 Error envelope

```json
{
  "error": {
    "code": "ERR-ASM-004",
    "message": "หมดเวลาสอบแล้ว ระบบไม่รับคำตอบเพิ่ม",
    "details": { "attemptId": "uuid", "endedAt": "2026-09-08T10:00:00Z" }
  }
}
```

---

## 2. Error codes กลาง (ทุก endpoint ใช้ร่วม)

รหัสนำหน้าตามโดเมน `ERR-<DOMAIN>-<NNN>`; ข้อความไทยที่นี่คือ default ต้องเหมือนกันทุกจุดที่ใช้รหัสเดียวกัน (single source อยู่ที่ `lib/errors.ts` ในอนาคต)

| Code | HTTP | ข้อความ (ไทย) | เงื่อนไข |
| --- | --- | --- | --- |
| ERR-AUTH-001 | 401 | ต้องเข้าสู่ระบบก่อนใช้บริการนี้ | ไม่มี session / session หมดอายุ |
| ERR-AUTH-002 | 401 | อีเมลหรือรหัสผ่านไม่ถูกต้อง | login พลาด (ไม่บอกว่าฟิลด์ไหนผิด) |
| ERR-AUTH-003 | 423 | บัญชีถูกล็อกชั่วคราว กรุณาลองใหม่ภายหลัง | lockout หลังพลาด N ครั้ง (§5) |
| ERR-AUTH-004 | 403 | กรุณายืนยันตัวตนสองชั้น (MFA) ก่อนดำเนินการต่อ | บัญชีที่บังคับ MFA (instructor/staff:*/super_admin) ยังไม่ผ่าน MFA — session ที่ login ได้เป็น "enrollment-only" และถูกปฏิเสธทุก protected request (เช็ค MFA claim ทุก request ตาม D11-11) |
| ERR-AUTH-005 | 400 | ลิงก์รีเซ็ตรหัสผ่านไม่ถูกต้องหรือหมดอายุ | reset token invalid/expired |
| ERR-RBAC-001 | 403 | คุณไม่มีสิทธิ์ดำเนินการนี้ | ผ่าน auth แต่ไม่มี permission |
| ERR-VAL-001 | 400 | ข้อมูลที่ส่งมาไม่ถูกต้อง | zod ไม่ผ่าน, `details` มี field/path |
| ERR-VAL-002 | 400 | รูปแบบ Idempotency-Key ไม่ถูกต้อง | ไม่ใช่ UUID |
| ERR-NF-001 | 404 | ไม่พบข้อมูลที่ต้องการ | generic (ไม่เปิดเผยการมีอยู่) |
| ERR-RATE-001 | 429 | มีการเรียกใช้บ่อยเกินไป กรุณารอสักครู่ | พร้อม header `Retry-After` |
| ERR-IDM-001 | 409 | คำขอนี้ถูกประมวลผลแล้ว (Idempotency-Key ซ้ำแต่ body ต่าง) | key reuse + body mismatch |
| ERR-SYS-001 | 500 | เกิดข้อผิดพลาดภายในระบบ กรุณาลองใหม่ | ไม่ leak stack |
| ERR-SYS-002 | 503 | ระบบไม่พร้อมให้บริการชั่วคราว | maintenance/DB down |
| ERR-CRS-001 | 404 | ไม่พบหลักสูตร หรือหลักสูตรยังไม่เผยแพร่ | guest เห็นเฉพาะ published |
| ERR-ENR-001 | 409 | คุณลงทะเบียนหลักสูตรนี้แล้ว | duplicate enroll — **รหัสภายใน**: BFF จับได้แล้วคืน **200 + enrollment เดิม** (idempotent ตาม SRS LRN-001 — DCR-3/D25) |
| ERR-ENR-002 | 422 | หลักสูตรนี้จำกัดเฉพาะทนายความที่ยืนยันใบอนุญาตแล้ว | audience = lawyer-verified |
| ERR-LRN-001 | 403 | ต้องลงทะเบียนหลักสูตรก่อนเรียน | ไม่มี enrollment |
| ERR-LRN-002 | 422 | ยังเรียนบทก่อนหน้าไม่ครบตามเงื่อนไข | prerequisite |
| ERR-ASM-001 | 422 | คุณใช้จำนวนครั้งการสอบครบตามกติกาแล้ว | max attempts (config Q2) |
| ERR-ASM-002 | 409 | มีการสอบที่ยังไม่จบอยู่แล้ว | active attempt exists |
| ERR-ASM-003 | 404 | ไม่พบรอบการสอบ หรือรอบนี้ปิดแล้ว | window ปิด |
| ERR-ASM-004 | 422 | หมดเวลาสอบแล้ว ระบบไม่รับคำตอบเพิ่ม | timeout |
| ERR-ASM-005 | 422 | บันทึกคำตอบไม่ได้เพราะส่งข้อสอบแล้ว | attempt submitted |
| ERR-ASM-006 | 403 | คุณไม่ใช่เจ้าของรอบการสอบนี้ | attempt ของคนอื่น |
| ERR-ASM-007 | 422 | ผ่านการสอบนี้แล้ว จึงสอบซ้ำไม่ได้ — ดูผลสอบได้ที่หน้าผลสอบ | passed = no retake (start_attempt B3.5 · 0049 — คู่พลังกับเฉลยเปิดเมื่อผ่านใน view เดียวกัน) |
| ERR-CERT-001 | — | (ยกเลิกการใช้ — คงรหัสไว้ในทะเบียน) เดิม 404 "ไม่พบประกาศนียบัตรจากรหัสอ้างอิงนี้" สำหรับ public verify — **ถอนตาม D8/D11-14**: `GET /certificates/{code}` ตอบ **200 เสมอ** และกรณีไม่พบใช้ 200 + `status="not_found"` | ห้ามใช้รหัสนี้กับ public verify อีก — จะเปิดใช้ใหม่ต้องผ่าน DCR |
| ERR-CERT-002 | — | (ยกเลิกการใช้ — คงรหัสไว้ในทะเบียน) เดิม 410 "ถูกลบตาม retention" สำหรับ public verify — **ถอนตาม D8: ตอบ 200 เสมอ**; ใบที่ถูกเพิกถอน/แทนที่ = 200 + `status=revoked/superseded`, ไม่พบ = 200 + `status="not_found"` | ห้ามใช้รหัสนี้กับ public verify อีก — จะเปิดใช้ใหม่ต้องผ่าน DCR |
| ERR-CRD-001 | 422 | กฎเครดิตนี้มีผลใช้งานแล้ว แก้ไขต้องสร้างฉบับใหม่ | immutable active rule |
| ERR-CRD-002 | 422 | การปรับ credit ต้องระบุเหตุผล | reason required |
| ERR-ADM-001 | 403 | การกระทำนี้ต้องใช้สิทธิ์เจ้าหน้าที่ระดับสูงขึ้น | staff sub-role ไม่พอ |

---

## 3. Endpoints ตามโดเมน

สัญลักษณ์บทบาท: ระบุ explicit ตาม BRIEF §4 — `guest`(ไม่ auth), `citizen`, `lawyer`, `instructor`, `staff:viewer`, `staff:content`, `staff:exam`, `staff:registrar`, `super_admin`; "เจ้าของทรัพยากร" = owner. การ map บทบาท→permission อยู่ใน RBAC-DESIGN.md §2.

### 3.1 Auth (โดเมน Identity)

| Method | Path | คำอธิบาย | บทบาท | Success | Errors |
| --- | --- | --- | --- | --- | --- |
| POST | /auth/register | สมัครสมาชิก — body ต้องมี `acknowledgeNotice` (รับทราบประกาศความเป็นส่วนตัว — บังคับ) แยกจาก `consents` เสริมแบบ optional/versioned (D11-18 — zod schema §4) · **as-built Wave F (1.2.8 · D-f-3)**: checkbox `marketingConsent`/`emailNotifyConsent` บนฟอร์ม (ไม่ติ๊กเป็นค่าเริ่มต้น — PDPA ห้าม pre-tick) → helper `buildSignupConsents` ใส่เฉพาะที่ติ๊กเป็น signUp `options.data.consents_granted` `[{key,version}]` (เวอร์ชันจาก `SIGNUP_CONSENT_POLICY_VERSION` ใน config — แหล่งเดียว) → trigger 0043 แทรกแถว `consents` เมื่อยืนยันอีเมล (ช่องไม่ติ๊ก = ไม่เกิดแถว) | guest | 201 + ส่งอีเมลยืนยัน | VAL-001, RATE-001 |
| POST | /auth/login | เข้าสู่ระบบ (สร้าง session) · **as-built Wave F (1.2.8 · D-f-1) สองขั้นเมื่อมี factor TOTP ที่ verified**: ขั้น 1 รหัสผ่านผ่าน → ยังไม่สร้าง session เส้นหลัก — ตั้ง pending cookie `ltc_mfa_pending` (httpOnly · path=/login · 300 วิ · **0045 (gate r1 F2/F5): cookie เก็บ uuid v4 ของ stash เท่านั้น** — คู่ token จริงถูกเข้ารหัส AES-256-GCM ฝั่งแอป (env `LTC_MFA_PENDING_KEY`) เก็บในตาราง `mfa_pending_stash` อายุ 300 วิ/single-use บังคับที่ DB · peek/consume split: พิมพ์ผิดทราบลองใหม่ได้ (take ไม่หมกเหตุ) — uuid ตายจริงเมื่อ verify สำเร็จ (consume RPC) ก่อนออก session · **0046 (gate r2 G1): ciphertext ผูกเจ้าของแถว+deadline ผ่าน AAD ของ AES-256-GCM** (`${userId}:${deadlineUnixSec}` — create ส่ง `p_expires_at` ที่ RPC ตรวจกรอบ > now และ ≤ now()+305s เก็บ as-is · take คืน jsonb `{payload, user_id, expires_at}` ให้แอปประกอบ AAD จากแถวจริงก่อนถอดรหัส — สำเนา ciphertext ที่ถูก re-host ไปแถวของผู้อื่น GCM auth ไม่ผ่าน = ถอดไม่ได้) · **0046 (gate r2 G3) consume fail-closed**: RPC ตรวจ `expires_at > now()` ณ consume และคืน boolean — แอปออก session เฉพาะ consume ยืนยันจริงเต็มรูปแบบ (false/แถวตาย = `state=expired` · RPC ล้ม = `ERR-SYS-001` ไม่มี session ทุกกรณี)) แล้ว redirect `/login/verify` · ขั้น 2 รหัส 6 หลัก (`mfa.challenge`+`mfa.verify` ใน session ชั่วคราว → aal2 → session จริง + ล้าง pending) หรือโค้ดสำรอง (RPC `mfa_backup_codes_consume` → mint aal2 ผ่าน ephemeral recovery factor — GoTrue admin API ไม่คืน secret ของ factor เดิม) · ไม่มี factor = เข้าระบบขั้นเดียวตามเดิม (บทบาทบังคับ MFA ที่ยังไม่ enroll โดน `mfa_required` ที่ protected request — enroll ได้ที่ /my/security) | guest | 200 + `Set-Cookie` httpOnly | AUTH-002/003/004, RATE-001 |
| POST | /auth/logout | ออกจากระบบ (ทำลาย session) | ทุกบทบาทที่ login แล้ว | 204 | AUTH-001 |
| POST | /api/v1/me/mfa/enroll | เริ่มผูก MFA (TOTP) — **as-built Wave F (1.2.8 · D-f-1)**: แถวนี้+สองแถวถัดไป+สองแถวสุดท้ายของตารางเดิมเป็น `/auth/mfa/*` ตั้งแต่ 1.0.0 — path จริงคือ REST `/api/v1/me/mfa/*` (UI เดิมใช้ Server Actions ที่ /my/security) · `mfa.enroll` คืน `{factorId, secret, otpauthUri}` (หน้า setup แสดง secret+otpauth URI+ขั้นตอนกรอกมือ — ไม่มีภาพ QR เพื่อกัน pending cookie บวม) · สถานะ factor ตรวจผ่าน user-level `auth.mfa.listFactors()` ด้วย user JWT ทุกจุดใน src (ทาง admin `POST /admin/users/{id}/factors` ตอบ 405 ใน GoTrue v2.164 — probe จริง · pipe-audit F-6) | ทุกบทบาท | 200 | AUTH-001 |
| POST | /api/v1/me/mfa/verify | ยืนยันรหัส TOTP เพื่อเปิดใช้ MFA — body `{code, factorId?}` (**`factorId` บังคับเมื่อยืนยัน enrollment ครั้งแรก** — การ verify ทาง REST รับ TOTP เท่านั้น โค้ดสำรองใช้ตอน login ขั้นสองเท่านั้น) · สำเร็จ = factor verified → เสนอสร้างโค้ดสำรองทันที (8-12 โค้ด `xxxx-xxxx` CSPRNG → RPC `mfa_backup_codes_replace` → แสดงครั้งเดียว no-store) · หมายเหตุ GoTrue: verify สั่ง kill session อื่นของ user — โทเคนที่ใช้ต่อต้องเป็น session ที่ verify คืนให้ (as-built) | ทุกบทบาท | 200 | VAL-001 |
| POST | /api/v1/me/mfa/disable | ปิด MFA — **v1: เฉพาะ citizen/lawyer (optional-MFA)**; staff/instructor/super_admin **block ใน v1** (403 `ERR-RBAC-001` ข้อความไทย); ต้อง recent-MFA ≤15 นาที · **D12-10 (ห้ามเหลือ 0 factor) บังคับจริงที่ RPC `mfa_backup_codes_replace` (0042) — path ปิด MFA กลับ unenroll ทุก factor verified โดยเจตนา** (ไม่มี factor = 404 — pipe-audit F-6) · **amr ของ GoTrue v2.164 ประทับ method เป็น `totp` เท่านั้น ไม่ใช่ `mfa/totp`** — ตัวตรวจ recent-MFA ยอมรับสองค่า (as-built 1.2.8 ไม่งั้นทุก gate recent-MFA ตายหลัง aal2 สด ๆ) · ปิด = `mfa.unenroll` + RPC `mfa_backup_codes_invalidate` (โค้ดสำรองทั้งชุดตายพร้อม factor) | citizen, lawyer | 200 | AUTH-001, AUTH-004, RBAC-001, NF-001 |
| POST | /auth/password-reset/request | ขอลิงก์ตั้งรหัสผ่านใหม่ (anti-enumeration — ตอบเหมือนกันทุกกรณี) — **as-built Wave G P1 (D72)**: 200 ข้อความไทยคงที่ทุกกรณี (อีเมลไม่มีในระบบ = ข้อความเดียวกัน · GoTrue `over_email_send_rate_limit` ถูกกลบเป็น 200 เสมอ เพราะนับเฉพาะอีเมลที่มีบัญชี = ช่อง enumeration) · body `{email}` (trim+lowercase ≤254) · rate `PWD_RESET` 5/ชม. คีย์ IP+อีเมล normalized · audit `AUTH_PASSWORD_RESET_REQUEST` เขียน**ก่อน**เรียก GoTrue (audit ล้ม = 503 ไม่ส่งเมล) · context `ip_hash` เดียวตาม allowlist 0008 · redirect_to แนบ**ทั้ง query param และ body** (GoTrue v2.164.0 อ่านเฉพาะ query — ส่ง body เปล่า ๆ เมลจะตก SITE_URL) | guest | 200 เสมอ | VAL-001, RATE-001, SYS-002 |
| POST | /auth/password-reset/confirm | ตั้งรหัสผ่านใหม่จาก session recovery (ลิงก์อีเมล → fragment ไม่ผ่าน server → หน้า /reset-password ตั้ง cookie ผ่าน server action ที่ `setSession` ตรวจกับ GoTrue ก่อน) — **as-built Wave G P1 (D72)**: ลำดับคงที่ห้ามสลับ PUT /auth/v1/user → logout scope=global (เพิกถอนทุกเซสชันทันที) → ล้าง cookie → audit `AUTH_PASSWORD_RESET_DONE` (actor = claim `sub` ของ token ผ่าน `context.user_id` ที่ RPC ยกเป็น actor แล้ว strip — gate r2 · context ที่เก็บจริง = `ip_hash` เดียวตาม allowlist 0008) · body `{password}` ≥12 (GOTRUE_PASSWORD_MIN_LENGTH — ข้อความ policy เดียวกับหน้า register) · GoTrue ปฏิเสธ token (400/401/403 ที่ PUT) = `ERR-AUTH-005` + ล้าง cookie · 422 weak_password = `ERR-VAL-001` · 429/5xx/network = 503 คง cookie ให้กดใหม่ (แบบ logout route — รวม error ที่ไม่ยืนยันตายจาก getSession ด้วย: **1.2.11 gate g-p1-r1 M2** ไม่ล้าง credential ที่ยังไม่พิสตาย) · audit ล้มหลัง PUT+logout สำเร็จ = 503 โดย**การล้าง cookie ถูก flush จริง** (**1.2.11 M3** — kind แยก `audit_failed`) · rate `PWD_RESET` คีย์ IP + คีย์รอง mirror IP (**1.2.11 M1**) · **หลักฐาน recovery บังคับ (1.2.11 B1)**: amr ของ access token ต้องมี method `otp` (ลิงก์ recovery ของ GoTrue v2.164 — probe จริง · refresh คง method ไว้) · session login ปกติ (method `password`) = 400 `ERR-AUTH-005` **โดยไม่ล้าง cookie** (auth-js ไม่ re-validate token ที่ยังไม่หมดอายุ — ผ่าน getSession อย่างเดียวไม่พอ) · actor ของ audit = claim `sub` ของ token เดียวกัน (**1.2.11 M4** — user object ใน cookie ปลอมได้) | guest (ผู้ถือ recovery session จากลิงก์อีเมล) | 200 | VAL-001, AUTH-001, AUTH-005, RATE-001, SYS-002 |
| POST | /auth/verify | ยืนยันอีเมลจาก token (เปิดใช้บัญชี) | guest | 200 | AUTH-005 |
| POST | /me/password | เปลี่ยนรหัสผ่าน (login อยู่ + พิสูจน์รหัสเดิม) — **as-built Wave G P1 (D72)**: path จริง `/api/v1/me/password` (แถวนี้วางเป็น /auth/change-password ตอนวางแผน) · ลำดับ: rate `AUTH` คีย์ IP + คีย์รอง claim `sub` อ่านจาก cookie header ตรง ๆ (decode ในเครื่องล้วน ไม่ผ่าน `getSession` ของ SDK ที่เช็ค `expires_at` แล้วอาย refresh ผ่าน network · gate g-p1-r3: **middleware ข้าม session refresh ของ POST เส้นนี้** — นับ quota ก่อน network แรกของ request (regression จับจริง: 429 ไม่มี Set-Cookie auth · token หมดจริง SDK refresh เองหลังนับ พลาด = 401) · **นับก่อนแตะ GoTrue/SDK ใด ๆ** ตาม 1.2.11 MINOR-2 · ปลอม sub ได้แค่แยก bucket ตัวเอง ขณะ bucket IP นับทุกครั้ง) แล้วจึง requireUser (ไม่มี session = 401 `ERR-AUTH-001`) → zod strict → re-auth รหัสปัจจุบันผ่าน standalone `signInWithPassword` (persistSession:false ไม่เขียน cookie) ผิด = 401 ไทยเจาะจง → `PUT /auth/v1/user` ด้วย session ผู้ใช้ (เซสชันปัจจุบัน**คงไว้** — เซสชันอื่นถูก GoTrue ฆ่าเอง: วัดจริง access 403 · refresh โดนลบ) → audit `AUTH_PASSWORD_CHANGE` context `['method','session_id']` ก่อนตอบ 200 (audit ล้ม = 503 `ERR-SYS-002` — ไม่มี mutation ลอด audit · ณ จุดนั้นรหัสเปลี่ยนแล้วจริง ข้อความไทยบอกผู้ใช้เข้าสู่ระบบด้วยรหัสใหม่ต่อ — 1.2.11 MINOR-1 การ์ดเฉพาะ `password-audit-failed`) · UI = ฟอร์มที่ /my/security | ทุกบทบาทที่ login แล้ว | 200 | VAL-001, AUTH-001, AUTH-002, RATE-001, SYS-001, SYS-002 |
| POST | /auth/otp/request | ขอ OTP ไปอีเมล/เบอร์มือถือ | guest | 202 (ไม่เปิดเผยว่ามีบัญชี) | RATE-001 |
| POST | /auth/otp/verify | ยืนยัน OTP (ช่วย login หรือผูกเบอร์) | guest | 200 | AUTH-002, RATE-001 |
| POST | /auth/logout-all | ออกจากระบบทุกอุปกรณ์ (revoke ทุก session ด้วย `logout?scope=global`) — **as-built Wave G P1 (D72)**: fail-closed เหมือน logout เดิม (ยืนยัน GoTrue revoke จริงก่อนล้าง cookie ไม่งั้น 503 · 401/403 ครั้งแรก = หมุน token แล้ว revoke ซ้ำครั้งเดียว · rotation ที่เกิดแล้ว commit ก่อน 503 เสมอ — **รวมสาย audit ล้ม** หลัง revoke สำเร็จ (1.2.11 MINOR-3: commit rotation ก่อนเข้าชั้น audit)) + rate `AUTH` คีย์ IP + คีย์รอง mirror IP (1.2.11 M1) + audit `AUTH_SESSION_REVOKE` context `['session_id','reason']` actor = claim `sub` ของ token ที่ใช้ revoke (1.2.11 M4 — user object ใน cookie ปลอมได้) **ก่อน**ล้าง cookie (audit ล้ม = 503 คง cookie — กดซ้ำจบทาง idempotent 204) · ไม่มี session/ตายยืนยันแล้ว = 204 idempotent โดยไม่ audit · UI = ปุ่ม "ออกจากระบบทุกเครื่อง" ที่ /my/security | ทุกบทบาทที่ login แล้ว | 204 | RATE-001, SYS-002 |
| GET | /api/v1/me/mfa/backups | ดูสถานะ backup codes — **คืนเฉพาะ metadata (RPC `mfa_backup_codes_status`: `generated`/`unused`/`total`/`lastGeneratedAt`) ไม่คืนโค้ดเด็ดขาด** (D11-12 · 0042) | ทุกบทบาทที่ login แล้ว (as-built: ไม่บังคับ aal2 — คืน metadata ของตัวเองเท่านั้น) | 200 | AUTH-001 |
| POST | /api/v1/me/mfa/backups/regenerate | สร้าง backup codes ชุดใหม่ — **CSRF-protected** (origin-check ที่ middleware `isCsrfAllowed` — เทียบ Origin ต่อ origin เต็ม + fallback `Sec-Fetch-Site` · fail-closed · as-built ไม่มี double-submit token — pipe-audit F-6), **ต้อง recent-MFA (ยืนยัน TOTP ภายใน ≤ 15 นาที)** + **0045 (gate r1 F1): RPC `mfa_backup_codes_replace` บังคับ aal2 ที่ตัวฟังก์ชันเอง** (JWT aal1 ยิง PostgREST ตรง = `ERR-AUTH-004` แม้ไม่ผ่าน BFF · advisory lock ต่อผู้ใช้กัน replace คร่อมกัน — F6 · **0046 gate r2 G4: invalidate (ขั้นปิด MFA) ถือ advisory lock คีย์เดียวกับ replace** + replace ตรวจ factor-verified **ใต้ lock** — การปิด MFA ที่แข่งกับ regenerate จบด้วย "โค้ดตายทั้งชุดเสมอ" ไม่มีทางเหลือโค้ดใช้ได้บนบัญชีที่ปิด MFA ไปแล้ว), response `Cache-Control: no-store`, **โค้ดแสดงครั้งเดียว**จาก response นี้เท่านั้น และ**ชุดเก่า invalid ทันที** (RPC atomic replace 0042 — บังคับมี TOTP verified ก่อน) — audit `AUTH_MFA_BACKUPS_REGENERATED` เขียนใน RPC (context = จำนวนเท่านั้น — D11-12) | ทุกบทบาทที่ login แล้ว (บังคับ recent-MFA ที่ handler) | 200 | AUTH-001, AUTH-004, RBAC-001, RATE-001, VAL-001 (no_verified_factor) |

หมายเหตุ: login สำเร็จ/ล้มเหลว → audit `AUTH_LOGIN_OK/FAIL` เสมอ (AUDIT-LOG-DESIGN.md)

### 3.2 Profile & License (โดเมน Identity & License)

| Method | Path | คำอธิบาย | บทบาท | Success | Errors |
| --- | --- | --- | --- | --- | --- |
| GET | /me | ข้อมูลโปรไฟล์ตัวเอง + บทบาทของตัวเอง | citizen, lawyer, instructor, staff:*, super_admin | 200 | AUTH-001 |
| PATCH | /me | แก้โปรไฟล์ — **เขต `display_name`/`phone`/`preferred_locale` เท่านั้น** (guard 0010 · D20-M2): `first_name`/`last_name` เป็นข้อมูลนิติบุคคล แก้ผ่านเจ้าหน้าที่ (DCR-11/DD) — ฟิลด์ sensitive (อีเมล) แยกทาง — **as-built Wave F (1.2.8 · D-f-2)**: ฟอร์ม/Server Action `POST /my/security/email` (password re-auth ผ่าน throwaway client ก่อน · บัญชีบังคับ MFA ต้อง aal2 — ERR-AUTH-004 · อีเมลซ้ำถูกกลบเป็นความสำเร็จ anti-enumeration) → GoTrue `updateUser({email})` + RPC `my_audit_email_change_request` (0044 — audit `USER_EMAIL_CHANGE_REQUEST` context sha256 ล้วน · **0045 gate r1 F3: ลำดับสลับ — RPC audit ก่อน updateUser เสมอ** ให้ guard aal2/role ที่ DB ตัดสินก่อนเกิด mutation · อีเมลซ้ำถูก audit ด้วยก่อนตอบ masked) · **ยืนยันสองลิงก์** (secure email change ค่า default ของ GoTrue: ลิงก์ 1 → อีเมลใหม่ · ลิงก์ 2 → อีเมลเดิม · เฉพาะลิงก์ 2 เปลี่ยนอีเมลจริง · ลิงก์ single-use · อีเมลเดิมใช้ login ได้จนกว่าจะยืนยันครบ) · หน้าผล `/email-change/callback` = client component (GoTrue ตอบสถานะทาง **fragment เท่านั้น** — server มองไม่เห็น · **0045 gate r1 F9: อ่าน fragment แล้ว `history.replaceState` เคลียร์ URL ทันที** — token/สถานะไม่ค้างใน address bar/history · **0046 รอบ gate r2 G2: หน้า callback เป็น force-dynamic server shell + client component แยกไฟล์** — nonce ของ CSP ทำงานเฉพาะ dynamic rendering (เดิมถูก prerender เป็น static ไม่มี nonce → inline bootstrap โดนบล็อก → replaceState ไม่ได้รัน)) · audit `USER_EMAIL_CHANGE_CONFIRMED` เกิดอัตโนมัติจาก trigger 0044 · pin: dcr15 7 เคส | ทุกบทบาทที่ login แล้ว | 200 | VAL-001 |
| PUT | /me/license | ยื่นคำขอผูก/แทนที่เลขที่ใบอนุญาตว่าความ — **multipart** (JSON ทำลาย binary ไม่ได้): ฟิลด์ `license_no` (`^\d{6,9}$`) + ไฟล์หลักฐาน jpg/png/pdf ≤10MB (BFF ตรวจ type+size ก่อนอัปโหลดบักเก็ตส่วนตัว `license-evidence` path `license-evidence/{user_id}/{uuid}.{ext}` ผ่าน service client จุดเดียว `src/lib/license/submit.ts`) → แถวคำขอ+audit `LICENSE_BIND` เขียน **atomic ใน RPC `my_submit_license_application`** (0035 · user-JWT + RLS `la_insert_owner` คุมต่อ · RPC ตรวจ evidence `uploaded_by = auth.uid()`) · มีคำขอ pending อยู่ = **409** · ไฟล์อยู่นอก DB TX โดยธรรมชาติ (ถ้า RPC ล้ม BFF ลบ media คืน — best-effort) | citizen, lawyer | 202 (รอเจ้าหน้าที่ตรวจ) | VAL-001 (400 รูปแบบ · 409 `pending_exists`), IDM-001 |
| GET | /me/license | สถานะคำขอ+ใบปัจจุบันของตัวเอง (IDENT-005): คำขอล่าสุด (`status` pending\|approved\|rejected + `submittedAt`/`decidedAt`/`rejectedReason` — reject เห็นเหตุผลของตัวเองได้) + ใบ verified ปัจจุบัน (`licenseNo`/`verifiedAt` — เลขเดิม ตาม RLS เจ้าของ) + `canResubmit` (ยื่นใหม่ได้ = ยังไม่เคยยื่น หรือคำขอล่าสุด `rejected` — `pending`/`approved` = false: กัน UI เสนอฟอร์มยื่นซ้ำแก่ผู้ที่รอผล/ผูกเลขแล้ว) — หน้า my/license ใช้อันนี้ | citizen, lawyer | 200 | AUTH-001 |
| GET | /me/credits | ยอด credit คงเหลือแยกตามรอบต่ออายุ | lawyer | 200 | AUTH-001 |
| GET | /me/transcript | transcript รายวิชา/ผลสอบ/credit ที่สะสม — `?format=json\|csv\|pdf` (default json · CSV UTF-8 BOM · PDF สร้างฝั่ง BFF) | citizen, lawyer | 200 (csv/pdf = ไฟล์) | AUTH-001 |

PDPA endpoints (สิทธิของเจ้าของข้อมูล — วางใต้ `/profile/*` ตามที่ SRS/RTM อ้าง แยกจาก `/me/*` ที่เป็นข้อมูลสด):

| Method | Path | คำอธิบาย | บทบาท | Success | Errors |
| --- | --- | --- | --- | --- | --- |
| GET | /profile/export | ขอส่งออกข้อมูลของตัวเอง (data portability — IDENT-008): RPC `my_request_data_export` สร้างแถว job `data_export_jobs` → ตอบ **202 `{jobId, status}` ทันที** (ไฟล์ไม่สร้างใน request) · มี job pending อยู่ = **409** · cooldown 24 ชม. นับจาก done ล่าสุด = **429** · worker (internal job route ตามแบบแผน email-dispatch — loop 30 วิ) ประกอบ JSON ทุกก้อน (โปรไฟล์/บทบาท/consent/ลงทะเบียน/ความคืบหน้า/ผลสอบ/ใบ/credit/การแจ้งเตือน) อัปโหลดบักเก็ตส่วนตัว `pdpa-exports` → event `data_export.ready` → แจ้งเตือน in-app+อีเมล — ตัวแปรเทมเพลตเป็น **ตัวระบุเท่านั้น** (`full_name`,`job_id`,`file_media_id`) แล้ว **email worker ลงนาม signed URL อายุ 7 วัน (604800s) จาก `media_assets` ตอนส่ง** (URL ไม่เก็บใน outbox/ไม่ log — 1.2.5) · worker claim = lease `claimed_at` 10 นาที + **claimToken (fence รอบถือครอง — 0039)** — worker ตาย claim ถัดไปยึดงานคืน (reclaim เมื่อเกิน 10 นาที **หรือ claimed_at IS NULL** — แถวค้างก่อนมี lease กลับมาได้ · M1: worker เก่าใช้ token เดิมปิดงาน = 400 `(ERR-VAL-001\|stale_lease)` errcode 22023) · audit `DATA_EXPORT_REQUEST` (สร้าง job) + `DATA_EXPORT_DONE` (worker สำเร็จ) · SLA 24 ชม. · **อีเมลพร้อมลิงก์ดาวน์โหลด = signed URL 604800s ที่ผู้รับกดได้โดยไม่ต้อง login/apikey** (route Kong `storage-v1-signed` GET เท่านั้น — storage ตรวจ token เอง · คีย์ object ที่อัปโหลด = `{user_id}/{job_id}.json` ในบักเก็ต ขณะที่ `media_assets.storage_path` เก็บ path เต็ม — B1 · host ของลิงก์ = `SUPABASE_PUBLIC_URL` มุมมองผู้รับ ไม่ใช่ `kong:8000` ของ container — hostname ruling · ค่าที่ไม่ใช่ http(s) หรือไม่มี hostname = fail แถว `download_url_invalid` ก่อนส่ง — r3 MINOR · **P0002 pin (D-f-6 · 1.2.8)**: worker RPC `complete_data_export_job` กับแถวที่มีจริงแต่ `status ≠ 'processing'` → RAISE `ERR-NF-001\|job_not_processing` errcode **P0002** → ผ่าน gateway = **HTTP 500 ทึบ** (ร่าง error ถูกตัด ไม่มี envelope ERR- ให้อ่าน) · TX abort ไม่แตะแถวงาน — pin จริง: dcr11-pdpa เคส d2 · ทางเลือกเปลี่ยน 22023 เปิดไว้เมื่อมี demand ตามแบบแผน B2) | ทุกบทบาทที่ login แล้ว | 202 (job) | RATE-001, VAL-001, IDM-001 |
| POST | /profile/delete | ขอลบบัญชี (SEC-012 soft-delete): RPC `my_request_account_deletion` สร้างแถว `account_deletion_requests` + token CSPRNG (เก็บ **sha256 hash เท่านั้น** อายุ 24 ชม.) · RPC คืน `userId` ของผู้ขอ (BFF ผูกเจ้าของอีเมลยืนยันด้วยตัวตนผู้ใช้ ไม่ใช่ id ของคำขอ — 1.2.5) → อีเมลยืนยัน (template `account.delete.confirm` — ธุรกรรมบังคับ NTF-005) พร้อมลิงก์ `/profile/delete/confirm?token=…` · **guard: ผู้ถือ `instructor`/`staff:*`/`super_admin` ห้ามลบเอง = 403** (ชี้ติดต่อผู้ดูแล — SoD) · ผลสอบ/audit เก็บต่อตาม retention ที่กฎบังคับ | ทุกบทบาทที่ login แล้ว | 202 (รอยืนยันทางอีเมล) | RATE-001, RBAC-001 |
| GET | /profile/delete/confirm | **ยืนยันการลบบัญชี — สาธารณะ ไม่ต้อง session** (คนละ route จาก /api/v1 ปกติ: ผู้ยืนยันเพิ่งคลิกจากอีเมล ยังไม่ได้ login ก็ต้องทำได้) — ตรวจ token (hash เทียบ) → RPC `confirm_account_deletion` **single-use**: GoTrue ban + `profiles.deleted_at` + display_name → "บัญชีที่ขอลบแล้ว" + audit `PROFILE_DELETE` ใน TX เดียว · token ผิด/หมดอายุ/ใช้ไปแล้ว = หน้าผล "ลิงก์ไม่ถูกต้องหรือหมดอายุ" (200 — ไม่เปิดเผยสถานะคำขอ) · ผู้ขอได้บทบาทพนักงาน/ผู้สอน **ระหว่างอายุ token** = การ์ดที่สาม "ไม่สามารถยืนยันการลบได้" (`sod_role_changed` — SoD re-check ใต้ lock เดียวกับ grant/revoke · TX กลิ้งทั้งหมด คำขอยัง pending · หลังยุติบทบาท token เดิมยืนยันได้ — 1.2.5) · สำเร็จแล้วอีเมลยืนยันการลบ (`account.deleted`) | guest (ผู้ถือ token จากอีเมล) | 200 (หน้าผลทั้งสองกรณี) | RATE-001 |
| GET | /profile/consents | ดู consent แบ่ง **2 sections** (D12-17): (1) `notice_acknowledgments` — การรับทราบประกาศความเป็นส่วนตัว **อ่านอย่างเดียว** (เกิดอัตโนมัติตอน register, ตาราง append-only ตาม DD); (2) `consents` — ความยินยอมเสริม (marketing/email_notify) พร้อมเวอร์ชัน/สถานะ active | ทุกบทบาทที่ login แล้ว | 200 | AUTH-001 |
| PATCH | /profile/consents | ให้/ถอน consent **เฉพาะ optional เท่านั้น** (section `consents` — ห้ามแตะ notice_acknowledgments ซึ่ง append-only) | ทุกบทบาทที่ login แล้ว | 200 | VAL-001 |

### 3.3 Catalog & Enrollment (โดเมน 2)

| Method | Path | คำอธิบาย | บทบาท | Success | Errors |
| --- | --- | --- | --- | --- | --- |
| GET | /categories | รายการหมวดหลักสูตร (tree) | guest | 200 | RATE-001 |
| GET | /courses | ค้นหา/กรองหลักสูตร (หน้า catalog) — guest เห็นเฉพาะ `published` · ฟิลด์รายการ: `level` + `credits` + `learnerCount` (จาก view `course_public_stats` — DCR-4) | guest | 200 + pagination | RATE-001 |
| GET | /courses/{id} | รายละเอียดหลักสูตร + โครงสร้างโมดูล (สถานะ draft มองเห็นเฉพาะเจ้าของ/staff:content) · เพิ่ม `level`, `outcomes` (จาก `outcome_highlights`), `credits`, `learnerCount`, `instructors` (view `course_instructors_public`), `exam` (view `course_exam_summary` — CAT-004 AC; **DCR-7: เพิ่มฟิลด์ `assessmentId`** = uuid ของข้อสอบปลายหลักสูตร published+is_final ล่าสุด — จาก `course_exam_summary.assessment_id` — PB-17) — DCR-4 | guest, citizen, lawyer, instructor, staff:content | 200 | CRS-001 |
| POST | /courses/{id}/enroll | ลงทะเบียนเรียน (ซ้ำ = 200 idempotent คืน enrollment เดิม — DCR-3/D25) | citizen, lawyer | 201 (ใหม่) / 200 (ซ้ำ) | ENR-002, RATE-001 |
| GET | /me/enrollments | รายการที่เรียน/ลงทะเบียนไว้ | citizen, lawyer | 200 + pagination | AUTH-001 |

### 3.4 Learning & Progress (โดเมน 3)

| Method | Path | คำอธิบาย | บทบาท | Success | Errors |
| --- | --- | --- | --- | --- | --- |
| GET | /courses/{id}/progress | สรุปความคืบหน้าของตัวเองในหลักสูตร (% ต่อโมดูล) | เจ้าของ enrollment | 200 | LRN-001 |
| GET | /lessons/{id}/quiz | อ่านข้อสอบย่อยก่อนทำ — **โจทย์+ตัวเลือกเท่านั้น ไม่มี `is_correct`/`explanation` ตลอดการทำ** (BFF อ่าน quiz_questions/quiz_options ผ่าน service path ตาม RLS ของ DD §3.3 — DCR-5 กันเฉลยใน client bundle) + กติกา quiz (pass_pct/max_attempts จาก lesson_quizzes) | เจ้าของ enrollment | 200 | LRN-001, CRS-001 |
| POST | /lessons/{id}/progress | บันทึกความคืบหน้าบทเรียน (วิดีโอตำแหน่ง, อ่านจบ) | เจ้าของ enrollment | 200 | LRN-001/002 |
| POST | /lessons/{id}/quiz/submit | ส่งแบบทดสอบย่อย (ได้เฉลยทันที — ไม่นับ credit) | เจ้าของ enrollment | 200 (คะแนน+เฉลย) | LRN-001/002 |

### 3.5 Assessment (โดเมน 4 — การสอบ)

| Method | Path | คำอธิบาย | บทบาท | Success | Errors |
| --- | --- | --- | --- | --- | --- |
| GET | /assessments/{id} | อ่านข้อมูลการสอบ + กติกา (เวลา จำนวนครั้ง เกณฑ์ผ่าน) — **read-only เสมอ ไม่สร้าง attempt** (B-10) | citizen, lawyer, instructor, staff:exam | 200 | ASM-003, NF-001 |
| POST | /assessments/{id}/attempts | **เริ่มสอบ**: ตรวจเงื่อนไข → สร้าง attempt + ส่งข้อสอบ (สุ่มแล้ว) โดยไม่มีเฉลย | citizen, lawyer (ที่ผ่านเงื่อนไขจบหลักสูตร) | 201 attempt + ชุดข้อ + `serverTime` + `deadlineAt` | ASM-001/002/003 |
| GET | /me/attempts | ประวัติการสอบของตัวเองทุกหลักสูตร | citizen, lawyer | 200 + pagination | AUTH-001 |
| POST | /attempts/{id}/answers | บันทึกคำตอบทีละข้อ (autosave — เรียกบ่อย, idempotent ต่อ question) | เจ้าของ attempt | 200 (savedAt) | ASM-004/005/006 |
| POST | /attempts/{id}/submit | ส่งข้อสอบ — **ต้องมี `Idempotency-Key`**; ตรวจคะแนน server-side ทั้งหมด — **DCR-6: ตอบผลตรวจทันที** (grading เป็น synchronous ใน RPC `submit_attempt` — คืน `status ∈ passed\|failed` + `scorePct` + `correctCount`/`questionCount`) | เจ้าของ attempt | 200 (ผลตรวจทันที) หรือ ซ้ำ → คืนผลเดิม (`already_submitted: true`) | ASM-004/005/006, VAL-002, IDM-001 |
| GET | /attempts/{id}/result | ผลสอบ + เฉลย + credit ที่ได้ — **DCR-6 (1.4.0): เฉลยเปิดตามกติกาปัจจุบัน `exam_review_mode` (0049)** — 4 คอลัมน์เฉลย non-null เมื่อ `submitted ∧ mode ≠ never ∧ ไม่มี attempt in_progress ของ (user, assessment) ∧ (นับเฉพาะครั้งส่งแล้ว ≥ max_attempts ปัจจุบัน หรือ ผ่าน non-voided ครั้งใด)` — บังคับใน `learner_attempt_view` (lateral current-rules สูตรเดียวกับ start_attempt · ไม่มีกติกามีผล = ปิดแม้เคยผ่าน fail-closed) · BFF เปิดเฉลยเองไม่ได้ | เจ้าของ attempt | 200 | ASM-006, NF-001 |

Flow สอบ (sequence): `GET /assessments/{id}` อ่านกติกาก่อนได้ (read-only) → `POST /assessments/{id}/attempts` ตรวจสิทธิ์/จำนวนครั้ง/หน้าต่างสอบ → สร้าง attempt (`started_at`, `deadline_at = now + duration`) → client จับเวลาจาก `serverTime` ไม่ใช่นาฬิกาตัวเอง → autosave ทุกข้อ → `submit` (idempotent) → server ตรวจ → ถ้าผ่านเกณฑ์ → งานเบื้องหลังสร้างรายการรอออกประกาศนียบัตร (registrar ออกภายหลัง — separation of duties)

### 3.6 Certificate (โดเมน 4)

| Method | Path | คำอธิบาย | บทบาท | Success | Errors |
| --- | --- | --- | --- | --- | --- |
| GET | /certificates/{code} | **ตรวจสอบสาธารณะ** ไม่ต้อง auth — `{code}` ยอมรับทั้ง `cert_no` (พิมพ์มือ, D10) และ `verify_code` (จาก QR, nanoid — D10) — **ตอบ 200 เสมอ (D8/D11-14)** ด้วย 4 ฟิลด์ snake_case เท่านั้น: `{code, course_title, issued_at, status}` โดย `status ∈ valid \| revoked \| superseded` และ **เมื่อไม่พบ = 200 + `status="not_found"`** (โครง 4 ฟิลด์เท่าเดิม — `course_title`/`issued_at` เป็น null **เฉพาะกรณี not_found**; ไม่มีฟิลด์ `revoked_at` ใน response) — **กติกา not_found: ตอบเหมือนกันทุกกรณี ไม่เปิดเผยว่ารหัสนั้นมีจริงหรือไม่** (กัน enumeration) และ**ไม่มีชื่อเจ้าของ** (ชื่อ-นามสกุลอยู่บน PDF ที่เจ้าของ/registrar ดาวน์โหลดเท่านั้น) | guest | 200 เสมอ | RATE-001 |
| GET | /me/certificates | ประกาศนียบัตรของตัวเอง (พร้อมลิงก์ PDF) | citizen, lawyer | 200 + pagination | AUTH-001 |
| GET | /certificates/{id}/pdf | ดาวน์โหลด PDF ตัวจริง (id = uuid ต้อง auth — ต่างจาก public verify ที่ใช้ code) | เจ้าของใบรับรอง, staff:registrar, super_admin | 200 `application/pdf` | NF-001, RBAC-001 |
| POST | /admin/certificates | ออกประกาศนียบัติรายใบ (จาก attempt ที่ผ่านเกณฑ์) — audit `CERT_ISSUE` | staff:registrar, super_admin | 201 | RBAC-001, VAL-001 |
| GET | /admin/certificates | รายการ/ค้นหาประกาศนียบัตรที่ออกแล้ว (filter: เลขที่ใบ/verify_code/สถานะ/ชื่อผู้ถือ/หลักสูตร) — keyset pagination แบบเดียวกับ eligible — audit `PII_ACCESS` (D12-23) | staff:registrar, super_admin | 200 + pagination | RBAC-001 |
| POST | /admin/certificates/bulk | ออกเป็นชุด — **โมเดล worker ของ 0027 (DCR-8 · แก้ gate r1 M2)**: สร้าง job ตาราง `cert_bulk_jobs` (DD §3.7) แล้วตอบ 202 **ทันที ไม่รันใน request** · body `{ courseId: uuid \| null }` strict (null = ทุกหลักสูตร) · ขาออก `{ data: { jobId, status, totalAttempts, issuedCount, failedCount } }` — ตอนสร้าง `status='pending'` + counts ทั้งสาม = 0 เสมอ · worker `admin_cert_bulk_issue_step` (pg_cron ทุกนาที — pickup ≤ 1 นาที) รัน **commit ต่อใบ**: ใบ + audit `CERT_ISSUE` context `mode:'bulk'` actor = ผู้สร้าง job · แถวที่ออกไม่ได้ถูกเดินผ่านด้วย cursor ที่ **commit ค้างในแถว job ข้าม CALL** (r1 M1 + r2 MAJOR-1 — worker รอบถัดไปเดินต่อจากจุดเดิม ไม่เริ่มหัวคิวใหม่) · ตามผลด้วย GET ด้านล่าง | staff:registrar, super_admin | 202 (job) | RBAC-001, VAL-001, SYS-002 |
| GET | /admin/certificates/bulk/{jobId} | สถานะ job ออกใบเป็นชุด — **เห็นความคืบหน้าสดระหว่าง worker รัน (commit ต่อใบ — 0027)** · ขาออก `{ data: { jobId, status, totalAttempts, issuedCount, failedCount, lastError, createdAt, finishedAt } }` — `status ∈ pending \| running \| completed \| failed` — job ที่สะสมครบ 100,000 แถว (issued+failed) ถูก worker ปิดเป็น `failed` ทันทีโดยไม่ประมวลผลแถวเพิ่ม (0028 r2 MAJOR-2 · lastError = ข้อความเพดาน ERR-SYS-002\|bulk_row_limit *เมื่อยังไม่มีข้อผิดพลาดจริงก่อนหน้า — coalesce ตามท้ายแถว*) · `lastError` = ข้อผิดพลาด**จริงล่าสุด**ของ job — DB เก็บข้อความไทยของใบที่ล้มล่าสุด (500 อักขระ) และ**คงค่าไว้เมื่อใบถัดไปสำเร็จ**; ข้อความเพดาน `ERR-SYS-002\|bulk_row_limit` ถูกบันทึกเฉพาะเมื่อ job ปิดที่เพดาน 100,000 โดยยังไม่เคยมีข้อผิดพลาดจริง (coalesce) **ตัดทอน 200 อักขระตอนตอบ** · nullable จริง: `lastError`, `finishedAt` (มีค่าเมื่อ job จบ) — ค่าที่เหลือไม่เป็น null · **การเห็น: เจ้าของ job (created_by = ผู้เรียก) หรือ super_admin เท่านั้น** — registrar คนอื่น = 404 เหมือนไม่มีจริง (กัน enumeration) | staff:registrar, super_admin | 200 | RBAC-001, NF-001, SYS-002 |
| GET | /admin/certificates/eligible | รายการ attempt ที่ผ่านเกณฑ์แล้ว**ที่ยังไม่มี certificate สถานะ `valid`** — คิวงานออกประกาศนียบัตร (pagination + filter ตามหลักสูตร/ช่วงเวลา) — audit `PII_ACCESS` (D12-23) | staff:registrar, super_admin | 200 + pagination | RBAC-001 |
| POST | /admin/certificates/{id}/revoke | เพิกถอน (บังคับ reason) — audit `CERT_REVOKE` + reversal credit อัตโนมัติใน TX เดียว · response additive: `creditReversedRows`/`creditReversedTotal` (0031 v2 · gate r1 B1) — **`creditReversedTotal` เป็นผลรวมติดลบ** (แถว reversal ถูกเขียนเป็น −amount ใน ledger จึง sum ≤ 0 เสมอ — AUDIT §2.5 CREDIT_REVERSAL · gate r2 BLOCKER-1) | staff:registrar, super_admin | 200 | RBAC-001, VAL-001 |
| POST | /admin/certificates/{id}/reissue | ออกใหม่แทนใบเดิม (ใบเดิมเปลี่ยน status=superseded + ชี้ `supersedes_cert_id` lineage — DD; **idempotent ต่อ enrollment เพราะ UNIQUE(enrollment_id) เป็น partial `WHERE status='valid'` จึงมี valid ได้ 1 ใบ/คน/หลักสูตร แต่เก็บ superseded ได้หลายใบ**; reissue ไม่กระทบ credit ที่ accrual ไปแล้ว — D12-14/15) — CRT-007, audit `CERT_REISSUE` | staff:registrar, super_admin | 201 (ใบใหม่) | RBAC-001 |

### 3.7 Credit Bank — เจ้าหน้าที่ (โดเมน 5)

> DCR-9 (1.1.0): ย้ายขึ้น `/admin/*` ตาม convention §3.8 — เส้นเดิมในเวอร์ชันก่อนหน้าเขียนสั้น (`/credit-rules` ฯลฯ)

| Method | Path | คำอธิบาย | บทบาท | Success | Errors |
| --- | --- | --- | --- | --- | --- |
| GET | /admin/credit-rules | รายการกฎเครดิต (มี version/สถานะ · keyset pagination) | staff:registrar, staff:viewer, super_admin | 200 + pagination | RBAC-001 |
| POST | /admin/credit-rules | สร้างกฎใหม่ (config-driven, อ้าง Q1 · status เริ่มต้น draft) + audit `CREDIT_RULE_CREATE` — **atomic RPC** `admin_create_credit_rule` (0032): mutation+audit ใน TX เดียว (audit ล้ม = rollback) · RPC ตรวจ roles + aal2 ซ้ำในตัว (gate r1 B2/B3) | staff:registrar, super_admin | 201 | CRD-001, VAL-001 |
| PATCH | /admin/credit-rules/{id} | แก้กฎ — **lifecycle เท่านั้น** (status draft→active→retired) — trigger `guard_rule_semantics` (0010) บังคับ: แก้คอลัมน์ semantic ของแถว active/ถูกอ้าง = ยกเว้นเฉพาะสร้างเวอร์ชันใหม่ + audit `CREDIT_RULE_UPDATE` — **atomic RPC** `admin_update_credit_rule_status` (0032 · ตรวจ aal2 ในตัว) | staff:registrar, super_admin | 200 | CRD-001 |
| GET | /admin/credits/{userId} | รายการ ledger ของผู้ใช้รายคน (keyset) (เข้าถึงข้อมูลรายบุคคล → audit `PII_ACCESS` **fail-closed**: audit RPC ล้ม สองครั้ง = 503 ไม่เปิดเผยข้อมูล — gate r1 B7) | staff:registrar, staff:viewer, super_admin | 200 | RBAC-001 |
| POST | /admin/credits/adjustments | ปรับ credit มือ (+/−) **ต้องมี reason 10-500 ตัวอักษร + สร้างรายการ adjustment ใหม่ใน ledger (append-only) + audit `CREDIT_ADJUST` ใน TX เดียวกัน (RPC `admin_credit_adjust` 0031 — user-JWT)** · แจ้งเจ้าของ = Phase 4 (NTF) | staff:registrar, super_admin | 201 | CRD-002, RBAC-001 |

### 3.8 Admin (โดเมน 7)

| Method | Path | คำอธิบาย | บทบาท | Success | Errors |
| --- | --- | --- | --- | --- | --- |
| GET | /admin/users | ค้นหา/กรองผู้ใช้ (display_name/อีเมล/สถานะ — keyset ผ่าน RPC `admin_list_users` · audit `PII_ACCESS` **fail-closed**: audit RPC ล้มสองครั้ง = 503 ไม่เปิดเผยข้อมูล — แบบแผน 1.1.2 B7 · **1.2.8 (Wave F D-f-5)**: แถวขาออกเพิ่ม additive **`isBanned`** (boolean — GoTrue `banned_until` ยังไม่หมดอายุ) + **`bannedUntil`** (ISO\|null — แบนหมดอายุ = `isBanned:false` พร้อมค่าอดีตค้างตามจริงของ auth.users — RPC v2 0041) · สิทธิ์เป็นสองชั้น: rbac.ts อนุญาต `user:view` แก่ staff:exam (BFF) แต่ RPC `admin_users_staff_guard` จำกัดจริง staff:viewer/registrar/super_admin → staff:exam = 403 `ERR-RBAC-001` ไม่มีข้อมูลรั่ว · pin: dcr13 6 เคส) | staff:viewer, staff:registrar, super_admin | 200 + pagination | RBAC-001 |
| POST | /admin/users | สร้างบัญชีเจ้าหน้าที่ (บังคับ MFA ตั้งแต่วันแรก) — body strict `{email (lowercase), displayName 1-120, role, reason 10-500}` · ชุด `role` = `instructor\|staff:viewer\|staff:content\|staff:exam\|staff:registrar` เท่านั้น (**ไม่มี super_admin/citizen/lawyer** — zod ปฏิเสธ 400 `ERR-VAL-001` `fields:["role"]` ก่อนแตะ DB) · กลไก: GoTrue `inviteUserByEmail` → RPC `admin_grant_role` atomic (mutation+audit TX เดียว) ผ่าน service lib จุดเดียว `src/lib/admin/users.ts` · อีเมลถูกใช้แล้ว = 400 `ERR-VAL-001\|email_already_registered` (GoTrue 422 แปลงที่ BFF) · audit `USER_CREATE` **durable ผ่าน RPC `admin_audit_user_created` (0038 · หลัง invite+grant)** — retry จำกัด ค้าง = 503 `ERR-SYS-002\|user_create_audit_failed` (บัญชีสร้างแล้ว ให้ตรวจซ้ำ — 1.2.5) | super_admin (`user:create`) | 201 `{userId, email, displayName, role, granted, invitedAt}` | RBAC-001, VAL-001 |
| PATCH | /admin/users/{id} | ปิด-เปิดใช้งานบัญชี (v1 · Wave E Phase 5) — body strict `{is_active: boolean, reason?: 10-500}` · cross-field: **`reason` บังคับเมื่อ `is_active=false`** → 400 ก่อนแตะ DB/GoTrue · **ปิดใช้งานบังคับจริงด้วย GoTrue ban/unban** (`ban_duration` = `876000h` ≈ 100 ปี / `"none"` — login ไม่ได้ทันที; ลำดับ = ban ก่อน `profiles.is_active` — ล้มหลัง ban บัญชีค้างถูกปิด = ทิศทาง fail-closed) · **ห้ามแตะบัญชี super_admin ตัวอื่น** โดยผู้ไม่ใช่ super_admin (BFF guard 403 อ่านบทบาทเป้าหมายผ่าน user-JWT ก่อน RPC) · `profiles.is_active` + audit `USER_DISABLE`/`USER_UPDATE` **atomic ใน TX เดียว** ผ่าน RPC `admin_set_user_active` (0038 · user-JWT · aal2 ตรวจใน RPC) — retry จำกัด 3 ครั้ง (guard 4xx ไม่ retry) · ค้างหลัง ban = **503 `ERR-SYS-002\|user_active_update_failed`** (บัญชีค้างถูกแบน fail-closed · 1.2.5) · การแก้ข้อมูลสมาชิก (first/last_name โดย registrar — `user:update`) = Wave F | super_admin (`user:disable` — RBAC §2) | 200 `{userId, isActive}` | RBAC-001, VAL-001 |
| GET | /admin/roles | รายการบทบาท + คำอธิบายสิทธิ์ | staff:viewer, super_admin | 200 | RBAC-001 |
| POST | /admin/users/{id}/roles | มอบบทบาท — body strict `{role, reason 10-500}` · atomic RPC `admin_grant_role` (0035 r2 · mutation+audit TX เดียว · aal2 ตรวจใน RPC): super_admin มอบ `lawyer`/`instructor`/`staff:*` ได้ · **staff:registrar มอบได้เฉพาะ `lawyer` หลังยืนยันใบอนุญาต** (ขั้น BFF resource-scope → `ERR-RBAC-001\|role_scope` · ขั้น RPC เช่นกัน) · **`super_admin` ห้ามผ่าน endpoint นี้** (bootstrap เท่านั้น — fail-closed) — **ชั้นที่ปฏิเสธ (1.2.3): BFF zod enum ไม่รับ `super_admin` เลย → 400 `ERR-VAL-001` `fields:["role"]` ก่อนแตะ RPC; แท็ก `ERR-VAL-001\|role_not_grantable` เกิดเฉพาะชั้น RPC (เรียก PostgREST ตรง)** · ถืออยู่แล้ว = idempotent `granted:false` (201) · audit `ROLE_GRANT` เฉพาะเมื่อเกิด mutation | super_admin; staff:registrar (เฉพาะ `lawyer` หลังยืนยันใบอนุญาต) | 201 `{userId, role, granted}` | RBAC-001, VAL-001 |
| DELETE | /admin/users/{id}/roles | ถอนบทบาท — **body บังคับ `{role, reason 10-500}`** (DELETE พร้อม body ตามที่ RPC กำหนด — reason เป็นของ audit `ROLE_REVOKE`; ไม่มี body/ผิดรูป = 400 ก่อนแตะ RPC) · atomic RPC `admin_revoke_role` (0035 r2 · เงื่อนไขสิทธิ์เดียวกับการมอบ) · **ถอน role สุดท้ายของบัญชี = 400** `ERR-VAL-001\|last_role` (errcode 22023 — บัญชีต้องมี role ≥1 เสมอ · TX rollback แถวเดิมคงใช้งาน) · ถอน `super_admin`/ค่านอก enum = 400 `ERR-VAL-001\|role_not_manageable` · `citizen` ถอนได้ตามปกติ (เงื่อนไขเดียวที่กันคือ last_role) · audit `ROLE_REVOKE` · **P0002 pin (D-f-6 · 1.2.8)**: ถอนบทบาทที่เป้าหมายไม่ถือ → RAISE `ERR-NF-001\|role_not_found` errcode **P0002** → **HTTP 500 ทึบ** ผ่าน gateway เช่นกัน · role_assignments คงเดิม — pin: dcr12 เคส c2 · ขณะ errcode ยัง P0002 ห้าม map เป็น 4xx | เช่นเดียวกับด้านบน | 204 | RBAC-001, VAL-001 |
| GET | /admin/courses | ทุกหลักสูตรทุกสถานะ | staff:viewer, staff:content, super_admin | 200 | RBAC-001 |
| PATCH | /admin/courses/{id} | เปลี่ยนสถานะหลักสูตร — ชุด action **`publish` \| `unpublish` \| `return`** (ADM-005: return = ส่งกลับให้ผู้สร้างแก้ไข — **comment บังคับ ≥10 ตัวอักษร** แบบแผน revoke) ผ่าน atomic RPC `admin_decide_course` (0035 · mutation+audit TX เดียว · aal2 ใน RPC) · audit `COURSE_PUBLISH`/`COURSE_UNPUBLISH`/`COURSE_RETURN` ตาม action · สถานะเป้าหมาย: published/draft (unpublish)/returned | staff:content, super_admin | 200 | RBAC-001, VAL-001 |
| GET | /admin/question-banks | ภาพรวมธนาคารข้อสอบ | staff:exam, staff:viewer, super_admin | 200 | RBAC-001 |
| POST | /admin/question-banks | สร้างคลัง/นำเข้าข้อสอบ | instructor, staff:exam | 201 | RBAC-001, VAL-001 |
| GET | /admin/question-banks/{id} | รายละเอียดคลังข้อสอบ (หัวคลัง + สถานะ is_active) — **Wave G P2 (D73/D78 · 1.3.0)**: `question_bank:view` ที่ BFF คู่ RLS ด้วย user-JWT · UUID ผิดรูป = 400 `ERR-VAL-001` `fields:["id"]` · **bank ไม่มี/เข้าไม่ถึง = 404 `ERR-NF-001`** (ไม่เปิดเผยการมีอยู่ — ต่างจากคลังที่มีแต่ยังไม่มีข้อ = 200) · envelope `{data}` | instructor (เจ้าของ), staff:viewer, staff:exam, super_admin | 200 `{data}` | RBAC-001, VAL-001, NF-001 |
| GET | /admin/question-banks/{id}/questions | รายการข้อของคลัง — **Wave G P2 (1.3.0)**: cursor `(created_at,id)` DESC · limit default 20/max 100 (zod) · **limit+1 probe** หา hasMore · envelope `{data,page}` · **แถวไม่มีเฉลย** (QuestionResource — options ไม่มี isCorrect · แยกจาก edit GET ข้อถัดไป) · bank ไม่มี/เข้าไม่ถึง = 404 · คลังว่าง = 200 `data:[]` | instructor (เจ้าของ), staff:viewer, staff:exam, super_admin | 200 + pagination | RBAC-001, VAL-001, NF-001 |
| GET | /admin/question-banks/{id}/questions/{qid} | ข้อเดี่ยวสำหรับฟอร์มแก้ — **คืนเฉลย (EditQuestionResource — options มี `isCorrect`)** เฉพาะ `question_bank:update` + **user-scoped client (ไม่ใช่ service role) กรองทั้ง bankId และ qid** → qid ผิด bank / instructor ต่างเจ้าของ = 404 ไม่เปิดเผยการมีอยู่ · **`Cache-Control: private, no-store` ทุก response รวมทาง error (D74)** · client fetch `no-store` ตอนเปิด modal — ห้ามส่งผ่าน RSC props/prefetch/shared store · instructor เจ้าของอ่านเฉลยข้อ active ของตัวเองได้ (แก้ไม่ได้ — RPC :917 ปฏิเสธ) · **1.3.1**: options เปล่าได้ (ข้อร่างที่ยังไม่มีตัวเลือก — schema ไม่มี min · ฟอร์มแก้โจทย์/คำอธิบายต่อได้ ตัวเลือกค่อยเติมทีหลัง — gate r1 M2) | instructor (เจ้าของ), staff:exam, super_admin | 200 `{data}` | RBAC-001, VAL-001, NF-001 |
| PATCH | /admin/question-banks/{id}/questions/{qid} | แก้ข้อสอบ — RPC `admin_update_question` (0019 · SECURITY DEFINER · FOR UPDATE · whitelist patch-keys · **version+1 เสมอ** · audit `QB_QUESTION_UPDATE` ใน TX เดียว · instructor แก้ข้อ active ไม่ได้ · options **update-or-insert เท่านั้น ไม่มี delete** — UI เพิ่ม/แก้ได้ ลบไม่ได้ ตาม D79) · **1.3.0 แก้คำเดิม**: ตัด "ข้อที่ใช้แล้วอ่านอย่างเดียว" ที่ขัด as-built — ดีไซน์จริง = **snapshot design** (attempt ที่เริ่มแล้วติด snapshot ของ version ที่เลือกไว้ แก้ย้อนหลังไม่กระทบการสอบเดิม — ไม่มี used-in-exam lock) · **1.3.1**: เปลี่ยน type → single_choice/true_false ของข้อที่ยังไม่มีตัวเลือก = ปฏิเสธ (UI ฟ้องที่ฟอร์มก่อนส่ง + trigger `trg_questions_type_correctness` 0005 rollback เป็น backstop — ต้องมีเฉลยถูกเพียง 1 ตัวก่อน · ไป multiple_choice ผ่าน — trigger ไม่ตรวจ path นั้น · gate r2 m2) | instructor (เจ้าของ), staff:exam, super_admin | 200 | RBAC-001, VAL-001 |
| PATCH | /admin/question-banks/{id}/questions/{qid}/status | เปิด/ปิดข้อสอบ — body strict `{"status": "active"\|"retired"}` — **RPC `admin_set_question_status(p_question_id, p_bank_id, p_status, p_request_id)` (0047 · D75)**: TX เดียว = FOR UPDATE lock → pre-check role (**staff:exam/super_admin เท่านั้น** — instructor = `ERR-RBAC-001`) → transition matrix `draft→active · active→retired · retired→active` (อื่น ๆ รวม same-status = 400 `ERR-VAL-001\|question_status_transition`) → เป้า active ต้องมีตัวเลือก ≥1 (400 `ERR-VAL-001\|question_needs_options`) → UPDATE status + **version+1** → audit `QB_QUESTION_STATUS` context `{question_id, from, to, version}` + **p_request_id ลงช่อง audit จริง** (แหล่งค่า header `x-request-id` ที่ middleware สร้าง → `text\|null` · ไม่มี header = null) · transition ถูกปฏิเสธ = ไม่บัมพ์ version ไม่ audit · trigger `guard_question_activation` (0010) = backstop (P0001 → 503) · **0047 revoke UPDATE ตรงบน public.questions จาก authenticated** — เขียนทางเดียวผ่าน RPC (INSERT/SELECT/service_role คงเดิม) | staff:exam, super_admin | 200 `{questionId, status, version}` | RBAC-001, VAL-001, NF-001 |
| GET | /admin/assessments | ชุดข้อสอบ/กติกาทุกชุด — **1.4.2 (gate GP3 r2 R2-M3)**: สรุปกติกา `rules` (version ล่าสุด = **version สูงสุด** · เกณฑ์เดียวกับฐาน max+1 ของ RPC · ครบทุกคอลัมน์รวม `requireCourseComplete` + `selection`) อ่านจาก **RPC `admin_latest_assessment_rules` (migration 0051 · SECURITY DEFINER · guard aal2 + `staff:exam\|staff:viewer\|super_admin`)** แล้ว merge ที่ BFF — **เลิก embed ตาราง**: 0050 เคย grant `select (selection)` ให้ authenticated ทั้ง role แต่ ar_read ให้ "ผู้เรียนที่ลงทะเรียน active ของหลักสูตร published" เห็นแถว assessment_rules ด้วย = ผู้เรียนอ่านขอบเขตคลังทางตรง PostgREST ได้ (เจ้าหน้าที่/ผู้เรียนคือ role เดียวกัน — column grant แยกไม่ได้) → **0051 revoke คืนตามเจตนาซ่อนของ 0005**: ผู้เรียนขอ `select=selection` ตรงผ่าน /rest/v1 = **42501** (IT กลุ่ม 6 พิสูจน์บนสแตกจริง — แถวยังอ่านได้ตาม ar_read เฉพาะคอลัมน์ที่ได้ grant) · RPC ล้ม/คืนผิดรูป = 503 ERR-SYS-002 fail-closed (`admin_assessments_rules_rpc_failed/_drift`) · UI ใช้ prefill โมดัลกติกาและส่งต่อ selection เดิม — ไม่ส่ง = RPC เขียน '{}' ทับ · **1.4.3 (gate GP3 r3 R3-M2/R3-m1/R3-M1)**: **migration 0052** ขยาย audience ของ RPC เป็น 2 สายแรกของ ar_read จริง — staff เต็ม (viewer/exam/registrar/super_admin) + instructor row-filter (เจ้าของหลักสูตร) — instructor/registrar กลับ 200 (0051 กรอบจน 503 หลังผ่าน RBAC ของ BFF) · ตรวจ drift ต่อแถว RPC ก่อนเชื่อ: assessment_id ต้องเป็น UUID + อยู่ในชุดที่ขอ + ไม่ซ้ำ และแถว 14 คอลัมน์ที่โปรเจกต์ต้องผ่าน strict schema — พัง = 503 `admin_assessments_rules_rpc_drift` ก่อน merge · query additive `id=<uuid>` ชี้แถวเดียว (ไม่มี/ไม่เข้าถึง = `data:[]`) — read-back ของโมดัลกติกา: สถานะ "ผลยังไม่แน่นอน" ของการ POST กติกาถูกจดทะเบียน (registry ระดับ module) รอดการปิดโมดัล ปลดล็อกเฉพาะเมื่อ read-back `?id=` เสร็จจริง — committed → prefill จากกติกาจริงบนเซิร์ฟเวอร์ · not_committed → บันทึกใหม่ได้ · อ่านไม่ได้ = คงล็อก fail-closed + ปุ่ม "ตรวจสอบอีกครั้ง" (กันสร้าง version ซ้ำ — ไม่มี request identity แยก committed_expected/newer ไม่ได้ จึง prefill จากความจริงบนเซิร์ฟเวอร์เสมอ) · **1.4.4**: อ่านไม่ได้ (unreadable) = fail-closed คงล็อก+คงรายการ ห้าม resolve (R4-M1) · POST ที่กำลังส่ง = in-flight รอดการปิดโมดัล ปุ่มล็อกจนคำขอเสร็จ (R4-M2) · ผล deferred จัดการ registry ทุกกรณีแม้ epoch ไม่ตรง · ห้าม read-back ระหว่าง in-flight · **1.4.5**: โมดัลที่เปิดอยู่สมัครรับการเปลี่ยน registry (subscription) — ผล POST ที่มาถึงหลังปิด/เปิดโมดัลใหม่: ผลยังไม่แน่นอน → ระบบเริ่มตรวจกับเซิร์ฟเวิร์งเอง (มีปุ่ม "ตรวจสอบอีกครั้ง" เมื่อตรวจไม่สำเร็จ — ไม่มีทางตาย) · คำขอที่ถูกปฏิเสธแน่นอน (4xx) → ปลดล็อกทันที + refresh หน้าทุกผล (R5-M1) | instructor (row-filter เจ้าของหลักสูตร), staff:exam, staff:viewer, staff:registrar, super_admin | 200 | RBAC-001 |
| POST | /admin/assessments | สร้างชุดข้อสอบ (กติกา: เวลา สุ่ม จำนวนครั้ง เกณฑ์ผ่าน — Q2) — **instructor ได้เฉพาะ `draft` ของหลักสูตรตัวเอง** (ตรง matrix RBAC §2.2, D12-23) | instructor (draft เจ้าของหลักสูตร), staff:exam, super_admin · **1.4.3 (gate GP3 r3 R3-M2)**: reload+merge กติกาหลัง INSERT ผ่าน RPC 0052 (audience ตาม ar_read จริง) — instructor ได้ 201 จริง: 0051 เคย 503 หลัง INSERT สำเร็จ (IT กลุ่ม 7 พิสูจน์ — POST draft หลักสูตรตัวเอง + GET เห็นเฉพาะของตัวเอง) | 201 | RBAC-001, VAL-001 |
| POST | /admin/assessments/{id}/rules | **เพิ่มกติกา version ใหม่** (1.4.0 · แก้กติกา = version ใหม่เสมอ — UPDATE ของ semantic columns ถูก trigger `guard_rule_semantics` ปิด) — body strict `AssessmentRuleInput + examReviewMode` (ห้ามส่ง version/effective_to — **version = max+1 คำนวณ server-side** ใน INSERT...SELECT เดียว · สอง TX ชน `uq_assessment_rules_version` = retry ×3 ภายใน RPC หมดแล้ว 503 `ERR-SYS-002\|rules_version_conflict`) · **RPC `admin_add_assessment_rules` (0049 · SECURITY DEFINER · search_path=public · owner app_owner)**: guards ตามลำดับ `requirePermission("assessment:update")` + role pre-check `canWriteRules` (route :203-211) → aal ≠ aal2 = 403 `ERR-AUTH-004` → ไม่ใช่ staff:exam/super_admin = 403 `ERR-RBAC-001\|rules_write_forbidden` → ค่ากติกาผิดขอบเขต (time 5-480 · qc ≥1 · pass_pct 1-100 · max ≥1 · cooldown ≥0) = 400 `ERR-VAL-001\|rules_values` (สถานะตามทะเบียน §2 — VAL-001=400 ทั้ง API · BFF zod ช่วงเดียวกันบล็อกก่อนถึง RPC อยู่แล้ว) → assessment ถูกลบ/ไม่มี = 404 `ERR-NF-001\|assessment_not_found` · ตอบ 201 + `AssessmentRuleResource` (แถวที่แทรก) · audit `ASSESSMENT_CONFIG_CHANGE` ยิงโดย **DB trigger ทุก INSERT ของ assessment_rules** (ปิด 3 ทาง: RPC/endpoint นี้ · POST v1 เดิม · insert ตรงผ่าน ar_write) | staff:exam, super_admin | 201 | RBAC-001, VAL-001, NF-001, SYS-002 |
| GET | /admin/reports/enrollments | รายงานการลงทะเบียน/การเรียน | staff:viewer, super_admin | 200 | RBAC-001 |
| GET | /admin/reports/assessments | รายงานผลสอบ | staff:exam, staff:viewer, super_admin | 200 | RBAC-001 |
| GET | /admin/reports/credits | รายงาน credit ตามรอบ | staff:registrar, staff:viewer, super_admin | 200 | RBAC-001 |
| GET | /admin/reports/{type}/export | ส่งออก CSV/JSON (audit `ADMIN_EXPORT`) — สูงสุดตาม §5 — **staff:exam ได้เฉพาะ report ผลสอบ** (ตรง matrix §2.4 report:export, D12-23) | staff:viewer, staff:registrar (credit), staff:exam (ผลสอบ), super_admin | 200 `text/csv` หรือ JSON | RBAC-001, RATE-001 |
| GET | /admin/audit-logs | อ่าน audit log — RPC `admin_list_audit_logs` (0036 · SECURITY DEFINER role-gate `audit_log:view`): filter action/actor/entity_type/entity_id/ช่วงเวลา + **keyset cursor** · **อ่านอย่างเดียว ไม่มี endpoint แก้/ลบ** (BRIEF §8, D6) · ทุกการอ่านเกิด audit `AUDIT_READ` | staff:viewer, super_admin | 200 + pagination | RBAC-001 |
| GET | /admin/license-applications | รายการคำขอผูกเลขที่ใบอนุญาต (รอตรวจ/ตัดสินแล้ว — keyset + filter สถานะ) — audit `PII_ACCESS` | staff:registrar, super_admin | 200 + pagination | RBAC-001 |
| PATCH | /admin/license-applications/{id} | ตัดสินคำขอ — atomic RPC `admin_decide_license_application(p_app_id, p_action approve\|reject, p_reason)` (0035 · SECURITY DEFINER · aal2 ตรวจใน RPC): TX เดียว = lock แถว pending → reject: reason บังคับ ≥10 · approve: ตรวจ `uq_lawyer_licenses_license_no_active_license` ก่อน (เลขซ้ำคนอื่น = 409 ชี้ชัด constraint ให้ registrar ตัดสินเอง) → INSERT `lawyer_licenses` (verified) + **มอบบทบาท `lawyer` idempotent** + audit `LICENSE_VERIFY`+`ROLE_GRANT` + event `license.application.approved\|rejected` (แจ้งผล ≤5 นาที ผ่าน pipeline แจ้งเตือน) | staff:registrar, super_admin | 200 | RBAC-001, VAL-001 |
| GET | /admin/categories | หมวดหลักสูตรทุกสถานะ | staff:content, staff:viewer, super_admin | 200 | RBAC-001 |
| POST | /admin/categories | สร้างหมวด | staff:content, super_admin | 201 | RBAC-001, VAL-001 |
| PATCH | /admin/categories/{id} | แก้ชื่อ/เลิกใช้หมวด (มีหลักสูตรอ้างอยู่ห้ามลบ) | staff:content, super_admin | 200 | RBAC-001 |
| GET | /admin/exams/monitoring | มอนิเตอร์ attempt ที่กำลังสอบ (จำนวน/ค้างเกินเวลา/แยกตามหลักสูตร) — **DCR-6: เลื่อน Wave E** (รวมกลุ่ม reports) | staff:exam, super_admin | 200 | RBAC-001 |
| GET | /admin/exams/statistics | สถิติผลสอบรวม (ผ่าน/ตก/คะแนนเฉลี่ย ต่อชุดข้อสอบ) — **DCR-6: เลื่อน Wave E** (รวมกลุ่ม reports) | staff:exam, staff:viewer, super_admin | 200 | RBAC-001 |
| GET | /admin/dashboard | ตัวเลขสรุปหน้าแรก admin — RPC `admin_dashboard_stats(p_from, p_to)` (0036 · aggregate สด lag ≤15 นาที — ไม่ต้อง materialized view): ผู้ใช้ใหม่/รวม · การลงทะเบียน · ผลสอบ (ผ่าน/เรท) · credit ที่ออก · กรองช่วงวันที่ (`from > to` = 400 `ERR-VAL-001\|date_range`) — ชุดสิทธิ์ `report:view` ตาม RBAC §2 (sv/se/sr/sa) | staff:viewer, staff:exam, staff:registrar, super_admin | 200 | RBAC-001 |

### 3.9 Notifications (โดเมน 6)

| Method | Path | คำอธิบาย | บทบาท | Success | Errors |
| --- | --- | --- | --- | --- | --- |
| GET | /me/notifications | แจ้งเตือนในระบบของตัวเอง (unreadFirst + pagination) | ทุกบทบาทที่ login แล้ว | 200 | AUTH-001 |
| POST | /me/notifications/{id}/read | ทำเครื่องหมายอ่านแล้ว (idempotent) | เจ้าของ notification | 204 (ซ้ำก็ 204) | NF-001 |
| GET | /me/notification-settings | ตั้งค่าช่องทาง/ประเภทแจ้งเตือนของตัวเอง | ทุกบทบาทที่ login แล้ว | 200 | AUTH-001 |
| PATCH | /me/notification-settings | แก้การตั้งค่าแจ้งเตือน (in-app/email ต่อประเภท) | ทุกบทบาทที่ login แล้ว | 200 | VAL-001 |

### 3.10 System

| Method | Path | คำอธิบาย | บทบาท | Success | Errors |
| --- | --- | --- | --- | --- | --- |
| GET | /api/health | liveness/readiness probe — **อยู่นอก prefix `/api/v1`** เพื่อไม่ผูกกับ version; ตอบ `{ status, db, time }` ไม่มีข้อมูลอื่น | guest | 200 | — (503 เมื่อ DB ไม่พร้อม) |

---

## 4. ตัวอย่าง Zod schemas (TypeScript — วางที่ `lib/schemas/v1/*.ts`)

```typescript
import { z } from "zod";

// 1) สมัครสมาชิก (D11-18) — แยก "รับทราบประกาศความเป็นส่วนตัว" (การประมวลผลจำเป็น) ออกจาก consent เสริม
export const RegisterRequest = z.object({
  email: z.string().email().max(254),
  password: z.string().min(12).max(128),           // นโยบายรหัสผ่านเป็น config
  displayName: z.string().min(2).max(100),
  phone: z.string().regex(/^0\d{8,9}$/).optional(), // ไทย
  acknowledgeNotice: z.literal(true),               // บังคับ — รับทราบการประมวลผลที่จำเป็นตามสัญญา (ไม่ใช่ consent แบบเลือกได้)
  consents: z.array(z.object({                      // ความยินยอมเสริม — ไม่ให้ก็สมัครได้; versioned
    key: z.enum(["marketing", "email_notify"]),     // ค่าตาม DD `consents.consent_type` (pdpa_essential ไม่ใช่ optional consent)
    version: z.string().min(1).max(20),
    accepted: z.boolean(),
  })).max(10).default([]),
});

// 2) เข้าสู่ระบบ
export const LoginRequest = z.object({
  email: z.string().email(),
  password: z.string().min(1).max(128),
  totp: z.string().regex(/^\d{6}$/).optional(),      // มี MFA ต้องใส่
});

// 3) ผูกเลขที่ใบอนุญาต
export const LicenseBindRequest = z.object({
  licenseNumber: z.string().regex(/^\d{6,9}$/),      // รูปแบบจริงรอยืนยัน Q3
  issuedYear: z.number().int().min(2500).max(2600).optional(),
});

// 4) ลงทะเบียนหลักสูตร (path param)
export const EnrollParams = z.object({ courseId: z.string().uuid() });

// 5) บันทึกความคืบหน้าบทเรียน (D12-12) — **ตัด `completed` ออก**: สถานะ "จบบท" ตัดสินฝั่ง server
//    (record_lesson_progress คำนวณจาก watch_sec_accum + completion_rule; บท quiz จบด้วยคะแนนสูงสุดตาม `progress_pass_score_policy=highest`)
export const LessonProgressRequest = z.object({
  positionSeconds: z.number().int().min(0).optional(), // วิดีโอ
  documentRead: z.boolean().optional(),                // เอกสาร — client attestation เท่านั้น
}).refine((v) => (v.positionSeconds !== undefined) !== (v.documentRead !== undefined),
          { message: "ส่งอย่างใดอย่างหนึ่งเท่านั้น: positionSeconds (วิดีโอ) XOR documentRead (เอกสาร)" });
// tradeoff ที่ยอมรับ (D12-12): documentRead เป็นคำยืนยันจาก client (เอกสาร static ไม่มี server-side elapsed time ให้วัด)
// — ชดเชยด้วย: วิดีโอคุมด้วย bounded playback intervals + ข้อสอบ/quiz ตรวจฝั่ง server ล้วน

// 6) ส่งแบบทดสอบย่อย
export const QuizSubmitRequest = z.object({
  answers: z.array(z.object({
    questionId: z.string().uuid(),
    choiceIds: z.array(z.string().uuid()).min(1).max(10),
  })).min(1).max(100),
});

// 7) บันทึกคำตอบสอบทีละข้อ
export const AnswerSaveRequest = z.object({
  questionId: z.string().uuid(),
  choiceIds: z.array(z.string().uuid()).min(1).max(10),
  clientSavedAt: z.string().datetime(),               // เทียบ deadline ที่ server เท่านั้น
});

// 8) ส่งข้อสอบ (Idempotency-Key อยู่ header ไม่ใช่ body)
export const AttemptSubmitRequest = z.object({
  unansweredQuestionIds: z.array(z.string().uuid()).max(500).default([]),
}).strict();

// 9) สร้างกฎเครดิต — mirror credit_rules ตาม DATA-DICTIONARY.md (M-03 + D12-16: +name/credit_type/status lifecycle)
export const CreditRuleCreateRequest = z.object({
  code: z.string().regex(/^[A-Z0-9_]{3,32}$/),
  name: z.string().min(2).max(200),                     // บังคับ (D12-16)
  courseId: z.string().uuid().nullable(),          // FK courses.id (null = ใช้กับทุกหลักสูตรในหมวด)
  creditType: z.string().min(1).max(50).default("general"),
  renewalCycle: z.enum(["LAWYER_STANDARD"]).default("LAWYER_STANDARD"), // รอ Q1
  credits: z.string().regex(/^\d{1,4}(\.\d{1,2})?$/)
    .refine((v) => parseFloat(v) > 0),             // numeric(6,2) ค่าเป็นบวกเท่านั้น
  validDays: z.number().int().min(1).max(3650).nullable().default(null), // null = อายุตามรอบต่ออายุ (ตาม DD)
  effectiveFrom: z.string().date(),
});
// lifecycle (D12-16): `status` draft → active → retired — ฉบับ active แก้ไม่ได้ (ERR-CRD-001; แก้ = สร้างฉบับใหม่
// หรือปิดด้วย retired) — API ไม่รับ field `status` ใน request นี้ (สร้างได้เฉพาะ draft)

// 10) ปรับ credit มือ
export const CreditAdjustmentRequest = z.object({
  userId: z.string().uuid(),
  renewalCycleId: z.string().uuid(),
  delta: z.number().int().refine((v) => v !== 0, { message: "delta ต้องไม่เป็น 0" }),
  reason: z.string().min(10).max(500),                // บังคับเหตุผล (ERR-CRD-002)
  evidenceUrl: z.string().url().optional(),
});

// 11) มอบบทบาท
export const RoleGrantRequest = z.object({
  role: z.enum(["citizen","lawyer","instructor","staff:viewer","staff:content",
                "staff:exam","staff:registrar","super_admin"]),
  reason: z.string().min(10).max(500),
});

// 12) Pagination กลาง (query ทุก list endpoint)
export const PageQuery = z.object({
  limit: z.coerce.number().int().min(1).max(100).default(20),
  cursor: z.string().max(512).optional(),
}).strict();

// 13) Response: ผลตรวจสาธารณะของประกาศนียบัตร (D11-14) — 4 ฟิลด์ snake_case เท่านั้น ไม่มีชื่อเจ้าของ
//     ตอบ 200 เสมอ (D8): ไม่พบ = 200 + status="not_found" — ไม่เปิดเผยว่ารหัสมีจริงหรือไม่
export const CertificatePublicView = z.object({
  code: z.string(),
  course_title: z.string().nullable(),
  issued_at: z.string().datetime().nullable(),
  status: z.enum(["valid", "revoked", "superseded", "not_found"]),
}).refine(
  (v) => v.status === "not_found" || (v.course_title !== null && v.issued_at !== null),
  { message: "course_title/issued_at เป็น null ได้เฉพาะ status=\"not_found\"" }
);

// 14) Error envelope ขาออก (validate ทุก error response)
export const ErrorEnvelope = z.object({
  error: z.object({
    code: z.string().regex(/^ERR-[A-Z]+-\d{3}$/),
    message: z.string(),               // ไทย
    details: z.unknown().optional(),
  }),
});
```

---

## 5. Rate limit matrix (canonical เดียว — D11-13 + D12-11)

**ค่าเดียว canonical ต่อ endpoint ใช้ทุก environment** — ทุกแถวชี้ config key ใน **SRS Appendix A** (ตรงชุด — D12-11; key ที่ยังไม่มีใน SRS เดิม worker-2 เพิ่มให้ครบทุกกลุ่ม: mfa 10 / read 120 / learn_write 120 / exam 60 / staff_write 60 / export 10); ห้าม hardcode (BRIEF §6, B-13); บังคับ 2 ชั้น: Cloudflare WAF/rate rule (prod) + Next.js middleware (dev/สำรอง prod)
**คีย์การนับ (D12-11):** **IP counter cumulative ทุกกลุ่ม** (คู่กับคีย์อื่นเสมอ — นับแยกทั้งคู่ ใครถึงขีดก่อนถูกจำกัดก่อน); pre-auth ที่ยังไม่มี user_id (login/register/otp) ใช้ **อีเมล normalized (lowercase + trim)** เป็นคีย์บัญชี ไม่ใช่ user_id

| Group | ใช้กับ (ตัวอย่าง) | หน้าต่าง | ขีดจำกัด (canonical) | Appendix A key | คีย์การนับ (IP cumulative ทุกกลุ่ม — D12-11) | เกิน → |
| --- | --- | --- | --- | --- | --- | --- |
| AUTH | /auth/login, /auth/register | 1 นาที | 10/min ต่อ IP + ต่อ **อีเมล normalized** (pre-auth ไม่มี user_id) | `auth_rate_limit_per_min` | ip + email(normalized) | 429 + Retry-After |
| OTP_REQUEST (specific) | /auth/otp/request | 1 ชั่วโมง | **3/ชม. ต่อเบอร์/อีเมลปลายทาง** | `otp_per_phone_per_hour` | ปลายทาง + ip | 429 |
| PWD_RESET | /auth/password-reset/* | 1 ชั่วโมง | **5/ชม. ต่อบัญชี** | `password_reset_per_hour_per_account` | อีเมลปลายทาง (normalized) + ip | 429 |
| MFA | /api/v1/me/mfa/* (as-built 1.2.8 — เดิม `/auth/mfa/*`) | 1 นาที | 10/min ต่อบัญชี | `mfa_rate_limit_per_min` | user_id + ip | 429 |
| PUBLIC_READ | /categories, /courses, /certificates/{code} | 1 นาที | 120/min ต่อ IP | `public_read_rate_limit_per_min` | ip | 429 |
| READ | /me*, /profile/* | 1 นาที | 120/min | `read_rate_limit_per_min` | user_id + ip | 429 |
| LEARN_WRITE | /lessons/*/progress, /lessons/*/quiz/submit, PUT /me/license, PATCH /me | 1 นาที | 120/min | `learn_write_rate_limit_per_min` | user_id + ip | 429 |
| EXAM | /assessments/*/attempts, /attempts/answers, /attempts/*/submit | 1 นาที | 60/min | `exam_rate_limit_per_min` | user_id + ip | 429 (log WARN) |
| STAFF_WRITE | /admin/*, /credit-* | 1 นาที | 60/min ต่อบัญชี | `staff_write_rate_limit_per_min` | user_id + ip | 429 (audit) |
| EXPORT | /admin/reports/*/export, /profile/export | 1 ชั่วโมง | 10/h ต่อบัญชี | `export_per_hour` | user_id + ip | 429 |

**กติกาการนับ (D11-13):**

1. **กลุ่มเฉพาะ (specific) ชนะกลุ่มทั่วไป** — คำขอเดียวที่ตรงหลายกลุ่มใช้ขีดจำกัดของกลุ่มที่เจาะจงที่สุด (เช่น `/auth/otp/request` อยู่ทั้ง AUTH และ OTP_REQUEST → ใช้ OTP_REQUEST 3/ชม./เบอร์; `/auth/password-reset/*` → ใช้ PWD_RESET ไม่ใช่ AUTH)
2. **ขีดจำกัด IP กับ user ใช้แบบ cumulative ทุกกลุ่ม (D12-11)** — ทุกกลุ่มมี IP counter เสมอ คู่กับคีย์อื่น (user_id / อีเมล normalized / ปลายทาง OTP) — นับแยกทั้งคู่ ใครถึงขีดก่อนถูกจำกัดก่อน (pre-auth ไม่มี user_id → ใช้อีเมล normalized แทน)
3. ค่า EXAM 60/min รองรับเป้า 5,000 คนสอบพร้อมกัน (BRIEF §7) — สูงกว่าอัตรา autosave ที่ client ส่ง (throttle ที่ client 10 วินาที/ข้อ)
4. 429 ทุกครั้ง → audit `RATE_LIMIT_HIT` (ระดับ WARN) เมื่อเป็นกลุ่ม STAFF_WRITE/EXAM

---

## 6. เปิดประเด็น (โยง Open Questions)

| ประเด็น | ผลกระทบ | สถานะ |
| --- | --- | --- |
| รูปแบบเลขที่ใบอนุญาตจริง | `LicenseBindRequest` regex | รอยืนยัน Q3 |
| จำนวนครั้งสอบ/เกณฑ์ผ่าน | ใช้ config ต่อ assessment ไม่กำหนดที่ API | รอยืนยัน Q2 |
| Proctoring อาจเพิ่ม endpoint (เช่น /attempts/{id}/events) | จะเป็น `/api/v1` additive — non-breaking | รอยืนยัน Q4 |
| รอบต่ออายุ+จำนวน credit | `CreditRuleCreateRequest.renewalCycle` enum | รอยืนยัน Q1 |

> เอกสารนี้เป็น part ของชุด `04-api-security/` — บทบาท↔permission ที่คอลัมน์ "บทบาท" อ้างถึง นิยามเต็มอยู่ที่ RBAC-DESIGN.md; event ที่กล่าวถึง (เช่น `COURSE_PUBLISH`) นิยามอยู่ที่ AUDIT-LOG-DESIGN.md
