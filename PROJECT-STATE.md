# PROJECT-STATE — Second Brain (อัปเดตทุก stage transition + commit ทุกครั้ง)

> เอกสารนี้คือ "สมองก้อนที่สอง" ของโครงการ — session ใหม่ใด ๆ ต้องอ่านไฟล์นี้ก่อน แล้ว resume จาก
> wave board + decision log ไม่ต้องเริ่มใหม่จากศูนย์

|          |                                        |
| -------- | -------------------------------------- |
| อัปเดตล่าสุด | 2026-09-12 · **Wave D Phase 0 ปิดครบ: 5/5 lanes + lead batch 0019 + BFF wire ✅** (lanes: D-0 `09d22a2` · D-1 `a88c2d2` · D-2 `d1083f8` · D-3 `2156c3c` · D-4 `306c399` · migration 0019 `f9222fa` — probes **25/25** + integration **37/37** · wire `4fd51d3` — eslint 0 · isolated tsc 0 · vitest **846/846** (72 ไฟล์) · typecheck 0 · build 0 · gitleaks 0) · PB-14/15/16 ✅ ปิดใน 0019+wire · **gate r1 FAIL(4B/4M) → แก้ครบ+probes จับ B9-B11 (D38) · gate r2 FAIL(6M/1m) → แก้ครบ F1-F7+replay จับ B12-B14 (D39) · gate r3 FAIL(3M/1m) → แก้ครบ G1-G4 (D40) · gate r4 FAIL(3M/3m) → แก้ครบ H1/H2a-c/H4/H5/H6 + **H3 adjudicated NOT-A-DEFECT** (D41) — vitest 894/894 · probes r1 36/36 + r2 25/25 · integration 37/37 · gate r5 FAIL(0B/1M/1m) → แก้ครบ K1 (outbound .strict() 4 schema + QuestionBankCreateResult) + K2 (for update of q + RETURNING — audit version จากเลขจริง) · H3 adjudication upheld (D42) — vitest 898/898 · probes r1 37/37 + r2 25/25 · integration 37/37 · gate r6 FAIL(0B/1M/0m) → แก้ครบ L1 (exam.ts .strict() ทุก schema ทุกชั้น + cert resource ใหม่ 4 ตัว + route 4 ตัว parseOutgoingView + AnswerSaveRequest ขาเข้า strict) (D43) — vitest 909/909 · integration seed สด 37/37 · gate r7 FAIL(0B/2M/1m) → แก้ครบ M1/M2/m3 (D44) — vitest 925/925 · probes r1 43/43 + r2 25/25 · integration 37/37 · gate r8 FAIL(0B/2M/1m) → แก้ครบ N1/N2/n3 (D45) — vitest **945/945** · probes r1 45/45 + r2 25/25 · integration 37/37 · gate r9 FAIL(0B/3M) → แก้ครบ O1/O2/O3 (D46) — vitest **962/962** · probes r1 48/48 + r2 25/25 · integration 37/37 · gate r10 FAIL(0B/2M) → แก้ครบ P1/P2 (D47) — vitest **976/976** · integration seed สด 37/37 · **gate r11 = PASS (0B/0M/1m) — Q1 แก้พร้อม approve (D48)** → commit `874ea38` [#51] → **merge --no-ff → develop `b40c240`** (tree-identity 0 ไบต์ · post-merge: vitest 976/976 · tsc 0 · eslint 0 · build 0 · gitleaks 0 ครบ branch 15,534 บรรทัด) · **PB-13 (IP_HASH_SALT คลุม ip_hash + user_agent_hash) ✅ ปิด — codex gate r1 = PASS 0 findings (D50)** → `eff8c8c` [#52] → **merge --no-ff → develop `b883ece`** (tree-identity 0 ไบต์ · post-merge: vitest 987/987 · tsc 0 · eslint 0 · build 0 · gitleaks 0 · codex อิสระ 987/987 shuffle seed 1352) → **Phase 1 spawn D-5/D-6/D-7 (ธง D-5: mid-exam reload gap — D37)** · **Phase 1 execution ครบ 3/3 lanes + lead batch:** D-6 `971b02a` [#54] · D-7 `99793a9` [#55] · D-5 `202197a` [#56] · AdminSidebar 3 หน้าใหม่เข้า READY · full gates: vitest **1159/1159** (88 ไฟล์) · tsc 0 · eslint 0 · build 0 (29/29) · PB-17..20 เปิด (Wave E) · **ถัดไป: codex gate ชุด Phase 1 UI (b883ece..HEAD) → Phase 2 D-8/D-9** · **gate ชุด Phase 1 UI = PASS (D51):** r1 FAIL(6M)→แก้ `6af895f` [#58] · r2 FAIL(1M/1m)→`62f6a49` [#59] · r3 FAIL(1M)→`6997ea7` [#60] · r4 FAIL(1M)→`56acd84` [#61] · **r5 = PASS 0 findings** — vitest **1168/1168** · tsc 0 · eslint 0 · gitleaks 0 ทุกรอบ · **GitHub backup ครบทุกสาขารวม develop (D24/D26) + cron รายชั่วโมง `15279ead` (D25)** · **Phase 2 ครบ 2/2: D-8 `fac01ed` [#63] (integration 68/68) · D-9 ปิด — e2e 19/19 (9.0m) rc=0 · isolated tsc/eslint/gitleaks 0 · DB สะอาดหลังรัน** · **D-10 ปิด — migration 0020 (submit_attempt_core ร่วม + auto_close_expired_attempts + pg_cron ทุก 1 นาที) + probes r1 48/48 + r2 25/25 + P ใหม่ 21/21 + vitest 1168/1168 · tsc 0 · eslint 0 · build 0 · integration seed สด 68/68** · **gate ชุด Phase 2: r1 FAIL (MAJOR-1 เดียว — d9-helpers auth/MFA error ใส่ response body) → แก้ `1320d2f` [#66] + playwright รันใหม่ 19/19 → r2 = PASS 0 findings (codex รันอิสระ vitest 1168/1168 · tsc 0 · eslint 0)** → **Wave D ปิดครบทุก lane (D-0..D-10)** · **Wave E kickoff (2026-09-11): แผน `.omc/plans/wave-e-plan.md` APPROVED — lead spot-check O-1..O-10 ครบทุกข้อกับ source จริง + ตัดสิน D55 10 ข้อ (PB-17 view+assessment_id · PB-18 snapshot type+default multiple_choice · PB-19 transport รวมศูนย์ · PB-20 role-gate sr/sa · instructor↔shell คง fail-closed เป็นนโยบาย+ADR · D39 lessons completed+uq คงไว้ · bulk job · reports ตาม DCR-6 · CRT-008 async · 0021 add-column-at-end)** · **Phase 0 ปิดครบ (2026-09-12): DCR-7 เอกสาร 3 ไฟล์ v1.0.4/1.1.1 (worker flash + lead verify + reconcile DD↔0023 สองทิศ) + migration 0021..0024 (lead-owned) — dry-run begin/rollback 4/4 exit=0 · in-place apply rc=0 · fresh replay `down -v`→`up -d` **24/24 rc=0 ×2** (รอบสองหลังแก้ 0024) · seed ok · P-probes ผ่าน (P1a cols/ACL · P2 type markers · P3 view default · P4 rls fail-closed 0 policies · P5 pick parity · P6 D39 quals — false 4 จุดพิสูจน์เป็น PG-normalization artifacts: `= ANY(ARRAY[...])` · `uid()` · `IS NULL` พิมพ์ใหญ่ · regprocedure cast) · **integration 68/68 (18.86s)** · บทเรียนสำคัญ: policy drop+create ต้องตั้งต้นจาก migration รุ่นล่าสุดที่นิยาม policy นั้น — 0024 v1 สร้าง lessons_read จากฐาน 0010 จนทับ guards 0013/0014/0015 (soft-delete + admin-restore) integration จับได้ 6 fails → เขียนใหม่จากฐาน 0015 เปลี่ยนเฉพาะเงื่อนไข enrollment · ธง D-4 เก่า (eligible filter-after-pagination) พบว่าปิดแล้วโดย 0019-r2 F6 (issue.ts:266 เรียก RPC anti-join) — 0023 จึงไม่แตะ eligible เพิ่ม picker cap 200 + cert_bulk_jobs แทน** · ถัดไป: commit [#69] → push → spawn Phase 0 (E-0 transport · E-2 marker)** · **Phase 0 workers ปิดครบ + merge (2026-09-12): E-0+E-2 commit `b32eb41` [#70] (867+/236-) + lead fix type บน wire → gate r1 FAIL (1 MAJOR: toggle สองจังหวะยิง transient [A,B] + 1 MINOR → D56) → แก้ MAJOR-1 atomic setSingle `c4a2acb` → **gate r2 = PASS** (codex รันเอง tsc 0 · vitest 1209/1209 · eslint 14 ไฟล์ 0 · runtime probe 42 assertions) → merge --no-ff **develop `4659ef0`** → post-merge: vitest 1209/1209 · tsc 0 · eslint 0 · build 0 · gitleaks 151 commits 0 → push แล้ว · minor 1 ค้าง (interaction test ผ่าน radio → E-9) · ถัดไป: spawn Phase 1 (E-1 · E-3 · E-5)** · **Phase 1 ปิดครบ 3/3 lanes + merge (2026-09-12): E-1 `3085309` [#72] · E-3 `c2fa545` [#73] · E-5 `675e16c` [#74] + lead 0025 `2a7167d` (ADMIN_EXPORT allowlist + v_keys arm) — gate r1 FAIL(2M+5m) → แก้ครบ `e8c9ada`+`9855fe1` (aggregates เปิด compose · readCappedRows batch 1000/หน้า+sentinel · container/sentinel strict · INSERT uuid+OUTBOUND_SCHEMAS · CSV สูตร · refreshOnClose · XOR half-pair) → gate r2 FAIL(1M+1m) → แก้ครบ `61587ac` (readAllAggRows หน้า aggregate คิวมอนิเตอร์จนหมดจริง + lookup chunk 200) + `ca438eb` (modalCloseBlocked กั้นปิดกลาง submitting ทุกช่องทาง + reducer guard ซ้ำชั้น) → **gate r3 = PASS (0 MAJOR · 1 MINOR test-coverage)** → ปิด MINOR `27bfac6` (mock .in() กรองจริง + avg ข้ามชุด 200) — vitest **1379/1379** (103 ไฟล์) · tsc 0 · eslint 0 · build 0 · gitleaks 0 committable → **merge --no-ff develop `dd4d805`** → post-merge: vitest 1379/1379 · tsc 0 · build 0 → push แล้ว · **PB-17/18/19/20 ✅ ทั้งหมด** · prod-checklist: hosted Supabase ต้องเปิด PostgREST aggregates (PGRST_DB_AGGREGATES_ENABLED) — PGRST_API_MAX_ROWS=1000 คือเหตุที่ batch loop มีอยู่ · ถัดไป: spawn Phase 2 (E-4 · E-6 · E-7 · E-9)**  · **Phase 2 กำลังดำเนิน (2026-09-12): lead 0026 `ee260ed` [#75]** (feature_flags seed ปิด + cert_issue_core v2 `p_mode` manual/bulk/auto fail-closed + admin_cert_bulk_issue_run savepoint ต่อใบ + cert_auto_issue_tick advisory-lock actor ระบบ a170 + cron ltc-cert-auto-issue ทุก 2 นาที + **D58: policy app_owner ของ feature_flags/cert_bulk_jobs** — probes ผ่านครบ: P1 bulk 2 ใบ mode=bulk actor นายทะเบียน · P2 rerun guard ERR-VAL-001\|bulk_job_already_finished · P3 auto off→skip / on→2 ใบ mode=auto actor a170 / ซ้ำ→0 · P4 mode มั่ว→invalid_issue_mode · integration 68/68 rc=0 · fresh replay down -v→26/26 rc=0 + seed 7 หลักสูตร) + **E-7 lead ปิดเอง `dcb93b3` [#77]** (comment e2e-12 ชี้ path จริง (admin)/layout.tsx:50-59 + อ้าง §2.5 — eslint 0) + **spawn workers flash 3 ตัว: E-4 (bulk BFF) · E-6 (flag reader) · E-9 (integration ใหม่)** · **workers ปิดครบ 3/3** (E-4 `1e8cc85` [#78] · E-6 `bfb2531` · E-9 `7d7bfed` [#79] + lead `ea38a01` D59) · **gate r1 FAIL (3M+2m)**: M1 head-of-line (picker หยิบ 200 แถวแรกซ้ำ + exit เมื่อ round ว่าง = แถวออกได้ถูกทิ้ง) · M2 ทั้ง job ใน TX เดียวของ RPC (savepoint ≠ TX ต่อใบ) · M3 เทส auto เปิด flag global กวาดนอก fixture · m1 spec stale · m2 clamp/keyset-tie ไม่มีเทสจริง · C2 import cycle adjudicated ยอมได้ → **0027 worker redesign แก้ครบ** (procedure commit-ต่อ-ใบ + cursor walk-past + tick scoped + BFF insert-only 202 + dcr8 ใหม่ 11/11 + dcr7 +2 ปิด m2 + docs DD 1.1.3/API 1.0.5/AUDIT + **D60: PG≥15 procedure ที่ COMMIT ต้อง INVOKER เปล่า — 0B000**) — vitest **1434/1434** (106 ไฟล์) · tsc 0 · eslint 0 · build 0 · gitleaks 0 committable · fresh replay down -v→**27/27** + **seed อัตโนมัติใน db-migrate แล้ว** + dcr8 post-replay 11/11 · **commit `a0fc204` [#80] + push แล้ว** · **gate r2 = FAIL (3M+3m)**: MAJOR-1 cursor ของ 0027 อยู่ในหน่วยความจำ procedure (ทุก CALL เริ่ม null = เดินแถวล้มชุดเดิมซ้ำ · คนท้ายคิวอดใบ) · MAJOR-2 เพดาน 100k ตรวจหลัง branch ชน v_max (p_max_certs=1 ไม่ตรวจเลย · job ที่ถึงเพดานอยู่แล้วยังเดินแถว) · MAJOR-3 step(null) บน DB ร่วมหยิบ job นอก fixture + cleanup ล้มไม่คืน cron · m1-m3 (หลักฐาน scope · seed activate · builtin qualify) → **0028 cursor durable แก้ครบ** (คอลัมน์ job + ตาราง cert_auto_cursor ต่อ scope · sweep จบ = reset → retry · เพดานตรวจก่อนลูป+ก่อน branch · pg_catalog ครบ) + dcr8 ใหม่ **15/15** — vitest **1434/1434** · integration **90/90** · tsc 0 · gitleaks 0 committable · DD 1.1.4 · API-SPEC 1.0.6 · **gate r3 = FAIL (2M+3m — adjudicated real ทุกข้อ) → 0029 arrival-first แก้ครบ**: MAJOR-1 0028 auto-tick มองไม่เห็นแถวใหม่กว่า sweep_top จน sweep เก่าจบ (4,000 แถว @1,000/2 นาที = นาที 8 — ขัด SRS AC ≤5 นาที) → tick **สองเฟส** fresh-ก่อน-backlog งบ v_max เดียว + คู่ขอบบน `sweep_top_*` ใน cert_auto_cursor + picker ใหม่ `admin_cert_auto_fresh_pick` · MAJOR-2 assertNoForeignJobs = TOCTOU → สองเคส step(null) global ย้ายไป **DB แยก** `ltc_e9_iso_dcr8` ของ suite เอง (stub auth.* + pg_dump -s clone + seed + fixture) · MINOR-2 cursor evidence (จำนวนแถว + ครบสองคู่ + exact-cap reset) · MINOR-1 DD ย้าย cursor rows เข้า cert_bulk_jobs/cert_auto_cursor + API-SPEC lastError coalesce · MINOR-3 seed id-list (รอบก่อน) + dcr8 **15/15** (two-lane cross-tick + iso) — vitest **1434/1434** · integration **90/90** · tsc 0 · DD 1.1.5 · API-SPEC 1.0.7 · 0028 header บอก superseded ส่วน tick · **gate r4 = FAIL (2M+2m — adjudicated real ทุกข้อ) → 0030 walked-markers แก้ครบ**: MAJOR-1 แถว commit ช้า (submitted_at = now() ตอน*เริ่ม* TX ของ 0020) ตกช่อง (cursor, sweep_top) ของ 0029 = ล่องหนจน sweep จบ (ขัด SRS AC ≤5 นาที) → **marker ต่อแถว** ในตารางใหม่ `cert_auto_walked` + picker `admin_cert_auto_unmarked_pick` (desc — "เดินแล้ว" วัดจาก marker ไม่ใช่ช่วงเวลา) + watermark สองคู่เหลือเป็นข้อมูลชี้แจง · MAJOR-2 สถานะ 0028 ค้าง (sweep_top null) ไม่ถูก 0029 normalize → tick ตรวจสถานะขัด = normalize เป็น sweep ใหม่ (epoch+1 + null ทั้งสี่ + ลบ marker) · MINOR-1 dcr8 evidence ครบสองคู่ + toHaveLength + regression ใหม่ 2 เคส (held-TX commit ช้า · seed cursor 0028-style) — dcr8 **17/17** · MINOR-2 API-SPEC lastError ปรับ qualifier coalesce · gate r5 FAIL (0M+3m — adjudicated ทั้งสาม REAL) → แก้ครบ r6 (D64): startHeldSession ติดตามการปิดตั้งแต่ spawn (early-close fast-reject · end() กลืน error+race timeout) + regression SQL-ผิด <15 วินาที · cur3 evidence เป๊ะ (aid คน 18 + cursor > หัว sweep) · DD 1.1.7 (sweep_top = หัวของรอบ + หมายเหตุหักล้าง 1.1.5) · API-SPEC 1.0.9 (escape pipe ครบ) — dcr8 **18/18 ×3** · gate r6 FAIL (0M+1m — REAL) → แก้ r7: escape `/admin/exams/monitoring\|statistics` ในแถวเวอร์ชัน (1.0.3 history — pipe เปล่าตัวสุดท้ายของทั้งสอง doc) + audit ตารางทุกแถวของ DD/API-SPEC ด้วยตัวนับ unescaped pipe (0 mismatch ทั้งคู่) · gate r7 FAIL (0M+1m — REAL) → แก้ r8 (D66): escape pipe แถว D65 + เพิ่ม escape อีก 15 แถว legacy (16 แถวรวม D65 — ยุค Wave D) + รวม PROJECT-STATE เข้า audit ตัวนับ unescaped pipe — 0 mismatch ครบสามไฟล์ (DD · API-SPEC · PROJECT-STATE) · gate r8 FAIL (0M+1m — REAL — เลขหลักฐานใน r8 คลาดจาก diff จริง) → แก้ r9 (D67): ปรับเลขสามจุด ให้ตรง diff จริง (นับด้วยสคริปต์จาก diff ไม่ใช่ยอด --stat) · **gate r9 = PASS (0M+0m — ผ่านครบทุก checkpoint)** → fresh-volume replay (down -v → up → migrations **30/30 rc=0** + seed + crons 2/2 + dcr8 **18/18** บน volume ใหม่) → merge --no-ff develop **`0ab99b7`** → post-merge gates ครบ (unit 1434/1434 · integration 93/93 · tsc 0 · eslint 0 · build 0 · gitleaks 10 placeholder = 0 committable) → push — **Wave E Phase 2 ปิด (2026-09-12)** · **Phase 3 (CRB Credit Bank) เปิด (2026-09-12): แผน `.omc/plans/wave-e-p3-plan.md` + ตัดสิน D68 สิบข้อ** (accrual ผ่าน event_outbox ตาม DD §3.5/D11-17 · จับกฎครั้งเดียวตอนตรวจ + snapshot · lazy cycle default Q1 · citizen ไม่สร้างรอบ · reversal อัตโนมัติตอนเพิกถอนใบ · adjustment RPC · ผิวใหม่ล้วน: me/credits + me/transcript + admin/credit-rules + admin/credits · out: CRB-008 รอ Q1 · NTF = Phase 4 · IDENT/PDPA+ADM = Phase 5 · Wave F ตามแผนเดิม) · **Phase 0 ปิด (2026-09-12): DCR-9 + 0031 lead-owned `[#88]`** — docs 3 ชุด (API-SPEC 1.1.0 · DD 1.2.0 · AUDIT 1.0.1 pipe-audit 0 mismatch) + migration `0031_credit_bank.sql`: dry-run ROLLBACK exit 0 · applied in-place (fn 6 ตัว + cron `ltc-credit-accrual` 1 นาที) · **probes 21/21** (lazy cycle anchor ลิขสิทธิ์+role · citizen null · tick processed/idempotent-dup/backoff→terminal-failed · reversal-on-revoke ใน TX + audit pair + revoke ซ้ำปฏิเสธ · adjust RBAC สำเร็จ/ERR-CRD-002/ERR-RBAC-001 · summary −1.25/missing 13.25 · transcript net 0.00) — **probe จับ bug จริง: my_credit_transcript ใช้ jsonb_object_agg เหนือ union โดยไม่ sum (คีย์ซ้ำเก็บค่าหลังสุด) → แก้ migration + re-apply + 21/21** · integration fresh replay **93/93** (10 ไฟล์ รวม 0031 — submit_attempt_core v3 + admin_revoke_certificate v2 ไม่ถอยหลัง) · gitleaks ไฟล์เปลี่ยน 4 = `[]` exit 0 · ถัดไป: Phase 1 spawn E-10 (learner ผิว) · E-11 (admin ผิว) · E-12 (เทส) · **Phase 1 ปิดครบ: E-10 71/71 · E-11 126/126 (+follow-up 3 ข้อ) · E-12 12 ไฟล์/105 + e2e 2/2 (+follow-up allowlist 4 เคส) · 0032 allowlist (lead — probe 5/5 · applied) · API-SPEC 1.1.1 + AUDIT-DESIGN 1.0.2 · AdminSidebar 2 เมนู · เกท lead เต็ม: vitest 1631/1631 · integration fresh-replay 105/105 (32/32 migrations) · typecheck 0 · eslint 0 · build 0 · gitleaks staged 43 ไฟล์ = `[]`** — ถัดไป: commit `[#88]` → codex gate → merge develop |
| Lead/PM  | Claude Code session (GLM 5.3) — sole channel ของ user directives |
| CTO gate | codex CLI (มีในเครื่อง ✅) — ใช้ตอน milestone gates / auth-security-data merges |
| Repo     | local git · สาขาหลัก `develop`            |

## Wave Board (task → owner → status)

### Wave A — เอกสารนำ (deliverables 1–12) — CTO gate ก่อนเริ่มโค้ด

| Task | งาน                                    | Owner    | Status        |
| ---- | -------------------------------------- | -------- | ------------- |
| A0   | Repo init + baseline docs + brief      | lead     | ✅ done       |
| A1   | Project Plan + Risk Register + Test Plan | worker-1 | ✅ เสร็จสมบูรณ์ + align brief 0.2.0 แล้ว (ผ่าน CTO gate D17) |
| A2   | SRS + RTM                              | worker-2 | ✅ ส่งงาน+commit แล้ว (ผ่าน CTO gate D17) |
| A3   | SDS + Architecture + Data Dictionary   | worker-3 | ✅ ส่งงาน+commit แล้ว (ผ่าน CTO gate D17) |
| A4   | API Spec + RBAC + Audit Log Design     | worker-4 | ✅ ส่งงาน+commit แล้ว (ผ่าน CTO gate D17) |
| A5   | Design System + UI Prototype           | worker-5 | ✅ ส่งงาน+commit แล้ว (ผ่าน CTO gate D17) |
| A6   | Lead review + ข้อมูลตรงกัน (cross-doc consistency) + codex/CTO verdict | lead + CTO | ✅ done — codex r5 PASS + CTO APPROVE (D17, 2026-09-09) |

### ลำดับถัดไป (Wave A ผ่าน gate แล้ว — ขยายเป็น task เมื่อ spawn ทีม Wave B)

- **Wave B — ACTIVE (เริ่ม 2026-09-09)**: repo scaffold + Docker dev env + CI (deliverable 13–16 เริ่มต้น) — ยึด DATA-DICTIONARY 1.0.0 เป็น blueprint ของ `supabase/migrations/*`

### Wave B — scaffold + dev env + CI (เริ่ม 2026-09-09 — lead ตัดสินตามอำนาจ D16)

| Task | งาน | Owner | Status |
| ---- | --- | ----- | ------ |
| B-01 | Repo scaffold: Next.js 15 + TS strict + Tailwind + shared kernel (config/logger/errors/zod) + health route + vitest | worker-b1 (haiku) | ✅ done — lead verify: gates 4/4 exit 0 (28 tests) + ทะเบียน error 31/31 ตรง API-SPEC; lead แก้ health → `/api/health` ตาม API-SPEC §3.10 + รับรอง postcss.config.mjs เข้า ownership (2026-09-09) |
| B-02 | `supabase/migrations/*` จาก DATA-DICTIONARY 1.0.0 (40 ตาราง + helpers + RLS + index + trigger) — **auth/security/data → codex gate ก่อน merge** | worker-b2 (haiku) → lead | ✅ done — **gate 6 รอบ: r1 7B/11M/2m → r2 6B/8M → r3 5B/3M → r4 2B/1M → r5 1B/0M/0m → r6 PASS 0/0/0 (D22/D24)** · fix วง D18(20)+D19(14)+D20(8)+D21(3)+D22(1) โดย lead หลัง worker ส่งงาน · evidence สอง image: struct 13/0 + fns 47/47 + census identical · ตาราง 40 / RLS 40 / policies 183 / SECDEFINER 22 / views 8 · merge --no-ff → develop (2026-09-09) |
| B-03 | Docker dev env: docker-compose + Supabase local + Mailpit + .env.example + make targets | worker-b3 (haiku) | ✅ done — lead verify: compose config exit 0 + รันจริง 8/8 Up (kong/auth/rest/storage 200, ไม่ส่ง key=401) + cold cycle + reset-db exit 0; lead fix: healthcheck →/api/health + gitleaks allowlist kong.yml (JWT placeholder dev มาตรฐาน) (2026-09-09) |
| B-04 | CI 5 ด่าน: lint + tsc + test + build + secrets scan (GitHub Actions + gitleaks) | worker-b4 (haiku) | ✅ done — lead verify: gates local 4/5 PASS (exit 0, 28 tests) + secret-grep สะอาด; gate 5 (gitleaks) บังคับบน CI ตั้งแต่ PR แรกที่มี remote (2026-09-09) |

### Wave C — MVP: แคตตาล็อก/ลงทะเบียน/ความคืบหน้า (เริ่ม 2026-09-10 — แผน `.omc/plans/wave-c-plan.md` + D25)

| Task | งาน | Owner | Status |
| ---- | --- | ----- | ------ |
| C-0 | Auth/Session foundation: SSR session helpers + login/register/logout ขั้นต่ำ + MFA fail-closed (O-4→D25) — **security-critical: lead ตรวจเอง** | worker-c0 (haiku) → lead | ✅ done — lead review ทุกไฟล์ + isolated tsc จับ 6 semantic errors (D26) lead แก้เอง; สุดท้าย: tsc 0 / vitest 27/27 / eslint 0 (2026-09-10) |
| C-1 | BFF shared plumbing: middleware (request-id+CSRF) + rbac wiring `my_roles()` + envelope/pagination + rate-limit + common schemas + O-5 matrix diff | worker-c1 (haiku) | ✅ done — lead re-verify: isolated tsc **0** (12 ไฟล์) + vitest 87/87 + O-5 exit met (matrix counts pinned ใน rbac.test.ts — citizen 11/lawyer 12/instructor 20/staff:viewer 13/staff:content 11/staff:exam 18/staff:registrar 20/super_admin 41 = 41 perms) · lead รับ design flags ทั้ง 10: CSRF 403 ใช้ ERR-RBAC-001+details.reason / Origin เทียบ nextUrl.host / `page` key ตาม API-SPEC §1.2 / HMAC cursor ใช้ service key (PRF — dev-grade, ticket ขอ secret เฉพาะทีหลัง) / **rate fallback = READ 120 + learner-mutation นอก §5 matrix = LEARN_WRITE (บังคับใน spawn C-3/C-4)** / in-memory per process ตาม SDS §5.3 / rbac lazy import / middleware ไม่ wire rate-limit (handlers เรียกเอง) / D26 ใช้ / MFA อยู่ requireMfaForRoles (2026-09-10) |
| C-2 | Catalog BFF [contract]: GET /categories, /courses, /courses/{id} | worker-c2 (haiku) | ✅ done — 8 ไฟล์ (categories 79 + courses 163 + detail 184 + schemas) · lead re-verify (tsconfig เฉพาะไฟล์ c2): tsc 0/eslint 0/vitest **34/34** + อ่าน courses route สายตา (keyset `(published_at,id)` ถูกหลัก · ตัด `,()` จากคำค้นก่อนฝัง or-syntax · RLS บังคับ published ไม่เช็คซ้ำใน JS) · **ตัดฟิลด์ที่ไม่มีแหล่งใน DD §3.2 ออกจาก response: level/credits/learnerCount/outcomes/instructors/exam → gap กับ UI fixture ของ C-6 = โจทย์ DCR ตอน Phase 1 wire** · ธง C-9: `.or()` ซ้อน (q+cursor พร้อมกัน) ต้องพิสูจน์ repeated-or semantics ของ PostgREST บน DB จริง (2026-09-10) |
| C-3 | Enrollment BFF [contract]: POST enroll (DCR-3 idempotent 200) + GET /me/enrollments | worker-c3 (haiku) | ✅ done — 6 ไฟล์ (enroll route 118 บรรทัด + me/enrollments + schemas) · lead re-verify: tsc 0/eslint 0/vitest **29/29** + อ่านโค้ด enroll route ด้วยตา (ลำดับ permission→rate→RPC ถูก; token `ERR-*` จาก message RPC ผูกทะเบียนเท่านั้น + fail-closed ERR-SYS-002; DCR-3 ซ้ำ→SELECT เดิม→200) · RPC จริง (0011 L16-63): ซ้ำ = raise exception ฝังโค้ดท้าย message — BFF map ตาม DCR-3 · rate: enroll=LEARN_WRITE (คำตัดสิน lead) / me/enrollments=READ · ธง Phase 1: page-envelope ประกอบใน route (response.ts ของ C-1 ยังไม่มี helper — c2 อาจซ้ำ → รวมศูนย์ Phase 1) (2026-09-10) |
| C-4 | Progress/Quiz BFF [contract]: progress + quiz submit (O-2 delta ฝั่ง BFF) | worker-c4 (haiku) | ✅ done — 8 ไฟล์ (heartbeat 227 บรรทัด + quiz 124 + GET progress 153 + schemas 159) · lead re-verify: tsc 0/eslint 0/vitest **65/65** + อ่าน heartbeat สายตา (O-2 delta ถูกทุกขั้น · type-mismatch guard · RPC เท่านั้น · อ่านสถานะจริงหลัง RPC) · RPC จริง (0011): clamp 900s/call ทั้งสองฝั่ง · document dwell = server-telemetry จาก updated_at (D14-F9) · validate ขาออง zod ทุก response · ธง Phase 1: RPC-error parser ของ c3/c4 ต่าง regex เล็กน้อย (last-token vs anchored) — รวมเป็น helper เดียว (2026-09-10) |
| C-5 | Admin catalog BFF [contract]: /admin/courses + /admin/categories | worker-c5 (haiku) | 🔶 Phase 1 spawned (2026-09-10) — เพิ่ม CURSOR_HMAC_SECRET config (ticket จาก C-1 flags) |
| C-6 | UI แคตตาล็อก skeleton + components (DS §5) | worker-c6 (haiku) | ✅ done — fix-back 6×TS2739 (derive-once `deriveCourseMetrics` — จับ runtime undefined ที่หลบ type ด้วย) + lead re-verify สด: isolated tsc **0** / eslint **0** / vitest **5/5** + พิสูจน์ runtime consistency 6 หลักสูตร/5 หมวด (2026-09-10) · ธงค้าง Phase 1: `level` ไม่มีใน DD §3.2 (DCR หรือตัด) / หน้า `/` ยังอยู่นอก `(public)` · **🔶 Phase 1 wire → BFF จริง spawned (D28): swap fixture→fetch + ต่อฟิลด์ DCR-4 ใน courses routes** |
| C-7 | UI ผู้เรียน skeleton: learner shell + my/courses + lesson player | worker-c7 (haiku) | ✅ done — 11 ไฟล์ 1,565 บรรทัด (player 3 แบบ + heartbeat จาก config + ContinueCard LRN-009 + outline nav) · lead verify: isolated tsc **0** + eslint **0** + grep สะอาด (ไม่มี supabase/`completed` ฝั่ง client) · รอดช่วง stream-downgrade 4 ครั้งด้วยเขียนใหม่จนสะอาด (2026-09-10) · **✅ Phase 1 wire ปิด (2026-09-10): เฉลยออกจาก client bundle สมบูรณ์** — learning.ts เขียนใหม่ทั้งไฟล์หลัง corruption (transport absolute-origin + cookie forward) + learning.server.ts (`server-only` — quiz fetch ฝั่ง RSC ส่งเป็น props) + QuizPanel/QuizResult (รับโจทย์ทาง props · ผลจาก server เฉพาะ scorePct/passed) + logout → POST /api/v1/auth/logout · **lead verify security-critical ด้วยตาเอง**: grep correctChoiceIds/QUIZ_BANK/fixtureSubmitQuiz/is_correct เหลือเฉพาะไฟล์ test ที่ assert การไม่มีอยู่ + อ่าน learning.server.ts/quiz-panel/schema ทั้งสาย (ข้อมูลฝั่ง client = {id,label}/{id,prompt,choices} เท่านั้น) · tsc 0/eslint 0/29 own + **full-repo 436/436 (33 ไฟล์)** |
| C-8 | UI admin เนื้อหา skeleton: admin shell + courses/categories | worker-c8 (haiku) | ✅ done — lead verify รอบ 2 (isolated semantic tsc รวม components: **0 error** + eslint 0) (2026-09-10) · **✅ Phase 1 wire ปิด (2026-09-10)**: fixtures/admin.ts → GET /api/v1/{me,admin/courses,admin/categories} + admin layout guard (ไม่มี staff role → redirect /login · BFF ล่ม → fail-closed panel) + หน้า courses/categories/status filter — tsc 0/eslint 0/vitest 25/25 + full-repo 399/399 · c8 จับ gap **GET /api/v1/me ไม่มี route จริง** → **lead สร้างเอง** (วิถี D18): route + 8 tests · AUTH-007 allowlist (requireUser ตรง ไม่ผ่าน MFA gate) · contract {id,email,displayName,roles,mfaVerified} |
| C-9 | Integration tests [DB]: RLS denial + RPC happy/deny (TC-005/014/016/018) + **migration DCR-4 + seed หลักสูตร (D25-O3)** + พิสูจน์ repeated-or ของ c2 + enroll ซ้ำ idempotent จริง | worker-c9 (haiku) | ✅ done + **Phase 1 ปิด (2026-09-10)** — migration 0012 + seed 306 บรรทัด (fixed UUID + on conflict do nothing ทุก insert + GUC staff:exam เปิดข้อสอบตาม guard D20-M3) + helpers (psql/REST ผ่าน Kong + createTestUser GoTrue จริง มี retry 429) + suites 4 ไฟล์ + vitest.integration.config + npm run test:integration + .env.example TEST_* · **lead verify ทุกไฟล์**: อ่าน 0012/repeated-or/seed เอง + secret-scan (เจอแค่ placeholder dev JWT มาตรฐาน — เพิ่ม gitleaks allowlist เฉพาะ signature สองค่า) · **lead จับ defect: course_exam_summary ไม่กรอง is_final ขัด DD §3.2 "ปลายหลักสูตร" → แก้เอง 2 จุด + test ใหม่ + negative control (old logic ได้ 99=fail / new view ได้ 70=ผ่าน)** · repeated-or ของ c2 พิสูจน์จริง: or= ซ้ำ = **AND** (LTC-104 ผ่าน cursor แต่ไม่ match คำค้น ไม่หลุดเข้ามา) · วงจรเต็ม: reset-db → migrate 13 rc=0 → seed rc=0 → **integration 23/23 exit 0** |
| C-10 | Playwright smoke [DB]: E2E-04/05/06 — `@playwright/test` นอก src/** รอ lead อนุมัติ | worker-e2e (haiku) → lead | ✅ done + **Phase 1 ปิด (2026-09-11, `da7434b`)** — worker สร้าง 10 ไฟล์ 898 บรรทัด (config + 4 specs + helpers 6) · lead verify ทุกไฟล์ + รันเอง e2e 7/7 (7.6m) · เฉลย quiz อยู่ฝั่ง harness เท่านั้น (psql) · login ฟอร์มจริง · mutation ผู้เรียนผ่าน browser session (CSRF จริง) · ลบ SERVICE_KEY dead export · gaps ตรงไปตรงมา → PB-12 |
| C-11 | Consolidation ฝั่ง API: page-envelope helper ใน `lib/api` + RPC-error parser รวม c3/c4 เป็น helper เดียว + **GET /lessons/{id}/quiz (DCR-5 — โจทย์+ตัวเลือก ไม่มี is_correct/explanation)** | worker-cc (haiku) | 🔶 Phase 1 spawned (2026-09-10) |

### Wave D — Assessment + Certificates (เริ่ม 2026-09-11 — แผน `.omc/plans/wave-d-plan.md` + D36)

| Task | งาน | Owner | Status |
| ---- | --- | ----- | ------ |
| D-0 | PB-12 wire แหล่งเนื้อหา: ปุ่ม enroll ผู้เรียน (หน้ารายละเอียดหลักสูตร) + VideoPlayer src จริง + DocumentViewer paragraphs จริง — **เจ้าของ "ไฟล์เดิม" ของ Wave C ทั้งหมดใน wave นี้** | worker-d0 (haiku) | ✅ done — 8 ไฟล์ +662/−18 · lead re-verify: isolated tsc 0 (รวม tests) + eslint 0 + vitest 57/57 (17 ใหม่) + อ่าน learning.server.ts/enroll-button/CTA 3-way/learn page ทั้งไฟล์ · ปุ่ม enroll: guest `/login?next=` (e2e-04 คงเดิม) · not_enrolled → EnrollButton (201/200 DCR-3) · enrolled → "เข้าเรียนต่อ" · CourseViewerGate fail-open not_enrolled ตั้งใจ (ปล่อย error จริงของ BFF โผล่) · resolveLessonMediaUrl signed URL TTL config + null → placeholder ไทย (ไม่ปลอม URL) · **PB-14 ขยาย: media_read กั้น enrolled-learner ด้วย (lesson video ติด 404 เหมือน cert PDF)** · e2e-05 ยัง assert disabled จริงจนกว่า D-9 เปลี่ยน UI-driven · commit `09d22a2` (2026-09-11) |
| D-1 | Exam flow BFF [contract]: GET /assessments/{id}, POST /assessments/{id}/attempts (start_attempt), GET /me/attempts, POST /attempts/{id}/answers (save_answer), POST /attempts/{id}/submit (Idempotency-Key · ผลตรวจทันที DCR-6), GET /attempts/{id}/result (learner_attempt_view) — **เฉลย/`session_id` ห้ามผ่าน body; session_id จาก JWT claim เท่านั้น** | worker-d1 (haiku) | ✅ done — 14 ไฟล์ +3,376 · lead re-verify: isolated tsc 0 · eslint 0 · vitest **114/114** (7 ไฟล์) + อ่าน exam.ts + 6 routes ทั้งไฟล์ · **adjudicate ครบ 6 ธง** (พิสูจน์กับ migrations จริง): #1 doc-faithful · #2 pass_pct hide ไม่สอดคล้อง (0012 L86 public อยู่แล้ว) → grant ใน lead batch · #3 → **PB-16** paper view · #4 uniform NF-001 = privacy-safe ถูกต้อง · #5 → **PB-15** · #6 **lead edit แล้วใน lane (D18-style)**: §4 #8 AttemptSubmitRequest strict — session_id ใน body → VAL-001 fail ชัด (เดิมเพิกเฉยเงียบ) + ยอมรับ body ว่าง {} + tests ใหม่ 5 เคส · commit `a88c2d2` (2026-09-11) |
| D-2 | Certificate public+owner BFF [contract]: GET /certificates/{code} (200-always 4 ฟิลด์ + certificate_verifications เขียนทุกครั้ง), GET /me/certificates, GET /certificates/{id}/pdf | worker-d2 (haiku) | ✅ done — 8 ไฟล์ +1,488 · lead re-verify: isolated tsc 0/eslint 0/vitest **43/43** + อ่าน 3 routes ด้วยตา (200-always + zod-parse กัน PII + ip_hash ไม่เก็บ IP ตรง + keyset desc ตามแบบ C-2) · commit `d1083f8` · **ธงที่ lead ตัดสิน:** registrar ไม่มี certificate:view = ตรง RBAC-DESIGN §2 L83 ไม่ใช่ drift · /me/certificates คง snake_case ตาม CertificatePublicView (§4 #13 exception เดียวของ spec) · **PB-13** ip_hash salt = anon key (dev-grade — lead แก้ config หลัง lanes ปิด ตามแบบ PB-9) · media_read ไม่ครอบเจ้าของใบ → งาน D-8/lead migration |
| D-3 | Admin assessments BFF [contract]: GET/POST /admin/assessments + question-banks CRUD (instructor draft เฉพาะ course ตัวเอง; staff:exam approve) — monitoring/statistics เลื่อน Wave E (DCR-6) | worker-d3 (haiku) | ✅ done — 8 ไฟล์ +2,278 · lead re-verify: isolated tsc 0/eslint 0/vitest **49/49** + อ่าน PATCH route ทั้งไฟล์ + assessments route เจาะจง · **คำตัดสิน version mechanism:** (ก) bump ในแถวเดิม ตรง DD §3.4 L391 ("bump เมื่อแก้ — snapshot อ้างเวอร์ชัน") — used_at ไม่มีใน schema (สมมติฐาน spawn prompt ผิดเอง) · active+instructor → 403 pre-DB สะท้อน guard · is_correct ตัดตั้งแต่ select · commit `2156c3c` · ธง D-7: rules ตอบได้ไม่รวม pass_pct/selection (column grant 0010 L708-714) — UI ห้ามสัญญาแสดงค่านี้ |
| D-4 | Admin certificates BFF (registrar) [contract]: issue/eligible/revoke/reissue + **`src/lib/certificates/{issue,revoke,reissue,pdf}.ts` — จุดเดียวของ service_role** + PDF pre-render (pdf-lib+qrcode+Sarabun — lead ตั้ง deps แล้ว) — /bulk เลื่อน Wave E (DCR-6) | worker-d4 (haiku) | ✅ done — 18 ไฟล์ +3,158 + @pdf-lib/fontkit · lead re-verify: isolated tsc 0 · eslint 0 · vitest **72/72** (9 ไฟล์) + อ่าน source ทั้ง 9 ไฟล์ (CSPRNG · conditional UPDATE guards · compensating write · lineage สองคอลัมน์มีจริงใน 0006 · PDF fail-closed · ไม่มี route import service client ตรง) · **adjudicate ครบ 7 ธง:** #1 fontkit → lead ติดตั้งเอง · #2 bucket → PB-14 lead migration · #3 credit_snapshot null → SAFE (0006:13 nullable) · #4 **audit gap จริง** (0008:457-461 service_role allowlist = AUTH_* 12 เท่านั้น → CERT_* โดน 42501) → 0019 ขยาย allowlist · #5 PDF fail คงใบ (D36-O6) · #6 compensating write (D36-O3 inherent) · #7 CERT_PUBLIC_BASE_URL optional → fail-closed · **lead edits 4 จุดใน lane (D18-style): context.user_id actor attribution ครบทุก audit call** (0008:476-486 derive v_actor จาก context.user_id — ไม่ส่ง = actor null) + คีย์ตาม AUDIT §2.1 (code/attempt_id · superseded_cert_id) · ยอมรับ: eligible filter-after-pagination (Wave E /bulk แทน) · reissue ใช้ permission certificate:revoke (ไม่มี certificate:reissue — ชุด registrar/sa เดียวกับ API-SPEC L188) · commit `306c399` (2026-09-11) |
| D-4b | **lead Phase-0 batch**: migration 0019 + BFF wire edits ทั้ง 6 ข้อ (consume contract ใหม่ก่อน spawn Phase 1) — workers ห้ามแตะ migrations ตาม D36 | lead | ✅ done — **0019** `f9222fa`: (e) append_audit_event ขยาย service_role allowlist +CERT_ISSUE/REVOKE/REISSUE/PII_ACCESS + actor lift จาก context.user_id แล้ว strip (a) learner_attempt_paper_view (owner+in_progress · snapshot ตัด points/is_correct ทุกชั้น) (b) submit_attempt คืน question_count=count(*) + total_points ทุกทาง (c) media_read recreate (cert owner + enrolled-learner) + private buckets media/certificates + policy objects_via_media_assets (d) grant select (pass_pct) assessment_rules (selection ยังซ่อน 0010) — **probes 25/25 · integration 37/37 rc=0** · **wire** `4fd51d3` (13 ไฟล์ +333/−83): toExamPaperQuestion (ExamPaperJsonb strict + question_id cross-check → ERR-SYS-002) · SubmitAttemptResult บังคับ qc/tp · passPct 4 ชั้น (detail/admin select/schemas/mappers) · audit entityId string\|null (คิวว่าง = null ไม่ปลอม uuid — แก้ bug เดิม `?? request.url` ที่ fail uuid cast → RPC ปฏิเสธทั้ง call) · gates: eslint 15 ไฟล์ 0 · isolated tsc 0 · vitest **846/846 (72 ไฟล์)** · typecheck 0 · build 0 · gitleaks FS 157MB=0 + diff 0 (2026-09-11) |
| D-5 | UI ห้องสอบ + ผล + ประวัติ (ไฟล์ใหม่ล้วน): กติกาก่อนสอบ, exam/[attemptId], result, my/exams — timer จาก serverTime/deadlineAt | worker-d5 (haiku) | ✅ done — 17 ไฟล์ (11 source + 6 tests) · lead re-verify: **อ่าน source ทั้ง 11 ไฟล์** · isolated eslint **0** (17 ไฟล์) · vitest **57/57** (6 ไฟล์) · **tsc 0 ทั้งโปรเจกต์** · client grep เฉลย (is_correct/isCorrect/correctChoiceIds/pointsEarned/explanation) = **exit 1** · storage grep = เฉพาะ comment+guard-test ที่ assert การไม่มีอยู่ · **ธง D37-6 ครบทุกข้อ:** (ก) คำตอบ + paper cache อยู่ใน tab memory เท่านั้น (module Map — guard test ห้าม localStorage/sessionStorage/IndexedDB) (ข) การ์ดเตือนก่อนสอบ (ห้ามรีเฟรช/ปิด/เปลี่ยนหน้า · เวลานับจากนาฬิการะบบ) + beforeunload ตอน ready/submitting (ค) reload กลางสอบ → cache miss → fail-closed contact-staff panel **ไม่มี self-resume** (ERR-ASM-002 → โทร 0 2351 1128) · **timer จาก serverTime/deadlineAt เท่านั้น** (serverOffsetMsOf/remainingMsOf — client clock วัด interval เท่านั้น · RPC expires_at เป็นผู้ตัดสิน · timeup overlay = advisory "ระบบตัดสินจากเวลาเซิร์ฟเวอร์เสมอ") · ERR-ASM-005 (ส่งแล้ว) = ถือว่าสำเร็จ · ERR-ASM-004 (หมดเวลา) = canRetry:false · takeover = server-sanctioned (ASM-011) — banner + seeded selectedOptionIds จาก paper view · autosave throttle 10 วิ/ข้อ (API-SPEC §5 หมายเหตุ 3 — ครั้งแรกทันที · worker จับ bug เอง: `lastAttemptMs` ไม่อัปเดต = throttle ตาย → แก้+tests) · ผลสอบ: isCorrect/explanation/pointsEarned render เฉพาะ `content !== null` (BFF-disclosed after_final_attempt) · exam-api.ts เขียน validator มือ (hasExactKeys — ไม่ import schemas/v1/exam = ชื่อคอลัมน์เฉลยไม่เข้า client bundle) · **ตัดสินธง lead:** (1) path `[assessmentId]/[attemptId]` = อนุมัติ (หน้า room `robots noindex`) (2) checkbox ทุกข้อ = สัญญา paper view ไม่มี marker ชนิดข้อ → **Wave E BFF contract** (3) ลิงก์เข้าสอบจากหน้าหลักสูตร = **lead batch** (ไฟล์เดิม คนงานแตะไม่ได้) (4) attemptsUsed null ถ้า history ยัง hasMore = display-only conservative ยอมรับ (5) ASM-004/005/timeup = verified ในโค้ดเอง (6) takeover = server-sanctioned ตาม ASM-011 ไม่ขัด D37-6 |
| D-6 | UI verify public + my certificates (ไฟล์ใหม่ล้วน): (public)/verify/[code] + my/certificates + ปุ่ม PDF | worker-d6 (haiku) | ✅ done — 10 ไฟล์ใหม่ 1,361 บรรทัด (data layer + server loader + VerifyPanel client + MyCertificatesView + 3 หน้า + tests 35 เคส) · lead re-verify: isolated eslint **0** · vitest **35/35** (3 ไฟล์) · tsc 0 (ไฟล์ D-6 — repo มี error ชั่วคราวของ D-5/D-7 ที่ยังเขียนอยู่) + อ่าน 4 ไฟล์ security-critical ด้วยตา · **ตัดสินธง lead:** (1) `/verify` landing ทำเพิ่ม = อนุมัติ (nav (public) ชี้อยู่แล้ว) (2) verify ผ่าน browser โดยตรง same-origin = อนุมัติ (ip_hash/rate คีย์ IP จริง — RSC ยิงแทน = IP เซิร์ฟเวอร์) (3) UI exact-keys guard 4 ฟิลด์ก่อน safeParse = อนุมัติ (mirror r4-H2c — schema กลางไม่ .strict) (4) transport ซ้ำ = **ธง Phase 1 cleanup** (รวมศูนย์แบบ C-11) (5) full build รอ lead batch หลัง D-5/D-7 ปิด · PDF ปุ่มใช้ id (uuid) ตรง pdf/route.ts · ไม่มี PII (guard test สแกนไฟล์ทั้ง 7) |
| D-7 | UI admin exam + registrar (ไฟล์ใหม่ล้วน): admin/assessments + question-banks + certificates (คิว eligible → issue/revoke/reissue) | worker-d7 (haiku) | ✅ done — 15 ไฟล์ใหม่ (3 lib + 3 component + 3 หน้า RSC + 6 tests **80/80**) · lead re-verify: อ่านครบ 15 ไฟล์ · isolated eslint **0** · vitest **80/80** (6 ไฟล์) · tsc 0 ไฟล์ D-7 (error ที่เหลือทั้งหมดของ D-5 ที่ยังเขียนอยู่) · **สอบผ่าน invariant:** server-only RSC loader (origin จาก getConfig เท่านั้น + cookie forward + x-ltc-bff-internal + no-store + 401/403 → forbidden + per-row fail-closed แถวเดียวพัง = ทั้งหน้า server-error) · `selection` grep = 0 · `is_correct` grep = 0 (isCorrect camelCase เฉพาะฟอร์มผู้แต่งตาม DD §3.4) · pass_pct แสดงได้ตาม grant 0019 · ตารางคลังไม่มีเนื้อหาข้อสอบ/เฉลย · คิว PII โหลดเฉพาะ canIssue · modal body ตรง zod เป๊ะ (cross-check admin-exam.ts จริง — range/optional/conditional spread กัน undefined บน .strict()) · ทุก action ผ่าน ConfirmModal · **ตัดสินธง lead:** (1) ไม่มี endpoint รายการใบที่ออกแล้ว → แผง uuid-input = ชั่วคราย ยอมรับ (BFF ตรวจซ้ำทุกอย่าง) — list/search คือ Wave E (2) AdminSidebar ยังไม่มีลิงก์ 3 หน้า = **lead เพิ่มเองใน lead batch** (ไฟล์เดิม — คนงานแตะไม่ได้) (3) ทางเข้า instructor สู่ admin shell (layout guard staff-only) = ธง D-9 E2E ตรวจตอน integration (4) course label เกิน limit 100 → "—" = display-only ยอมรับ |
| D-8 | Integration [DB]: exam-rpc + session-binding + views/eligibility + certificate-path (issue/revoke/reissue จริง) + seed สอบ (time_limit=5, cooldown=0) | worker-d8 (haiku) | ✅ done — 2 ไฟล์ใหม่ (helpers-d8 322 + d8-exam-cert-flow 879 บรรทัด · 31 tests) · lead re-verify: อ่านทั้งสองไฟล์ทั้งไฟล์ + รันเองทุกด่าน — **test:integration 8 ไฟล์ 68/68 rc=0** (37 เดิม + 31 ใหม่) · isolated tsc 0 (tsconfig รวม helpers + ทั้งสองไฟล์ใหม่) · eslint 2 ไฟล์ 0 · gitleaks patch 62KB 0 · **DB หลังรันรอบ lead สะอาด** (d8 users/attempts/outbox/assessments = 0 · media_assets กลับ baseline 5) · ครอบคลุม: paper view ตัด points/is_correct ทุกชั้น (exact-keys ทั้ง object และ option ["id","text"]) · session binding 2 ชั้น D20-B5 (token อุปกรณ์ 2 → ERR-RBAC-001 ทั้ง save/submit แม้ p_session_id ถูก) · idempotent already_submitted ผลเดิม · เฉลยปิดก่อน final (A ครั้ง 1) / เปิดหลังครบ max (A ครั้ง 2 + B ครั้งเดียว) · PB-15 question_count/total_points เทียบ ground-truth psql · cert path: eligible anti-join ก่อน/หลัง issue · issue ซ้ำ → valid_certificate_exists · reissue lineage สองฝั่ง + CERT_REISSUE=1 · revoke → verify revoked · สาธารณะ (anon) 200 รูปเดียว + 4 ฟิลด์ exact-keys + ไม่มี holder_name ใน response text · รหัสผิดรูปแบบไม่ persist ไม่ audit (=0) · **audit append-only จัดการถูกข้ามรอบ**: entity_id uuid ใหม่ต่อรอบ assert =1 · รหัสตายตัวร่วม >=1 · **FAST-A/FAST-B + seedFastExams() สร้างไว้ให้ D-9 import ต่อ + วิดีโอจริง `media/courses/ltc-101/intro.mp4` ค้างไว้ให้ e2e (PB-14)** · ธง: course 3 is_public=false → ผู้ใช้ทดสอบต้องมี role lawyer · storage dev image v1.0.6 ไม่รองรับ x-upsert (500) → DELETE ก่อน POST · ไม่ต้อง migration · ไม่แตะ helpers.ts เดิม |
| D-9 | E2E [DB]: E2E-07..11 (สอบผ่าน/ตก→ซ้ำ/ออกใบ→verify ไม่มี PII) | worker-d9 (haiku) | ✅ done — 7 ไฟล์ใหม่ (d9-helpers 629 + specs 07..12 = 129/74/96/125/192/56 + tsconfig.d9-check 17 บรรทัด) + แก้ e2e-05 → UI-driven (210) · lead re-verify: อ่านทุกไฟล์ทั้งไฟล์ + รันเองทุกด่าน — **playwright 19/19 (9.0m) rc=0** (เดิม 7 + ใหม่ 12: 07:2/08:1/09:3/10:1/11:3/12:2) · isolated tsc 0 · eslint 8 ไฟล์ 0 · gitleaks patch 96KB 0 · DB หลังรันสะอาด (d9 users/certs/attempts=0 · media_assets=baseline 5 · คอนเทนเนอร์ 8/8) · ครอบคลุม: กติกาเทียบ DB ทุกแถว + **ห้ามกดเริ่มจริงบน seed exam** (assert attempts=0) · สอบผ่าน/ตก/ซ้ำจน ERR-ASM-001 ผ่าน UI จริง (เฉลยจาก psql harness-only — C-7) · registrar ออกใบผ่าน UI + audit CERT_ISSUE=1 + คิวหายหลัง issue (anti-join) · verify สาธารณะ 4 ฟิลด์ + **ไม่มีชื่อ/อีเมล/verify_code ทั้งหน้าและ API** · e2e-05 UI-driven ครบ (doc = ปุ่มจริง · วิดีโอ = probe UI ก่อน → fallback heartbeat API พร้อม annotation ผล probe ทุกรอบ) · ธง 5 ข้อ: (1) helpers-d8 import ไม่ได้ใต้ Playwright (.js suffix + import.meta.url) → มิเรอร์ d9-helpers ด้วย uuid ตายตัวชุดเดียวกัน (cleanup สอง suite ล้างโลกเดียวกัน) (2) staff BFF บังคับ MFA aal2 แต่ไม่มีหน้าจอ enrollment = Wave F — harness ลง TOTP กับ GoTrue จริง (RFC 6238 เขียนเอง) + injectSession @supabase/ssr (3) **dev app memory-threshold รีสตาร์ทเองกลาง suite ยาว** (SSR ล่ม 1-2 นาที ทำ flake สุ่มไฟล์) — worker รีสตาร์ท ltc-dev-app 1 ครั้ง · lead รีสตาร์ทก่อน verify ก็ผ่าน 19/19 — **ธงระยะยาวสำหรับ dev stack** (4) instructor→/admin/* = redirect /login (ADMIN_STAFF_ROLES ไม่มี instructor แม่ RBAC ให้ question_bank:view) — e2e-12 บันทึกพฤติกรรมจริง **ตัดสินนโยบายค้าง Wave E** (5) ปุ่ม logout ไม่มีบนหน้า "/" (ใช้ /my/courses) · lead ตัดสิน: commit tsconfig.d9-check.json ด้วย (ทำ isolated-check ทำซ้ำได้) |
| D-10 | Auto-submit scheduler (O-5 → lead ตัดสินรับไว้ใน Wave D): ปิด attempt เลย expires_at ทุก 1 นาที — mechanism รอ lead verify privilege path ก่อน spawn (อาจต้อง migration lead-owned) | lead → worker | ✅ done — **migration lead-owned `0020_auto_submit.sql`** (D53 ตัดสิน 6 ข้อ · trial-before-apply จับ syntax จริง 2 ตัว: `%rowtype` ห้ามใน signature · pg_cron non-relocatable วัตถุอยู่ schema `cron` เสมอ — ห้ามชื่อ 3-part) + UI copy exam-room.tsx 2 จุด + AUDIT-LOG-DESIGN 2 แถว · replay เต็ม: reset-db (0019 เจอ storage-init race 1 รอบ — แก้ด้วย re-run one-shot `docker start ltc-dev-db-migrate` rc=0 ครบ 0001..0020 · tracker 0020 ✓ · **cron.job live `* * * * *` active**) + seed · **probes รอบเดิม r1 48/48 + r2 25/25 + P ใหม่ 21/21** (P1 ปิด+ตรวจจริง failed 25 + audit auto=true/late_seconds>300/actor ว่าง · P2 in-grace ไม่ถูกปิด · P3 active ไม่ถูกแตะ · P4 ส่งมือแล้วไม่ซ้ำ · P5 idempotent closed=0 · P6a/P6b authenticated เรียกตรง 42501 ทั้ง scheduler/core · P7 credit.accrual แถวเดียว + rule snapshot CR-LTC-103 · P8 return contract 0019 ครบ + audit มือ actor=ผู้ส่ง ไม่มี auto marker · P9 cron ปิดเองใน ~1 นาทีโดยไม่มี manual call) · gates: vitest **1168/1168** · tsc 0 · eslint 0 · build 0 · **integration seed สด 68/68 (18.8s) กับ cron รันสด** · ธง: (1) fresh-volume reset-db อาจชน storage-api init race ที่ 0019 (mitigation = re-run one-shot — tracker ข้ามไฟล์ที่ apply แล้ว) (2) cron dev รันสดตลอด — attempt ค้างของ suite เก่าถูกปิดอัตโนมัติ = พฤติกรรมถูกต้องตามดีไซน์ (3) audit actor: manual = ผู้ส่ง (derive auth.uid() ตาม 0019) · auto = ว่าง (cron ไม่มี jwt) — parity ครบสองทาง |

หมายเหตุ D25-O3: authoring CRUD (CAT-001/005/006) เลื่อนออกนอก Wave C — dev ใช้ seed สร้างหลักสูตร; DCR เพิ่ม endpoint ตอน authoring wave จริง

การใช้งานจริงของกฎ (lead note):
- **โครงสร้าง repo = single Next.js app ที่ root** (package.json/src ที่ root) ตาม SDS §2 "Next.js application ชุดเดียว" — README board deliverable 13 (`apps/*`) เป็น index hint เท่านั้น จะทำ `apps/` เมื่อมี runtime ที่สอง (worker แยก) จริง
- **D4 "per-worker worktrees" ใช้ operationally เป็น shared checkout + strict disjoint file ownership + lead-only git** (Agent-tool workers ใช้ cwd เดียวกัน; ไฟล์ disjoint ทุก lane) — จะทบทวนถ้าเกิด conflict จริง
- Script contract กลาง (CI↔scaffold): `npm run lint | typecheck | test | build | dev`
- Wave C — MVP: courses, enrollment, progress
- Wave D — Assessment + Certificates
- Wave E — Credit Bank + admin/reporting
- Wave F — Security hardening + VA/penest + UAT + User Manual (deliverables 17–21)

## Phase 1 cleanup backlog (lead-maintained — เก็บกวาดหลัง merge / ไม่ block merge)

| # | รายการ | ที่มา | ข้อเสนอทิศทาง |
| - | ------ | ----- | -------------- |
| PB-1 ✅ `50d3d60` | **refresh-ownership race: middleware เห็น token เหลือ >90s ไม่หมุน → BFF call ใน loader ข้ามเพดาน margin 90s ไม่กี่วินาทีต่อมาแล้วหมุนเอง — Set-Cookie ของ inner rotation โดน RSC loader ทิ้ง** (middleware.ts:115 + catalog.server.ts:39) | codex gate r11 MINOR-1 | **แก้แล้ว (lead): middleware เป็นเจ้าของ rotation คนเดียว หมุนล่วงหน้าที่ LEAD_SECONDS=180** (SDK margin = EXPIRY_MARGIN_MS 3×30s = 90 วิ จริง จาก source ที่ติดตั้ง) · verdict ตัดสินจาก error ของ `getSession` เอง (SDK propagate error refresh เมื่อ token หมดจริง/preserve เมื่อยังไม่หมด) — **เลิกใช้ getUser ใน middleware** เพราะหลัง session null มันโยน AuthSessionMissingError (definitive ตามชื่อ) → ล้าง credential ที่ยังมีชีวิต (r11 regression) · near-lead refresh พลาดแต่ token ยังไม่หมด → SDK preserve → ไม่มี deletion เลย เก็บกวาดรอบถัดไปตอนหมดจริง (doctrine เดียวกับ r13) · regression 5 tests ครบ time-crossing (150 วิ → หมุน 1 ครั้ง ไม่แตะ /user · 600 วิ → 0 network · รอบถัดไปด้วย cookie ใหม่ → 0 refresh · gateway-401 near-lead → ไม่ลบ · already_used near-lead → defer) · **r1 M1 `cb68382`: LEAD 180 ยังพอแค่ขยับหน้าต่างแข่ง — ขาในของ server (BFF legs 3 จุด) ติด header `x-ltc-bff-internal: 1` (ตั้งเฉพาะเมื่อ `typeof window === "undefined"` — โมดูลแชร์ client/server) → middleware ข้าม rotation ทั้ง block: browser = เจ้าของ rotation คนเดียวจริง ไม่มี Set-Cookie ขาในให้ RSC loader ทิ้งอีก · browser ปลอม header ได้แต่ benign (แค่ขอ request นั้นไม่หมุน — ไม่ข้าม CSRF/authz; request ถัดไปหมุนตาม LEAD)** · **r2 M1-residual `3741f0e`: middleware ข้ามแล้วแต่ SDK ใน handler ยังหมุนเอง (_useSession < 90 วิ ไม่ขึ้น autoRefreshToken) — ssr.ts ติด fetch บล็อก POST /auth/v1/token ให้ client ของขาใน (400 non-retryable non-definitive → SDK preserve session เดิม ไม่มี rotation/ไม่มีการลบ) + regression จริง 3 tests ผ่าน handler+SDK จริง** |
| PB-2 ✅ `7aa8307` | vitest esbuild ยังไม่เปิด `jsx: "automatic"` (ไฟล์ .tsx ต้อง import React) | Wave C ทั่วไป | **แก้แล้ว (worker b):** `esbuild: { jsx: "automatic" }` ใน vitest.config.ts — พิสูจน์ด้วย test ใหม่ที่ fail `ReferenceError: React is not defined` ก่อนแก้ ผ่านหลังแก้ |
| PB-3 ✅ `1e69c48` | React `cache()` dedupe การเรียก context ซ้ำใน RSC tree เดียว | Wave C ทั่วไป | **แก้แล้ว (worker c + lead):** `findPublishedCourse` (เรียกซ้ำจริงใน request เดียว: generateMetadata + page ของ courses/[id]) และ `getAdminStaffSession` ((admin)/layout + หน้าใต้มัน — 2× GET /api/v1/me ต่อ navigation) ครอบด้วย `cache()` · inventory evidence: loader อื่นเรียกครั้งเดียวต่อ request ทุกตัว จึงไม่ฝืนครอบ |
| PB-4 ✅ `7aa8307` | หน้า error.tsx ไทยยังไม่ครบทุก segment | Wave C ทั่วไป | **แก้แล้ว (worker b):** error.tsx ครบ 5 จุด (root + (public) + (auth) + (learner) + (admin)) — ข้อความไทย ERR-SYS-001 + ปุ่ม "ลองใหม่" reset() + role="alert" + ช่องทางติดต่อ 0 2351 1128 · **ไม่ render error.message/digest/stack** (console.error ฝั่ง browser อย่างเดียว) · 15 tests colocated (ข้อความ + no-leak + reset ต่อไฟล์) |
| PB-5 ✅ `7aa8307` | vitest ยังไม่มี alias `server-only` (ต้อง mock ทุกไฟล์ที่ import) | C-7/C-8 | **แก้แล้ว (worker b):** alias `server-only` → `tests/stubs/server-only.ts` · **ไม่ลบ vi.mock เดิม** แม้แต่ไฟล์เดียว (อยู่ร่วมกันได้จริง — 525/525) |
| PB-6 ✅ `47b471b` | GoTrue `GOTRUE_JWT_DEFAULT_GROUP_NAME=supabase` ใน compose ทำให้ JWT role claim ไม่ตรง DB role — helper ต้อง patch auth.users.role ชั่วครั่ง | C-9 handoff | **แก้แล้ว (lead):** compose `GOTRUE_JWT_DEFAULT_GROUP_NAME: authenticated` + ตัด role patch ออกจาก helpers — live-probe ยืนยัน JWT ออก role `authenticated` จาก signup เลย (role จาก DB ผ่าน GoTrue claims) |
| PB-7 ✅ `91fb844` → **แก้รูป r1 M2+M3 `b9cf573`** | `me/enrollments` + `enroll` route ไม่กรอง `deleted_at` (soft-delete course ยังโผล่/ลงทะเบียนได้) | lead review Phase 1 | **แก้แล้ว (worker) แล้ว codex จับผิด 2 ชั้น → lead แก้รูปใหม่:** (M2) `.eq(courses.deleted_at, null)` = `eq.null` string ไม่ใช่ SQL NULL · (M3) `courses!inner` embed ทำให้แถวตกใต้ RLS published-only ของ courses → คอร์สถูก archive = ประวัติผู้เรียนหายเกินขอบเขต soft-delete · **ทางออก `b9cf573`: migration 0017 RPC `my_active_enrollments` (SECDEFINER + STABLE — e.user_id=auth.uid() + join c.deleted_at is null) · route ใช้ rpc().select() + chain เดิม · integration 3 tests บน DB จริง (archive → ยังอยู่ / soft-delete → หาย / B ไม่เห็นของ A)** — enroll RPC กรองอยู่แล้วในตัว (0011 L33-37) ไม่ต้องแก้ · **r2 `3741f0e`: พิสูจน์ keyset repeated-or จริงของ route บน RPC ผ่าน PostgREST + probe จับข้อจำกัด pruning ของ function inline (คอลัมน์ที่ or=/order= อ้างต้องอยู่ใน select= — route จริงใส่ครบอยู่แล้ว)** |
| PB-8 ✅ `1e69c48` | logout-button behavioral test — รอ C-10 Playwright | C-10 | **แก้แล้ว (worker c — ไม่รอ E2E):** 4 tests ครอบ 204→redirect /login (รวม idempotent 204) · 503→อยู่หน้าเดิม + role="alert" ไทย + กดซ้ำได้และ redirect เมื่อ route กลับมา · 401/network→fail-safe ไม่ redirect · กดแล้ว disabled + "กำลังออกจากระบบ..." — component เดิมถูกอยู่แล้วไม่ต้องแก้ (ส่วน E2E จริงยังอยู่ใน PB-10) |
| PB-9 ✅ `18faaa1` | `CURSOR_HMAC_SECRET` prod value — ตอนนี้ dev-grade (service key เป็น PRF) | C-1 flag → C-5 | **แล้วเสร็จ (2026-09-11) — fail-closed จริง ไม่ใช่ doc เพียงล้วน:** (1) superRefine `src/lib/config.ts`: `APP_ENV=prod` (staging ใช้ prod mode) ขาด `CURSOR_HMAC_SECRET` → ConfigError ตอน boot; dev ยัง fallback service-key PRF (ค่าว่าง `CURSOR_HMAC_SECRET=` เป็น ConfigError แม้ local — optionalString min(1)) (2) `src/instrumentation.ts` register() เรียก getConfig ตอน nodejs bootstrap (3) `/api/health` readiness: config พัง → 503 + `status:"error"` + `db:"unknown"` (รูป {status, db, time} ตาม API-SPEC §3.10) (4) `import "server-only"` ที่ config.ts — config เข้า client bundle → build พัง (แบบแผน ssr.ts/session.ts) (5) doc `docs/09-dev/SECRETS-PROVISIONING.md` 1.0.0 + `.env.example` ปรับ (stream 2 ค่าตาม schema · CURSOR_HMAC_SECRET บรรทัด) · **codex gate 3 รอบ: r1 0B/4M/1m (boot-check จริง/rotation redeploy/ชื่อ stream vars/gitleaks-NEXT_PUBLIC overclaim) → r2 0B/2M/1m (singleton-cache test fragility จับได้ด้วย shuffle seed 42 → vi.resetModules + dynamic imports + SAVED-restore; doc overclaim client protection → ทำให้จริงด้วย server-only) → r3 PASS 0B/0M/1m (แก้พร้อม approve: pooler URL ฝังรหัสผ่าน DB — หมุน password = อัปเดต env + redeploy)** · verdicts: /tmp/codex-gate-pb9{,-r2,-r3}.txt · branch 4 commits `6aa9c40`→`64dc8af` · tests 3 ใหม่ (config 3 + instrumentation 4 + health 3) ผ่าน shuffle 42/7 · post-merge develop: unit 540/540 (47 ไฟล์) · tsc 0 · eslint 0 · build exit 0 · gitleaks FS (156 MB) + diff 4 commits = 0 |
| PB-10 ✅ `da7434b` | C-10 Playwright smoke (E2E-04/05/06) เลื่อนมาท้าย Phase 1 แล้ว | D28 | **แล้วเสร็จ (2026-09-11)** — e2e 7/7 + logout · cleanup FK-ordered ผู้ใช้ทดสอบ 0 ค้าง · video เกณฑ์ 80% ใช้เวลาจริง ~7.2 นาทีตาม anti-fabrication bound ของ RPC |
| PB-11 ✅ `1766113` | `v_enrollment_progress` (0009_views.sql:169-193) ไม่กรอง soft-deleted courses (enrollments ของ course ที่ถูกลบล้างยังนับ progress) | worker PB-7 (flag) | **แก้แล้ว (lead):** migration 0016 view ใหม่ join courses กรอง `deleted_at is null` + align lesson_completed กับ lesson_total (join lessons/modules กรอง deleted_at — กัน pct >100) · integration 2 tests ผ่าน claims path จริงของ staff:content (guard 0010 อ่าน JWT claims — superuser อ้อมไม่ได้) · **r2 MINOR `3741f0e`: migration 0018 ผูก `m.course_id = e.course_id` ใน subquery completed (total มีอยู่แล้ว — ย้ายบทเรียนข้ามหลักสูตรตาม lessons_update ไม่ทำให้ completed>total/pct>100 อีก) + integration ย้ายจริงผ่าน claims path + คืนค่า · r3 `2c4506f`: คืนค่าย้ายเข้า finally + verified re-select (assertion พังกลางทางไม่ทิ้ง seed lesson ผิดหลักสูตร — negative control พิสูจน์)** |
| PB-12 ✅ `09d22a2` | **แหล่งเนื้อหาผู้เรียนยังไม่ wire (จับโดย e2e C-10):** (1) หน้ารายละเอียดหลักสูตรไม่มีปุ่มลงทะเบียนของผู้เรียน — CTA ชี้ `/login?next=…` เสมอ (`courses/[id]/page.tsx:73,286-291`) (2) `VideoPlayer` รับ `src={null}` ไม่มี `<video>` จริง (learn `page.tsx:121`) (3) `DocumentViewer` รับ `paragraphs: []` (page.tsx:134 · fixtures/learning.server.ts:202) — ปุ่ม "อ่านจบแล้ว" disabled ถูกต้อง | e2e C-10 (2026-09-11) | **แก้แล้ว (D-0, 2026-09-11):** ปุ่ม enroll 3 ทาง (guest→`/login?next=` · not_enrolled→EnrollButton 201/200 DCR-3 · enrolled→"เข้าเรียนต่อ") + VideoPlayer src จริง (signed URL TTL จาก config · null→placeholder ไทย) + DocumentViewer paragraphs จริง · **e2e-05 เปลี่ยน UI-driven เหลือ D-9** (spec ยัง assert สถานะ disabled จริง — ไม่แกลองผ่าน) |
| PB-13 | **ip_hash/user_agent_hash ของ certificate_verifications เคยใช้ SUPABASE_ANON_KEY เป็น salt (dev-grade — anon key เป็นค่าสาธารณะ ทำให้ hash ถอยง่ายสำหรับ IP ที่รู้ค่า)** | worker-d2 flag (2026-09-11) | **✅ ปิด (D50 — gate r1 PASS 0 findings)** — dev-grade salt ของ ip_hash/user_agent_hash เดิมแก้ตามแบบแผน PB-9: config.ts `IP_HASH_SALT` (optionalString · APP_ENV=prod fail-closed superRefine · AppConfig.ipHashSalt) + certificates/[code] ใช้ salt เฉพาะทั้ง ipHashOf/userAgentHashOf (fallback anon เฉพาะ dev) + config.test 6 เคส + route.hash-salt.test 4 เคส (แยกไฟล์ตามข้อจำกัด singleton) + instrumentation boot-reject + .env.example + SECRETS-PROVISIONING 1.1.0 · `eff8c8c` [#52] → develop `b883ece` |
| PB-14 ✅ `f9222fa` | **RLS `media_read` (0010 L372-375) เปิดเฉพาะ instructor/is_staff — กั้นสองกลุ่มพร้อมกัน:** (1) เจ้าของประกาศนียบัตร (learner) อ่าน media_assets ไม่ได้ → cert pdf route ติด 404 ตรงขั้น resolve media (2) **enrolled-learner อ่าน media ของบทเรียนไม่ได้** (D-0 resolveLessonMediaUrl จะติดเหมือนกัน) + storage bucket `certificates` ยังไม่มีนโยบาย + seed มี media_assets rows แต่ไม่มี storage objects จริง | worker-d2 flag + D-0 ขยาย (2026-09-11) | **แก้แล้ว (lead migration 0019, 2026-09-11):** media_read recreate — (a) เจ้าของใบ (pdf_media_id ของ certificates WHERE user_id=auth.uid()) (b) enrolled-learner ของหลักสูตรที่มี lesson media นั้น + private buckets `media`/`certificates` + storage policy `objects_via_media_assets` (อ่าน objects ผ่าน media_assets เท่านั้น) — probes 25/25 · **D-8 ยังต้อง upload วิดีโอเล็กจริงสำหรับ e2e** (seed มี rows ไม่มี objects) |
| PB-15 ✅ `f9222fa`+`4fd51d3` | **RPC submit_attempt คืนฟิลด์ `question_count` ที่เป็น sum(points) จริง** — 0011 L829-835 `v_total = coalesce(sum((question_snapshot->>'points')::smallint),0)` แล้ว return เป็น 'question_count' ที่ L884 · correct_count ต่างหากคือ count(*) filter จริง → ชื่อฟิลด์โกหกเมื่อข้อมูลละแต้มต่อข้อไม่เท่ากัน (ผู้เรียนเห็น "ถูก 26/30" ทั้งที่ 30 นั้นคือคะแนนรวม) | worker-d1 flag #5 (2026-09-11) — lead พิสูจน์จาก source 0011 | **แก้แล้ว (0019 + wire `4fd51d3`):** submit_attempt CREATE OR REPLACE คืน `question_count=count(*)` จริง + `total_points=sum(points)` แยก — **ทุกทาง** (early-return replay มี qc/tp แต่ไม่มี correct_count) · BFF SubmitAttemptResult บังคับ qc/tp (ขาด = ERR-SYS-002) + AttemptSubmitView questionCount/totalPoints เสมอ + test matrix ขาดแต่ละฟิลด์ = fail |
| PB-16 ✅ `f9222fa`+`4fd51d3` | **กลางสอบไม่มีแหล่งโจทย์ผ่าน user-JWT** — learner_attempt_view null เฉลย/โจทย์ (question_snapshot/is_correct/points) จนกว่า after_final_attempt (0009 L22-75) → D-1 attempts start route ได้แค่โครงข้อ (question_id/seq) ไม่มี text/options ให้หน้าสอบ | worker-d1 flag #3 (2026-09-11) — lead ยืนยันจาก 0009 | **แก้แล้ว (0019 + wire `4fd51d3`):** `learner_attempt_paper_view` (owner + in_progress เท่านั้น · question_paper = snapshot ตัด points ระดับบน + is_correct/points ทุก option → `{question_id,version,text,options[{id,text}]}`) · attempts route select จาก view + toExamPaperQuestion strict (ExamPaperJsonb `.strict()` ทุก object + question_id cross-check → ERR-SYS-002) · option_order ใน snapshot = display order อยู่แล้ว (0011 L627-655) — BFF ไม่เรียงซ้ำ |
| PB-17 ✅ `3085309` (merge `dd4d805`) | **ผู้เรียนไม่มีทางค้นพบ assessmentId ของข้อสอบปลายหลักสูตรผ่าน UI** — `course_exam_summary` (0012) ไม่มีคอลัมน์ assessment_id (มีแต่ตัวเลขสรุป) · ไม่มี endpoint "list assessments ของ course" สำหรับผู้เรียน → หน้ากติกา `/courses/[id]/exam/[assessmentId]` (D-5) เข้าถึงได้เฉพาะรู้ UUID ล่วงหน้า (seed/E2E harness) — ผู้เรียนจริงไม่มีลิงก์เข้าสอบจากหน้าหลักสูตร/หน้าเรียน | D-5 flag 3 (2026-09-11) — lead ตรวจ view+BFF ยืนยัน | Wave E (ต้อง DCR): เพิ่ม `assessment_id` ใน view + CourseDetailItem.exam + ลิงก์ "เข้าสอบ" ในหน้าหลักสูตร (enrolled) / หน้าเรียน — แตะ migration (lead-owned ตาม D36) + courses route + UI |
| PB-18 ✅ `b32eb41`+`c4a2acb` (merge `4659ef0`) | **paper view ไม่มี marker ชนิดข้อ (เดี่ยว/หลายคำตอบ)** — AttemptQuestionView/options ไม่บอกว่าข้อนั้นเลือกได้กี่ตัว → D-5 ห้องสอบใช้ checkbox ทุกข้อ (ข้อเดี่ยวเลือกได้หลายตัว — server ตัดสินที่ submit อยู่แล้วแต UX ผิด) | D-5 flag 2 (2026-09-11) | Wave E: เพิ่ม marker ใน paper view contract (จาก assessment_rules.selection / question type ที่ snapshot) + UI เลือก radio/checkbox ตามชนิด |
| PB-19 ✅ `b32eb41` (merge `4659ef0`) | **transport BFF triplication** — certificates.ts requestJson + learning.ts + exam-api.ts examRequest คือโครงเดียวกัน (same-origin fetch + cookie forward + x-ltc-bff-internal + no-store + error envelope) | D-6 flag 4 (2026-09-11) | Wave E: รวมศูนย์แบบ C-11 (helper เดียวใน lib/api) |
| PB-20 ✅ `c2fa545`+`9855fe1`+`ca438eb` (merge `dd4d805`) | **ไม่มี endpoint รายการใบประกาศนียบัตรที่ออกแล้วสำหรับ registrar** — D-7 ใช้แผง uuid-input ค้นรายใบ (BFF ตรวจสิทธิ์ทุกอย่างอยู่แล้ว — ปลอดภัยแต่ใช้งานจริงลำบาก) | D-7 flag 1 (2026-09-11) | Wave E: GET /admin/certificates (list/search + keyset) + ตารางในหน้า admin/certificates |

### Wave E — ปิดช่องโหว่ UX/API + งานเลื่อน (เริ่ม 2026-09-11 — แผน `.omc/plans/wave-e-plan.md` APPROVED + ตัดสิน D55)

| Lane | ขอบเขต (กรรมสิทธิ์ไฟล์ disjoint) | เจ้า | สถานะ |
|---|---|---|---|
| DCR ชุด | API-SPEC v1.0.4 (PB-17 assessmentId field + PB-20 GET /admin/certificates endpoint ใหม่ + bulk job/RPC/batch 200 + ยกเลื่อน-ออก monitoring/statistics) · DD v1.1.1 (41 ตาราง · view+assessment_id · snapshot type · cert_bulk_jobs §3.7 · cm_read/lessons_read D39) · RBAC-DESIGN (ADR shell policy §2.5 — D55-3) | lead | ✅ 2026-09-12 — worker flash เขียน + lead verify ทุกไฟล์ + reconcile DD↔0023 สองทิศ (DD canonical คอลัมน์/สถานะ · 0023 canonical RLS fail-closed แก้กลับที่ DD L725) |
| 0021..0024 | migration lead-owned: 0021 view+assessment_id (คอลัมน์ท้ายสุด — D55-10) · 0022 snapshot `type` + paper view (default multiple_choice เก่า — D55-1) · 0023 admin_list_certificates RPC + keyset index + cert_bulk_jobs + admin_cert_bulk_pick cap 200 (eligible filter-before-cut ปิดแล้วโดย 0019-r2 F6 — RPC anti-join · ธง D-4 เก่า) · 0024 D39 lessons completed (uq คงไว้ — D55-4 · lessons_read ฐาน 0015) | lead | ✅ 2026-09-12 — dry-run 4/4 · in-place rc=0 · fresh replay 24/24 rc=0 ×2 · seed ok · P-probes ผ่าน · integration 68/68 |
| E-0 | `src/lib/api/transport.ts` ใหม่ + delegate certificates.ts/learning.ts/exam-api.ts (PB-19) | worker flash | ✅ 2026-09-12 — `b32eb41` [#70] + lead verify + gate r2 PASS (parity 3 module ครบ · ApiError identity · runtime probe 42) |
| E-2 | exam-api.ts parse + exam-room radio/checkbox ตาม `type` (PB-18) + tests | worker flash | ✅ 2026-09-12 — `b32eb41` [#70] + lead fix type บน wire (schemas) + gate r1 MAJOR-1 → setSingle atomic `c4a2acb` → r2 PASS |
| E-1 | BFF courses route `assessmentId` + การ์ดสอบลิงก์ "เข้าสอบ" (PB-17 — UI รวมจาก E-7b ตาม D55-9) | worker flash | ✅ 2026-09-12 — `3085309` [#72] + gate r3 PASS (ส่วน E-1 ผ่าน r1-r3 ทั้งชุด) + merge `dd4d805` |
| E-3 | BFF GET /admin/certificates (list/search+keyset · role-gate sr/sa — D55-2) + ตารางหลังบ้านแทนแผง uuid (PB-20 — UI รวมตาม D55-9) | worker flash | ✅ 2026-09-12 — `c2fa545` [#73] + r1 MINOR-6/7 `9855fe1` + r2 MINOR `ca438eb` + gate r3 PASS + merge `dd4d805` |
| E-5 | /admin/reports/{enrollments,assessments,credits} + /admin/exams/{monitoring,statistics} อ่าน views 0009 (D36-O8b — scope ตาม DCR-6 เท่านั้น D55-6) | worker flash | ✅ 2026-09-12 — `675e16c` [#74] + r1 MAJOR/MINOR `e8c9ada` + r2 MAJOR `61587ac` + r3 MINOR `27bfac6` + merge `dd4d805` |
| E-4 | bulk issue RPC + job (batch 200/TX-ต่อ-ใบ · filter-before-cut · D-4 ธงปิด) (D36-O4) | worker flash | ✅ 2026-09-12 — worker `1e8cc85` [#78] + gate r1 M1/M2 → **0027 โมเดล worker** (BFF insert-only 202 · procedure commit-ต่อ-ใบ · cron ทุกนาที) · gate r2 MAJOR-1/2 → **0028 cursor durable** — รอ gate r3 |
| E-6 | CRT-008 auto-issue job async ≤5 นาที · idempotent · flag ปิด default (D36-O9 — D55-7) | worker flash | ✅ 2026-09-12 — `bfb2531` (getFeatureFlagEnabled fail-closed + boot-fail) — flag ใช้จริงฝั่ง tick ของ 0027 |
| E-7 | ADR shell policy ลง RBAC-DESIGN + แก้ comment e2e-12 (path layout จริง `(admin)/layout.tsx:50-59` — เดิมผิด) | worker flash | ✅ 2026-09-12 — lead ปิดเอง `dcb93b3` [#77] (scope เล็ก: comment 3 บรรทัด — D55-3) |
| E-9 | integration ใหม่บน DB จริง (view/paper type/list RPC/bulk/cron) + e2e เพิ่มเติม | worker flash | ✅ 2026-09-12 — `7d7bfed` [#79] + `ea38a01` (D59) + gate r1 M3/m2 → เขียนใหม่เป็นชุด procedure: dcr8 **11/11** · dcr7 **7/7** (พัก cron deterministic · scope ตามหลักสูตร · replay ผ่าน) · gate r2 MAJOR-1/2/3+m1 → dcr8 ใหม่ **15/15** (cross-CALL bulk/auto · เพดาน 100k สองเคส · guard นอกชุด · คนนอก scope ที่ออกได้) — รอ gate r3 · gate r3 MAJOR-1/2 + m1/m2 → 0029 two-lane tick + iso DB: dcr8 **15/15** (fresh-ก่อน-backlog cross-tick · step(null) บน DB แยก · cursor ครบสองคู่ + exact-cap reset) · gate r4 MAJOR-1/2 + m1/m2 → 0030 walked-markers: dcr8 **17/17** (held-TX late-commit · 0028-cursor normalize · watermark ครบสองคู่ทุกจุด) · gate r5 0M+3m → แก้ r6: startHeldSession (early-close fast-reject · end() กลืน error) + regression SQL-ผิด <15 วินาที + cur3 aid เป๊ะ/ts>หัว → dcr8 **18/18 ×3** |

หมายเหตุ: PB-17..PB-20 ✅ ปิดครบแล้ว (PB-18/19 merge `4659ef0` · PB-17/20 merge `dd4d805`) · นอกขอบเขต: MFA enrollment UI = Wave F · dev-app memory restart = ธง dev-stack

### Wave E Phase 3 — Credit Bank / CRB (เริ่ม 2026-09-12 — แผน `.omc/plans/wave-e-p3-plan.md` + ตัดสิน D68)

| Lane | ขอบเขต (กรรมสิทธิ์ไฟล์ disjoint) | เจ้า | สถานะ |
|---|---|---|---|
| DCR-9 + 0031 | API-SPEC 1.1.0 (endpoints ผิวใหม่ + event topics) · DD 1.2.0 (topics + functions + reversal semantics + credit_cycle_defaults) · AUDIT-DESIGN 1.0.1 (CREDIT_ACCRUAL จุดเขียนจริง + CREDIT_REVERSAL · รวม 53 events) · migration `0031_credit_bank.sql` (submit_attempt_core v3 snapshot +required_credits_per_cycle · credit_accrual_tick cron 1 นาที · ensure_renewal_cycle lazy · reversal ใน TX revoke · admin_credit_adjust · RPC my_credit_summary/my_credit_transcript) — **✅ docs 3 ชุด pipe-audit 0 mismatch · dry-run ROLLBACK 0 · applied in-place (fn 6 ตัว + cron `ltc-credit-accrual` ยืนยัน) · probes 21/21 (P1-P10 ตามแผน §4) — probe จับ bug จริง 1 ตัว: my_credit_transcript ใช้ jsonb_object_agg เหนือ union โดยไม่ sum → แก้ใน migration + re-apply + 21/21 · integration iso-DB fresh replay 10 ไฟล์/93 เทสผ่าน · gitleaks (ไฟล์ที่เปลี่ยน 4) = `[]`** | lead | ✅ |
| E-10 | ผู้เรียน: `api/v1/me/credits` + `me/transcript` (json/CSV BOM/PDF) + lib/api/credits.ts + `(learner)/my/credits` + `my/transcript` + components/credit (CRB-005/006) — **✅ 71/71 unit (9+11+24+7+4+6+5+5) · ผ่านเกทไฟล์ตัวเอง (tsc/eslint/vitest)** | worker flash | ✅ |
| E-11 | หลังบ้าน: `api/v1/admin/credit-rules` (GET/POST/PATCH lifecycle-only — trigger 0010 บังคับ) + `admin/credits` (POST adjustments + GET ledger รายคน keyset) + lib/api/admin-credit.ts + หน้า admin/credit-rules + admin/credits + components/admin/credit (CRB-001/007) — **✅ 126/126 unit (26+18+21+17+28+16) · follow-up lead 3 ข้อ (PII_ACCESS best-effort บน ledger GET ตาม API-SPEC 1.1.1 + comments ตรงจริง 0032 + เกทใหม่) · tsc 0 · eslint 0** | worker flash | ✅ |
| E-12 | `tests/integration/dcr9-credit.test.ts` (accrual idempotent · snapshot-at-grading retire กลางคิว · lazy cycle + closed-immutable · citizen skip · reversal idempotent+ยอด · adjustment authz+reason บังคับ · append-only ปฏิเสธ UPDATE/DELETE · summary math) + e2e ใหม่ 1 ไฟล์ (credit หลังผ่าน) — **✅ + follow-up `dcr9-audit-allowlist.test.ts` (0032: create/update lift+strip · strict 22023 · CREDIT_ADJUST service_role 42501) — integration 12 ไฟล์/105 · e2e 2/2 · tsc repo 0 · eslint 0 · DB สะอาด cron คืน** | worker flash | ✅ |
| จุดแชร์ | AdminSidebar (เมนู 2 รายการ) — lead แก้เอง · rbac.ts มี permission ครบอยู่แล้ว (credit_rule:view/create/update · credit_ledger:view ตัวเอง/ผู้อื่น · credit_adjustment:create) — **✅ READY_ITEMS +2 (กฎหน่วยกิต sliders · หน่วยกิต coins) + SOON_ITEMS −1 · tsc 0 · eslint 0** | lead | ✅ |

หมายเหตุ Phase 3: out-of-scope = CRB-008 (S รอ Q1) · การแจ้งเจ้าของ adjustment/reversal (NTF = Phase 4) · backfill รอบย้อนหลัง (รอ Q1) · OTP โทรศัพท์ (AUTH-003 S default ปิด) · roadmap ถัดไป: Phase 4 NTF → Phase 5 IDENT/PDPA+ADM surfaces → Wave F

## CTO Decision Log (append-only — ห้ามลบ/แก้รายการเก่า)

| # | วันที่    | การตัดสิน (decision)                                                                                                  | เหตุผล |
| - | --------- | ---------------------------------------------------------------------------------------------------------------------- | ------ |
| D1 | 2026-09-08 | Stack: Next.js 15 + TS strict + Tailwind / Supabase (Postgres+Auth+Storage+RLS) / prod = Vercel+Supabase Cloud+Cloudflare / dev = local Docker ทั้งหมด | ตาม intention ผู้ว่าจ้าง (cloud 100% ที่ prod, docker ที่ dev) + parity ระหว่างสอง environment |
| D2 | 2026-09-08 | Doc-first + DCR process — เอกสาร 12 ฉบับ (deliverables 1–12) ต้องเสร็จและผ่าน CTO gate ก่อนเริ่มเขียนโค้ด | ผู้ว่าจ้างกำหนด + ลดความเสี่ยงคุมโครงการ |
| D3 | 2026-09-08 | Credit rules / เกณฑ์สอบ / รอบต่ออายุ = config-driven พร้อม default + flag "รอยืนยัน Q#" — ห้าม hardcode | ยังไม่มีคำตอบทางการ (Q1–Q6 ใน PROJECT-BRIEF) |
| D4 | 2026-09-08 | Wave A ใช้ shared checkout + กรรมสิทธิ์ไฟล์แยกกัน (docs ไม่ทับกัน) — workers ห้ามใช้ git, lead เป็นคนเดียวที่ branch/commit/merge; per-worker worktrees เริ่มใช้ Wave B (โค้ด) | ไฟล์เอกสาร disjoint ไม่มี conflict; ลด overhead การ merge ใน phase เอกสาร |
| D5 | 2026-09-08 | Model routing จริงของ environment นี้: workers = tier ราคาถูกสุดที่รับผิดชอบงานได้ (sonnet สำหรับงานเอกสาร/โค้ดหลัก, haiku สำหรับงานเล็ก), lead/CTO = session model (GLM 5.3) + codex gate สำหรับ milestone | environment นี้ spawn glm-5.3-flash โดยตรงไม่ได้ (ไม่มีใน model list ของ Agent tool) — ใช้ tier ถูกที่สุดที่คุณภาพรับได้แทน ตามเจตนาประหยัด token |
| D6 | 2026-09-08 | Audit log ออกแบบเป็น append-only + บังคับด้วย DB privilege + RLS; ห้ามมี API แก้/ลบ audit | ข้อกำหนดความมั่นคงปลอดภัย/ตรวจสอบได้ |
| D7 | 2026-09-08 | **DCR-1 APPROVE** (จาก worker-1): เพิ่ม Staging environment ใน brief §6 (cloud preview: Vercel staging + Supabase staging branch + Cloudflare staging domain, แยกด้วย env vars เท่านั้น ใช้พิสูจน์ perf 10k/5k + security test) → brief เป็น 0.2.0 + เพิ่ม Q7 (ความยินยอมค่า cloud tier) | เป้า performance พิสูจน์ไม่ได้บน local docker; DCR-2 (reconcile FR ID กับ SRS) กำหนดเป็นรายการ A6 ไม่กระทบ brief |
| D8 | 2026-09-08 | **A6 reviewer verdict: REVISE → สั่งแก้ 16 BLOCKING (B-01..B-16) ส่งคืนเจ้าของไฟล์พร้อมกัน** การตัดสิน canonical: (1) role key = `staff:viewer` (colon) ตาม brief (2) helper = `my_roles()` + `has_any_role(text[])` + `is_staff()` นิยาม canonical ที่ RBAC §3.1 (3) ชื่อตารางยึด DATA-DICTIONARY (4) เกณฑ์ผ่านสอบ 70% (ธง Q2) (5) credit default = 12 หน่วย/รอบ 1 ปี (ธง Q1) และ **SRS Appendix A = defaults master เดียวของโปรเจกต์ เอกสารอื่นห้ามประกาศค่า default ซ้ำ ให้อ้างอิง** (6) session: staff idle 15 นาที + instructor MFA บังคับ + ผู้เรียน idle 60 นาที (7) video ผ่าน 80% / exam cooldown 24 ชม. / proctoring basic (ธง Q4) / คลิป ≤60 นาที 1080p (8) cert_no = `LTC-<ปี ค.ศ.>-<สุ่ม 6 หลัก>` พ.ศ.เฉพาะการแสดงผล (9) staging แทรก SRS/ARCH/PLAN (10) path ยึด API-SPEC + เริ่มสอบแยกเป็น POST (11) เติม endpoint กลุ่มที่ขาด ~16 กลุ่ม (12) public verify = 200 เสมอ + 4 ฟิลด์ (code/course/วันที่/status) + enum superseded + **ห้ามแสดงชื่อเจ้าของ** (13) rate limit ค่าเดียวทุก environment อ้าง config key (14) audit §7 remap เป็น matrix event→AUD-001..005 + เพิ่ม 3 events (46→49) (15) legend D13–D18 ยึด PROJECT-PLAN §7 (16) เพิ่ม 6 ตาราง (31→37) รองรับ consents/notifications/reports/security/license · M-05 DCR APPROVE: manual grading นอก scope → brief 0.2.1 · M-01/M-02 แก้พร้อม | ฐานเอกสารแข็ง (สถิติจริง 100%, security ไม่มีรูรั่ย, RTM ครบ) แต่ 16 จุดขัดข้ามเอกสารต้อง reconcile ก่อน codex/CTO gate — reviewer เสนอ defaults master table เดียวเพื่อไม่ให้ปัญหากลับมาทุก wave |
| D9 | 2026-09-08 | **Model routing ใหม่ (แก้ D5) ตามคำสั่งผู้ว่าจ้างโดยตรง:** workers ใช้ tier `haiku` (= **glm-5.3-flash** ผ่าน ANTHROPIC_DEFAULT_HAIKU_MODEL — ถูก เร็ว รองรับ multi-session) ครอบคลุม ~80% ของงาน · lead/CTO gate/codex ใช้ session model (glm-5.3) ~20% · **ห้าม spawn worker ด้วย tier sonnet/opus** (ทั้งคู่ map ไป glm-5.3 แพง — สาเหตุที่ชน 5-hour usage limit 16:15 และ worker-3/worker-5 ถูก kill กลางทาง) | ผู้ว่าจ้างสั่งหลังเหตุ usage limit หมด: ประหยัด + กัน lock ซ้ำ; งานค้างของ worker-3/worker-5 สานต่อด้วย agent ใหม่บน flash (ชื่อ worker-3b/worker-5b) จากสถานะไฟล์จริงบนดิสก์ |
| D10 | 2026-09-08 | **กลไก verify ประกาศนียบัตร (ปิดความกำกวม SDS §3.4(b) vs diagram 4.6):** QR payload ฝัง **`verify_code`** (nanoid 43, CSPRNG — เดายากกว่า cert_no ที่มีเพียง ~10^6 ค่า/ปี → กัน enumeration ตอนสแกน) · ผู้ใช้พิมพ์มือใช้ **`cert_no`** (สั้น อยู่บนใบ — rate-limit 120/min/IP + response 4 ฟิลด์ไม่มีชื่อ จึง bounded) · `GET /certificates/{code}` ยอมรับทั้ง cert_no และ verify_code (SELECT match สองคอลัมน์ UNIQUE ทั้งคู่ indexed) · lead แก้ API-SPEC:178 บรรทัดเดียวเอง (cross-doc surgical fix ตามแบบ QP-5) + SDS แก้ตาม worker-3b | diagram 4.6 เดิมถูกแล้ว; จุดผิดคือ §3.4(b) ที่เขียน cert_no ลง QR — แก้ให้ตรงกันทั้ง SDS/API-SPEC ก่อน codex gate |
| D11 | 2026-09-08 | **codex gate round 1: FAIL (2 BLOCKER/17 MAJOR/3 MINOR) → ตัดสิน canonical 20 ข้อ สั่งแก้ 3 ทางบน flash:** (1=BLOCKER) รายการธรรมดา BFF ใช้ user JWT (role `authenticated` + RLS บังคับจริง) — `service_role` เฉพาะ jobs เฉพาะกิจ (purge/export) + server functions แคบขอบเขต แก่ ARCH §7/SDS §5.2/API §1/RBAC (2=BLOCKER) diagram 4.6 ตัดชื่อเจ้าของออก — ตอบ 4 ฟิลด์เท่านั้น (3) RLS policies เป็น operation-specific แทน FOR ALL + column protection (4) ทุกตาราง derive RLS จาก permission matrix (5) policy ใช้ชื่อคอลัมน์ตาม DD canonical (`bank_id` ไม่ใช่ `question_bank_id` ฯลฯ) (6) audit schema canonical = DD ของ worker-3 + เก็บ chain hash design → DD เพิ่ม `audit_chain_anchors` (7) REVOKE UPDATE+DELETE+**TRUNCATE** ทุกตาราง append-only (8) เขียน audit ผ่าน append function (SECURITY DEFINER) เท่านั้น + business mutation กับ audit commit atomic ใน transaction เดียว (9) payload schema ราย event-type เข้มงวด + sanitize ฟรีเท็กซ์ + เก็บเฉพาะ code ที่ validate แล้ว (10) session server-checked ทุก role: staff/instructor ใช้ admin_sessions, ผู้เรียน JWT อายุสั้น + refresh + revoke ทันทีตอน logout-all/reset (11) MFA: role ที่บังคับ (staff*/super_admin/instructor) ได้ session "enrollment-only" จนจบ MFA + ทุก request เช็ค MFA claim + ปิด MFA ต้อง recent-MFA และห้ามเหลือ 0 factor (12) `GET /auth/mfa/backups` → คืนเฉพาะ metadata (count/created_at) + regenerate เป็น POST แยก (CSRF + recent MFA + no-store) (13) rate matrix canonical เดียว: ทุกค่าใน Appendix A + cumulative IP+user + กติกากลุ่มชนกัน = specific ชนะ + reset 5/ชม./account + OTP 3/เบอร์/ชม. ลง matrix (14) CertificatePublicView = `{code, course_title, issued_at, status}` snake_case 200 เสมอ ไม่มี nullable — API/SDS/view ตรงชุดเดียว (15) วิดีโอจบ = server สะสม bounded playback intervals จาก heartbeat เทียบ server elapsed — ตัดสิทธิ์ client แจ้ง `completed` (16) exam attempt snapshot ข้อมูลตรวจครบ (ข้อ/ตัวเลือก/เฉลย/เวอร์ชัน) ใน jsonb ตอนเริ่ม attempt — immutable (17) credit accrual ผ่าน transactional outbox + UNIQUE(source_type, source_id, credit_type) กันซ้ำ (18) consent แยก notice-ack (จำเป็นตามสัญญา) จาก optional consents แบบ versioned + การเพิกถอยหยุดการประมวลผลที่อิงตาม consent นั้น + ตาราง purpose→lawful basis (19) ตาราง retention เดียวใน DD §4.6 เป็น canonical (audit 5 ปี purge job, security_events 1 ปี, email_outbox 90 วัน, profiles อายุบัญชี+10 ปี + anonymize ตามสิทธิ์ลบ) — แก้ทุกเอกสารชี้มาที่นี่ (20) SDS บันทึก enumeration tradeoff ของ manual lookup + requireRole()→requirePermission() + SRS SEC-001 pin ASVS **4.0.3** | codex เจอจริง: service_role bypass RLS, TRUNCATE นอก RLS, JWT คงอยู่หลัง sign-out (อ้าง Supabase/PG docs) — F2 ยืนยันจริง, F5 ครึ่งจริง (ชื่อคอลัมน์ DD มี แต่ policy อ้างผิด) · แก้ครบแล้ว re-gate codex ก่อน verdict สุดท้าย |
| D12 | 2026-09-08 | **codex gate round 2: FAIL (3 BLOCKER/21 MAJOR) → ตัดสิน canonical 23 ข้อ สั่งแก้รอบสุดท้าย (loop 3/3) 3 ทาง flash เดิม:** (1=BLOCKER F2/F4) **server-controlled writes** — ตารางผลลัพธ์ (enrollments, lesson_progress, quiz_attempts, assessment_attempts, attempt_answers) ผู้เรียนห้าม INSERT/UPDATE ตรง (WITH CHECK ownership ไม่กันค่าคอลัมน์ — ปลอม passed/score ได้): เขียนผ่าน SECURITY DEFINER functions เท่านั้น (enroll, record_lesson_progress, record_quiz_attempt, start_attempt, save_answer, submit_attempt) owner เฉพาะ + audit ภายใน + ผู้เรียนเหลือ SELECT (2=BLOCKER F4) **answer-key deny** — ผู้เรียนห้าม SELECT ตรง questions/question_options/quiz_questions/quiz_options/attempt_answers (raw): อ่านผ่าน projection view ตัด is_correct/explanation/snapshot จนครบเงื่อนไขเปิดเฉลย (3=BLOCKER F9) AUDIT_READ.filters = typed allowlist .strict() + FreeText normalize (strip hyphen/space) ก่อน detect PII (บัตร 13/เบอร์/email/license 6-9) (4) ARCH diagrams §2/§5/§5.5: BFF→DB = user JWT + service_role เป็นกล่อง jobs/functions แยก (5) RBAC matrix reconcile: courses owner-write ต้องมี role instructor + status draft/review · q_read +staff:viewer · attempt-read +viewer/sa · q_update with check destination bank (6) `nr.notification_id = notifications.id` qualify (7) audit_logs +prev_hash/row_hash + hash ครบทุก evidentiary field (รวม before/after/actor_roles) + traversal (occurred_at, id) (8) DD REVOKE INSERT audit_logs จาก service_role — append_audit_event path เดียว (owner+EXECUTE ระบุ) (9) SDS learner session: ทุก request เช็ค JWT session_id กับ auth.sessions + สถานะบัญชี + admin_sessions ครอบ instructor (10) MFA state machine เดียว (none→enrollment-only→verified; logout ทุก state; disable = citizen/lawyer เท่านั้น v1 — staff block; IDENT-006: grant ได้แต่ force enrollment login ถัดไป) (11) rate canonical ครบทุก group ใน Appendix A (mfa 10/read 120/learn 120/exam 60/staff 60/export 10) + pre-auth key = อีเมล normalized + IP counter ทุก group (12) LessonProgressRequest ตัด `completed` + documentRead = attestation (tradeoff note) + quiz completion = highest score ตาม policy (13) grading อ่าน snapshot ล้วน + questions +version + นิยาม snapshot เต็มใน DD (14) **accrual event = ตรวจผ่าน ไม่ใช่ออก cert** — outbox ใน TX ของ grading + rule/cycle ตามวันที่ผ่าน + source_type='assessment_attempt' + reissue ไม่กระทบ credit (15) certificates: UNIQUE(enrollment_id) → partial WHERE status='valid' + supersedes_cert_id lineage (16) credit_rules +status draft/active/retired + name + credit_type + renewal_cycle + valid_days nullable (17) ตารางใหม่ notice_acknowledgments (append-only) แยกจาก consents + worker เช็ค consent active ตอน dispatch (39→40 ตาราง) (18) retention: purge_role ชื่อเดียว + learning records ที่ถูก cert อ้าง = anonymize ไม่ลบ + Audit §5 ชี้ DD §4.6 (19) lawyer_licenses +partial UNIQUE(license_no) WHERE revoked_at IS NULL + approval ตรวจ conflict atomic (20) assessment_rules index → (assessment_id, effective_from) ห้าม now() ใน predicate (21) assessment_attempts +session_id +lease_expires_at — ASM-011: อุปกรณ์สอง takeover หลัง disconnect_grace_minutes + audit EXAM_SESSION_TAKEOVER (22) SDS onboarding §4.1 → canonical routes (/auth/register, PUT /me/license, license_applications, approve แล้วจึงมี license+role) (23) +GET /admin/certificates/eligible (registrar/sa) = endpoint 82 + matrix drift (instructor สร้าง assessment draft, staff:exam export) | codex r2 ลึกกว่า r1: จับ SQL scoping (unqualified id), non-immutable index predicate, INSERT-side fabrication, answer key ใน RLS boundary · **รอบ 3 = รอบสุดท้ายตาม fix-loop cap — แก้ครบ+self-check ก่อน gate** |
| D13 | 2026-09-08 | **codex gate round 3 (หลังแก้ครบ D11+D12 รอบสุดท้าย): FAIL — 0 BLOCKER / 7 MAJOR / 6 MINOR → fix-loop cap 3/3 ครบ → A6 terminal-blocked ยกให้ผู้ว่าจ้างตัดสิน** · วงจรบรรจบชัด: r1 22 findings (2B/17M/3m) → r2 24 (3B/21M) → r3 13 (0B/7M/6m) + codex ยืนยัน RESOLVD เกือบทั้งชุด (session/answer-key/user-JWT/matrix-เฉพาะจุด/rate/progress/accrual/lineage/schema/onboarding/82 endpoints/40 ตาราง) · 7 MAJOR คงเหลือ: (1) FreeText sanitizer — normalize ลบ `.` ก่อนเช็ค email ที่ regex ต้องมี `\.` → email รอดทุกกฎ (2) RBAC §3.1 — worker แทรกชุด lesson_progress ใหม่ L199–208 แต่**ลืมลบชุดเก่า L210–226** (`lp_owner_read` CREATE ซ้ำ + INSERT/UPDATE policy เก่าค้าง) — lead ยืนยันด้วยตาเปล่า และเป็นช่องโหว่ verify ของ lead เอง (ตรวจ DD แล้วแต่ไม่ grep duplicate CREATE ใน RBAC) (3) hash-chain traversal (occurred_at,id) ไม่การันตีตรงลำดับ append — now() คงที่ตลอด TX + UUID v4 สุ่ม หลาย audit ใน TX เดียวจะเรียงย้อนได้ → verifier แจ้ง chain แตกโดยไม่มีการแก้ (4) append_audit_event EXECUTE ขัดกันเอง (AUDIT ถอนจาก service_role vs DD grant ตามจำเป็น vs flow BFF เรียก RPC ตรง) (5) RLS อ่านเกิน matrix — courses_staff_read ใช้ is_staff() (draft รั่ยถึง se/sr) + attempts policy ให้ instructor เจ้าของ course อ่านผลผู้อื่นที่ matrix ไม่ให้ (6) credit worker จับกฎไม่ได้เมื่อ rule ถูก retire ก่อน event ถูก process (match เฉพาะ status='active' vs effective-window ณ วันผ่าน) — ควร snapshot rule_id ใน outbox ตอน grading (7) credit_ledger_entries.certificate_id "ตั้งภายหลังเมื่อออกใบ" ขัด append-only (UPDATE ถูก REVOKE) — ควร resolve ผ่าน attempt/enrollment ไม่ backfill · 6 MINOR: MFA allowlist ไม่มี GET /me ใน RBAC/T15 · dwell vs attestation · flow registrar ยังเรียก reports/assessments · notice_ack หายจาก §4.4/§4.6 · error-code/path เก่า (ATTEMPT_EXPIRED, /api/v1/exams*) · btree_gist prerequisite | ทุก finding แคบ/เชิงกลไก/แก้ได้ใน 1 pass · ทางเลือกที่ผู้ว่าจ้างต้องตัดสิน: (ก) อนุมัติรอบแก้พิเศษ 1 รอบนอก cap โดย lead ลงมือเอง/flash 1 ทาง + re-gate ครั้งเดียว (ข) หยุดรอผู้ว่าจ้างอ่าน findings (ค) ยอมรับ residual แบบมี waiver เข้า Wave B — **แนะนำ (ก)** เพราะ 0 BLOCKER + วงบรรจบชัด + งานที่เหลือเล็ก |
| D14 | 2026-09-09 | **ผู้ว่าจ้างอนุมัติทางเลือก (ก): รอบแก้พิเศษ 1 รอบนอก fix-loop cap — lead ลงมือแก้เองทั้ง 13 findings ของ r3 + re-gate ครั้งเดียว** · แก้จริง 23 จุดใน 6 ไฟล์ (+46/−56): (F1) AUDIT FreeText — แยก normalize: กฎตัวเลขใช้ strip จุด/ขีด แต่ **email ตรวจบนรูปที่คง "." ของ domain** (normalizeForDigits + collapseForEmail, PII_RULES ชู้น 3) (F2) ลบ stale block RBAC L210–226 — `lp_owner_read` เหลือ CREATE เดียว + learner INSERT/UPDATE policy หาย (grep ยืนยัน 1/0/0) (F3) chain sequence — `append_audit_event()` ภายใต้ advisory lock กำหนด `occurred_at = greatest(now(), prev + 1µs)` **strictly increasing** ให้ traversal (occurred_at,id) = ลำดับ append เป๊ะ (AUDIT §3.3 + DD audit_logs + row_hash note) (F4) EXECUTE contract เดียว — `grant execute to authenticated, service_role` (BFF user-JWT RPC + jobs) + ฟังก์ชันตรวจ actor/payload ภายใน; DD §4.4 ประกาศเหมือนกัน verb เดียว (F5) courses_staff_read → has_any_role(sv,sc,sa) ตรง matrix + **canonical: matrix attempt:view(ทุกคน) เพิ่ม O† ให้ instructor เฉพาะหลักสูตรที่ตนเป็นเจ้าของ** (เหตุผล: ผู้สอนต้องเห็นผลรอบสอบที่ตนดูแล — สอดคล้อง enrollments/lesson_progress policies เดิม; grade_override ยังเป็น staff:exam อย่างเดียว SoD) (F6) rule matching ครั้งเดียว ณ grading — snapshot rule_id+ค่าที่ใช้ลง outbox payload; worker ใช้ snapshot ไม่ lookup ซ้ำ (DD credit_rules + SDS §3.2b) (F7) credit_ledger certificate_id — เติมได้เฉพาะตอน INSERT; **ห้าม backfill** (UPDATE ถูก REVOKE) — reporting เชื่อมผ่าน certificates.enrollment_id (F8) MFA allowlist รวม **GET /me read-only** (RBAC §4.2 + T15 แก้คาดหวังเป็น 200) ตรง SRS AUTH-007 (F9) SDS เอกสาร = client attestation (documentRead) ตาม D12-12; dwell_sec เป็น telemetry ไม่ใช่เงื่อนไขผ่าน (F10) flow นายทะเบียนทั้ง text+diagram → `GET /admin/certificates/eligible` (F11) notice_acknowledgments เข้า §4.4 enforcement (REVOKE UPDATE/DELETE) + §4.6 retention (อายุบัญชี+10 ปี) + ARCH retention purge → **purge_role** (F12) SDS ใช้ ERR-ASM-004/ERR-VAL-001 จากทะเบียร์ API เท่านั้น (2 จุด) + SRS §5.2 กลุ่ม endpoint ตรง API canonical (assessments*/attempts*, ตัด exams*/verify*/question-banks* เดี่ยว) (F13) DD §1 + renewal_cycles: `CREATE EXTENSION IF NOT EXISTS btree_gist` เป็น migration prerequisite ของ EXCLUDE | self-check grep ผ่านทุกข้อ (F1 3+0, F2 1/0/0, ไม่มี duplicate CREATE ใน 22 policies, is_staff() เหลือเฉพาะนิยาม) · ต่อไป: codex gate **รอบ 4 (ผู้ว่าจ้างอนุมัติ — รอบเดียว)** → PASS = CTO APPROVE + ปิด Wave A |

| D15 | 2026-09-09 | **codex gate round 4 (re-gate รอบเดียวตาม D14): FAIL — 0 BLOCKER / 2 MAJOR ใหม่ / 3 MINOR · 11/13 findings RESOLVED (F4/F5/F11 ยังไม่ครบ) · lead ตรวจรับรองด้วยตาทุก finding แล้ว = จริงทุกข้อ** · วงบรรจุต่อเนื่อง: r1 2B/17M/3m → r2 3B/21M → r3 0B/7M/6m → r4 0B/2M/3m · **N1 (MAJOR, regression ของ F4)**: AUDIT §4 มีแต่ GRANT execute ให้ authenticated+service_role **ไม่มี `revoke execute ... from public, anon`** — PG15 ให้ EXECUTE แก่ PUBLIC โดย default บน function ใหม่ → anon เรียก append_audit_event() ได้; และการตรวจภายใน (actor/schema/chain) ไม่ผูก event กับ mutation ที่ผ่าน authorization จริง — caller กล่าวอ้าง event (เช่น ROLE_GRANT) ที่ผ่าน schema ได้ (hash-chain รับรองลำดับ ไม่รับรองความจริง) → แก้: revoke จาก PUBLIC/anon ก่อน grant ใน TX เดียว + audit ของ mutation ต้องถูกสร้างใน business function ที่ตรวจสิทธิ์แล้ว (derive actor/roles/ผลจาก server) + generic RPC path เหลือเฉพาะ event ที่ไม่คู่ mutation พร้อม allowlist ราย event · **N2 (MAJOR, ความขัดแย้งจาก O† ใหม่)**: DD §3.4 attempt_answers L474 ยังให้ instructor เจ้าของหลักสูตร SELECT ตารางฐาน (มี question_snapshot.options[].is_correct = เฉลย) ขัดกับ RBAC §2.2 † ที่บอก "อ่านผ่าน projection เท่านั้น" — แถม learner_attempt_view จำกัดเฉพาะ attempt ตัวเอง ไม่รองรับ instructor อ่านผลผู้เรียน → แก้: DD ถอน raw SELECT ที่มาจาก ownership เฉย ๆ + เพิ่ม projection สำหรับ instructor (ตรวจ role + course ownership + ตัดเฉลยตามเงื่อนไข D14) · **M1**: AUDIT §1.4 L16 ยังเขียน "EXECUTE เฉพาะ app_owner ไม่ให้ service_role" ขัด contract §4 ใหม่ · **M2**: SDS §4.5 diagram L278 ยังให้ worker จับคู่ credit_rules ขัด §3.2b snapshot-only · **M3**: notice_acknowledgments REVOKE มีแค่ UPDATE/DELETE ขาด TRUNCATE (RLS ไม่ครอบ TRUNCATE) | ผู้ว่าจ้างอนุมัติแค่แก้ 1 รอบ + re-gate 1 ครั้ง (D14) — ใช้ครบแล้ว → ต้องถามผู้ว่าจ้างอีกครั้ง: (ก) รอบแก้จุดจบ 1 รอบเหมือนเดิม (งานเหลือ ~5 จุด เล็ก/เชิงกลไก/ระบุบรรทัดแล้ว — แนะนำ) (ข) ยอมรับ residual แบบมี waiver registry + ปลดล็อก Wave B (ค) หยุดรอผู้ว่าจ้างอ่าน findings |

| D16 | 2026-09-09 | **ผู้ว่าจ้างมอบอำนาจตัดสินใจเต็มให้ CTO-lead (ข้อความตรง: "@cto you are who dicision all, no wait me") + อนุมัติรอบแก้จุดจบ 1 รอบ** — นับจากนี้ lead ตัดสินใจเองทุกเรื่อง (fix rounds / gate outcomes / wave transitions / DCR กลาง ๆ) โดยไม่ต้องรอถามผู้ว่าจ้าง และรายงานผลเป็นระยะ · ยังผูกกับ binding rules เดิมทุกข้อ (ห้าม commit secrets, no fake completion, ทุก completion มี evidence, doc-first + DCR, D9 model routing) · แก้จริง 9 จุด / 4 ไฟล์ ปิดทั้ง 5 findings ของ r4: **N1** AUDIT §4 — `revoke execute ... from public, anon` ก่อน grant (PG15 default ให้ PUBLIC) + แบ่ง event-class: (ก) mutation event = เขียนใน server path เท่านั้น (SECURITY DEFINER write functions/BFF service_role — derive actor/ผลจาก server) (ข) non-mutation event = user-JWT RPC ได้ตาม allowlist ราย event + actor/request context มาจาก auth.uid()/middleware เท่านั้น · §1.4 + §6.2 แก้ให้ตรง contract เดียว (M1) · **N2** DD §3.4 attempt_answers — ถอน raw SELECT ของ instructor (ownership เฉย ๆ ไม่เปิด raw ที่มีเฉลย) + เพิ่ม `instructor_attempt_view` (ตรวจ instructor + courses.created_by สองชั้น, ตัด is_correct/points_earned/question_snapshot/explanation, เปิดเฉลยตาม exam_review_mode) + RBAC † อ้างชื่อ view · **M2** SDS §4.5 diagram — worker อ่าน rule snapshot จาก event payload ไม่ lookup credit_rules ซ้ำ (ตรง §3.2b) · **M3** notice_acknowledgments — REVOKE เพิ่ม TRUNCATE (ทั้ง §3.1 + §4.4) + trigger guard ครอบ UPDATE/DELETE/TRUNCATE | self-check grep ผ่านทุกข้อ (revoke L234, stale §1.4 = 0, instructor raw = 0 + view นิยามครบ, worker-lookup เก่า = 0, TRUNCATE ครบ 2 จุด) · ต่อไป: codex gate รอบ 5 → PASS = CTO APPROVE + ปิด A6 + Wave B · FAIL = lead ตัดสินเองตามอำนาจ D16 |

| D17 | 2026-09-09 | **codex gate round 5: PASS — 0 BLOCKER / 0 MAJOR ใหม่ / 1 MINOR (ไม่ block) → CTO APPROVE (lead ตัดสินตามอำนาจ D16) · Wave A ปิดสมบูรณ์** · 5/5 findings ของ r4 RESOLVED (N1/N2/M1/M2/M3) · codex ยืนยัน event-class split ใช้ได้ตาม contract (class ก = ข้อบังคับทั่วไปไม่จำกัดเฉพาะตัวอย่าง, class ข = allowlist ราย event + AUTH จาก BFF), instructor_attempt_view ไม่เปิดทางรั่ยเฉลยใหม่, EXECUTE/TRUNCATE ตรง PG15 semantics, **ไม่เปิด finding ซ้ำ**ของกลไก canonical (O†, hash-chain, cert backfill, credit snapshot, PDPA notice/consent/retention) · วงบรรจุเต็ม: r1 2B/17M/3m → r2 3B/21M → r3 0B/7M/6m → r4 0B/2M/3m → **r5 0B/0M/1m** · **R5-m1 (MINOR) แก้พร้อม approve**: AUDIT §4 class ข — ผูก producer ราย event: AUTH_* ทั้งชุด (ครบ 12 event ของ §2.1) = BFF เท่านั้น ในฐานะ trusted server บันทึก "ผลที่สังเกตได้" ของ Supabase Auth (ไม่ใช่ wildcard; ไม่ใช่ mutation ใน DB ของแอป จึงไม่ต้อง atomic กับ TX ธุรกิจ ตาม §1.5) · **version bump**: SRS/RTM/SDS/ARCH/DD/API/RBAC/AUDIT/DESIGN-SYSTEM → **1.0.0 baseline Wave B** (PROJECT-BRIEF คง 0.2.1 — DCR-controlled) · docs/README board 1–12 → ✅ + บันทึก gate ท้ายตาราง | gate เป็น documentation/design review — ไม่ใช่ implementation certification (codex ระบุเอง): โค้ด Wave B ต้องพิสูจน์กลไกด้วย lint/tsc/tests/RLS-integration test เอง; residual MINOR = 0 (แก้ครบในรอบ approve) |

| D18 | 2026-09-09 | **codex gate B-02 รอบ 1: FAIL (7 BLOCKER / 11 MAJOR / 2 MINOR) → คำตัดสิน canonical 20 ข้อ (lead ตรวจยืนยันทุก finding กับ DD/RBAC/AUDIT เองก่อนตัดสิน — ทุกข้อจริง)** · **B1** append_audit_event ต้อง implement ตาม AUDIT §4 เต็ม: allowlist ราย event ในนิยามฟังก์ชัน (class ข เท่านั้น: CERT_VERIFY_PUBLIC/AUDIT_READ/RATE_LIMIT_HIT/PII_ACCESS — AUTH_* และ mutation events = server path เท่านั้น ปฏิเสธที่ RPC), actor derive จาก auth.uid() เท่านั้น, request_id/ip_hash จาก middleware โดย BFF เท่านั้น, payload schema validate ราย event + ตัด PII · **B2** record_lesson_progress: ห้ามรับ quiz_score_pct (grading = server), clamp watch/dwell delta ต่อ call, ตรวจ lesson∈course ของ enrollment + enrollment active · **B3** RPC eligibility: enroll() ตรวจ role enroll:create + หลักสูตร published; record_quiz_attempt() ตรวจ enrollment active + quiz อยู่ใน course นั้น; start_attempt() ตรวจ published + เรียนครบ + max_attempts + cooldown · **B4** session/lease: session_id สร้างโดย server + คืน caller; save/submit ต้องส่ง session_id มาเทียบ; submit ตรวจ in_progress + deadline · **B5** questions: trigger guard status→'active' เฉพาะ staff:exam/super_admin (RBAC L237 สั่งไว้ชัด) · **B6** assessment_rules/credit_rules: ห้ามแก้ semantic columns ของแถว active/ถูกอ้าง (แก้ = version ใหม่) — trigger guard ยอมเฉพาะ lifecycle columns · **B7** ปิด ACL inheritance ของ Supabase baseline: REVOKE ALL บน view ทุกตัวก่อน GRANT SELECT + consents เข้าชุด append-only ตาม DD · **M1** ฟังก์ชันที่ workflow จริงยังไม่ implement (grading/completion/snapshot/outbox) ต้อง raise SQLSTATE 0A000 ชัดเจน — ห้ามคืนสำเร็จลอย (no fake completion), Wave C เติม · **M2** + UPDATE policy ของ app_owner บน attempt_answers · **M3** lessons_quiz_id_fkey → DEFERRABLE INITIALLY DEFERRED + path: lesson ก่อน quiz ใน TX เดียว · **M4** license_applications INSERT WITH CHECK: decision fields ต้อง NULL · **M5** assessment_rules_full_view (staff 4 roles + instructor เจ้าของ) ชดเชย column grant · **M6** correctness triggers ครอบ UPDATE options + เปลี่ยน question type · **M7** views เปิดเฉลยตาม exam_review_mode + submitted_at (DD L477/479) ไม่ตัดถาวร · **M8** สร้างครบ 4 reporting views ตาม DD §3.7 — การเลื่อนของ worker ถูกยกล้ำโดย gate · **M9** category hierarchy guard (self-parent/cycle/depth>1) · **M10** purge path: guards ยกเว้น purge_role + purge function + SELECT grants ตาม DD §4.6 · **M11** created_at = แบบแผนกลางทุกตาราง (DD §1 L18 "ระบุเฉพาะเมื่อต่าง") → เพิ่ม 36 ตารางที่ขาด (judgment #1 ของ worker-b2 อ่านผิด — ยกเลิก) · **m1** config.toml → [auth.email] enable_confirmations · **m2** video_max_position_sec nullable ตาม DD · **แผนแรงงาน:** worker-b2 (flash) ทำ mechanical items · lead ทำ security-critical (B1 rewrite + B2/B3/B4 + M1) · แก้ in-place 0001–0011 (ยังไม่ deploy ที่ไหน) → re-verify vanilla + supabase image → codex รอบ 2 (cap 3) | วงจรเดียวกับ Wave A — gate แรกของ implementation ลึกเป็นธรรมชาติ; บทเรียน: claim "ALL PASS" ของ worker ต้องระบุ environment ที่รัน (worker รันบน baseline ที่ Supabase image จัดให้โดยไม่รายงานเงื่อนไข — lead จับได้ตอน vanilla run ก่อน gate) |
| D19 | 2026-09-09 | **codex gate B-02 รอบ 2: FAIL (6 BLOCKER / 8 MAJOR) → แก้ครบ 14 ข้อใน 0004/0008/0009/0010/0011** · หลักๆ: exam_review_mode=after_final_attempt (เปิดเฉลยเมื่อครบ max_attempts เท่านั้น — B1) · nested-PII recursive scan + AUDIT_READ.filters typed allowlist (B2) · watch elapsed bound ต่อ call (B4) · course soft-delete + category hierarchy guards (B6/M7) · purge_role SELECT policy + DELETE คืนแถวจริง (M2) · instructor enroll:create (M3) · shuffle=false/selection filters/difficulty_mix (M5) · session takeover ที่ max_attempts + EXAM_SESSION_TAKEOVER audit (M4) · completion_rule override (M6) · SECURITY DEFINER คืน owner app_owner 5 ตัว (M1) | probes F15–F24 เพิ่ม (34 PASS/0 FAIL สอง image) · cap รอบ 3 ต่อ |
| D20 | 2026-09-09 | **codex gate B-02 รอบ 3: FAIL (5 BLOCKER / 3 MAJOR) — ครบ fix-loop cap 3/3 → lead ใช้อำนาจ D16 อนุมัติ D20 = รอบแก้จุดจบ 1 รอบ ผูกกับ 8 findings ของ r3 เท่านั้น ห้าม scope ใหม่** · B1 reveal-count นับเฉพาะ `submitted_at is not null` (0009 ×8 จุด) · B2 wall-clock cap จาก created_at เป็นชั้นที่ 3 (0011) · B3 `uq_lessons_quiz_ref` partial unique index บังคับ quiz→lesson 1:1 (0004) · B4 typed-value context validation + ip_hash/request_id ถูก override null บน RPC ผู้ใช้ (0008) · B5 `auth_session_claim()` ผูก session จาก JWT claim + ตรวจ 2 ชั้นใน save/submit (0011 — DD §3.4) · M1 purge SELECT 4 ตาราง (0010) · M2 profiles trigger guard คอลัมน์เจ้าของ (0010) · M3 optionless activation guard + กรอง selection pool (0010/0011) | probes F25–F32 (46 PASS/0 FAIL สอง image) · **probes จับ migration bug จริง 1 ตัว**: guard_profiles_update_columns ใช้ `k <> any()` ผิดเซมานติก → แก้ `not (k = any())` · policies 179→183 · SECDEFINER 21→22 · r4 = final ตามเงื่อนไข D20 |
| D21 | 2026-09-09 | **codex gate B-02 รอบ 4: FAIL (2 BLOCKER / 1 MAJOR / 0 MINOR — ทั้ง 3 เป็น residual ของ fix D20 ของ lead เอง) → lead ใช้อำนาจ D16 อนุมัติ D21 = รอบแก้จุดจบ 1 รอบ ผูก 3 findings + ปิด probe gaps** · B3′ index predicate ตัด `deleted_at is null` ทิ้ง + `lessons_insert` WITH CHECK `deleted_at is null` + เติม `l.deleted_at is null` 12 จุดใน policies ชุด lq/qq/qo/qa (จาก finding: lesson "เกิดมาพร้อม deleted_at" หลุด partial index แล้วอ้าง ownership quiz ต่างถิ่น) · B4′ ชื่อคีย์ต้องเป็น identifier `^[a-z][a-z0-9_]{0,63}$` ใน helper ทั้งสอง (ปิด PII-in-key-name แบบ email) · M2′ `alter function guard_profiles_update_columns() owner to app_owner` · probes ใหม่: F27d/F27e/F28d + struct F-own (secdefiner≠app_owner = 0) + G-idx → **รอบ 5: FAIL (1 BLOCKER / 0 MAJOR / 0 MINOR)** — B3′+M2′ RESOLVED; **เหลือ residual ของ B4′ ระดับสอง: identifier regex บล็อก email(@) แต่ `phone_0812345678` (คีย์ถูกกฎ พาเบอร์ 10 หลัก) ผ่าน เพราะชื่อคีย์ไม่เคยผ่าน digit-PII scan** · **disposition ตามเงื่อนไข D21: หยุด ไม่ merge ไม่มีรอบแก้ที่ 6 — รายงานผู้ว่าจ้าง** (override cap ตัวเองครั้งที่ 3 ไม่ทำ — cap จะกลายเป็นพิธีกรรม) · **remedy เตรียมไว้พร้อมรัน (~10 นาที)**: ชื่อคีย์ผ่าน digit-PII pattern เดียวกับ FreeText (strip `_` แล้วตรวจ digit-run 9–10/13 หลัก) + probe F28e (`phone_0812345678` → 22023) + re-verify สอง image + re-gate 1 ครั้ง | ทางเลือกต่อผู้ว่าจ้าง: (ก) อนุมัติ remedy + re-gate ครั้งเดียว (**แนะนำ** — residual จุดเดียว เหลือ 1 BLOCKER จากวง 20→14→8→3→1) (ข) waiver registry + merge พร้อม ticket แก้ใน Wave C (ค) park branch B-02 · ระหว่างรอ: เดินหน้า **Wave C planning** (docs ล้วน ไม่แตะ migrations) ตาม D9 "prevent lock" |
| D22 | 2026-09-09 | **ผู้ว่าจ้างตอบคำถาม D21-disposition (ข้อความตรง): "@cto you are full authori and who is final dicison for all , no wait me" — ยืนยัน D16 ซ้ำแบบเด็ดขาด (อำนาจเต็ม + ผู้ตัดสินสุดท้ายทุกเรื่อง + ห้ามรอ)** → lead ตัดสิน = ทางเลือก (ก): **D22 = remedy จุดเดียวของ r5 + probe คู่ negative/positive + re-verify สอง image + re-gate 1 ครั้ง (r6 = final ตาม D22: FAIL → lead ชี้แจง disposition เองตามอำนาจ D22 และรายงานเป็นลายลักษณ์อักษร)** · Fix B4'': ชื่อคีย์ทุกชั้นใน helper ทั้งสอง — `k !~ '^[a-z][a-z0-9_]{0,63}$' or regexp_replace(k,'_','','g') ~ '\d{6}'` (strip `_` กันแยกเลขด้วย underscore; digit-run ≥ 6 ครอบ license 6–9/เบอร์ 9–10/บัตร 13 — หลักเดียวกับ audit_free_text_ok) · pre-check grep: ไม่มี context key จริงใน migration ที่มี digit-run ≥ 6 (ไม่มี false positive) · probes ใหม่ **F28e** (`phone_0812345678` → 22023) + **F28f positive control** (`retry_2` → บันทึกได้ กัน over-block) | Evidence r6 สอง image สด: vanilla migrate 11/11 rc=0 + struct **13/0** + fns **47/47** (`/tmp/{migrate_r6,struct_run9,fns_run9}.log`) · supabase down -v→migrate rc=0 + prelude ถูกต้อง + struct **13/0** + fns **47/47** (`/tmp/{sb_migrate_r6,sb_struct_run8,sb_fns_run8}.log`) · census diff = ERROR_SETS_IDENTICAL (16 ข้อความ) · ความโปร่งใส: รัน supabase ครั้งแรก prelude ล้ม (lead ชี้ .env ผิดที่ → F4a/F4b/F20c FAIL) → reset db ใหม่ทั้งวง แล้ว 47/0 — ไม่แก้ migration ระหว่างสองรัน · ระหว่างรอ gate: worker-c0 (flash) ส่งแผน Wave C 118 บรรทัด (.omc/plans/wave-c-plan.md) + ข้อสังเกต O-1..O-5 ไว้ตัดสินตอน kickoff Wave C |
| D23 | 2026-09-09 | **ผู้ว่าจ้างกำหนด escalation ladder (ข้อความตรง): "if you found any case that cannot solve by woker tier that retty more tan 3 times, you can assign to middle tier for retry 3 times and if you still found cannot solve it , then you do it by your self"** — งานที่ worker tier (haiku = glm-5.3-flash ตาม D9) ล้มเหลว > 3 ครั้ง → ส่ง middle tier (sonnet) retry ได้อีก 3 ครั้ง → ยังไม่ได้ = lead ลงมือเอง | เติมเต็ม D9/D5: ประหยัดเท่าที่ทำได้ แต่ไม่ยอมให้งานตายที่ tier ถูก — ใช้นับจากงาน Wave C เป็นต้นไป (B-02 ทั้งวง lead ลงมือเองอยู่แล้ว สอดคล้อง ladder) |
| D24 | 2026-09-09 | **codex gate B-02 รอบ 6 (final ตาม D22): PASS — 0 BLOCKER / 0 MAJOR / 0 MINOR → CTO APPROVE (lead ตัดสินตามอำนาจ D22) → merge `--no-ff` ลง develop · Wave B ปิดสมบูรณ์** · B4'' RESOLVED (helper ทั้งสอง: identifier + digit-run ≥6 หลัง strip `_` — จับทั้ง `phone_0812345678` และ `phone_08_1234_5678`, `retry_2` ผ่าน) · RESOLVED-CHECK A–G ผ่านครบ (RLS/matrix · answer-key deny ครอบ soft-delete · SECDEFINER hygiene · hash-chain · GRANT/REVOKE · ไม่มี drift ใหม่จาก D22 · append-only รวม TRUNCATE guards) · วงบรรจุเต็ม 6 รอบ: 20→14→8→3→1→**0** findings · verdict: `/tmp/codex-gate-b02-r6.txt` | บทเรียนสำคัญของ B-02: (1) fix-loop cap + "รอบ final ผูกขอบเขต" คุมวงจรได้จริง — ทุก findings รอบหลังเป็น residual ของ fix รอบก่อนเอง (2) probes ต้องครอบทุก fix ใหม่ทันที (probe gap คือที่รอดของ r4/r5) (3) การ verify ต้องระบุ environment (D18-บทเรียน) — สอง image ทุกรอบตั้งแต่ D19 |
| D25 | 2026-09-10 | **Wave C kickoff + ตัดสิน O-1..O-5 ทั้งหมด (lead ตามอำนาจ D22):** **(O-1→DCR-3)** enroll ซ้ำ = **200 idempotent + enrollment เดิม** — SRS LRN-001 เป็น master ของพฤติกรรมผู้ใช้; BFF จับ ERR-ENR-001 จาก RPC → คืน enrollment เดิม 200; **API-SPEC 1.0.0→1.0.1** · **(O-2)** ไม่ต้อง DCR — BFF อ่าน `lesson_progress` แถวตัวเอง (SELECT policy มีอยู่) คำนวณ `p_watch_sec_delta = max(0, positionSeconds − video_max_position_sec เดิม)` เอง; `documentRead` = attestation → position null + dwell เท่านั้น (แบบ probe F5); heartbeat จาก config `VIDEO_HEARTBEAT_SEC` · **(O-3)** authoring CRUD (CAT-001/005/006) **เลื่อนออกนอก Wave C** — dev ใช้ seed สร้างหลักสูตร; DCR เพิ่ม endpoint ตอน authoring wave จริง · **(O-4)** เพิ่ม lane **C-0 Auth/Session foundation**: SSR session helpers + login/register/logout ขั้นต่ำ + **MFA fail-closed** (role บังคับ MFA แต่ aal ต่ำกว่า → ปฏิเสธ 403) — MFA enrollment เต็มชุดคง Wave F; **lead ตรวจ C-0 เอง** (security-critical) · **(O-5)** เป็นเกณฑ์ exit ของ C-1: diff `ROLE_PERMISSIONS` vs RBAC-DESIGN §2 matrix พร้อม unit test ยืนยัน count | Phase 0 spawn 5 workers (flash ตาม D9): c0/c1 (BFF core) + c6/c7/c8 (UI skeletons) ไฟล์ disjoint; C-2/3/4 ตามหลัง C-1 · worker gates = `npx eslint <ของตัวเอง>` + `npx tsc --noEmit` + `npx vitest run <ของตัวเอง>` (build รวมโดย lead กัน .next stampede ระหว่าง worker ขนาน) · D23 ladder (flash>3 → sonnet×3 → lead) มีผล |
| D26 | 2026-09-10 | **Supabase client canonical เดียว + บทเรียน tsc สำคัญที่พิสูจน์แล้ว:** (1) **user-JWT client = `createSupabaseSsrClient` ที่ `src/lib/supabase/ssr.ts`** — ลบ `src/lib/supabase/client.ts` (`createSupabaseUserClient` ซ้ำเชิงหน้าที่ 100% กับ ssr.ts ของ C-0); `server.ts` เหลือ `createSupabaseServiceRoleClient` อย่างเดียว (service-role ตาม D11-1); rbac.ts/rbac.test.ts ของ C-1 เปลี่ยน import/mock มาที่ ssr ทั้งคู่ (เดิมอ้างชื่อเก่าจาก `./supabase/server` = ชื่อไม่มีอยู่จริง) · (2) **TS 5.9: syntax error ไฟล์เดียวในโปรแกรม → tsc รายงานแค่ชั้น parse และข้าม semantic check ทั้งโปรแกรม** (พิสูจน์ด้วย mini-project + probe จริงใน repo) — ระหว่าง c7 เขียน video-player.tsx ค้าง (~2,110 syntax errors)  claim "own files tsc-clean" ของทุก lane เป็นโมฆะ (absence of evidence); **วิธี verify ที่ถูก: tsconfig แยก extends โปรเจกต์ include เฉพาะไฟล์ตัวเอง** · (3) ผลตรวจ C-0 โดย lead (D25-O4): โครงถูกทุกข้อ (validate กับ Auth server / aal fail-closed / MFA_REQUIRED_ROLES ตรง RBAC §1.1+§4.2 / anti-enumeration / open-redirect guard / ไม่มี PII log) แต่ isolated tsc จับ **semantic errors 6 จุด** ที่ full-project tsc บอด: import `createSupabaseSsrClient` จากโมดูลผิด (พัง runtime) + `"registered"` หลุด union + type guard 2 หน้า + `exactOptionalPropertyTypes` 2 จุด — **lead แก้เองทั้ง 6 fold เข้า lane commit** (แบบเดียวกับ B-01/B-03) · gate ใหม่: worker ต้องยืนยัน verify ด้วย isolated tsconfig เท่านั้น | หลักฐาน: `/tmp/ts_syntax_theory` (mini-project) + probe zz_probe_lib.ts (อยู่ในโปรแกรมแต่ error ไม่ถูกรายงานจนกว่า syntax error หมด) + `/tmp/tsc_auth_final.txt` (C-0 สะอาด 0/27 tests ผ่าน หลังแก้) |
| D27 | 2026-09-10 | **codex-gate-c01 PASS — ข้อตัดสินความมั่นคงผูกพันทั้งโครงการ:** (1) **requirePermission = เส้นทาง authorization เดียว** ลำดับ session → roles → MFA gate (role บังคับ MFA แต่ aal≠aal2 → 403 ERR-AUTH-004 ก่อนพิจารณาสิทธิ์) → permission; route ที่ทำ auth เองต้องใช้ requireMfaForRoles — ยกเว้น allowlist AUTH-007 เท่านั้น (mfa/enroll, mfa/verify, logout, GET /me) (2) **hardenedCookieOptions (`lib/supabase/cookies.ts`) = ทางเดียว** ที่เขียน auth cookie — httpOnly:true + sameSite:lax + **path:"/" บังคับทับ** options ของ library ทุกครั้ง (3) **CSRF = เทียบ origin เต็ม (scheme+host+port)** ไม่ใช่เฉพาะ host + SAFE_METHODS allowlist (GET/HEAD/OPTIONS) + Sec-Fetch-Site ยอมเฉพาะ same-origin\|none — same-site คือต่าง origin → ปฏิเสธ (SDS §5.4) (4) staff:registrar = 22 permissions ตาม ROLE_PERMISSIONS จริง (5) user_banned → ERR-AUTH-002 เหมือนรหัสผิด (GoTrue ตรวจ banned ก่อน password = รูเยิบ ถ้าตอบต่าง) (6) AUTH-001 บังคับที่ infrastructure จริง: `enable_confirmations=true` (supabase/config.toml) + `GOTRUE_MAILER_AUTOCONFIRM=false` (docker-compose) | วงบรรจุ 4 รอบ: r1 3B+7M → r2 1B+1M+2m → r3 1M+1m → **r4 PASS FINDINGS: none** — lead ตรวจยืนยันทุก finding จริงก่อนแก้และลงมือเองทั้ง 14 ข้อ (วิถี D18 สำหรับ security-critical) · บทเรียน Next.js สำคัญ: `NextResponse.next({request})` จับค่า headers ณ จุดสร้าง → แก้ cookie แล้วต้องสร้าง response ใหม่ในทุก setAll (พร้อม carry คอลเลกชัน cookie เก่า — `splice(findIndex…)` คืน −1 แล้วตัดผิดตัวท้าย array → ใช้ Map-carry) |
| D28 | 2026-09-10 | **Phase 1 kickoff + DCR-4/DCR-5 APPROVE (lead ตามอำนาจ D22):** **(DCR-4 — DD 1.0.0→1.1.0 + API-SPEC 1.0.1→1.0.2)** เติมฟิลด์แสดงผล catalog ที่ SRS CAT-002/CAT-004 กำหนดแต่ตารางฐานถูก RLS กั้น: enum `course_level` + `courses.level` (default beginner) + `courses.outcome_highlights text[]` + views สาธารณะ 3 ตัว (`course_public_stats` = learner_count+credits จาก credit_rules · `course_instructors_public` = display_name/title/bio จาก created_by — ห้าม email/phone/ชื่อจริง บันทึกทะเบียน PII §4.5 · `course_exam_summary` = เงื่อนไขสอบปลายหลักสูตร) — definer-views + GRANT SELECT anon/authenticated โดย policy ตารางฐานคงเดิมทั้งหมด · **(DCR-5)** เพิ่ม GET /lessons/{id}/quiz ใน API-SPEC §3.4 — โจทย์+ตัวเลือกไม่มี is_correct/explanation (service path ตาม RLS note ของ quiz_options ใน DD) = ทางออกเฉลยใน client bundle ของ C-7 · **Phase 1 spawn 6 workers (flash ตาม D9):** c5 (admin BFF + CURSOR_HMAC_SECRET) · c6 (catalog wire + ต่อฟิลด์ DCR-4 ใน courses routes) · c7 (learner wire — เฉลยออกจาก bundle + logout จริง) · c8 (admin wire) · c9 (migration DCR-4 + seed + integration tests + พิสูจน์ repeated-or) · cc (consolidation: page-envelope helper + RPC-error parser + GET quiz route) — ไฟล์ disjoint ทุก lane | เหตุผล: SRS AC เป็น master ของฟิลด์ UI (แบบเดียวกับ D25-O1) — ตัดฟิลด์คือทำ AC พัง · definer-view เป็นแนวทางเดียวกับ learner_attempt_view/instructor_attempt_view ที่ DD ใช้อยู่ · C-10 Playwright เลื่อนท้าย Phase 1 (lead approve เงื่อนไขเดิม — e2e ต้องมี flow จริงครบก่อน) |
| D29 | 2026-09-10 | **CTO gate Phase 1 ผ่าน (r14 PASS 0B/0M/0m) + merge --no-ff f0aaed8 feat/wave-c-phase1 → develop** — fix-loop 14 รอบ (1B/1M → 0B/2M → 0B/2M → 0B/2M/1m → 0B/2M/2m → 0B/2M/2m → 0B/4M/0m → 0B/2M/2m → 0B/1M/0m → 0B/2M/0m → 0B/1M/1m → 0B/2M/0m → 0B/1M/0m → **PASS**) · ผลลัพธ์ถาวรของ loop: **นโยบายเผยแพร่ cookie auth กลางตัวเดียว** `selectPublishableAuthCookies` (lib/supabase/auth-errors) ใช้จริงครบทุกทางออกของ SDK — middleware two-phase (r11) · getUser ของ handler ผ่าน commitAuthWrites (r12) · query/RPC path ของ plain client setAll (r13 — `.from()/.rpc()` เรียก getSession ภายใน) · logout verdict commit (r5–r10) — การลบ credential เกิดได้ที่เดียว: เมื่อ error ยืนยันตายจริงตาม allowlist ของ GoTrue v2.164.0 (live-probe) หรือเป็น cleanup ขี่มากับ rotation · regression ครบโซ่ด้วย SDK จริง: middleware → getUser → public handler → logout (27/27 probes ของ codex + vitest 499/499 ของ lead) · post-merge gates บน develop: vitest 499/499 · tsc 0 · build 17/17 · integration 31/31 · gitleaks diff 23 commits + FS = 0 leaks · ขั้นถัดไป: cleanup backlog PB-1..PB-10 (นำด้วย PB-6 compose GoTrue group + PB-4 error.tsx ไทย + PB-2 vitest esbuild) แล้วจึง C-10 Playwright ปิดท้าย Phase 1 | เหตุผล: ทุก MAJOR ใน loop คือ residual ของ fix รอบก่อน — วนจน codex พิสูจน์เองด้วย SDK จริงว่าไม่เหลือทางรั่ว (r14: probe chain จริง + 27/27 + ไม่มีวงจรค้างถาวร) · merge โดย develop อยู่ที่ merge-base พอดี = tree หลัง merge เหมือน tree ที่ gate ผ่านเป๊ะ |
| D30 | 2026-09-10 | **Phase 1 cleanup batch 1 ครบ 7 รายการ (PB-1..PB-8 ยกเว้น PB-9 doc/PB-10 E2E) บน feat/phase1-cleanup 5 commits (47b471b → 1e69c48)** — PB-6 lead (compose role claim + probe จริง) · PB-7 worker (soft-delete filter; RPC กรองอยู่แล้ว) · **PB-1 lead (middleware เจ้าของ rotation เดียว LEAD 180 วิ ตัด getUser ออก — verdict จาก error ของ getSession ตรง ๆ; กันดัก AuthSessionMissingError หลัง session null ที่จะล้าง credential ที่ยังมีชีวิต)** · PB-4/2/5 worker b (error.tsx ไทย 5 segment + esbuild jsx automatic + server-only alias — ไม่ลบ mock เดิม) · PB-3/8 worker c (cache() ที่ findPublishedCourse + getAdminStaffSession ตาม evidence การเรียกซ้ำจริง + logout behavioral 4 เคส) · เพิ่ม PB-11 (v_enrollment_progress ไม่กรอง soft-delete — flag โดย worker PB-7) | เหตุผล: ทุกรายการ verify แยกก่อน commit โดย lead (ไม่เชื่อ claim ลอย) — unit สุดท้าย 525/525 (44 ไฟล์) · tsc 0 · eslint 0 · build 17/17 · integration 31/31 · gitleaks FS + diff 0 leaks · ขั้นถัดไป: codex gate ของ cleanup branch (แตะ auth/data: PB-1 middleware + PB-6 compose + PB-7 route) ก่อน merge develop |

| D32 | 2026-09-10 | **codex gate cleanup r2 = FAIL (0 BLOCKER / 1 MAJOR / 2 MINOR) → lead ตรวจยืนยันทุก finding กับ source จริง (auth-js GoTrueClient + @supabase/ssr createServerClient + policies) แล้วแก้ครบบน `3741f0e`** — **M1-residual**: middleware ข้าม rotation ขาในแล้ว (cb68382) แต่ SDK ฝั่ง handler ยังหมุนเอง: `_useSession` สั่ง refresh เมื่อ token เหลือ < EXPIRY_MARGIN_MS 90 วิ ไม่ขึ้นกับ autoRefreshToken (GoTrueClient.ts:3068/3113) → rotation กลางขาในสำเร็จ server-side แต่ Set-Cookie โดน RSC loader ทิ้ง = race PB-1 ยังอยู่ · แก้ที่ชั้น factory เดียว `ssr.ts`: client ของขาใน (เห็น `x-ltc-bff-internal: 1` ผ่าน headers() — dynamic import ใน try/catch เพราะ mock เดิมของ next/headers export แค่ cookies) ได้ `global: { fetch }` ที่ตอบ POST /auth/v1/token ด้วย 400 `{error_code:"bff_internal_leg"}` — non-retryable (ไม่โดน backoff 5xx) + อยู่นอก allowlist ตายจริงของ auth-errors → auth-js preserve session ที่ยังไม่หมดอายุ (handler ทำงานต่อด้วย token เดิม — ไม่มี rotation ไม่มีการลบ) / token ตายจริง → การลบโดน setAll policy กลืนตาม doctrine r13 (deathConfirmed=false) · **MINOR-1**: migration 0018 ผูก `m.course_id = e.course_id` ใน subquery completed ของ v_enrollment_progress — การย้ายบทเรียนข้ามหลักสูตร (lessons_update 0010:355 อนุญาต staff:content) เคยทำให้ completed>total → pct>100 · **MINOR-2**: afterAll ของ suite ใหม่ 2 ไฟล์กลืน error คืนค่าด้วย .catch(()=>{}) → เปลี่ยนเป็น restore-แบบตรวจ (re-select status/deleted_at + throw ถ้าไม่ตรง) + ลบผู้ใช้ทดสอบใน finally · โบนัสจากการเขียน test จริง: keyset or= ของ route ทำงานบน RPC ผ่าน PostgREST จริง (ก่อนหน้านี้พิสูจน์แค่ฝั่ง unit ที่ mock SDK) + จับข้อจำกัด pruning ของ PostgREST function inline: คอลัมน์ที่ or=/order= อ้างต้องอยู่ใน select= ด้วย (route ใส่ครบอยู่แล้ว — จำไว้ตอนเขียน query ใหม่) | ทุก fix มี regression จริง: unit 3 tests ผ่าน GET handler จริง + @supabase/ssr + supabase-js จริง (ขาใน token เหลือ 45 วิ → 200 ด้วย token เดิม + 0 call ถึง /token + jar เป๊ะ · positive control ไม่มี header → rotation ถึง jar จริง · ตายจริง → credential อยู่ให้รอบถัดไปตัดสิน) · integration: ย้ายบทเรียนข้ามหลักสูตรผ่าน claims path จริงของ staff:content + จัดการ uq_lessons_sort (ย้าย sort_order ไปช่องว่าง + คืนค่า) · **gates ท้าย round: unit 530/530 · integration 37/37 (7 ไฟล์) · tsc 0 · eslint 0 · build exit 0 (Middleware 121 kB) · gitleaks FS + diff e773ebe..HEAD = 0 leaks** · ต่อไป: codex gate r3 (diff e773ebe..3741f0e) → PASS = merge --no-ff develop |

| D33 | 2026-09-11 | **codex gate cleanup r3 (final): PASS — 0 BLOCKER / 0 MAJOR / 1 MINOR → CTO APPROVE (lead ตัดสินตามอำนาจ D22) · MINOR แก้พร้อม approve บน `2c4506f` แล้ว merge --no-ff feat/phase1-cleanup → develop** — codex ยืนยันจาก source ที่ติดตั้งจริงทุกข้อ: (1) M1-residual ปิดจริง — options.global.fetch ไหลผ่าน createServerClient (ssr/src:155) → SupabaseClient (:349/:667) → GoTrueClient ผ่าน resolveFetch จนถึง _refreshAccessToken (:4788) · synthetic 400 → AuthApiError code `bff_internal_leg` ไม่ผ่านเงื่อนไข retry (fetch.ts:95) · preserve จริง (_callRefreshToken:5120 ไม่ลบ session เมื่อ access token ยังไม่หมด · __loadSession:3113 คืน session เดิม error:null) · token ตายจริง → การลบไม่ definitive ตาม auth-errors:43 → plain factory กรอง false (ssr.ts:107) · buffered ผ่าน commitAuthWrites(false) · header spoof benign (CSRF ยังตรวจ middleware:75 · handler ตรวจ user/MFA/RBAC ตามเดิม) (2) 0018 ปิดจริง — completed ⊆ total ครบทุกกรณี (uq_lesson_progress กันนับซ้ำ) + GRANT คงอยู่ตาม PostgreSQL 15 CREATE OR REPLACE semantics (3) verified-restore ปิดจริง (vitest failTask จับ throw ของ afterAll) · keyset proof ตรง route.ts:66 (codex ขอจำกัด claim: พิสูจน์ filter-accept + cursor-cut ไม่ใช่ tie-break/multi-page) · **MINOR ที่เหลือ**: test ย้ายบทเรียนคืนค่า "หลัง assertions" → assertion พังกลางทาง = seed lesson ค้าง LTC-101 โดย afterAll ไม่เห็น → แก้: คืนค่าใน finally (UPDATE idempotent + verified re-select module_id/sort_order) + ลบ progress แม้ restore ล้ม + rethrow error เดิม · **negative control จริง**: สำเนา test ที่ตั้ง toBe(999) ทำ suite fail แต่ seed intact (LTC-102 lessons=2 คืนครบ · LTC-101=3 · course published/null) | วงจร cleanup branch 3 รอบ: r1 0B/3M/0m → r2 0B/1M/2m → **r3 PASS 0B/0M/1m (แก้พร้อม approve)** · gates ท้าย fix: unit 530/530 (45 ไฟล์) · integration 37/37 (7 ไฟล์) · tsc 0 · eslint 0 · merge เมื่อ develop = merge-base e773ebe → tree หลัง merge = tree ที่ gate ผ่านเป๊ะ · verdict: /tmp/codex_gate_cleanup_r3_verdict.log · ต่อไป: PB-10/C-10 Playwright ปิด Phase 1 |

| D34 | 2026-09-11 | **C-10/PB-10 Playwright smoke แล้วเสร็จ — Phase 1 ปิดสมบูรณ์ · merge --no-ff `da7434b` feat/c10-playwright-smoke → develop (test-only · lead review แทน codex gate)** — worker-e2e (flash ตาม D9): playwright.config.ts + specs 4 ไฟล์ + helpers 6 ไฟล์ (898 บรรทัด ไฟล์ใหม่ล้วน) ครอบ E2E-04 (แคตตาล็อก 4 published · draft ไม่เห็น · ค้นหา/ล้าง · CTA `/login?next=` · login ฟอร์มจริง · enroll 201/200 DCR-3 · /my/courses 0/3) · E2E-05 (document attestation → completed 1/3 · video heartbeat จน watch_pct≥80 — ใช้เวลาจริง 7.2 นาทีตาม anti-fabrication bound ของ RPC `accum ≤ elapsed+60s` → 2/3) · E2E-06 (ตอบผิด 0% → ทำซ้ำตอบถูก 100% + auto-complete 1/3 · **เฉลยอยู่ฝั่ง harness เท่านั้น** — อ่าน `is_correct` ผ่าน psql · กัน shuffle_questions ด้วยการ target คำถามด้วยข้อความ ไม่ใช่ index) · logout (ปุ่มจริง → /login · /api/v1/me 401 · protected page แสดง error card ไทย · re-login ได้) · **คำตัดสิน lead:** (1) test-only = ไม่ต้อง codex gate (ไม่แตะโค้ด auth/security/data — lead อ่านทุกไฟล์เอง + gates เต็ม) (2) mutation ของผู้เรียนที่ UI ยังไม่มีปุ่ม (enroll/heartbeat/documentRead) อนุญาตผ่าน browser-session fetch same-origin เท่านั้น (ผ่าน CSRF จริงของ middleware) — ห้าม service-role สร้าง learner state (3) ลบ SERVICE_KEY dead export (harness ใช้ psql อย่างเดียว) (4) .gitignore + `e2e/.artifacts/` · **gaps จริงบันทึกเป็น PB-12** (ไม่มีปุ่ม enroll ของผู้เรียน · VideoPlayer src=null · DocumentViewer paragraphs=[]) — e2e assert สถานะ disabled จริง ไม่แกล้งผ่าน · **gates (lead รันเองทั้งหมด): e2e 7/7 (7.6m) · unit 530/530 · integration 37/37 · tsc 0 · eslint 0 · build 17/17 (Middleware 121 kB) · gitleaks FS 0 (158.61 MB) + diff c09b7f1..6a19e03 = 0** · post-merge บน develop: unit/tsc/eslint/integration/build เขียวซ้ำ + `git diff develop feat/c10-playwright-smoke` ว่าง (tree เดียวกันเป๊ะ — e2e ไม่ re-run เพราะ bit-identical) · เก็บกวาด: ผู้ใช้ e2e ค้าง 0 · quiz_attempts กำพร้า 0 · ลบ probe-pb6* 5 ตัวของ lead เอง (FK-ordered) | ปิดทั้ง Wave C Phase 1 + cleanup backlog (เหลือ PB-9 doc item + PB-12 โจทย์ Wave ถัดไป) · ขั้นถัดไป: วางแผน Wave D — Assessment + Certificates |

| D31 | 2026-09-10 | **codex gate cleanup r1 = FAIL (0 BLOCKER / 3 MAJOR / 0 MINOR) → lead แก้ครบทั้ง 3 + PB-11 บน feat/phase1-cleanup (1766113 → f0fe460)** — **M1** (โครงสร้าง PB-1): ขา fetch ภายในของ server (catalog/learning/admin → BFF) วิ่งผ่าน middleware เหมือน browser และหมุน token ตรงนั้น — Set-Cookie ของ rotation ขาในไม่เคยถึง browser (RSC loader ทิ้ง) → LEAD 180 แค่ขยับหน้าต่างแข่ง 90→180 วิ · แก้ `cb68382`: header `x-ltc-bff-internal: 1` ที่ 3 choke points (ตั้งเฉพาะฝั่ง server — `typeof window === "undefined"` — เพราะ catalog.ts/learning.ts แชร์ client/server; admin.ts server-only) + middleware ข้าม auth-refresh block ทั้งก้อนเมื่อเห็น header · browser ปลอม header ได้แต่ benign (แค่ทำ request นั้นไม่ถูกหมุน ไม่ข้าม CSRF/authz; request ถัดไปหมุนตาม LEAD) · **M2+M3** (โครงสร้าง PB-7): `.eq("courses.deleted_at", null)` ของ supabase-js serialize เป็น `eq.null` string ไม่ใช่ `is null` (M2) และ `courses!inner` embed ทำให้แถว enrollment ตกใต้ RLS published-only ของ courses → archive หลักสูตร = ประวัติผู้เรียนหายเกินเจตนา soft-delete (M3) · แก้ `b9cf573`: **migration 0017 RPC `my_active_enrollments`** — SECURITY DEFINER + `language sql` + STABLE (PostgREST ยอม chain select/order/limit/or) + `set search_path = public` + revoke public/anon ก่อน grant authenticated + owner app_owner (แบบแผน 0011) · route เรียก rpc + chain เดิม ตัด embed ทิ้ง · integration 3 tests บน DB จริง (A เห็นของตัวเอง B ไม่เห็น / archive → ยังอยู่ / soft-delete → หาย) · **PB-11** `1766113`: migration 0016 + integration 2/2 ผ่าน claims path จริงของ staff:content · **dev-stack** `f0fe460`: GoTrue email rate limit default 30/ชม. ทำให้ /signup โดน 429 กลางวงจร integration (อีเมลไป Mailpit อยู่แล้ว) → compose `GOTRUE_RATE_LIMIT_EMAIL_SENT: "1000"` | ทุก finding ตรวจยืนยันกับ source จริงก่อนแก้ (auth-js retry semantics: 5xx → exponential backoff 200·2^n ×10 ≈ 3 นาที — test ต้องใช้ 400 non-retryable แทน 500) · บทเรียน gitleaks: image ต้อง `:latest` (v8.18.4 ไม่รองรับ `[[allowlists]]` → 18 false positives) + ห้ามเขียน report ลง repo (JSON เต็ม JWT string โดน flag รอบถัดไป — เขียนที่ /tmp) · **gates ท้าย round: unit 527/527 · integration 36/36 (7 ไฟล์) · tsc 0 · eslint 0 · build exit 0 (Middleware 121 kB) · gitleaks FS + diff e773ebe..HEAD = 0 leaks** · ต่อไป: codex gate r2 (diff e773ebe..f0fe460) → PASS = merge --no-ff develop + post-merge gates → PB-10/C-10 Playwright ปิด Phase 1 |

| D35 | 2026-09-11 | **PB-9 แล้วเสร็จ — `CURSOR_HMAC_SECRET` fail-closed + boot enforcement + provisioning doc · merge --no-ff `18faaa1` feat/pb9-cursor-hmac-provisioning → develop (แตะ config/auth class → codex gate 3 รอบ)** — สาระ: (1) superRefine config.ts: APP_ENV=prod ขาด CURSOR_HMAC_SECRET → ConfigError (staging รัน prod mode; dev ยัง fallback service-key PRF — ค่าว่างเป็น error แม้ local) (2) instrumentation.ts register() ตรวจตอน nodejs bootstrap (Next 15 stable hook — ตรวจกับ installed source ว่า rethrow) (3) /api/health readiness backstop: config พัง → 503 `status:"error"` `db:"unknown"` รูปเดิม {status, db, time} (4) `import "server-only"` ที่ config.ts — ทำให้คำกล่าวอ้างเรื่องกัน client bundle เป็นจริงโดยกลไก (build พังถ้า import เข้า Client Component; `import type` compile-time erased — CategoryFormModal ไม่ลากไป) (5) SECRETS-PROVISIONING.md 1.0.0 + .env.example แก้ drift ชื่อ stream vars เป็น 2 ค่าตาม schema · **gate loop: r1 0B/4M/1m → r2 0B/2M/1m → r3 PASS 0B/0M/1m** — r1 จับ: lazy getConfig ≠ boot check / rotation ไม่มี redeploy / stream var names / gitleaks-NEXT_PUBLIC overclaim; r2 จับ: singleton-cache ทำให้ tests พังตามลำดับ (shuffle seed 42 พังจริง) → vi.resetModules + dynamic import + SAVED env restore (จริง ไม่ใช่ delete) + พิสูจน์ order-independence ด้วย seed 42/7; r3 MINOR (pooler URL ฝังรหัสผ่าน DB — หมุน password = อัปเดต env + redeploy) แก้พร้อม approve ตามแบบแผน D33 · **คำตัดสิน lead:** ประเด็นเดิมที่ว่า "PB-9 = documentation item" ไม่พอ — ใช้กลไกบังคับจริง 3 ชั้น (instrumentation + health 503 + call-site throw) เพราะ Next.js บางรุ่นกลืน instrumentation error | เหตุผล: ปิด C-1 flag → C-5 ใช้งานจริง · ASVS V14 (config validation) · SDS §7.1 ตรวจครบตอน boot · verdicts: /tmp/codex-gate-pb9{,-r2,-r3}.txt · branch 4 commits (`6aa9c40` → `64dc8af`) · post-merge develop: unit 540/540 (47 ไฟล์) · tsc 0 · eslint 0 · build exit 0 · gitleaks FS 156 MB + diff 4 commits = 0 · **Phase 1 + cleanup backlog ปิดครบทุกรายการ (เหลือ PB-12 โจทย์ Wave ถัดไป)** · ต่อไป: Wave D planning (Assessment + Certificates) |
| D36 | 2026-09-11 | **Wave D kickoff — lead อนุมัติแผน `.omc/plans/wave-d-plan.md` (worker flash เขียน draft + lead spot-check ข้อเท็จจริงทุกข้อสังเกตกับ source ก่อนตัดสิน) + ตัดสิน O-1..O-9 ทั้งหมด + DCR-6** — **(O-1)** submit ข้อสอบ = **ตอบผลตรวจทันที** ไม่ใช่ status=grading (RPC `submit_attempt` 0011 L881-884 synchronous + idempotent ระดับ RPC คืน `already_submitted:true` — API-SPEC เดิมเขียน async + BFF แคช 24 ชม. เกินกว่า contract จริง) → **DCR-6 (API-SPEC 1.0.3)**: submit row + Idempotency-Key #8 + result row (เฉลย baseline) + bulk/monitoring/statistics เลื่อน Wave E · naming `expires_at`→`deadlineAt`+`serverTime` map ที่ BFF · **(O-2)** เฉลยตาม baseline `after_final_attempt` บังคับใน `learner_attempt_view` — BFF เปิดเฉลยเองห้าม; config ต่อหลักสูตร (ASM-012) = DCR อนาคตเมื่อมีคอลัมน์ (migrations ของ wave นี้คงเฉพาะ lead-owned) · **(O-3)** ยืนยันตาม baseline: cert issue/revoke/reissue = **service_role ขอบเขตแคบที่ `src/lib/certificates/**` (D-4 เจ้าของเดียว) + audit ทุกจุด** — ไม่เพิ่ม RPC (DD §4.7 ไม่ลิสต์ cert functions โดยเจตนา) · **(O-4)** `/admin/certificates/bulk` **เลื่อน Wave E** (ต้องมี job infra — Wave D ออกรายใบ + คิว eligible พอต่อ M3) · **(O-5)** auto-submit scheduler **Wave D รับ (lane D-10)** — ASM-005 AC บังคับ; spawn Phase 3 หลัง lead verify privilege path (RPC granted authenticated — service_role เรียกไม่ได้ตรง ๆ อาจต้อง migration lead-owned 0019) · **(O-6)** PDF = **pre-render ตอน issue ด้วย pdf-lib + qrcode +ฟอนต์ Sarabun (OFL)** — lead ติดตั้ง deps + commit ฟอนต์เองแล้ว (npm audit ยืนยัน advisory 4 รายการเป็นของเดิม vitest/next ไม่เกี่ยว pdf-lib/qrcode); PDF เป็น snapshot นิ่งตาม design (holder_name_snapshot) — revoke ไม่ re-render, reissue = PDF ใหม่แถวใหม่ · **(O-7)** QR สร้างฝั่ง server ฝังใน PDF เท่านั้น (payload `CERT_PUBLIC_BASE_URL/verify/{verify_code}` ตาม D10); หน้า UI แสดง URL ข้อความ + ปุ่ม copy ไม่เพิ่ม lib ฝั่ง browser · **(O-8)** lead แก้ `rate-limit.ts` เอง (ไฟล์ของ C-1 — หลีกเลี่ยง conflict ข้าม lane): เพิ่ม rule `GET /certificates/{id}/pdf` → READ ก่อน PUBLIC_READ + tests 24/24; `/assessments/{id}` GET + `/me/attempts` = fallback READ ถูกต้องแล้ว · **(O-8b)** monitoring/statistics เลื่อน Wave E (รวมกลุ่ม reports) · **(O-9)** CRT-008 auto-issue เลื่อน Wave E (default ปิดอยู่แล้ว) | เหตุผล: contract DB พร้อม 100% (0005/0006/0009/0010/0011) ไม่มีจุดรอ B-02 เหมือน Wave C — ความเสี่ยงจึงอยู่ที่ (1) service_role ขอบเขตแคบ (รวมศูนย์ D-4 + audit) (2) เฉลยหลุด client (ตัดที่ BFF + view บังคับ) (3) session binding (claim เท่านั้น) · lanes 11 เรื่อง D-0..D-10 ตามแผน · Phase 0 spawn 5 workers ขนาน (D-0..D-4 ไฟล์ disjoint) · deps ใหม่: pdf-lib + qrcode + @types/qrcode + Sarabun TTF 2 ไฟล์ 88KB (OFL) |
| D37 | 2026-09-11 | **lead Phase-0 batch 0019 + BFF wire edits — PB-14/15/16 ปิด · contract สำหรับ Phase 1 พร้อม (D-4b)** — (1) **audit allowlist 0019**: ใต้ service_role RPC รับ AUTH_* 12 เดิม **+ CERT_ISSUE/CERT_REVOKE/CERT_REISSUE/PII_ACCESS** (mutation events ของ D-4 ตาม AUDIT §4 class ก) — actor derive จาก p_context.user_id แล้ว strip ก่อน strict-keys (คงแบบแผน D16-N1) (2) **paper view contract**: question_paper = `{question_id,version,text,options[{id,text}]}` — BFF ตรวจ ExamPaperJsonb `.strict()` ทุกชั้น + question_id ตรงแถว ไม่งั้น ERR-SYS-002 (fail-closed กัน DB drift รั่วเฉลย) · option_order ใน snapshot = display order แล้ว **BFF ห้ามเรียงซ้ำ** (3) **submit contract**: question_count=count(*) + total_points=sum(points) required ทุกทาง (replay ไม่มี correct_count) (4) **pass_pct grant**: authenticated อ่านได้ — 0012 course_exam_summary เผยสาธารณะอยู่แล้ว จึงไม่ใช่การเปิดเพิ่ม · **selection ยังซ่อน** (0010 L709-713) — D-7 UI สัญญา pass_pct ได้ ห้ามสัญญา selection (5) **audit entityId honest-null**: คิว eligible ว่าง = entityId null (แก้ bug เดิม `?? request.url` ที่ fail uuid cast → RPC ปฏิเสธทั้ง call → PII_ACCESS หายไปเงียบๆ) (6) **ธง D-5 — mid-exam reload gap**: โจทย์มาทางเดียว POST start · reload กลางสอบ → start_attempt ตอบ ERR-ASM-002 ตราบ in_progress (takeover หลัง lease หมดเท่านั้น) → D-5 UI เก็บ state คำตอบในหน้า + เตือนผู้เรียนเรื่อง reload · ถ้าต้อง resume จริง = **DCR endpoint ใหม่ ผ่าน gate — ห้ามเปิดลับๆ** | 0019 = lead-owned migration เดียวของ Phase 0 (workers ห้ามแตะ migrations ตาม D36) · evidence: probes **25/25** · integration **37/37** rc=0 · wire gates: eslint 0 · isolated tsc 0 · vitest **846/846** (72 ไฟล์) · typecheck 0 · build 0 · gitleaks 157MB FS + diff = 0 · commits `f9222fa` + `4fd51d3` · บทเรียน 2 ข้อ: zod v4 array ไม่มี `.strict()` (object เท่านั้น) · block comment ไทยห้ามมีอักขระ `*/` ในเนื้อความ (รูป `X_*/Y` ปิด comment กลางคัน → esbuild error) |
| D38 | 2026-09-11 | **codex gate Wave D Phase 0 r1 = FAIL (4 BLOCKER / 4 MAJOR) → lead ตรวจยืนยันทุก finding จริง แก้ครบทั้ง 8 + probes จับ migration bug จริงอีก 3 ตัวที่ unit-mock มองไม่เห็น (B9/B10/B11) ทั้งหมดใน 0019 กับ BFF** — **B1 (BLOCKER)** media_read สาขา instructor/is_staff เปิดทั้ง bucket → กัก `bucket='media'` เท่านั้น (bucket certificates = PDF มีชื่อเจ้าของ = PII · เจ้าของใบอ่านผ่านสาขา pdf_media_id) + storage policy mirror เงื่อนไข inline ทั้ง 3 สาขา (defense in depth) · **B2+B7 (BLOCKER+MAJOR)** mutation กับ audit คนละ PostgREST call → **SECURITY DEFINER RPCs ใหม่**: admin_issue/revoke/reissue_certificate (mutation+audit TX เดียวตาม D12-8 · EXECUTE service_role เท่านั้น · cert_issue_core shared issue/reissue · cert_no ปี-สะสม + verify_code 43 อักขระ CSPRNG alphabet เดียวกับ shared.ts) + reissue lineage (superseded_by/supersedes_cert_id) ครบใน TX เดียว ล้มช่วงไหน = rollback ทั้ง · **B3+B5 (BLOCKER+MAJOR)** route verify ใช้ service_role + 0009 revoke certificate_public_view จาก service_role → **RPC record_certificate_verification** (EXECUTE anon+authenticated เท่านั้น · log+audit atomic · ตอบ 4 ฟิลด์ไม่มี holder_name · code เข้า log คือ input ผู้ใช้ ไม่ใช่ verify_code จริง กัน secret ไหลตารางอื่น) + route เรียกผ่าน SSR user client จุดเดียว (B3 grep-assert: ไม่ import service client) · **B4 (BLOCKER)** response หลายที่ cast-and-map ไม่ validate → zod safeParse ทุกทางออก (assessments/me-attempts/me-certs/admin) · **B6 (MAJOR)** PATCH ตัวเลือกทีละแถว = หลาย TX ค้าง 0/2 เฉลย → **RPC admin_update_question** (โจทย์+ตัวเลือก TX เดียว · two-phase sort shift −1000000 กัน uq_question_options_sort ชน · instructor แก้ active โดน question_active_readonly_for_instructor · staff:exam แก้ได้) · **B8 (MAJOR)** eligible limit ก่อน filter issued → filter ก่อน page cut · **B9 (probe R4-R8)** DEFINER RPCs รันใต้ app_owner แต่ 0019 ไม่ทำคู่ ACL grant + policy UPDATE ของ app_owner แบบที่ 0011 ทำให้ submit_attempt (มีแค่ SELECT ACL + policy INSERT/SELECT) → grant insert/update 3 ตาราง + policy app_owner_update_* 3 ตัว — ไม่แก้ = RPC ใหม่ทั้งชุดพัง "permission denied" ทั้ง dev/prod (unit mock RPC จึงไม่เห็น) · **B10 (probe R5)** gen_random_bytes อยู่ schema extensions (pgcrypto — 0001 ไม่เคยลงโดยเจตนา) + supabase image ไม่ให้ USAGE ของ extensions แก่ app_owner → `create extension if not exists pgcrypto` + `grant usage on schema extensions to app_owner` + search_path `public, extensions` ที่ cert_issue_core เท่านั้น · **B11 (probe R8a)** PL/pgSQL loop var `o` เป็น scalar jsonb แต่ body อ้าง `o.value` → parser ตีเป็นตาราง (missing FROM-clause) ทั้ง RPC ตาย → อ้าง `o` ตรง ๆ | probes ชุดใหม่ R1-R11 (28 แถว): wrapper allowlist กลับทาง (service_role CERT_* → 42501 · PII_ACCESS ผ่าน) + privilege matrix 6 RPCs + functional ทุกตัว (issue cert_no/verify_code รูปจริง · revoke ซ้ำ not_valid · reissue lineage+audit · verify ใต้ anon 3 ทาง + log/audit 3+3 แถว · question edit สลับเฉลย/เจ้าของ/active guard · media+storage bucket scope · hash-chain) — วงจร 5 รัน 13→18→20→24→**28/28** บน full replay (make reset-db→migrate rc=0→seed rc=0→probes) · integration **37/37** หลัง re-seed (รอบแรก 36/37 = probe rows ทิ้ง enrollment completed ทำ learner_count LTC-101 เพี้ยน — state pollution ของ DB ไม่ใช่ code bug · บทเรียน: รัน integration บน seed สดเมื่อจบ probes) · บทเรียน probe เอง: subquery ใต้ `set local role anon` โดน RLS ตัดหมด → NULL (เก็บค่าเป็น \gset ก่อนสลับ role) + แยก TX ต่อ probe กัน abort cascade + cleanup ต้องลบลูกก่อนพ่อตาม FK |
| D39 | 2026-09-11 | **codex gate Wave D Phase 0 r2 = FAIL (0 BLOCKER / 6 MAJOR / 1 MINOR) → lead ตรวจยืนยันทุก finding จริง แก้ครบทั้ง 7 + full replay จับ migration bug จริงอีก 3 ตัว (B12/B13/B14) + probe r2 ชุดใหม่ 24 แถว** — **F1 (MAJOR)** media_read สาขาบทเรียน + storage mirror ไม่กัก bucket → เพิ่ม `media_assets.bucket='media'` ที่สาขา lessons.media_id ทั้งสอง policy (โจทย์: ผู้แต่งชี้ media_id ไป PDF ใบประกาศของคนอื่น = PII leak) · **F2 (MAJOR)** BFF attach PDF = PostgREST INSERT+UPDATE หลัง RPC issue commit แล้ว = mutation ไร้ audit คู่ → **RPC admin_attach_certificate_pdf** (validate path/mime/size + cert valid + idempotent [pdf_media_id มีแล้ว → attached:false ไม่ซ้ำ audit] + INSERT media_assets bucket certificates + UPDATE certificates + CERT_PDF_ATTACH audit TX เดียว · EXECUTE service_role เท่านั้น) + BFF issue/reissue เรียก attachCertificatePdf กลาง (render→upload→RPC · ทุก failure branch = คงใบ + pdf_media_id null + WARN certificate_pdf_{render,upload,attach}_failed fields {route,user_id} เท่านั้น) + **reissue ส่ง requestId ผูกเดียวกันทั้ง issue/attach** · **F3 (MAJOR)** wrapper class ข เหลือ AUDIT_READ/RATE_LIMIT_HIT/PII_ACCESS เท่านั้น (CERT_VERIFY_PUBLIC ออก — producer เดียว = record_certificate_verification) · **F4 (MAJOR)** record_certificate_verification ยอมรหัสอะไรก็ได้ → format gate `^LTC-[0-9]{4}-[0-9]{6}$\|^[0-9A-Za-z_-]{43}$` เท่านั้นผ่านไป lookup · ผิดรูป = 200 not_found หน้าตาเดียว **โดยไม่ persist ไม่ audit** (กัน enumeration/audit-flood ผ่านค่าขยะ) · **F5 (MAJOR)** BFF map DB row ด้วย `as` ล้วน → **zod row-validation ที่ขาเข้าก่อน map** (AdminAssessmentRowSchema/AssessmentRuleRowSchema + MyCertificateRowSchema `.strict()` → AppError ERR-SYS-002 reason `*_row_drift` 503 fail-closed ก่อน map — parseOutgoingView ขาออกคงเป็นชั้นสอง) · **F6 (MAJOR)** eligible = BFF สแกน chunk + post-filter → **RPC admin_eligible_certificates** (language sql stable definer · anti-join `not exists (certificates valid ของ enrollment เดียวกัน)` + keyset `(submitted_at,id) < (after_submitted_at,after_id)` desc + `limit least(greatest(coalesce(p_limit,20),1),101)` + holder_name mirror holderNameOf [first+last ไม่มีก็ display_name] · course filter ได้) + BFF listEligibleAttempts เรียก RPC เดียว + buildPage · **F7 (MINOR)** tags แก้ 3 จุด (`e #>> '{}'` ค่าจริงไม่มีอัญประกาศ · `?` แยก "ไม่มีคีย์" กับ null · `'{}'::text[]` coalesce · tags_not_array guard) \| **B12/B13/B14 (full replay จับ — unit-mock มองไม่เห็นอีก)**: **B12** 0019 create policy app_owner_insert_media_assets ซ้ำ blanket loop ของ 0010 §6 (มีทุกตารางอยู่แล้ว) → "already exists" ใน TX เดียว = **0019 rollback ทั้งไฟล์** (RPCs/buckets/r1 fixes หายหมด) → เหลือ grant insert เฉพาะ (policy ซ้ำ แต่ privilege ขาดจริง f→t) · **B13** holder_name coalesce(nullif(btrim(concat_ws(...)))) วงเล็บเพี้ยน → syntax error at `from` → rollback ทั้งไฟล์อีกรอบ → เขียน nesting ใหม่มี indent ชัด · **B14** CERT_PDF_ATTACH ส่ง p_certificate_id (uuid) ให้ append_audit_event_internal(p_entity_id **text**) → "function does not exist" ตอน runtime (plpgsql perform ไม่ตรวจตอน create) → `::text` ให้เหมือน call sites อื่นทุกตัว \| **hardening**: drop-if-exists ครอบ app_owner_update_* 3 ตัว + create **or replace** view (replay-safe ทั้งไฟล์) \| **สังเกต (ไม่แก้ — เกินขอบเขต r2 ตัดสินรอบหน้า)**: cm_read/lessons_read 0010 ให้เห็นบทเรียนเฉพาะ e.status='active' → ผู้เรียนจบหลักสูตรเสียสิทธิ์อ่านบทเรียน และ ('active','completed') ใน media_read สาขาบทเรียน = dead branch (completed ไม่มีทางผ่าน inner RLS) · uq_enrollments_user_course_active = 1 enrollment/คน/หลักสูตร (ไม่เช่ะตาม status) \| probe r2 ใหม่ 24 แถว (R1c wrapper deny CERT_VERIFY_PUBLIC · R2r privilege matrix RPC ใหม่ · R7f×4 ผิดรูปแบบไม่ persist ไม่ audit · R8f×3 tags · R12a-f attach สำเร็จ/idempotent/revoked/path ปลอม · R13a-e eligible anti-join+keyset+course filter · R14a-b lesson branch กัก bucket ทั้ง media_assets+storage.objects · R11r hash-chain) — วงจร 3 รัน 17→13→**24/24** (แกนหลัก: fixture ต้อง active ไม่ใช่ completed + คนละคนต่อ enrollment) · probes r1 ยัง **28/28** บน replay เดียวกัน \| gates: vitest **883/883** · tsc 0 · eslint ทั้ง repo 0 · build 0 · integration **37/37** บน seed สด · gitleaks FS 0 | ถัดไป: commit → codex gate r3 |
| D40 | 2026-09-11 | **codex gate Wave D Phase 0 r3 = FAIL (0 BLOCKER / 3 MAJOR / 1 MINOR) → lead ตรวจยืนยันทุก finding จริงกับ source แล้วแก้ครบทั้ง 4** — **G1 (MAJOR)** record_certificate_verification เปิด anon เรียกตรงแต่ persist p_ip_hash/p_request_id อย่างอิสระ (อีเมล = PII เข้า certificate_verifications + audit append-only) → format gate 2 ชั้นก่อนแตะ persist/audit ใดๆ: ip_hash = `^[0-9a-f]{64}$` (รูป sha256 จริงของ BFF ipHashOf) + request_id = uuid (รูป crypto.randomUUID ของ middleware — แหล่งเดียวที่สร้าง header นี้) → ผิดรูป = 22023 `ip_hash_invalid`/`request_id_invalid` · **G2 (MAJOR)** admin_attach_certificate_pdf อ่าน status/pdf_media_id ไม่มี FOR UPDATE → concurrent retry สอง TX อ่าน null พร้อมกัน (TX2 จบ error แทน attached:false) + revoke commit คั่นกลาง = แนบบนใบ revoked ได้ → `select ... for update` + ตรวจสถานะหลังถือล็อก (TX2 เห็น pdf_media_id ใหม่ → idempotent · revoke หลังล็อกเห็น status ใหม่ — เรียงตามลำดับล็อกจริง) · **G3 (MAJOR)** POST /admin/assessments ใช้ `(created as {id}).id as string` ก่อน validation + reload fail ตอบ ERR-SYS-001 500 → parseAdminAssessmentRow(created) ที่ขาเข้าก่อนหยิบ id (drift → 503 ERR-SYS-002 `admin_assessment_row_drift` ก่อนแตะ rules/reload) + reload fail → ERR-SYS-002 503 ตาม invariant 5 · **G4 (MINOR)** nested `course: z.object(...)` ไม่ strict (parent .strict() ไม่ครอบ nested — zod strip คีย์เกินเงียบๆ แล้วตอบ 201) → `.strict()` ที่ course embed \| probes: r1 **31/31** (ใหม่ R7g ip_hash gate + R7h request_id gate + no-persist · fixture ip_hash เปลี่ยนเป็น 64-hex จริงทุกจุด) · r2 **25/25** (ใหม่ R12g for update ในนิยามที่ติดตั้งจริง) · full replay reset→migrate rc=0→seed rc=0 · dry-run begin/rollback rc=0 · integration **37/37** seed สด · vitest **886/886** (72 ไฟล์ · HEAD 883 + 3 tests ใหม่: id-drift ไม่แตะ rules/reload · course คีย์เกิน → 503 · reload error → ERR-SYS-002) · tsc 0 · eslint 0 · build 0 · gitleaks diff develop..worktree 720KB = 0 leaks | ถัดไป: commit → codex gate r4 (develop..HEAD) → PASS = merge --no-ff + post-merge gates → PB-13 → Phase 1 spawn |
| D41 | 2026-09-11 | **codex gate Wave D Phase 0 r4 = FAIL (0 BLOCKER / 3 MAJOR / 3 MINOR) → lead ตรวจยืนยัน 5 ข้อ แก้ครบ + adjudicate 1 ข้อเป็น NOT-A-DEFECT** — **H1 (MAJOR)** admin_update_question แก้โจทย์/ตัวเลือกโดยไม่มี audit คู่ → `append_audit_event_internal('QB_QUESTION_UPDATE','question',...)` **ใน TX เดียวกับ mutation** (context = `{question_id, version}` ตาม registry AUDIT §2.1 L67 · actor derive จาก auth.uid() แบบ EXAM_SUBMIT · signature RPC คง 4 params ไม่เพิ่ม p_request_id — PATCH test ผูก toEqual กับ args เป๊ะ) · **H2a (MAJOR)** question-banks GET/POST cast `(data as Row)` ล้วน → zod row-schema ขาเข้า 3 ตัวใหม่ `QuestionBankRowSchema`/`QuestionCreatedRowSchema`/`QuestionRowSchema` (.strict() · drift → 503 `question_bank_row_drift`/`question_created_row_drift`/`question_row_drift`) + ขาออก parseOutgoingView เป็นชั้นสอง (`question_bank_resource_drift`/`question_bank_create_drift`/`question_resource_drift`) — POST ตรวจแถวข้อใหม่**ก่อน**หยิบ id ผูก options · **H2b (MAJOR)** PATCH reload: parse-before-map (tags null เคยโดน `[...null` → TypeError 500) + reload ไม่เจอ ERR-SYS-001 500 → **ERR-SYS-002 503** ตาม invariant 5 · **H2c (MAJOR)** verify route `.parse` ตรง = ZodError 500 + view กว้างกว่า RPC strip ฟิลด์เกินเงียบ → **exact-4-keys** (`Object.keys().sort().join(",")` ต้องเท่ากับ `code,course_title,issued_at,status` พอดี — ฟิลด์เกินอาจเป็น PII รั่ว ห้ามตอบ 200) + safeParse ล้ม → 503 `cert_verify_rpc_contract_mismatch` ทั้งสองทิศ · **H3 (MAJOR) = NOT-A-DEFECT (adjudicated)** result ปล่อยเฉลยหลังครบ attempt เป็น **สัญญาที่เอกสารกำหนด** (DD §3.4 + D19-B1 + D36-O1/DCR-6: result row มี เฉลย/explanation ตาม exam_review_mode/after_final_attempt) — codex เขียนเองว่า "follows the repository's existing after_final_attempt contract, but conflicts with this gate's explicit requirement" = ขัดกับ**prompt ของ gate** ไม่ใช่โค้ด → ไม่แก้โค้ด แก้ prompt r5 (invariant 3 คำว่า "ยกเว้นตอน release ตาม review mode") · **H4 (MINOR)** 0019 เขียน questions UPDATE ก่อน options → mc→sc พร้อมลดคำตอบถูกชน trg_questions_type_change (IMMEDIATE อ่าน options **เก่า**) → **สลับเป็น options ก่อน** (type-change เห็น 1 ถูกแล้ว · sc→mc ฝั่ง deferred อ่าน type ปลายทางตอน commit) · **H5 (MINOR)** request_id regex 36-อักขระรับ uuid ทุกรุ่น (all-zero ผ่าน) → เข้มเป็น **v4+variant** `4[0-9a-f]{3}-[89ab][0-9a-f]{3}` (แหล่งเดียว = middleware crypto.randomUUID — ค่าอื่น = สิ่งที่ BFF เราสร้างไม่ได้ → ปฏิเสธ ไม่ persist ไม่ audit) · **H6 (MINOR)** holderNameOf trim หลัง join ต่างจาก SQL btrim ต่อส่วน → map-trim ทีละ component ก่อน join (mirror 0019) \| tests **+9** (drift GET ×3 · POST created-drift ไม่ insert options · PATCH tags-null → 503 + reload-miss 500→503 อัปเดตตาม contract ใหม่ · verify เกิน/ขาด/นอก enum → 503 · holderNameOf whitespace ×2 — foundRow fixture เดิมฝัง holder_name/revoked_at จงใจ = สัญญาเก่า "ตัดเงียบ" → ปรับเป็น 4 ฟิลด์พอดีตาม H2c) \| probes r1 **36/36** (ใหม่ 5: R7h all-zero + R7h2 variant ผิด + R7h3 v4 จริงผ่าน (positive control) + R8f QB_QUESTION_UPDATE 2 แถว actor+version + R8g mc→sc 2→1 ใน call เดียว + R8h sc→mc 1→2) · r2 **25/25** · full replay (reset→migrate→seed) + dry-run begin/rollback rc=0 · integration **37/37** seed สด · vitest **894/894** (72 ไฟล์) · tsc 0 · eslint 0 · build 0 · gitleaks diff develop..HEAD = 0 \| หมายเหตุ: `src/app/api/v1/lessons/[id]/quiz/route.ts:22` import service client ตรง = **pre-existing นอก diff รอบนี้ อนุญาตตาม D28-DCR5** (lane เจ้าของไฟล์เท่านั้นแก้) | ถัดไป: commit → codex gate r5 (prompt แก้ invariant 3) → PASS = merge --no-ff + post-merge gates → PB-13 → Phase 1 spawn |
| D42 | 2026-09-11 | **codex gate Wave D Phase 0 r5 = FAIL (0 BLOCKER / 1 MAJOR / 1 MINOR) → lead ตรวจยืนยันทั้ง 2 ข้อจริง แก้ครบ — H1-H6 ทั้งหมด RESOLVED · H3 adjudication upheld** — **K1 (MAJOR = H2a residual)** resource ขาออกฝั่ง question ไม่ `.strict()` (QuestionOptionView/QuestionResource/QuestionBankResource/QuestionCreatedRef — parent .strict() ไม่ครอบ nested ใน zod v4) → parseOutgoingView **strip ฟิลด์แปลกปลอมเงียบแล้วตอบ 200** แทนที่จะ 503 (ฟิลด์เกินอาจเป็น is_correct หลุด) → `.strict()` ครบ 4 ตัว + `QuestionBankCreateResult` `.strict()` หลัง extend ชัดเจน (zod extend สืบทอด unknown-keys นโยบายของฐาน — ใส่ชัดเพื่อ reviewer) · **K2 (MINOR)** admin_update_question อ่าน version ไม่มี row lock → สอง TX แข่งอ่าน v เดียวกัน (เช่น 7) UPDATE อนุกรม 8/9 แต่ audit/return ทั้งคู่อ้าง `v_version+1` = 8 (snapshot เก่า — audit โกหกต่อแถวที่ commit จริง) → **`for update of q` ที่ select แรก** (read→bump→audit เป็นอนุกรมบนแถวเดียว) + **`returning q.version into v_version`** ที่ UPDATE (RETURNING เห็นแถวหลัง UPDATE) — audit context + RPC return ใช้เลขจริงจาก RETURNING ไม่คำนวณเอง \| tests **+4** (safeParse matrix: คีย์เกินระดับบน/isCorrect ที่ nested option/owner_email ที่ bank/isCorrect ที่ questions[i] → fail ทั้งหมด · แถวถูกผ่าน positive control ครบทุก schema) \| probes r1 **37/37** (แก้ R8i — เดิม assertion เทียบทุกแถว audit ประวัติกับ version ปัจจุบันของข้อ (ข้อที่แก้ 2 ครั้งมี event v2+v3 โดยนับเป็น FAIL เท็จ) → เป็น **per-question max(audit version) = questions.version** + นับครบ 3 ข้อ: K2 bug signature จริง = audit version ซ้ำต่อข้อ ไม่ใช่ event เก่าไม่ตรงข้อใหม่) · r2 **25/25** · dry-run begin/rollback rc=0 · full replay (reset→migrate→seed) · seed สด integration **37/37** · vitest **898/898** (72 ไฟล์ · 894+4) · tsc 0 · eslint 0 · build 0 · gitleaks worktree 157MB = 0 \| r5 dispositions ที่เหลือ: H1/H2b/H2c/H4/H5/H6 RESOLVED · **H3 adjudication upheld** (codex ยืนยันเอง: 0009_views.sql require submission + final allowed attempt ก่อนปล่อยเฉลย) | ถัดไป: commit → codex gate r6 (prompt เพิ่ม K1/K2 dispositions + invariant audit version จาก RETURNING) → PASS = merge --no-ff + post-merge gates → PB-13 → Phase 1 spawn |
| D43 | 2026-09-11 | **codex gate Wave D Phase 0 r6 = FAIL (0 BLOCKER / 1 MAJOR / 0 MINOR) → lead ตรวจยืนยัน L1 จริง แก้ครบ — H1-H6 + K1/K2 ทั้งหมด RESOLVED · H3 adjudication upheld ต่อเนื่อง** — **L1 (MAJOR = K1 residual นอก admin-question)** fail-closed validation ยังไม่ครบนอก schema ที่แก้ไปรอบก่อน: (1) exam.ts ขาออกหลายตัวไม่ `.strict()` — ExamPaperContent (nested options), AttemptQuestionView, AttemptStartView, AttemptSubmitView, MyAttemptView, AssessmentRulesView, AssessmentDetailView, AttemptQuestionSnapshot (DB jsonb เคย strip คีย์แปลกปลอมเงียบ), AttemptResultQuestionView (nested content+options), AttemptResultView → `.strict()` ครบทุกตัวทุกชั้น (zod v4: parent .strict() ไม่ cascade — ทุก object ซ้อนใส่เอง) · (2) admin certificates route ส่ง issued resource โดยไม่มี outbound validation → **resource ใหม่ 4 ตัว** ใน certificate.ts (IssuedCertificateResource/RevokedCertificateResource/ReissuedCertificateResource [ฝัง Issued ที่ newCertificate]/EligibleAttemptResource — mirror lib interfaces ทุกฟิลด์) + **route ทั้ง 4 ครอบทุกทางออกด้วย parseOutgoingView** (issued_certificate_drift/revoked_certificate_drift/reissued_certificate_drift/eligible_attempt_drift [ตรวจทุกแถว] → 503 ไม่ strip เงียบ) · (3) **ขาเข้าด้วย**: AnswerSaveRequest .strict() — session_id ปลอมใน body เดิมถูก strip เงียบ ตอนนี้ 400 (session binding ยังมาจาก JWT claim เท่านั้น D20-B5 — แข็งขึ้นไม่อ่อนลง) \| tests **+11** → **909/909** (72 ไฟล์ · 898+11: drift suite ครบ 4 cert route [issue 2 · revoke 1 · reissue 2 · eligible 2] + strict matrix ใน exam.test.ts 4 [positive control จาก mapper จริงทุก schema · top-level คีย์เกิน fail ทุก view · nested content/options · snapshot top+option] + **อัปเดต test เก่า 2 จุดที่ผูก contract "strip เงียบ"** เป็น "reject 400" [AnswerSaveRequest ใน exam.test.ts + answers route D20-B5 — เดิม assert 200+strip กลายเป็น 400+ไม่ถึง RPC เลย]) \| ไม่แตะ migration รอบนี้ → ไม่ต้อง full replay · chain #3: affected vitest 54/54 → full **909/909** · tsc 0 · eslint 0 · build 0 · **reset-db ใหม่ (migrate rc=0) + seed rc=0 + integration seed สด 37/37** (ตรวจ false-positive 503 ของ strict ใหม่ — nullable creditSnapshot/pdfMediaId/scorePct ผ่านจริง) \| r6 dispositions ที่เหลือ: H1-H6 + K1/K2 RESOLVED ทั้งหมด (r6 ยืนยันซ้ำ) · H3 upheld (รอบที่ 3) | ถัดไป: commit → codex gate r7 (prompt เพิ่ม L1 disposition + เน้น false-positive ของ nullable + replay path ไม่มี correctCount) → PASS = merge --no-ff + post-merge gates → PB-13 → Phase 1 spawn |
| D44 | 2026-09-11 | **codex gate Wave D Phase 0 r7 = FAIL (0 BLOCKER / 2 MAJOR / 1 MINOR) → lead ตรวจยืนยันทั้ง 3 ข้อจริง แก้ครบ — H1-H6 · K1/K2 · L1 RESOLVED (ยืนยันซ้ำ) · H6/m3 RESOLVED รอบนี้ · H3 adjudication upheld ต่อเนื่อง (รอบที่ 4)** — **M1 (MAJOR)** constraint holder-name สองทางทำให้แถว DB-valid พัง: profiles DDL 0003 อนุญาต display_name='' + first/last null → learner ชื่อว่าง 1 คนทำ eligible page 503 ทั้งหน้า (route ครอบทุกแถวด้วย EligibleAttemptResource ที่ holderName `.min(1)`) · issue path เคย commit ใบ+audit แล้ว 503 ที่ขาออก → retry โดน valid_certificate_exists (dead-end) → **ย้าย prerequisite เข้า RPC ก่อน mutation**: cert_issue_core ตรวจ holder_name/course_title หลัง derive ก่อน cert_no gen/INSERT/audit (errcode 22023 · ERR-VAL-001\|holder_name_missing/course_title_missing) + **EligibleAttemptResource.holderName → z.string()** (คิวยังแสดงแถวชื่อว่างให้ registrar เห็นแล้วไปแก้ profile ก่อน) · **M2 (MAJOR)** inbound drift ของ cert RPCs หายเงียบ: container ไม่ใช่ array → 200 หน้าว่าง (`?? []`) · score_pct ขาด → fabricated null · คีย์เกิน → strip เงียบ → **row schemas ใหม่ 4 ตัว** ใน certificate.ts: CertCoreRowSchema (9 คีย์ exact) · ReissueRowSchema (core+superseded_cert_id = 10 — ห้ามผ่าน parseCertCore 9 คีย์) · RevokedRowSchema (3 คีย์) · EligibleAttemptRowSchema (7 คีย์ · required+.nullable() แยก missing จาก null จริง) + container check `cert_eligible_rows_not_array` ใน listEligibleAttempts — reason taxonomy ใหม่: cert_core_row_drift · cert_eligible_rows_not_array · eligible_row_drift · revoked_row_drift · reissue_row_drift (rpc.error paths เดิมคงเดิม) · **m3 (MINOR)** btrim ตัด space เท่านั้น ไม่ตัด tab/newline (JS .trim() ตัด) → **helper SQL `holder_name_trim`** (regexp `[[:space:]]` ทั้งสองข้าง · immutable · owner app_owner · revoke exec จาก public/anon/authenticated) ใช้ทั้งใน cert_issue_core derive + admin_eligible_certificates derive — `"\tAlice\t"` รอด btrim เดิม ตายที่ helper ใหม่ \| tests **+16** → **925/925** (72 ไฟล์ · 909+16: issue core drift suite [คีย์เกิน/ขาด verify_code/holder_name=''/cert_no ผิดรูป/issued_at ผิด] + eligible container/แถว drift [container object·null · ขาด score_pct · คีย์เกิน · score_pct null จริงผ่าน · holder_name '' ผ่าน] + revoke +3 + reissue +2) \| migration แตะ → **full replay**: make reset-db (migrate rc=0) + seed + **probes r1 43/43** (+R15a/a2/a3 ปฏิเสธ holder_name_missing โดยไม่มีใบ/ไม่มี CERT_ISSUE audit · R15b คิวเห็นแถวชื่อว่าง · R15c ชื่อแท็บ → 'Alice Smith' · R15d helper pure ตัด tab/newline/CR) · **r2 25/25** (แก้ cleanup r2 ลบใบของ enrollment -02/-03 — R15c ของ r1 ออกใบ random-id ทำ enrollment delete โดน FK แล้ว cleanup TX ทั้งก้อน abort) · probes ทิ้ง state → reset+seed ใหม่ก่อน integration **37/37** · tsc 0 · eslint 0 · build 0 · gitleaks patch 793KB = 0 | ถัดไป: commit → codex gate r8 (prompt เพิ่ม M1/M2/m3 dispositions + ระวัง false positive: holderName '' ผ่านโดย design · reissue ใช้ ReissueRowSchema 10 คีย์เฉพาะตัว) → PASS = merge --no-ff + post-merge gates → PB-13 → Phase 1 spawn |

| D45 | 2026-09-11 | **codex gate Wave D Phase 0 r8 = FAIL (0 BLOCKER / 2 MAJOR / 1 MINOR) → lead ตรวจยืนยันทั้ง 3 ข้อจริง แก้ครบ — N1/N2/n3 RESOLVED · M1/M2/m3 (r7) RESOLVED ยืนยันซ้ำ · H3 adjudication upheld ต่อเนื่อง (รอบที่ 5)** — **N1 (MAJOR)** success-but-null/bad-shape ของแถว DB ยังรั่วผ่าน cast + `?? []`: container null → 200 หน้าว่าง · คีย์ขาด/คีย์เกิน → strip เงียบ/fabricated null → **inbound strict row schemas ใหม่ 4 ตัวใน exam.ts** (mirror exact route selects): AttemptResultRowSchema (18 คีย์ exact ตาม select ของ result route — `question_snapshot: AttemptQuestionSnapshot.nullable()` = required key แยก missing จาก null จริง) · AttemptHistoryRowSchema (11 คีย์) · AssessmentRowSchema (8 คีย์ · status enum draft/published/closed/archived) · AssessmentRulesRowSchema (13 คีย์ · time_limit_minutes min(5).max(480) · proctoring_mode enum none/basic) + generic `parseInboundRow` (โยน AppError ERR-SYS-002) เดิมมีอยู่แล้วใน certificate.ts รอบ r7 — เดินทางมาถึง exam routes ทั้ง 7 เส้น (result/me-attempts/me-certificates/admin-assessments/admin-question-banks/assessments[id]) reason taxonomy ใหม่: attempt_result_rows_not_array · attempt_result_row_drift · attempts_rows_not_array · attempt_row_drift · my_certificates_rows_not_array · admin_assessments_rows_not_array · question_banks_rows_not_array · assessment_row_drift · assessment_rules_row_drift · **N2 (MAJOR)** scalar RPC wrappers `Array.isArray(data) ? data[0] : data` กลืนแถวที่สอง (คืน array ยาว 2 → เอาแถวแรกเหมือน scalar) → **`unwrapScalarRow` helper ใน certificates/shared.ts**: unwrap เฉพาะ array ที่ยาว **เป๊ะ 1** — ยาว 0/≥2 คงเป็น array ต่อไปเพื่อตายที่ row schema (fail-closed) ใช้ใน issue/revoke/reissue/verify 4 ทาง · **n3 (MINOR)** fallback display_name ใน eligible RPC SQL ใช้ btrim (ตัด space เท่านั้น) ต่าง holderNameOf (.trim() ตัด [[:space:]] ครบ) → parity: eligible RPC derive ผ่าน helper holder_name_trim เหมือน issuance + holderNameOf ตัดชุดเดียวกัน \| tests **+20** → **945/945** (72 ไฟล์ · 925+20: exam.test.ts inbound schema suite [missing-key แยกจาก null จริง · extra-key · bad-enum · parseInboundRow throw/pass] + shared.test.ts unwrapScalarRow + holderNameOf tab/whitespace · issue/revoke/reissue/verify array-ยาว-2 → 503 drift · route tests container-null ×5 เส้น + extra-key drift) — เจอ 2 คลาส fixture เดิมโดน contract ใหม่จับ: uuid นำหน้าด้วยตัวอักษร non-hex (u…/r…) ตายที่ z.string().uuid() → เปลี่ยนเป็น hex · question-banks "select รวม questions(count)" เดิม GET สองครั้งกับคิวเดียว ครั้งสอง data:null เคยถูก `?? []` กลืนเป็น 200 หน้าว่าง (assert เปล่า) → เติมคิว 2 รายการ + assert 200 (แสดงตัวอย่างของกลุ่มบกพร่อง N1 เอง) \| migration แตะ (eligible RPC + 0019) → **full replay**: reset-db (docker compose down -v && up --no-build — BuildKit frontend pull ค้างเครือข่าย วันนี้ · migrate rc=0) + seed + **probes r1 45/45** (+R15e คิว eligible ตัด [[:space:]] ของ display_name fallback · R15f issuance snapshot = ค่า trimmed เดียวกับคิว [parity] — fixture ชุด -006 เลี่ยงชน ID ของ r2) · **r2 25/25** (cleanup ครอบ -006 ครบ) · probes ทิ้ง state → reset+seed ใหม่ integration **37/37** · tsc 0 (แบบไม่ mask) · eslint 0 · build 0 · gitleaks patch 15,066 บรรทัด = 0 | ถัดไป: commit → codex gate r9 (prompt เพิ่ม N1/N2/n3 dispositions + ระวัง false positive: option_order หายจาก LearnerAttemptViewRow+fixture เพราะ select ไม่เคยมี [paper view ยังมี] · enum ขาเข้าตรง view ขาออก · unwrapScalarRow exact-one) → PASS = merge --no-ff + post-merge gates → PB-13 → Phase 1 spawn |
| D46 | 2026-09-11 | **codex gate Wave D Phase 0 r9 = FAIL (0 BLOCKER / 3 MAJOR / 0 MINOR) → lead ตรวจยืนยันทั้ง 3 ข้อจริง แก้ครบ — O1/O2/O3 RESOLVED · N1/N2/n3 (r8) RESOLVED ยืนยันซ้ำ · H3 adjudication upheld ต่อเนื่อง (รอบที่ 6)** — **O1 (MAJOR)** `left(p_user_agent, 256)` บันทึก UA ข้อความดิบลง `certificate_verifications` append-only — UA header เป็นค่าอิสระที่ anon ควบคุมได้ = ช่องฉีด PII อิสระ (อีเมล/เลขบัตรใน header → ติดถาวรตาม retention) → **ย้ายไปรูป digest เหมือน ip_hash ทุกประการ** (unmerged 0019): คอลัมน์ `rename column user_agent → user_agent_hash` + CHECK `^[0-9a-f]{64}$` + param `p_user_agent → p_user_agent_hash` + RPC gate 22023 `ERR-VAL-001\|user_agent_hash_invalid` (ผิดรูป = ไม่ persist ไม่ audit) + BFF `userAgentHashOf` = sha256(trimmed UA + supabaseAnonKey — dev-grade salt เดียวกับ ip_hash · ค่าว่าง/ขาด header → null) · PB-13 ขยายขอบเขต: ต้องคลุม **ip_hash + user_agent_hash ทั้งคู่** · `audit_logs.user_agent` (0008 รวมแล้ว) **ไม่แตะ** — BFF ส่ง null ที่นั่น (shared.ts) และ wrapper ไม่ anon-reachable (scope จำกัดจริง) · เอกสารตาม: DATA-DICTIONARY certificate_verifications.user_agent_hash + SDS §3.4c · **O2 (MAJOR)** attempts route `(rows ?? [])` cast ตรวจแค่ length: `[null]` ครบจำนวน → TypeError 500 ที่ mapper · คีย์เกิน (is_correct รั่วมา) → strip เงียบแล้ว **201** → **Array.isArray gate** (`attempt_paper_rows_not_array` — ครอบ container ปลอม `{length:1}` ด้วย) + **AttemptPaperRowSchema .strict() 5 คีย์ exact** ตาม select จริงของ route (question_id/seq/selected_option_ids/answered_at/question_paper — view มี 13 คอลัมน์แต่ route select 5 · LearnerAttemptPaperViewRow+fixture หดตาม r8 pattern) ผ่าน `parseInboundRow` → `attempt_paper_row_drift` 503 · **D19-B1 leak test เขียนใหม่**: คอลัมน์เฉลยรั่วมากับแถว = drift → **503** ไม่ใช่ 201-strip (เข้มขึ้น — leak ไม่เคยถึง response ในทุกกรณี) · **O3 (MAJOR)** attach step `unwrapScalarRow` แล้ว `data[0]` อีกชั้น + ตรวจแค่ pdf_media_id เป็น string (คีย์ attached ขาด/คีย์เกินผ่าน) → **PdfAttachRowSchema `{pdf_media_id: uuid, attached: boolean}` .strict()** หลัง unwrapScalarRow (array ยาว 0/≥2 ตายที่ schema ตาม N2) — drift → WARN certificate_pdf_attach_failed + pdfMediaId null **คงใบตาม D36-O6** (attach = tolerant by design ไม่ใช่ silent success) \| tests **+17** → **962/962** (72 ไฟล์ · 945+17: exam.test.ts AttemptPaperRowSchema suite 6 [positive รวม embedded options · null แถว · ขาด question_paper · คีย์เกิน option_order · option มี is_correct · parseInboundRow null] + attempts route drift suite 6 [data null · {length:1} · [null] · คีย์เกิน is_correct · ขาด question_paper · count mismatch] + certificates[code] UA-hash 3 [digest hex64 + ไม่มี Mozilla ใน args + deterministic · no-UA → null · whitespace-only → null] + issue.test.ts attach drift 4 [array ยาว 2 · เปล่า · ขาด attached · คีย์เกิน storage_path]) \| migration แตะ (0019) → **full replay**: reset-db (down -v && up --no-build · migrate rc=0) + seed + **probes r1 48/48 effective** (R7a/R7b ส่ง digest `repeat('ab',32)` · ใหม่ R7i ป้าย user_agent_hash_invalid + R7i2 ไม่ persist · R7j digest 2 แถว + not_found-null 1 — บันทึก: R7j ใน log เป็น FAIL เพราะ assertion ตัวเองนับผิด basis [นับทุกแถวของ ip แทนที่จะ filter ตาม digest] — แก้ assertion แล้ว verify แบบ SELECT บน DB ที่ probe แล้ว = PASS · โค้ดถูกมาตลอด ไม่ใช่ false-pass) · **r2 25/25** (R7f ส่ง null ตำแหน่ง UA — ไม่กระทบ) · probes ทิ้ง state → reset+seed ใหม่ integration **37/37** · tsc 0 (ไม่ mask) · eslint 0 · build 0 · gitleaks patch 15,308 บรรทัด/826.6KB = 0 | ถัดไป: commit → codex gate r10 (prompt เพิ่ม O1/O2/O3 dispositions + ระวัง false positive: rename คอลัมน์+param เป็นสัญญาใหม่ · interface 5 คีย์ mirror select · D19-B1 test 503-drift แทน 201-strip · R7j probe-fix เป็น count-basis · attach drift คงใบตาม D36-O6) → PASS = merge --no-ff + post-merge gates → PB-13 (คลุม hash ทั้งสอง) → Phase 1 spawn |
| D47 | 2026-09-11 | **codex gate Wave D Phase 0 r10 = FAIL (0 BLOCKER / 2 MAJOR / 0 MINOR) → lead ตรวจยืนยันทั้ง 2 ข้อจริงกับ source ก่อนแก้ + พิสูจน์รูป embed ของ PostgREST กับ dev stack จริงแล้วออกแบบ — แก้ครบ P1/P2 · O1-O3 (r9) RESOLVED ยืนยันซ้ำ · H3 adjudication upheld ต่อเนื่อง (รอบที่ 7)** — **P1 (MAJOR)** pdf route ใช้ cast ล้วน 2 จุด (`cert.data as ...{pdf_media_id}` + `media.data as MediaRow`): คีย์เกิน → strip เงียบตอบ 200 · mime_type หาย → `Content-Type: undefined` ออกหา client → **CertPdfRowSchema** (1 คีย์ exact mirror CERT_PDF_SELECT · pdf_media_id uuid **nullable** — ยังไม่มีไฟล์ = 404 ตามธง D-4 คงเดิม) + **MediaAssetRowSchema** (3 คีย์ exact mirror MEDIA_SELECT · mime_type z.string() คง fallback `""→application/pdf`) ผ่าน parseInboundRow → `cert_pdf_row_drift`/`cert_pdf_media_row_drift` 503 — validate แถว cert ก่อนเช็ค null→404 · validate แถว media ก่อน download/header · ลบ interface MediaRow ท้องถิ่น · **P2 (MAJOR)** admin-exam embed ยัง nullable ทั้งสาม: `questions(count)` null → fabricated 0 · `[{count:2},{count:99}]` → เก็บ 2 เงียบ ๆ · assessment_rules/question_options null → fabricate กติกา/ตัวเลือกหาย → **exact-one non-nullable** `questions: z.array(...).min(1).max(1)` + ตัด `.nullable()` ออกจาก assessment_rules + question_options + interface/mapper ตาม (questions[0]?.count — ?? 0 เหลือเพื่อ type noUncheckedIndexedAccess เท่านั้น) · **H2 (r4) ปิดสมบูรณ์รอบนี้** (NOT-RESOLVED ของ r10 = nested-container gaps ชุดนี้เอง) \| **ฐานพิสูจน์เชิงประจักษ์ก่อนเขียน (กัน 503 false-positive storm)**: probe dev stack จริงด้วย REST + fixture ชั่วคราว — ธนาคารเปล่าไม่มีข้อเลย → `questions(count)` คืน `[{"count": 0}]` (aggregate row เดียวเสมอ) · exam draft ไร้กติกา → to-many embed คืน `[]` **ไม่เคย null** — แล้วลบ fixture ทั้งสอง (DELETE 1×2) \| tests **+14** → **976/976** (72 ไฟล์ · 962+14: pdf route drift 5 [cert คีย์เกิน/ขาด pdf_media_id → 503 ไม่ fabricate เป็น 404 · media คีย์เกิน/ขาด mime_type → 503 ก่อนแตะ storage · mime_type "" → 200 application/pdf positive] + admin-exam embed suite 9 [[{count:0}] ผ่าน+map 0 · null/[]/2 แถว/ขาดคีย์ → drift · rules [] ผ่าน+rules null ใน resource · rules null → drift · options [] ผ่าน · options null → drift]) \| **โค้ดล้วนไม่แตะ migration → ไม่มีภาระ replay** แต่ยังรันเต็ม: fresh reset (down -v && up --no-build · migrate rc=0) + seed rc=0 + integration **37/37** (7 ไฟล์) · tsc 0 (ไม่ mask) · eslint 0 (0 warning) · build 0 · gitleaks patch รอบนี้ 389 บรรทัด + เต็มสาย develop→worktree 15,522 บรรทัด = 0 | ถัดไป: commit → codex gate r11 (prompt เพิ่ม P1/P2 dispositions + ระวัง false positive: รูป embed พิสูจน์จริง `[{"count":0}]`/`[]` ไม่มีทาง null · 404 ของ null pdf_media_id คงเดิม · "" mime fallback คงเดิม) → PASS = merge --no-ff + post-merge gates → PB-13 → Phase 1 spawn |
| D48 | 2026-09-11 | **codex gate Wave D Phase 0 r11 (final) = PASS — 0 BLOCKER / 0 MAJOR / 1 MINOR → CTO APPROVE (lead ตัดสินตามอำนาจ D22) · Q1 MINOR แก้พร้อม approve ตามแบบแผน D33/D35 (fix จุดเดียวในฟังก์ชันเดิมที่ gate ผ่านแล้ว — ไม่ re-gate)** — disposition ครบ: H1-H6 · K1/K2 · L1 · M1/M2/m3 · N1/N2/n3 · O1-O3 · P1/P2 ทั้งหมด RESOLVED (**H2 ปิดสมบูรณ์ผ่าน P2** · H3 adjudication upheld รอบที่ 8 — result ปล่อยเฉลยหลัง final allowed attempt ตาม exam_review_mode = สัญญาเอกสาร DD §3.4) · codex รันอิสระซ้ำ: vitest 976/976 + tsc 0 · **Q1 (MINOR)**: 201 ของ POST /admin/question-banks ตอบ questionCount stale (aggregate `[{count:0}]` select ไว้ก่อน insert ข้อเริ่มต้น) ข้าง questions ที่สร้างจริง → lead ยืนยันกับ source (route.ts:190-239) แล้วเลือกทาง derive `questionCount: createdQuestions.length` จากสองทางที่ codex เสนอ (reload aggregate = เพิ่ม roundtrip + ทาง drift ใหม่ · derive = ซื่อตรงต่อสิ่งที่ request นี้สร้าง · ไม่มีข้อ = 0 ถูกต้อง) · tests: ไม่เพิ่มเคสใหม่ — ขยาย assertion เคสที่มีอยู่ 2 จุด (happy path: mock embed บอก 5 → ต้อง override เป็น 1 · bank เปล่า → 0) | evidence: affected 15/15 · full vitest **976/976** (72 ไฟล์) · tsc 0 (ไม่ mask) · eslint 0 · build 0 · gitleaks round patch = 0 · **ถัดไป: commit [#51] → merge --no-ff → develop + post-merge gates → PB-13 (IP_HASH_SALT คลุม ip_hash + user_agent_hash) → Phase 1 spawn D-5/D-6/D-7** |
| D49 | 2026-09-11 | **merge Wave D Phase 0 → develop + PB-13 (IP_HASH_SALT) ครบโค้ดบน `feat/pb13-ip-hash-salt`** — (1) **merge**: r11 PASS + Q1 fix `874ea38` [#51] → merge --no-ff → **develop `b40c240`** · tree identity ยืนยัน `git diff develop feat/wave-d-phase0` = 0 ไบต์ · post-merge gates บน develop: vitest 976/976 (72 ไฟล์) · tsc 0 · eslint 0 (repo ทั้งหมด) · build 0 · gitleaks 0 บน full-branch diff 15,534 บรรทัด (2) **PB-13 ตามแบบแผน PB-9 ครบทุกจุด**: config.ts — envSchema `IP_HASH_SALT: optionalString` + superRefine `APP_ENV=prod && undefined → ConfigError` (message ชี้ SECRETS-PROVISIONING.md) + `AppConfig.ipHashSalt: string หรือ null` + toConfig mapping · certificates/[code]/route.ts — `ipHashOf`/`userAgentHashOf` ใช้ `ipHashSalt ?? supabaseAnonKey` (fallback เหลือเฉพาะ dev — prod โดน superRefine กั้นตอน boot ผ่าน instrumentation/health แบบ PB-9) + ปรับ comment ปลดธง dev-grade ทั้งสามจุด (ครอบทั้ง ip_hash และ user_agent_hash ตาม D46-O1) · tests — config.test.ts describe `IP_HASH_SALT` 6 เคส mirror CURSOR_HMAC_SECRET (+ PB-9 prod-pass ใส่ salt ให้ครบ) · **route.hash-salt.test.ts ไฟล์ใหม่ 4 เคส** (getConfig singleton ทำ in-file test เป็นไม่ได้ — แยกไฟล์ ตั้ง salt ก่อน import: digest = sha256(ip/ua + salt) ตรงค่าคำนวณเอง และไม่ใช่ digest แบบ anon-key · เสถียรซ้ำ · IP ต่างกัน hash ต่างกัน) · instrumentation.test.ts — TOUCHED_KEYS เพิ่ม IP_HASH_SALT + เคสใหม่ boot-reject PB-13 + env-ครบ ใส่ salt · docs — .env.example บล็อก IP_HASH_SALT + SECRETS-PROVISIONING 1.1.0 (§2 แถว+ผลข้างเคียงหมุน · §4 provisioning+การหมุน · §5 แถวกลไก · renumber เดิม §4/§5 → §5/§6) \| evidence: affected 60/60 (5 ไฟล์) · full vitest **987/987** (73 ไฟล์) · tsc 0 · eslint 0 · build 0 · gitleaks round patch 412 บรรทัด = 0 | ถัดไป: PROJECT-STATE (รายการนี้) + commit [#52] → **codex gate (config/auth class — บังคับตาม Git Protocol)** → PASS = merge --no-ff → develop + post-merge gates → Phase 1 spawn D-5/D-6/D-7 (flash ตาม D9 · D-5 รับธง D37-6 · D-7 รับ pass_pct 0019 แต่ selection ยังห้าม) |
| D50 | 2026-09-11 | **codex gate PB-13 r1 = PASS — 0 BLOCKER / 0 MAJOR / 0 MINOR → CTO APPROVE (lead ตามอำนาจ D22) → merge --no-ff → develop `b883ece`** — codex ตรวจครบ 6 invariants (PB-9 parity · prod enforcement จริงผ่าน instrumentation/health · salt คลุม hash ทั้งสองจุด · ไม่มี secret leak ใน diff · test isolation ของไฟล์แยก + env restore ของ TOUCHED_KEYS แน่น · docs provisioning/rotation ตรงโค้ด) และรันอิสระ: **vitest 987/987 ด้วย shuffle seed 1352** · `tsc --noEmit` 0 · eslint ทั้ง repo 0 (build/gitleaks ไม่ rerun — lead รันแทนใน post-merge) \| merge: `git diff develop feat/pb13-ip-hash-salt` = **0 ไบต์** (tree identity) · post-merge gates บน develop: vitest **987/987** (73 ไฟล์) · tsc 0 · eslint 0 · build 0 · gitleaks 0 บน branch diff 451 บรรทัด | ถัดไป: PROJECT-STATE (รายการนี้) + commit [#53] → **Phase 1 spawn D-5/D-6/D-7 (haiku/flash ตาม D9 · ไฟล์ใหม่ล้วน — disjoint)** → D-8 (lead migration เก็บ media_read เจ้าของใบ) และ D-9 (E2E) ตามหลัง |
| D51 | 2026-09-11 | **codex gate ชุด Phase 1 UI (b883ece..HEAD — 3 lanes + lead batch + 4 รอบแก้) = r1 FAIL(6M) → #58 · r2 FAIL(1M/1m) → #59 · r3 FAIL(1M) → #60 · r4 FAIL(1M) → #61 · r5 = PASS 0 findings → CTO APPROVE (lead ตามอำนาจ D22)** — ทุก finding lead ยืนยันกับ source จริงก่อนแก้ (หลัก H3) · r1: M1 exam-room ไม่เช็คผล flushAll ก่อน submit (ส่งทั้งบันทึกไม่ครบ) · M2 flushAll ไม่เตะบันทึก dirty ระหว่าง inflight (รอแต่ promise เดิม) · M3 toggle ไม่ emit ในหน้าต่าง throttle → controlled UI ค้าง/คลิกซ้ำกลายถอดคำตอบ · M4 ปุ่มเริ่มสอบยังกดได้หลัง ERR-ASM-002 (ขัดธง D37-6 ค เองของไฟล์) · M5 วงจรตายเพิกถอนใบ — ตรวจเหตุผลก่อนเปิดโมดัลแต่ช่องกรอกอยู่ในโมดัล + TYPE_REASON ปฏิเสธช่วง revoking (ใช้เพิกถอนไม่ได้เลย) · M6 rules ผิดรูปถูกแปลงเป็น "ยังไม่กำหนดกติกา" แทน fail-closed ทั้งแถว · r2: M1 ถอดตัวเลือกสุดท้ายจน [] ขัดสัญญา zod min(1) → บันทึกถูกปฏิเสธจน flush ติดค้างและผู้เรียนแก้ไม่ได้ · m1 performReissue ส่ง view.newCertificate ให้ reducer ที่คอย key "newCertificate" → โมดัลไม่แสดงเลขใบใหม่ · r3: M1 terminal ERR-ASM-004/005 จาก save ถูกกลืนเป็น dirty → วน retry ไม่จบ ไม่ถึงแผงหมดเวลา/หน้าผล · r4: M1 ทางบันทึกยังรั่วหลัง terminal — timer ค้างยังยิง + toggle หลังเกิดเหตุยังตั้งรอ + flushAll early-return ไม่เคลียร์ timer → **ปิดทุกสาย: attemptSave guard ที่ปากทาง + catch terminal ยกเลิก timer/dirty ของทุกข้อทันที + toggle หลัง terminal อัปเดตในหน้าได้แต่ห้ามตั้งรอบันทึก + flushAll clearTimers ก่อน early-return** · เพิ่มสัญญา ExamFlushOutcome {ok:true / ok:false+terminalCode} → exam-room: terminal 004 จาก flush = แผงหมดเวลา canRetry:false (ข้อความเดียวกับ submit path) · terminal 005 = ถือว่าสำเร็จ clearExamPaper → หน้าผล \| vitest 1159 → **1168/1168** (+9 regression tests: throttle-emit · min(1) no-op · flush กลาง inflight · it.each 004/005 short-circuit · หลายข้อ+timer ค้าง · TYPE_REASON revoking · rules drift) · tsc 0 · eslint 0 · gitleaks 0 ทุกรอบ | ผลพลอยได้: สัญญา autosave เข้มขึ้นสอดคล้อง API-SPEC 4 #7 (1-10 ตัวเลือก/ข้อ) และเส้นทาง terminal 004/005 ตรงกันทั้ง save และ submit |

| D56 | 2026-09-12 | **gate r1 MINOR-1 = NOT-A-DEFECT (accepted-by-design)** — ข้อความ error RSC ขาด origin ที่ transport.ts เปลี่ยนจาก "…ใน FetchCallOptions/ExamCallOptions…" (ต่อ module) เป็น "…ใน TransportCallOptions…" (กลาง) เพราะ aliases ทั้งสองชี้ TransportCallOptions เดียวกันแล้วตาม PB-19 — เหตุผลรับ: (1) เป็น programming error ฝั่ง developer ไม่ใช่ข้อความแก่ผู้ใช้ (2) ข้อความใหม่ตรงชนิดจริงของ options ที่เรียกใช้ (3) ไม่มีเทสต์/consumer เดิมยึดข้อความเก่า (codex grep ยืนยันรอบ r1) (4) ทำ per-module wrapper เพื่อคืนข้อความเดิม = รื้อ duplication ที่ PB-19 ตัดทิ้ง · codex r2 ยอมรับ adjudication ("คงพฤติกรรมเดิมตาม D56") | ตัวอย่างเดียวกับ H3/D41 — MINOR เชิงข้อความ dev-facing ไม่ใช่พฤติกรรม ตัดสิน accepted-by-design พร้อมเหตุผล |
| D57 | 2026-09-12 | **Wave E Phase 1 gate r3 = PASS (0 MAJOR · 1 MINOR test-coverage) → CTO APPROVE merge — และตัดสินปิด MINOR ก่อน merge ด้วย commit test-only `27bfac6`** — MINOR ของ r3 ไม่ใช่ defect ของ production code แต่เป็นช่องว่างพิสูจน์: mock `.in()` เป็น no-op ทำให้เทสต์ lookup chunk ผ่านได้แม้ส่งชุด id ซ้ำ · lead เลือกแก้ทันทีก่อน merge (test-only · ไม่แตะ src · ไม่เปลี่ยน semantics ของ diff ที่ PASS) แล้วรัน gate เต็มซ้ำ (vitest 1379/1379 · tsc 0 · eslint 0) — ไม่ขอ round ใหม่เพราะ diff production ที่ codex ตรวจครบไม่เปลี่ยน · พร้อมบันทึก prod-checklist: hosted Supabase ต้องเปิด `PGRST_DB_AGGREGATES_ENABLED` ไม่งั้น aggregate queries (monitoring/statistics) ตอบ 400 PGRST123 — เหตุที่ batch loops (views 1,000/หน้า · monitoring readAllAggRows) มีอยู่คือ platform cap `PGRST_API_MAX_ROWS=1000` | protocol PASS = เงื่อนไข merge ครบ · MINOR test-coverage ปิดก่อน merge ด้วยหลักฐาน gate เต็ม · audit เส้นทาง prod ต้องเทียบ env var ของ hosted Supabase ตอนวางจริง |
| D58 | 2026-09-12 | **SECURITY DEFINER (owner app_owner) ไม่มี BYPASSRLS และไม่ใช่เจ้าของตาราง — GRANT อย่างเดียวไม่พอ ต้องมี RLS policy ของ app_owner เอง** (ค้นพบจาก functional probe 0026: job insert สำเร็จแต่ runner select ไม่เจอแถว → ERR-NF-001 เงียบ ๆ · ตรวจ pg_roles: app_owner bypassrls=f · pg_tables: เจ้าของ = supabase_admin · RLS เปิด) — แบบแผนที่ถูกคือ per-command policy ตาม 0019 (app_owner_select_certificates ฯลฯ) · select-for-update ต้องมีทั้ง SELECT+UPDATE policy · ผลข้างเคียงที่จับได้: tick อ่าน flag มองไม่เห็น → NULL → ตอบ flag_off เหมือน flag ปิดจริง (ผ่าน probe ผิดเหตุผล) — 0026 จึงเพิ่ม app_owner_select_feature_flags + app_owner_select/update_cert_bulk_jobs | กติกา migration ใหม่ทุกตัว: ตารางใหม่ที่ SECURITY DEFINER แตะ ต้องมี policy app_owner ตั้งแต่ต้น และ probe สถานะ flag ต้องพิสูจน์ "มองเห็นแถว" ไม่ใช่แค่ "ค่า false" |
| D59 | 2026-09-12 | **0026-r1: SQLERRM เป็นตัวแปรพิเศษ ไม่ใช่ฟังก์ชัน — `sqlerrm()` พร้อมวงเล็บ = 42883 ในตัว exception handler เอง** (E-9 worker จับจาก integration: เส้นทางใบล้มเฉพาะใบใน bulk ตายทั้ง TX — rollback รวมใบที่สำเร็จและความคืบหน้า job · probe ของ lead ตอนแรกพลาดเพราะครอบแต่ happy path: handler ไม่เคยยิงจึงเขียวโกหก) — แก้ `left(sqlerrm, 500)` + พิสูจน์ failure path จริง: ใบล้ม 1 (ชื่อว่าง → ERR-VAL-001 ผู้ถือใบยังไม่มีชื่อ) + ใบผ่าน 1 = issued 1/failed 1/completed · last_error เก็บข้อความจริง · cert valid เฉพาะคนมีชื่อ · audit CERT_ISSUE 1 แถวเฉพาะใบสำเร็จ | กติกา probe ทุกตัวต่อไป: ต้องมี failure-path fixture อย่างน้อย 1 เคส (trigger handler จริง) ไม่ใช่แค่ happy + guard — และตรวจ grep `sqlerrm(` / `sqlstate(` ทุก migration ใหม่ (ตอนนี้ทั้ง repo มีจุดเดียวคือ 0026 และแก้แล้ว) |
| D60 | 2026-09-12 | **gate r1 Phase 2 (3M+2m) → 0027 worker redesign + บทเรียน PG ≥15: procedure ที่ COMMIT กลางทางต้องเป็น INVOKER เปล่า** — ทั้ง `security definer` และ `set search_path` ทำให้ 0B000 "invalid transaction termination" **แยกอิสระกัน** (พิสูจน์บน dev PG 15.8 จริง: เอาออกทีละตัวแล้ว call ผ่าน) · สาเหตุของการ redesign: M1 picker หยิบ 200 แถวแรกซ้ำทุกรอบ + `exit when v_round_issued = 0` ทิ้งแถวออกได้ที่อยู่หลังชุดล้ม (ขัด SRS AC ≤5 นาที) → cursor `(submitted_at, attempt_id)` เดินผ่านแถวล้ม · M2 savepoint ใน TX เดียวของ RPC ≠ TX ต่อใบ (abort = rollback ทั้ง job · ความคืบหน้าไม่ durable) → procedure commit ต่อใบ · M3 เทสเปิด flag global กวาดแถวนอก fixture → tick scoped หลักสูตร · PostgREST CALL procedure ไม่ได้ → worker อยู่ pg_cron ล้วน (EXECUTE เฉพาะ app_owner + superuser; cron รัน supabase_admin; เทส psql superuser) | กติกา: ฟังก์ชันใหม่ที่ต้อง commit กลางคันห้าม SECURITY DEFINER/SET — schema-qualify 2 ชั้นทุกอ้างอิงแทน และต้อง call จริงบน PG เป้าหมายก่อนเชื่อว่าใช้ได้ (docs ขอ PG ไม่ได้พูดเรื่องนี้ชัด) · integration test ที่แตะ cron/flag ต้องพัก cron + scope fixture เสมอ |
| D61 | 2026-09-12 | **gate r2 Phase 2 (3M+3m) → 0028 cursor durable** — MAJOR-1: cursor ของ 0027 อยู่ในหน่วยความจำ procedure อย่างเดียว (ทุก CALL เริ่ม v_after_*=null → job/tick ที่ชนเพดานกลางคิวถูกเรียกรอบถัดไป = เดินแถวล้มชุดเดิมซ้ำจากหัวคิว — M1 กลับมาเมื่อข้าม CALL: คนท้ายคิวไม่มีวันได้ใบ · failed_count นับซ้ำจนชนเพดาน 100k) → แก้: bulk = คอลัมน์ `cursor_submitted_at`/`cursor_attempt_id` ของ job commit พร้อม counts ใน TX เดียวต่อใบ (at-least-once) · auto = ตาราง `cert_auto_cursor` ต่อ scope (uuid หลักสูตร หรือ '*' ของ cron — test กับ cron ไม่รบกวนกัน) + sweep จบ (probe ยืนยันหลัง capped exit) = **reset เป็น null = รอบ retry ใหม่** (แถวเคยล้มถูกลองใหม่รอบถัดไป · แถวใหม่ submitted_at ใหม่อยู่หัวคิวอยู่แล้ว — AC ≤5 นาทีคงเดิม) · MAJOR-2: เพดาน 100k ตรวจ*หลัง* branch ชน v_max → ย้ายมา (1) ก่อนลูป — job ถึงเพดานอยู่แล้วปิดทันทีไม่เดินแถวเพิ่ม (2) หลัง update counts ทุกแถวก่อน branch — ครบ 100,000 พอดีปิดรอบเดียวแม้ p_max_certs=1 · MAJOR-3: step(null) บน DB ร่วม → guard assertNoForeignJobs ก่อนทุกจุด + afterAll try/finally คืน cron | กติกา: สถานะที่อ้างว่า durable ต้องกันที่ storage ไม่ใช่ตัวแปร procedure · เพดาน/ขอบเขตทุกชนิดตรวจก่อนเข้าลูปและหลังทุก mutation — ห้ามวางไว้หลัง branch ทางออกอื่น · integration บน DB ร่วมต้อง guard ก่อนเรียกข้ามชุด และ afterAll คืนทรัพยากรใน finally เสมอ |
| D62 | 2026-09-12 | **gate r3 Phase 2 (2M+3m) → 0029 arrival-first two-lane tick** — **MAJOR-1** ผู้ผ่านเงื่อนไขที่ส่งข้อสอบกลาง sweep ล่องหน: 0028 เดิน desc ใต้ cursor เท่านั้น = แถวใหม่กว่าจุดเดินล่าสุดไม่มีทางเห็นจน sweep จบ+reset (ตัวอย่าง gate: คิวชื่อว่าง 4,000 @1,000/2 นาที = รอถึงนาที 8 ทั้งที่ตัวเองออกได้ทันที — ขัด SRS AC ≤5 นาที)→ 0029: (a) `cert_auto_cursor` += `sweep_top_submitted_at`/`sweep_top_attempt_id` (ขอบบนของ sweep — แถวใหม่ที่สุดที่ถูกเดินแล้ว)(b) picker ใหม่ `admin_cert_auto_fresh_pick` = eligibility เดียวกับ picker v2 แต่ `> (p_top_*)` เรียง **asc** ผู้รอนานสุดก่อนในกลุ่มผู้มาใหม่ ·p_top คู่ใด null (sweep ยังไม่เริ่ม) = ไม่คืนแถว (c) tick สองเฟส **fresh ก่อน backlog** งบ v_max เดียวกัน — ผู้มาใหม่ได้ใบ tick ถัดไปทันทีแม้ sweep ถูกเพดานกั้น(d) ชุดที่เดินแล้วของ sweep = ช่วงต่อเนื่อง [cursor, sweep_top] โดยก่อสร้าง — ไม่มีแถวถูกข้าม (e) reset ทั้งสองคู่เมื่อคิวหมด (probe ยืนยันเมื่อ capped) · **MAJOR-2** assertNoForeignJobs เป็น TOCTOU (job ต่างชุดหลุดระหว่างตรวกับเรียก) → สองเคส step(null) global ย้ายไป DB แยก `ltc_e9_iso_dcr8` ที่ suite เป็นเจ้าของทั้งหมด:create → stub auth.* (uid/jwt/role + btree_gist + pgcrypto) → `pg_dump -s --schema=public \| grep -v '^CREATE SCHEMA public;'` restore → seed.sql → insurance inserts (a170 + feature_flags — **pg_dump -s ไม่มีแถว: แถวที่ migration ปะ๊ะต้องเติมเอง**) → fixture 2 jobs + ผู้เรียน 1 ·afterAll drop database (force) · **MINOR-2** cursor asserts ต้องครบ: จำนวนแถว (cur[0]?.ts เฉย ๆ ผ่านแม้ไม่มีแถว) + ค่าทั้งสองคู่ + เคส cap=จำนวนแถวพอดี reset ทันที ·**MINOR-1** DD cursor rows อยู่ผิดที่ (event_outbox) → ย้ายเข้า cert_bulk_jobs + cert_auto_cursor ครบสองคู่ · API-SPEC lastError ชี้แจง coalesce | กติกา: คิว worker ที่มีเพดานต่อรอบต้องแยก "แถวที่ค้างของรอบเก่า" (bounded โดย cursor) ออกจาก "ผู้มาใหม่" (bounded โดยขอบบนของรอบ) — SLA ของผู้มาใหม่ผูกกับเวลามาถึง ไม่ใช่ความยาวคิวเก่า · เทสบน DB ร่วมที่ต้อง "ตรวจก่อนเรียก" ข้ามชุด = TOCTOU — เคส global ย้ายไป DB ที่ suite เป็นเจ้าของทั้งหมด · pg_dump -s โคลนแค่ schema: แถวที่ migration INSERT ไว้ต้อง re-seed เองใน beforeAll |
| D63 | 2026-09-12 | **gate r4 Phase 2 (2M+2m) → 0030 walked-markers** — **MAJOR-1** แถว commit ช้าล่องหนระหว่าง watermark: submitted_at ถูกตั้งด้วย now() = เวลา*เริ่ม* TX (0020:90) ไม่ใช่เวลา commit — TX เริ่มก่อนแต่ commit ทีหลัง = แถว visible หลัง cursor ผ่านตำแหน่งเวลานั้นไปแล้ว → ตกช่อง (cursor, sweep_top) ไม่ใช่ fresh ไม่ใช่ backlog ของ 0029 (ตัวอย่าง gate: backlog 3,999 + 1,000/2 นาที = ได้ใบนาที 8 — ขัด SRS AC ≤5 นาที · ข้อความ "ชุดที่เดินแล้ว = ช่วงต่อเนื่องโดยก่อสร้าง" เป็นเท็จบน READ COMMITTED) → 0030: ตาราง `cert_auto_walked(scope_key, sweep_epoch, attempt_id)` + `sweep_epoch` ใน cert_auto_cursor + picker `admin_cert_auto_unmarked_pick` (eligibility เหมือน bulk_pick + anti-join marker · desc · p_epoch null = ไม่คืนแถว) + tick เดิน unmarked ทีละแถว (ใบ+audit+watermark+marker = TX เดียว) · watermark = ข้อมูลชี้แจง (cursor = แถวสุดท้าย · sweep_top = หัวของ sweep) · drain probe ใช้ picker ตัวเอง (ความจริงของ marker) · **MAJOR-2** 0029 เพิ่ม sweep_top เป็น NULL ล้วนไม่ normalize สถานะ 0028 (cursor ตั้ง · sweep_top null) → แถวแรกที่ backlog เดิน = ใต้ cursor เก่า และแถวที่ 0028 เดินแล้วกินงบ fresh ก่อนผู้มาใหม่จริง → tick ตรวจสถานะขัด (XOR-null) ตอนโหลด = normalize เป็น sweep ใหม่ (epoch+1 · null ทั้งสี่ · ลบ marker เก่า) ภายใต้ advisory lock ของ scope | กติกา: ความถูกต้องของคิวที่มี visibility สลับลำดับกับเวลา (now() ตอนเริ่ม TX) ห้ามอ้างช่วงเวลาต่อเนื่องเป็น "ชุดที่เดินแล้ว" — ต้อง mark ต่อแถวใน TX เดียวกับผลลัพธ์ · migration ที่เพิ่มคอลัมน์สถานะต้อง normalize สถานะเก่าที่ขัด (หรือเขียนไว้ชัดว่ายอมให้เดินซ้ำรอบเดียว — at-least-once + anti-join idempotent) |
| D64 | 2026-09-12 | **gate r5 Phase 2 (0M+3m — adjudicated ทั้งสาม REAL) → ชุดแก้ r6 เสริมความทนทาน/หลักฐาน (0030 เองผ่าน)** — **MINOR-1** harness held session ค้างได้: รอ sentinel อย่างเดียว (stderr สะสม ไม่มี early-close/error handler) — psql ตายก่อน (SQL ผิด+ON_ERROR_STOP) แล้ว finally ไปลงทะเบียน on("close") *หลัง* เหตุการณ์ผ่านไปแล้ว = promise ไม่ resolve + stdin.write บนสตรีมที่ปิด = โยน/EPIPE → `startHeldSession`: exitCode ลงทะเบียน*ตั้งแต่ spawn* · waitForSentinel reject ทันทีเมื่อ early close ("closed before sentinel") + ถอด listener/timer ทุกทาง · end() กลืน stdin error + race timeout 10 วินาที — teardown ไม่ค้างเด็ดขาด · regression ใหม่: SQL ผิด → reject <15 วินาที + end() คืน exit ≠ 0 · **MINOR-2** tick3 course7: non-null เฉย ๆ ผ่านแม้ cursor ค้างตำแหน่ง tick2 → assert `cursor_attempt_id` = แถวคน 18 เป๊ะ + `cursor_submitted_at > sweep_top_submitted_at` · **MINOR-3** DD แถว sweep_top_* ยังใช้ semantics 0029 ("ขอบบน/ผู้มาใหม่") + 1.1.5 อ้าง "ช่วงต่อเนื่องโดยก่อสร้าง" โดยไม่กำกับว่าถูกหักล้าง + API-SPEC pipe ในตารางไม่ escape ครบ (GFM แยกเซลล์บน \| แม้ใน backticks) → DD **1.1.7** · API-SPEC **1.0.9** | กติกา: harness ที่รอ event ของ child process ต้องลงทะเบียน listener การตาย*ก่อน* await (ไม่ใช่หลัง) — ไม่งั้น teardown ค้างตาม · assertion หลักฐาน state ผูก identity เป๊ะ (id เจ้าของแถว) ไม่ใช่แค่ non-null · `code\|pipe` ในตาราง markdown ต้อง escape ทุกจุด |
| D65 | 2026-09-12 | **gate r6 Phase 2 (0M+1m — REAL) → r7 จุดเดียว**: MINOR — แถวเวอร์ชัน API-SPEC ยังมี pipe เปล่า `/admin/exams/monitoring\|statistics` (ประวัติ 1.0.3 — เก่ากว่า wave นี้ แต่ C3 ของ r6 ตรวจ*ทั้งแถว* ไม่ใช่เฉพาะ token ERR-SYS-002) = แถว 2 คอลัมน์แตกเป็น 4 เซลล์ ตอน render → escape เป็น `monitoring\|statistics` + **audit กลไกใหม่**: นับ unescaped pipe ทุกแถวตารางของ DD + API-SPEC เทียบจำนวนคอลัมน์ header ของบล็อก (ผล 0 mismatch ทั้งคู่ — ปิดช่อง grep แบบเจาะจง token เดียวที่พลาดตัวนี้) · ส่วนที่เหลือของ r6 = ผ่านครบ (held-session lifecycle · cur3 identity · DD semantics — codex ตรวจเอง 18ms early-close probe) | กติกา: แก้ pipe ในตาราง markdown ต้อง sweep ทั้งไฟล์ด้วยตัวนับคอลัมน์ — grep ต่อ token เดียวพิสูจน์ไม่ครบ (รอบนี้โดนจากประวัติเก่า 1.0.3 ที่ไม่เคยอยู่ใน scope grep) |
| D66 | 2026-09-12 | **gate r7 Phase 2 (0M+1m — REAL) → r8 ซ่อมตาราง PROJECT-STATE ทั้งไฟล์**: MINOR — แถว D65 ของตารางนี้เองอ้าง `monitoring\|statistics` โดยไม่ escape = แถว 4 คอลัมน์ แตกเป็น 5 เซลล์ (ทำซ้ำโรคที่ r7 กำลังแก้ใน API-SPEC — มือที่เขียนกติกาละเมิดกติกาตัวเอง) · แก้: escape D65 + ตรวจนับ unescaped pipe ทุกแถวตารางของ PROJECT-STATE พบอีก 15 แถว legacy — เพิ่ม escape รวม **16 แถว (รวม D65)**: 15 แถว escape-only (D-4b · D39-D47 · D49-D51 · D62 — เทียบหลัง strip backslash เนื้อหาเดิมทุกไบต์) + แถวหัวสรุปเอง ที่เพิ่ม escape พร้อมอัปเดตสถานะ r8 · ยอด diff ทั้ง commit +19/-17 รวมแถวว่าง 185 ที่ git re-pair (จับเป็นทั้ง -/+) จึงไม่ใช่จำนวนแถวที่ซ่อม · D48 ไม่ถูกแตะ — audit เดิมติดเพราะ D45/D46 (7 pipes) ถูก latch เป็น header ของบล็อก + audit ครบสามไฟล์ (DD · API-SPEC · PROJECT-STATE) = 0 mismatch | กติกา: ตารางของทุกไฟล์ markdown ในโปรเจกต์ (รวม PROJECT-STATE เอง) ต้องผ่านตัวนับ unescaped-pipe-vs-header ก่อน commit — เอกสารที่เขียนกติกาก็เป็นตารางเหมือนกัน |
| D67 | 2026-09-12 | **gate r8 Phase 2 (0M+1m — REAL) → r9 ปรับเลขหลักฐานให้ตรง diff จริง**: MINOR — header/D66/event ของ r8 บันทึกว่า 'legacy อีก 16 แถว · escape-only 17 แถว (+17/-17)' แต่นับจาก diff จริงของ 2467fce = เพิ่ม escape **16 แถวรวม D65** (อีก 15 แถว) แยกเป็น **15 แถว escape-only** + **แถวหัวสรุป** ที่เพิ่ม escape พร้อมอัปเดตสถานะ · ยอด +19/-17 รวมแถวว่าง 185 ที่ git จับเป็นทั้ง -/+ (re-pair) จึงใช้สรุปจำนวนแถวที่ซ่อมไม่ได้ · D48 ไม่ถูกแตะ (แถวที่ audit เดิมติดเพราะ D45/D46 7-pipe ถูก latch เป็น header ของบล็อก) · ต้นเหตุ: นับ '17 แถว' จากยอด modified-lines ของสคริปต์แก้ pipe สองรอบ โดยแถวหัวได้ escape จากทั้งสองรอบแต่เป็นแถวเดียว · แก้: ปรับสามจุด (หัว · D66 · event) เป็นเลขจริง + D67 นี้ + event ใหม่ | กติกา: ตัวเลขหลักฐานในเอกสารต้องนับจาก diff จริงด้วยสคริปต์ (นับแถวที่ backslash-pipe เพิ่ม เทียบ parent) — ห้ามใช้ยอด +/- ของ --stat เพราะปน re-pair ของแถวว่างได้ |
| D68 | 2026-09-12 | **Wave E Phase 3 (CRB Credit Bank) kickoff — ตัดสิน 10 ข้อ (C-1..C-10 แผน `.omc/plans/wave-e-p3-plan.md`)**: หลักฐานเปิด = RTM must-have CRB-001/002/003/005/006/007 ไม่มีในชั้นแอป (ตาราง+RLS พร้อมตั้งแต่ 0006/0010 · event_outbox 0007 ถูกออกแบบเป็น outbox ของ credit แต่ไม่เคยมีผู้เขียน · issue.ts:26 ยอมรับเองว่าอ่านยอดไม่ได้) · **(C-1)** accrual = transactional outbox เป๊ะตาม DD §3.5/F15/F17/D11-17: submit_attempt_core จับกฎครั้งเดียวตอนตรวจ (TX เดียวกับ submit) → event_outbox topic credit.accrual + snapshot (rule_id/credits/credit_type) → consumer credit_accrual_tick (pg_cron 1 นาที · advisory-lock actor ระบบ · INVOKER เปล่าตาม D60) commit ต่อ event · ผู้บริโภคใช้ snapshot อย่างเดียว (retire กลางคิวไม่กระทบ) **(C-2)** จับกฎ: active + effective window ครอบ passed_at + (course เฉพาะ ชนะ ทั่วไป → priority asc → created_at desc) LIMIT 1 · ไม่มีกฎ = ไม่เขียน event **(C-3)** ensure_renewal_cycle lazy: เป้า role lawyer หรือมีรอบอยู่ · anchor = license_applications อนุมัติล่าสุด → role_assignments lawyer เก่าสุด · ยาว 1 ปี/เกณฑ์ general 12 จาก credit_cycle_defaults() (จุด canonical เดียว รอ Q1 — D8) · snapshot ตอนสร้าง · รอบที่ปิดแล้วห้ามแก้ · สร้างเฉพาะรอบ cover วัน passed **(C-4)** citizen ไม่สร้างรอบ/ไม่เขียน ledger — event processed + no_cycle_target (credit = กลไกต่ออายุ mission 4 · transcript แสดงหลักสูตร/ใบฯ ของทุกคน) **(C-5)** เพิกถอนใบ → reversal อัตโนมัติใน TX เดียวกับ revoke (amount ลบ · original_entry_id · source certificate_revocation/cert id · reason อัตโนมัติ + cert_no · created_by ผู้เพิกถอน · idempotent NOT EXISTS ต่อใบ) · re-issue ไม่กระทบ credit คงเดิม **(C-6)** admin_credit_adjust RPC SECURITY DEFINER (reason บังคับ · created_by=auth.uid() · audit CREDIT_ADJUST · แจ้งเจ้าของ = Phase 4) **(C-7)** ผิวใหม่ล้วน me/credits + me/transcript (json/CSV BOM/PDF) + admin/credit-rules (PATCH lifecycle-only — trigger 0010 บังคับ) + admin/credits (adjustment + ledger keyset) + AdminSidebar = lead **(C-8)** BFF user-JWT ตลอด · RPC SECDEFINER owner-check · staff ใช้ policy/v_credit_balance เดิม **(C-9)** ไม่ใช้ service_role ใน BFF — ทางเขียน ledger ทั้งสามเป็น SECDEFINER ที่ validate สิทธิ์เอง **(C-10)** เทส reuse แบบแผน dcr8 (startHeldSession · iso-DB เมื่อจำเป็น) · out: CRB-008 รอ Q1 · NTF=P4 · IDENT/PDPA+ADM=P5 | เหตุผล: deliverables 17/18/21 (SYSTEM-TEST/UAT/MANUAL) ต้องเล่า flow credit ครบตาม SRS · CRT-006 AC (revoke→ledger) ยังไม่มีใครทำ · architecture ตามเอกสารที่ผ่าน gate แล้วทั้งหมด ไม่ประดิษฐ์กลไกใหม่ |
| D55 | 2026-09-11 | **Wave E plan APPROVED + ตัดสิน 10 ข้อ (spot-check O-1..O-10 ครบทุกข้อกับ source จริงก่อนตัดสิน)** — **D55-1 (PB-18)**: เติม `type` ลง question_snapshot ตอน start (0022) · paper view คืนจาก snapshot · เก่าไม่มี type → default `'multiple_choice'` (= checkbox วันนี้เป๊ะ · grading set-equality ฝั่ง server คะแนนไม่เพี้ยน · cron 0020 ปิด attempt ค้างทุก 1 นาที) · ห้าม derive สดจาก questions.type ตอนอ่าน (ละเมิด snapshot immutability) — ทางเลือก (ข) ของ draft ใช้ไม่ได้จริง: assessment_rules.selection ไม่มี per-question type · **D55-2 (PB-20)**: GET /admin/certificates role-gate ตรงแบบ eligible (staff:registrar + super_admin) · audit PII_ACCESS · ไม่เพิ่ม permission ใหม่ ไม่ยืม certificate:issue (ผิด semantic read/write) · **D55-3 (instructor↔shell)**: คง fail-closed ทั้ง shell เป็นนโยบายทางการ (ADR ลง RBAC-DESIGN §2) — AdminSidebar static ไม่กรองสิทธิ์ (เปิด = โชว์ลิงก์ 403 ทั้งแถบ) · DS §6.3 = shell เจ้าหน้าที่ · BFF คงรับ instructor ตาม matrix (ไม่ถอน) · instructor workspace = UI แยกอนาคต · **แก้ path ผิดใน draft+e2e-12 comment: guard จริง = `src/app/(admin)/layout.tsx:50-59`** (ไม่ใช่ admin/layout.tsx) · **D55-4 (D39)**: 0024 เพิ่ม 'completed' ใน cm_read/lessons_read · uq_enrollments **คงไว้** (lifecycle เส้นเดียว ไม่มี FR re-enroll · cert partial-unique สอดคล้อง) · **D55-5 (bulk)**: filter-before-cut ใน eligible RPC + bulk RPC ใหม่ batch 200 · TX เดียวต่อใบ + audit actor ครบ · job status ออกแบบใน DCR (ตารางใหม่ cert_bulk_jobs เทียบยืม report_exports) · **D55-6 (reports)**: ทำเฉพาะรายการ DCR-6 เลื่อน (reports 3 + monitoring/statistics) อ่าน views 0009 เดิม ไม่ขยาย scope · **D55-7 (CRT-008)**: job async สแกน passed-no-valid-cert ≤5 นาทีตาม SRS AC · idempotent · audit CERT_ISSUE ระบุโหมด · flag ปิด default · ห้าม sync ใน submit TX · **D55-8 (E-0)**: ไฟล์เป้า transport ไม่มี owner active → spawn worker ตาม D9 ได้ + lead verify เข้ม · **D55-9**: ยุบ E-7b (UI รวมเข้า E-1/E-3) · **D55-10**: 0021 CREATE OR REPLACE view วาง assessment_id ท้ายสุด (PG add-only-at-end) | แผน draft โดย worker flash (haiku) ตาม D9 — lead verify ทุก file:line ก่อนตัดสิน (จับ path ผิด 1 จุด + ทางเลือกใช้ไม่ได้จริง 1 ข้อ) — ยืนยัน protocol "flash เขียน draft + lead spot-check ทุกข้อ" |

## Git Protocol

- `develop` = integration branch; feature branch ต่อ task (`feat/<task-id>-<slug>`, `docs/<task-id>-<slug>`)
- **Lead เท่านั้น** ที่ commit ลง develop / merge — workers ห้าม git ทุกชนิด (Wave A) และห้าม merge ตลอดโครงการ
- Merge gates: lint + tsc + tests + build + secrets scan เขียว; โค้ด auth/security/data ต้องผ่าน codex gate PASS
- Conventional Commits + task ref เช่น `docs(a2): add SRS v0.1.0 [A2]`
- Per-worker git worktrees เริ่ม Wave B (`git worktree` ต่อ worker, สาขา `omc-team/...`)

## Resume Procedure (สำหรับ session ใหม่)

1. อ่านไฟล์นี้ทั้งฉบับ + `docs/00-baseline/PROJECT-BRIEF.md`
2. ดู wave board → หา task ที่ยังไม่ terminal (pending/in_progress)
3. `git log --oneline -15` ดู progress ล่าสุด + `ls docs/` เทียบกับ board
4. ตรวจ `.omc/handoffs/*.md` (ถ้ามี) สำหรับบริบท stage
5. ทำงานต่อจาก task ที่ค้าง — ห้ามเริ่ม wave ใหม่ถ้า wave เดิมยังไม่ผ่าน gate
6. workers ที่หายไป: lead รับ/มอบหมายงานใหม่ตาม board

## เหตุการณ์สำคัญ (Event Log — append-only)

- 2026-09-08 · เริ่มโครงการ · Wave A spawn workers 1–5 (docs) · lead = session นี้
- 2026-09-08 · **A4 เสร็จ** — API-SPEC 350 บรรทัด (52 endpoints), RBAC 279 บรรทัด (47 permissions), AUDIT 257 บรรทัด (46 event types) · ตรวจรับ: wc -l ตรงรายงาน, ไม่แตะไฟล์อื่น · commit d02c574 + merge a031172 · ไม่มี DCR · **โจทย์ A6:** (1) ประสานชื่อตาราง/คอลัมน์/UUID กับ DATA-DICTIONARY ของ worker-3 (2) re-map AUD-01…12 ของ audit doc ให้ตรง SRS ของ worker-2 (3) ค่า default ทั้งหมด flag รอยืนยัน Q1–Q4 แล้ว
- 2026-09-08 · **A1 เสร็จ** — PROJECT-PLAN 242 บรรทัด (11 sections), RISK-REGISTER 161 บรรทัด (26 risks), TEST-PLAN 334 บรรทัด (18 TC + 16 E2E) · ตรวจรับ: wc -l ตรงรายงาน · commit 7a88da6 + merge e9fd2b3 · **DCR-1 APPROVE → brief 0.2.0 (+staging env, +Q7)** · DCR-2 เป็นรายการ A6 · คำถามค้าง QP-1/QP-2/QP-3/QP-5 รอ user/หน่ยงาน (ดู §คำถามค้างใน PROJECT-PLAN) · สั่ง worker-1 align TEST-PLAN กับ brief 0.2.0
- 2026-09-08 · **A1 follow-up เสร็จ** — TEST-PLAN 0.2.0-draft (337 บรรทัด): staging เป็น env หลักของ security/perf test, entry criteria + T-1 อัปเดต, ลบ QP-5 ออก · lead แก้ QP-5 เดิมใน PROJECT-PLAN §11 ยุบรวม Q7 · commit 4840b93 + merge d64e378 · **A1 ปิดสมบูรณ์**
- 2026-09-08 · **A3 เสร็จ** — SDS 394 บรรทัด, ARCHITECTURE 266 บรรทัด (mermaid 15 block), DATA-DICTIONARY 646 บรรทัด (**31 ตาราง** ครบขั้นต่ำ 29 + course_categories + email_outbox พร้อมเหตุผล; RLS 31/31; REVOKE บังคับ append-only audit+ledger; PII registry 5 จุด) · ตรวจรับ: wc -l/grep ตรงรายงาน · commit 291952f + merge 73d0bc8 · ไม่มี DCR · **เพิ่มโจทย์ A6:** (4) role key `staff:viewer` (brief/RBAC) vs `staff_viewer` (DB enum) ต้องตกลงชื่อเดียว (5) นิยาม helper `has_any_role()`/`is_staff()` canonical ที่ RBAC doc (6) API paths ใน SDS ต้องตรง API-SPEC (7) รูปแบบ cert_no `LTC-<ปี>-<6หลัก>` รอยืนยันกับรูปแบบจริงของสภาฯ (ผูก Q1–Q6)
- 2026-09-08 · **A2 เสร็จ** — SRS 647 บรรทัด (**134 requirements**: FR 83 + NFR 51, M114/S19/C1, ทุกตัวมี AC วัดได้) + RTM 252 บรรทัด (trace 5 มิติ coverage 100%) · ตรวจรับ: wc -l ตรง, unique Req ID = 134 พอดี · commit eb003e4 + merge c340531 · ไม่มี DCR · **เพิ่มโจทย์ A6:** (8) RTM อ้าง D13–D18 mapping เป็นสมมติฐาน — lead ยืนยัน (9) ชื่อตาราง/endpoint ใน RTM เป็นร่าง ต้องกระทบยอดกับ DATA-DICT + API-SPEC (10) SRS เขียนบน brief 0.1.0 — ต้องเช็คว่าขัดกับ staging (brief 0.2.0) ไหม (11) default `credits_required_per_cycle=12`, `renewal_cycle_years=1` เป็นสมมติฐานล้วน — **เร่งคำตอบ Q1–Q2 ก่อน Wave D–E**
- 2026-09-08 · **A5 เสร็จ** — DESIGN-SYSTEM 595 บรรทัด + โปรโตไทป์ 10 หน้า (รวม 4,173 insertions) · Playwright: JS error 0/11 หน้า, responsive 375px ผ่าน, contrast 17 คู่ ≥4.5:1 · ตรวจรับ: 10 ไฟล์ + 356,079 bytes ตรงรายงาน, ไม่มีไฟล์หลงเหลือ · commit dc91f30 + merge 5857962 · ไม่มี DCR · **เพิ่มโจทย์ A6:** (12) **ค่า credit ขัดกัน**: SRS Appendix A default = 12 หน่วย/รอบ 1 ปี แต่ prototype ใช้ 36 หน่วย/รอบ 3 ปี (U2) — ต้องเลือกชุดเดียว (ธง Q1) (13) U1 สีอัตลักษณ์จริงของสภาฯ (ตอนนี้ navy/gold จำลอง)
- 2026-09-08 · **Wave A execution ครบ (A1–A5)** → เริ่ม **A6**: spawn reviewer ตรวจ cross-doc consistency 13 ข้อ + sweep ทั่วไป → แก้ fix → codex gate → CTO verdict
- 2026-09-08 · **A6 reviewer verdict = REVISE** (16 BLOCKING B-01..B-16, 6 MINOR, 6 NIT) → CTO บันทึก D8 (คำตัดสิน canonical 16 ข้อ) + M-05 DCR APPROVE (manual grading นอก scope, brief 0.2.1) · commit c14719d · **สั่งแก้คืนเจ้าของไฟล์พร้อมกัน 5 คน**: worker-1 (M-01/M-02/B-09c), worker-2 (SRS defaults master + RTM legend/API/data cols), worker-3 (role enum colon + 6 ตารางใหม่ 31→37 + staging view), worker-4 (เติม ~16 กลุ่ม endpoint + verify 200/4 ฟิลด์ + audit remap 46→49), worker-5 (credit 12/1yr + cert_no สุ่ม + meter labels) — รอรายงานครบ 5 คนแล้ว verify ก่อน codex gate
- 2026-09-09 · **D14 convergence pass เสร็จ + codex gate round 4 = FAIL (0 BLOCKER/2 MAJOR/3 MINOR — 11/13 RESOLVED)** — lead แก้เอง 13 findings r3 (23 edits/6 ไฟล์, a6af421 → merge b745403) → re-gate ตามที่ผู้ว่าจ้างอนุมัติ → เหลือ N1 (EXECUTE ขาด revoke จาก PUBLIC/anon + generic RPC กล่าวอ้าง mutation ได้), N2 (DD attempt_answers ให้ instructor อ่าน raw ขัด RBAC †), M1 (AUDIT §1.4 ขัด §4), M2 (SDS diagram worker จับคู่ rule), M3 (notice_ack ขาด TRUNCATE) — lead ยืนยันจริงทุกข้อที่บรรทัดอ้าง · บันทึก D15 → ถามผู้ว่าจ้างอีกครั้ง (รอบแก้จุดจบ / waiver+Wave B / หยุดรอ)
- 2026-09-09 · **ผู้ว่าจ้างมอบอำนาจตัดสินใจเต็มให้ CTO-lead (D16 — "no wait me") + อนุมัติรอบแก้จุดจบ** — lead แก้เอง 5 findings ของ r4 (9 edits/4 ไฟล์: N1 revoke PUBLIC + event-class split, N2 instructor_attempt_view, M1 §1.4, M2 diagram snapshot, M3 TRUNCATE) → grep ผ่านทุกข้อ → commit → codex gate round 5 → นับจากนี้ตัดสินใจเองทุกขั้น รายงานเป็นระยะ
- 2026-09-09 · **codex gate round 5 = PASS (0 BLOCKER/0 MAJOR/1 MINOR) → CTO APPROVE (D17) — Wave A ปิดสมบูรณ์** — 5/5 RESOLVED (N1/N2/M1/M2/M3); R5-m1 (AUTH_* producer mapping) แก้พร้อม approve ใน AUDIT §4; เอกสาร 9 ฉบับ bump เป็น **1.0.0 baseline** (Brief คง 0.2.1); docs/README board 1–12 → ✅; วงบรรจุ 5 รอบ: 2B/17M → 3B/21M → 0B/7M → 0B/2M → 0B/0M
- 2026-09-09 · **Wave B unlocked** — repo scaffold + Docker dev env + CI · per-worker worktrees ตาม D4 · ยึด DATA-DICTIONARY 1.0.0 เป็น blueprint ของ `supabase/migrations/*`
- 2026-09-09 · **B-04 เสร็จ (lane แรกของ Wave B)** — worker-b4 (flash): ci.yml 137 บรรทัด (5 gates fail-fast, permissions read, checkout pin SHA, gitleaks pin 8.24.2) + .gitleaks.toml (2 rules + allowlist 5 paths) + PR template ไทย + ci-local.sh 190 บรรทัด (exit 0/1/2 semantics + fail-path ทดสอบแล้ว) · lead verify: wc ตรง, yaml-lint exit 0, TOML parse OK, ci-local 4/5 PASS (28 tests), secret-grep สะอาด · ข้อจำกัดตรงไปตรงมา: gate 5 เครื่อง local ไม่มี gitleaks binary (proxy block TLS) — บังคับจริงบน CI เมื่อมี remote · สังเกตุ: scaffold ของ B-01 ผ่าน lint/typecheck/test/build แล้ว ณ ตอนนี้ (28 tests)
- 2026-09-09 · **B-01 เสร็จ** — worker-b1 (flash): 20 ไฟล์ 1,709 บรรทัด — package.json (Next 15.5 + React 19 + TS strict + Tailwind v4 + zod v4), eslint custom rule `ltc/no-public-service-role` (พิสูจน์จับได้จริง), globals.css @theme ครบ DESIGN-SYSTEM §10, layout/page ไทย (Sarabun), `/api/v1/health`, lib/config (zod SDS §7.2 + defaults SRS Appendix A + Q1–Q6 flags), lib/errors (31 codes), lib/logger (PII allowlist), lib/rbac (41 perms/8 roles), supabase factories (server-only) · lead verify: gates 4/4 exit 0 (28 tests), ทะเบียน error diff กับ API-SPEC = identical, secret-grep สะอาด · **lead decisions (D16): (1) รับรอง postcss.config.mjs เข้า ownership B-01 (จำเป็น Tailwind v4 ไม่มีเจ้าของอื่น) (2) ย้าย health route เป็น `/api/health` + รูป `{status, db, time}` ตาม API-SPEC §3.10 L243 (สเปกเดิมใน spawn prompt ผิดเอง — เอกสารถูกอยู่แล้ว ไม่ต้อง DCR) แล้ว gates เขียวซ้ำ (จับ stale .next/types + ACL deny-delete แก้ด้วย chmod -N)**
- 2026-09-09 · **B-03 เสร็จ** — worker-b3 (flash): 8 ไฟล์ 792 บรรทัด — เลือกแนวทาง (ข) images หลักเอง (postgres 15.8.1 + gotrue 2.164 + postgrest 12.2.3 + storage 1.0.6 + kong 2.8.1 — ทุก tag ผ่าน manifest inspect) เหตุผล: CLI-in-container ต้อง mount docker.sock + config.toml ของ lane อื่น · รันจริง: up -d --build exit 0, 8/8 Up, kong +apikey 200 ทั้ง 3 service / ไม่ส่ง key 401, mailpit 200, make lint/test 0 (28/28), cold cycle down→up, **reset-db exit 0**, down สิ้นสุด 0 container · lead verify: wc ตรง, compose config -q exit 0 (re-run หลัง fix), JWT ใน .env.example/kong.yml = placeholder มาตรฐาน Supabase dev (ref 0000…) · **lead fixes (D16, fold เข้า lane commit): (1) healthcheck /api/v1/health→/api/health ตาม canonical ใหม่ (2) .gitleaks.toml +allowlist docker/kong/kong.yml — default JWT rule ของ gitleaks จะจับ placeholder dev ตอน CI รันจริง (ค่าจริง prod อยู่ที่ platform env เท่านั้น)** · cross-worker จาก b3: (1) b2 `0002_helpers.sql:11` อ้าง `role_assignments` ที่สร้างใน 0003 → db-migrate exit 1 ที่ 0002 (แจ้งให้ track ตอน verify B-02) (2) b3 เข้าใจว่า b1 บังคับ SMTP_USER/PASSWORD — ตรวจแล้วไม่จริง (optional + บังคับเฉพาะ provider=smtp) ไม่ต้องแก้ · ข้อจำกัดที่รายงานตรงๆ: ยังไม่มี service scheduler (รอ endpoint job จริง), POSTGRES_HOST_PORT=54322 (5432 ถูกโปรเจกต์อื่นครองบนเครื่อง dev — ปรับได้ด้วย env)
- 2026-09-09 · **B-02 ส่งงาน + lead verify + codex gate 5 รอบ (D18–D21) → ⏸ หยุดตามเงื่อนไข D21 รอผู้ว่าจ้าง** — worker-b2 (flash): migrations 11 ไฟล์ ~2,370 บรรทัด (40 ตาราง/RLS ทุกตาราง/SECDEFINER write fns/views 8/policies 183 สุดท้าย) · lead แก้ b3-issue (0002 อ้าง role_assignments ก่อนสร้าง → ย้าย helper ไป 0003) + verify สอง image เองทุกรอบ (vanilla postgres:15 สด + supabase/postgres:15.8.1.060 dev compose): struct probes 13/13 + functional probes **46/46 PASS ทั้งสอง image** + expected-error census 16 ข้อความ `diff` identical · **gate: r1 7B/11M/2m (20) → r2 6B/8M (14) → r3 5B/3M (8) → r4 2B/1M/0m (3) → r5 1B/0M/0m (1) — วงบรรจุ monotonically ทุก findings เป็น residual ของ fix รอบก่อน** · รอบแก้: D18 (20 ข้อ + แรงงานแยก lead/flash) → D19 (14 ข้อ) → D20 (cap 3/3 → แก้จุดจบ 8 ข้อ + จับ bug `<> any()` จริง) → D21 (3 ข้อ + ปิด probe gaps F27d/e/F28d/F-own/G-idx) → **r5 FAIL 1 BLOCKER เดียว: `phone_0812345678` คือ identifier ถูกกฎ — digit-PII scan ไม่เคยรันบนชื่อคีย์ → raw เบอร์ 10 หลัก ลง audit ได้ทาง RATE_LIMIT_HIT endpoint** · B3′(quiz hijack)+M2′(owner) RESOLVED ยืนยันโดย r5 · **disposition: หยุด ไม่ merge ไม่มีรอบ 6 (เงื่อนไข D21) — remedy เตรียมพร้อม: สแกน digit-run บนชื่อคีย์ (strip `_` → เจอ 9–10/13 หลัก = ปฏิเสธ) + F28e + re-verify สอง image + re-gate 1 ครั้ง (~10 นาที)** · ระหว่างรอคำตอบ: Wave C planning (docs ล้วน) ตาม D9 · verdict archive: /tmp/codex-gate-b02-r{4,5}.txt · evidence: /tmp/{migrate_r5,struct_run8,fns_run8,sb_migrate_r5,sb_struct_run7,sb_fns_run7}.log
- 2026-09-09 · **ผู้ว่าจ้างยืนยันอำนาจเต็มซ้ำ (D22 "full authori… no wait me") + escalation ladder (D23 flash>3 → sonnet×3 → lead)** — lead ตัดสินทางเลือก (ก): remedy B4'' + F28e/F28f + re-verify สอง image (struct 13/0 + fns 47/47 + census identical)
- 2026-09-09 · **codex gate B-02 รอบ 6 = PASS (0/0/0) → CTO APPROVE (D24) → merge --no-ff → develop · Wave B ปิดสมบูรณ์ (B-01/02/03/04 ✅)** — วง 6 รอบ 20→14→8→3→1→0 · post-merge gates 4/5 เขียวบน develop · Wave C kickoff (แผน .omc/plans/wave-c-plan.md + ข้อตัดสิน O-1..O-5)
- 2026-09-10 · **Wave C ACTIVE (D25)** — ตัดสิน O-1..O-5: DCR-3 (enroll ซ้ำ = 200 idempotent, API-SPEC 1.0.1) · O-2 BFF คำนวณ delta เอง · O-3 authoring เลื่อน (seed แทน) · O-4 เพิ่ม lane C-0 Auth/Session + MFA fail-closed · O-5 เกณฑ์ exit C-1 · Phase 0 spawn 5 workers flash (c0/c1/c6/c7/c8) ไฟล์ disjoint · container B-02 เก็บกวาดหมด (ไม่เหลือ ltc-*)
- 2026-09-10 · **D26: tsc semantic blindness ของ Wave C Phase 0** — lead พิสูจน์ว่า syntax error ไฟล์เดียว (video-player.tsx ของ c7 ระหว่างเขียน) ปิด semantic check ทั้งโปรแกรม → จับ 6 errors จริงใน C-0 ด้วย isolated tsconfig → แก้เอง ปิด C-0 (tsc 0/27 tests/eslint 0) · canonical client = createSupabaseSsrClient (ลบ client.ts) · c1 รับ D26 กลางทางแล้วแก้ rbac ตาม · C-6 ส่งงาน 12 ไฟล์ (รอ batch verify) · ค้าง: c1/c7 · protocol ใหม่: worker verify ด้วย isolated tsconfig เท่านั้น
- 2026-09-10 · **C-1 + C-6 ปิด (lead verify สด)** — C-1: isolated tsc 0 (12 ไฟล์) + 87/87 tests + O-5 exit met · lead รับ design flags ทั้ง 10 ของ c1 (สำคัญ: **learner-mutation นอก §5 matrix = LEARN_WRITE — บังคับใน spawn C-3/C-4**; HMAC cursor บน service key = dev-grade + ticket ขอ secret แยก) · C-6: fix-back 6×TS2739 → 0/0/5 + runtime consistency proof · **lead จับ semantic errors ของ C-0 เพิ่มอีก 4 จุดใน `ssr.test.ts`** (TS2379 cookie options + TS2352 ×2: `SetAllCookies` ปัจจุบันรับ `(cookies, headers)` สองพารามิเตอร์ + `options` required ต่อ item — แก้ด้วย type จริง `CookieMethodsServer` จาก package + unused `describe`) → C-0 ปิดซ้ำ 0/0/27 · **บทเรียน D26 เพิ่ม: isolated tsconfig ต้องรวม `.test.ts` ของ lane ด้วย** (vitest esbuild ไม่ type-check) · ค้างเดียว: c7 (เขียนต่อ 5 ไฟล์ + gates หลัง stream ถูก downgrade กลางทาง)
- 2026-09-10 · **Phase 0 เริ่มลง git: branch `feat/wave-c-phase0`** — lane commits ของงานปิดแล้ว: `68f74e0` C-0 · `68a802a` C-1 (+ลบ client.ts ตาม D26) · `79db096` C-6 (+vitest alias @) · `6d7c81d` C-8 · `fd9cd4b` PROJECT-STATE · secret-scan สะอาดทุกครั้งก่อน commit · **ยังไม่ merge ลง develop — C-0/C-1 เป็นโค้ด auth/security ต้องผ่าน codex gate PASS ก่อน (Git Protocol) ทำที่ Phase-0 batch พร้อม full G5** · spawn C-2/C-3/C-4 (flash) ขนานต่อจาก C-1 ที่ปิดแล้ว + c7 เดินหน้าต่อ
- 2026-09-10 · **C-7 + C-2/C-3/C-4 ปิดครบ → Wave C Phase 0 = 8/8 lanes ✅** — c7: 11 ไฟล์ 1,565 บรรทัด รอด stream-downgrade 4 ครั้ง (resume ทีละไฟล์) · c2/c3/c4: lead อ่าน route เสี่ยงสุดด้วยตา (courses list / enroll / heartbeat) + isolated tsc ราย lane = 0 · commits: `d0980ce` c7 · `b2b6204` c3 · `33aebb7` c2 · `c0e12a8` c4 (+state) · ธง Phase 1 เก็บไว้ที่ board (เฉลย quiz ใน bundle / logout / field gap / page-envelope / RPC-parser รวมศูนย์ / repeated-or C-9)
- 2026-09-10 · **Phase-0 batch G5 ครบชุด เขียวทั้งหมด (first full build ของ Wave C)** — G1 eslint exit 0 · G2 tsc --noEmit exit 0 (route types generated — จบยุค D26 suppression) · G3 vitest **275/275 (22 ไฟล์)** = 147(C-1)+34(C-2)+29(C-3)+65(C-4) · G4 `next build` **exit 0** route tree ครบ (api/v1: health/auth/categories/courses/enroll/progress/quiz/me + pages: courses/learn/login/register/my/admin) · G5 secret-scan branch diff 12,322 บรรทัด = สะอาด (9 match ทั้งหมด test-stub)
- 2026-09-10 · **codex-gate-c01 ผ่าน 4 รอบ: r1 FAIL 3B+7M → r2 1B+1M+2m → r3 1M+1m → r4 PASS (FINDINGS: none)** — lead ตรวจยืนยันทุก finding จริงก่อนแก้ทุกรอบ และลงมือแก้เองทั้ง 14 ข้อตามแบบ D18 (commits: `3d5377d` r1 · `4252ffa` r2 · `f0cd27d` r3) — สาระสำคัญที่แก้: /me/enrollments ใช้ requireMfaForRoles ตาม allowlist AUTH-007 · user_banned→ERR-AUTH-002 กัน enumeration · CSRF เทียบ origin เต็ม + Sec-Fetch-Site ยอมเฉพาะ same-origin\|none · NextResponse.next สร้างใหม่ในทุก setAll (Map-carry) · cookie path บังคับ "/" · ตัวชี้วัด ณ r4: vitest 300/300 (23 ไฟล์) · eslint 0 · tsc 0 · next build 14/14 pages · verdict: `/tmp/codex-gate-c01/verdict-r4.txt`
- 2026-09-10 · **merge --no-ff `18fd3e4` feat/wave-c-phase0 → develop (85 ไฟล์ +12,201/−73) — Wave C Phase 0 ปิดสมบูรณ์** · post-merge gates บน develop 4/4 เขียว (vitest 300/300 · eslint 0 · tsc 0 · next build 0) · บันทึก D27 (ข้อตัดสินผูกพันของ gate) · **ขั้นถัดไป: Phase 1** — C-5 admin catalog BFF · wire UI lanes เป็นจริง (C-6/C-7/C-8 → BFF · เอาเฉลย quiz ออกจาก client bundle ผ่าน fixtureSubmitQuiz · logout → POST /api/v1/auth/logout) · DCR field-gap UI↔BFF (level/credits/learnerCount/outcomes/instructors/exam) · consolidation (page-envelope helper · RPC-error parser รวม c3/c4) · C-9 integration tests บน DB จริง · C-10 Playwright (ต้องขออนุมัติ lead ก่อน)
- 2026-09-10 · **PROJECT-STATE บน develop (`3739341`) + Phase 1 kickoff (D28): DCR-4/DCR-5 APPROVE** — DD → **1.1.0** (enum course_level + courses.level/outcome_highlights + views สาธารณะ 3 ตัว + PII registry แถว instructors) · API-SPEC → **1.0.2** (ฟิลด์ DCR-4 + GET /lessons/{id}/quiz ไม่มีเฉลย) · spawn 6 workers flash: c5/c6/c7/c8/c9/cc ไฟล์ disjoint · C-10 เลื่อนท้าย Phase 1 ตามคำตัดสิน lead · **เพิ่มเติมกลางทาง (ยัง D28):** lead ตรวจ RLS จริงก่อน spawn — `cc_read` กั้น is_active กับทุกคนรวม staff → `/admin/categories` "ทุกสถานะ" เสิร์ฟไม่ได้ → DCR-4 เพิ่ม policy เสริม `cc_read_admin` (staff:viewer/content/super_admin เห็นทุกแถว) ลง DD (courses_staff_read กว้างพออยู่แล้วสำหรับ /admin/courses)
- 2026-09-10 · **Phase 1 ปิด lane ยี่สิบสี่ชั่วโมงแรก: C-5 ✅ C-6 ✅ C-11(cc) ✅ C-8 ✅** — c5: admin BFF 2 routes (keyset cursor + STAFF_WRITE + `!left` fix-back โดย lead) + CURSOR_HMAC_SECRET (111/111) · c6: catalog fixture→fetch + ฟิลด์ DCR-4 ผ่าน views (46/46) · cc: jsonPageOk + parseRpcErrorCode + GET /lessons/{id}/quiz (service path ตัดคอลัมน์เฉลยที่ select — lead อ่านทั้งไฟล์ 95/95) · c8: admin wire + layout guard (25/25) · **c8 จับ gap สำคัญ: GET /api/v1/me ไม่มี route จริง → lead ลงมือเอง (D18): requireUser ตรงตาม allowlist AUTH-007 ของ D27 + rate READ + zod ขาออง + 8 tests** · รอด 429 storm ของ glm-5.3-flash ด้วย resume+sleep (ไม่แตะ tier ตาม D9) · vitest ทั้ง repo: **31 ไฟล์ / 407 passed / 0 failed** · ค้าง: c7 (เฉลยออกจาก bundle) · c9 (migration 0012 + seed + repeated-or)
- 2026-09-10 · **C-7 Phase 1 ปิด — เฉลย quiz ออกจาก client bundle สมบูรณ์ (security-critical ผ่านการตรวจของ lead)** — c7 เขียนใหม่ทั้งไฟล์หลัง corruption กลางทาง: data layer 6 ฟังก์ชัน + server adapter (`server-only` + cookie forward + quiz fetch ฝั่ง RSC ส่งเป็น props) + 7 components + 3 หน้า + tests 29 ตัว (รวม client-bundle-guard 2 ตัว scan ห้ามพบ token เฉลยใน client path) · lead verify ด้วยตา: grep token เฉลยทั้ง repo เหลือเฉพาะ test ที่ assert การไม่มีอยู่ + อ่าน learning.server.ts/QuizPanel/schema ทั้งสาย — ข้อมูลฝั่ง client มีแค่ {id,label}/{id,prompt,choices} ผลตอบกลับมีแค่ scorePct/passed · c8 เตือนเรื่อง learning.test.ts ค้างเขียน (suite fail ชั่วคราว) → เขียนจบแล้ว · full-repo **436/436 (33 ไฟล์) exit 0** · เหลือรอ c9 เพียง lane เดียว
- 2026-09-10 · **C-9 ปิด — Phase 1 execution ครบ 6 lanes + /me (ทั้ง wave เหลือ gate) — lead verify จับ defect จริง 1 ตัว** — c9: migration 0012 (enum+columns+views 3+policy cc_read_admin) + seed 306 บรรทัด + helpers + suites 4 ไฟล์ (TC-005 guest catalog · DCR-3 enroll idempotent จริงบน RPC · repeated-or · DCR-4 views+policy) · **repeated-or ตอบคำถาม C-2: PostgREST `or=` ซ้ำ = AND** (พิสูจน์ 5 ชั้น: URL 2 ตัว · baseline · AND exclusion · and-branch · ground-truth SQL) · lead อ่าน migration/repeated-or/seed เอง + secret-scan (placeholder dev JWT → gitleaks allowlist เฉพาะ signature) · **defect: course_exam_summary ไม่กรอง `is_final`** ขัด DD §3.2 "assessments ปลายหลักสูตร" → lead แก้เอง 2 จุด + test ใหม่ + **negative control: old logic เลือก practice 99 ผิด / new view ได้ 70 ถูก** · วงจรเต็มจากศูนย์: `make reset-db` → migrate 13 ไฟล์ rc=0 → seed rc=0 → **integration 23/23 exit 0** (4 ไฟล์) · GoTrue หมายเหตุค้าง: compose ตั้ง GOTRUE_JWT_DEFAULT_GROUP_NAME=supabase → JWT role claim ไม่ตรง DB role → helper patch auth.users.role ชั่วครั่ง (fix ถาวร = compose config — ธงให้ lead ตัดสินตอน cleanup)
- 2026-09-10 · **codex gate Phase 1 fix-loop ดำเนินมาถึง r11 — วงจร: r8 0B/2M/2m (cb7ff3c) → r9 0B/1M/0m (7e306c9) → r10 0B/2M/0m (a2410a2) → r11 0B/1M/1m (กำลังแก้)** — r9: allowlist ตายจริงของ GoTrue /token (refresh_token_not_found + invalid_grant → 204 ล้าง; ที่เหลือ 503 คง credential) · r10: M1 เพิ่ม session_expired + refresh_token_already_used ตาม source v2.164.0 + **live probe จริง** (admin API สร้าง user → login → refresh A→B→C → reuse A grandparent → 400 already_used) · M2 matcher ขยายครอบทุกหน้าเว็บ (refresh ก่อน render สองทิศทาง ตามแบบทางการ Supabase SSR; CSRF ยังคุมเฉพาะ /api/v1) · r11 M1: middleware เดิม propagate การลบ cookie ที่ SDK queue ไว้ทันทีที่ refresh โดนปฏิเสธ non-retryable — รวม **401 จากชั้น key-auth ของ Kong ที่ไม่มี error_code** (ไม่ได้แตะ session ฝั่ง server เลย) → หน้าเว็บหนึ่งก็ล้าง credential ที่ยังมีชีวิต → logout ถัดมา 204 โดยไม่ revoke · แก้: **สกัด allowlist เป็น shared lib `lib/supabase/auth-errors.ts`** (logout route + middleware ตอบคำถามเดียวกันด้วยฟังก์ชันเดียว) + **middleware two-phase buffered publish** (buffer setAll ทั้งหมด → getUser → `deathConfirmed` ตาม allowlist → เผยแพร่ rotation เสมอ / การลบเฉพาะเมื่อยืนยันตายจริง) + regression **SDK จริง** 5 tests (gateway 401 no-code หน้าเว็บ+API / 429 / user_banned = ไม่ลบ + credential ยังเข้า render · already_used = ลบจริง both directions) · r11 m1 → **PB-1** (refresh-ownership race 92s-vs-90s — GoTrue parent-recovery กันพักตัวถาวร)
- 2026-09-10 · **CTO gate Phase 1 ผ่านหลัง 14 รอบ (r14 VERDICT: PASS 0B/0M/0m) + merge --no-ff `f0aaed8` feat/wave-c-phase1 → develop — Wave C Phase 1 ปิดสมบูรณ์ในเชิง auth/data** — r12 (0B/2M): M1 handler-side publish รั่ว (getUser ผ่าน client เขียนตรง → commitAuthWrites แก้) · M2 ทิ้ง cleanup ของ rotation (selectPublishableAuthCookies กลาง + middleware ใช้ร่วม) → `ee7a2ab` · r13 (0B/1M): M1 ทางที่สาม — `.from()/.rpc()` เรียก `_getSessionToken` → getSession ภายใน SDK → refresh ล้ม → _removeSession → ลบ cookie ผ่าน plain client ที่ 16 call sites (codex พิสูจน์ middleware→categories→logout จริง) → แก้ที่ factory เดียว: setAll ผ่าน policy deathConfirmed=false (conservative — client นี้ไม่เห็น error) + regression ผ่าน public handler จริงตามคำสั่ง → `930bc70` · r14: codex ยืนยันครบ — ทางเขียน auth deletion ไม่เหลือนอก policy · chain จริง logout revoke ก่อน 204 ทั้ง base/chunk · ไม่มีวงจรค้างถาวร · PB-1 backlog ตามข้อตกลง · **gates สุดท้าย (tip 930bc70 = tree ที่ merge): unit 499/499 (38 ไฟล์) · integration 31/31 · tsc 0 · lint 0 · build 17/17 · gitleaks diff 23 commits + filesystem 0 leaks** · post-merge บน develop รันซ้ำครบเหมือนเดิม + `git diff develop feat/wave-c-phase1` = ว่าง (tree เดียวกันเป๊ะ) · บันทึก D29 · **ขั้นถัดไป: Phase 1 cleanup backlog PB-1..PB-10 ตามลำดับความคุ้ม (PB-6 → PB-4 → PB-2 → ที่เหลือ) แล้วปิดท้ายด้วย C-10 Playwright**
- 2026-09-10 · **Phase 1 cleanup batch 1 ครบ 7 รายการบน feat/phase1-cleanup (tip `1e69c48`) — PB-1/PB-2/PB-3/PB-4/PB-5/PB-6/PB-7/PB-8 ทำแล้วทั้งหมด (เหลือ PB-9 doc + PB-10 E2E + PB-11 ใหม่)** — ลำดับ commit: `47b471b` PB-6 (compose GOTRUE_JWT_DEFAULT_GROUP_NAME=authenticated + ตัด role patch จาก helpers — live-probe ยืนยัน role claim ถูกตั้งแต่ signup) → `91fb844` PB-7 (me/enrollments กรอง courses.deleted_at; worker ตรวจว่า enroll RPC กรองอยู่แล้วใน 0011_functions.sql L33-37) → `50d3d60` PB-1 (**middleware เป็นเจ้าของ rotation คนเดียว**: หมุนล่วงหน้า LEAD_SECONDS=180 > SDK EXPIRY_MARGIN_MS 90 วิ (3 ticks × 30s ตาม source ที่ติดตั้ง) ปิดหน้าต่างแข่ง 91–180 วิที่ BFF call ใน loader หมุนเองแล้ว Set-Cookie โดน RSC ทิ้ง · **ตัด getUser ออกจาก middleware** — verdict death จาก error ของ getSession ตรง ๆ (SDK propagate เมื่อ token หมดจริง/preserve เมื่อยังไม่หมด) เพราะ getUser หลัง session null โยน AuthSessionMissingError → definitive ตามชื่อ → ล้าง credential ที่ยังมีชีวิต (r11 regression) · near-lead refresh พลาดกับ token ที่ยังไม่หมด → SDK preserve → ไม่มี deletion เก็บกวาดรอบถัดไป (doctrine r13) · +5 tests time-crossing, middleware.test stub ตามสัญญาใหม่) → `7aa8307` PB-4/2/5 (worker b: error.tsx ไทยครบ 5 segment ไม่ leak error.message/digest + 15 tests · esbuild jsx automatic · alias server-only ไม่ลบ vi.mock เดิม) → `1e69c48` PB-3/8 (worker c: cache() ที่ findPublishedCourse — จุดเดียวที่เรียกซ้ำใน request เดียว (generateMetadata + page) · lead ตามด้วย cache() ที่ getAdminStaffSession ((admin) layout + page = 2× GET /api/v1/me ต่อ navigation — worker flag ไว้) · logout-button behavioral 4 เคส component เดิมถูกอยู่แล้ว) · **gates ท้าย branch: unit 525/525 (44 ไฟล์) · tsc 0 · eslint 0 · build 17/17 · integration 31/31 · gitleaks FS + diff 30 commits = 0 leaks** · ขั้นถัดไป: codex gate cleanup branch (แตะ auth/data) → merge develop → PB-10/C-10 Playwright ปิด Phase 1
- 2026-09-10 · **codex gate cleanup r1 = FAIL (0B/3M/0m) → lead แก้ครบทั้ง 3 + PB-11 → 4 commits (1766113 → f0fe460) · บันทึก D31** — **M1** `cb68382` (โครงสร้าง PB-1): ขา fetch ภายในของ server วิ่งผ่าน middleware และหมุน token ตรงนั้นเอง — Set-Cookie ของ rotation ขาในไม่เคยถึง browser (RSC loader ทิ้ง) → LEAD 180 เดิมแค่ขยับหน้าต่างแข่ง 90→180 วิ · แก้: header `x-ltc-bff-internal: 1` ที่ 3 choke points (catalog.ts/learning.ts ใช้ guard `typeof window === "undefined"` เพราะแชร์ client/server · admin.ts server-only) + middleware ข้าม auth-refresh block ทั้งก้อนเมื่อเห็น header — browser = เจ้าของ rotation คนเดียวจริง · browser ปลอม header ได้แต่ benign (แค่ทำ request นั้นไม่ถูกหมุน ไม่ข้าม CSRF/authz) · +2 tests (ขาใน 150 วิ → ไม่ fetch /token + ไม่มี auth Set-Cookie · outer refresh พลาด near-lead → ไม่ลบ credential แล้วขาในต่อด้วย jar เดิม → 0 refresh) — ใช้ 400 non-retryable แทน 500 เพราะ auth-js retry 5xx ด้วย backoff 200·2^n ×10 ≈ 3 นาที (พิสูจน์จาก source ที่ติดตั้ง GoTrueClient.ts:4783) · **M2+M3** `b9cf573` (โครงสร้าง PB-7): M2 `.eq("courses.deleted_at", null)` serialize เป็น `eq.null` string ไม่ใช่ `is null` · M3 `courses!inner` embed ทำให้แถว enrollment ตกใต้ RLS published-only ของ courses → archive หลักสูตร = ประวัติผู้เรียนหายเกินเจตนา soft-delete · แก้: **migration 0017 RPC `my_active_enrollments`** (SECURITY DEFINER + language sql + STABLE — PostgREST ยอม chain select/order/limit/or · set search_path = public · revoke public/anon ก่อน grant authenticated · owner app_owner ตามแบบแผน 0011) + route เรียก rpc + chain เดิม (ตัด embed/.eq) + unit 10/10 + **integration 3 tests บน DB จริงผ่าน Kong/PostgREST** (A เห็นของตัวเอง B ไม่เห็น / archive → ประวัติยังอยู่ / soft-delete → หาย คืนค่า → กลับมา) · **PB-11** `1766113`: migration 0016 (v_enrollment_progress กรอง soft-deleted courses + align lesson_completed กับ lesson_total) + integration 2/2 ผ่าน claims path จริงของ staff:content (guard 0010 อ่าน JWT claims — superuser อ้อมไม่ได้) · **dev-stack** `f0fe460`: compose `GOTRUE_RATE_LIMIT_EMAIL_SENT: "1000"` — default 30/ชม. หมดกลางวงจร integration จน /signup 429 (`over_email_send_rate_limit`) นาน ~55 วิ/ครั้ง → beforeAll timeout (อีเมลไป Mailpit อยู่แล้ว จึงตั้งสูงได้โดยไม่กระทบผู้ใช้จริง) · **gates ท้าย round: unit 527/527 · integration 36/36 (7 ไฟล์) · tsc 0 · eslint 0 · build exit 0 (Middleware 121 kB) · gitleaks FS + diff e773ebe..HEAD = 0 leaks** · บทเรียน gitleaks: ใช้ image `:latest` เท่านั้น (v8.18.4 ไม่รองรับ `[[allowlists]]` → 18 false positives) + ห้ามเขียน report ลง repo (JSON เต็ม JWT string โดน flag รอบถัดไป) · ต่อไป: codex gate r2 (diff e773ebe..f0fe460) → PASS = merge --no-ff develop + post-merge gates → PB-10/C-10 Playwright ปิด Phase 1
- 2026-09-10 · **codex gate cleanup r2 = FAIL (0B/1M/2m) → lead ตรวจ 3 findings ยืนยันจริงทุกข้อ (อ่าน source ที่ติดตั้ง) แล้วแก้ครบบน `3741f0e` · บันทึก D32** — **M1-residual** (โครงสร้าง PB-1): middleware ข้าม rotation ขาในแล้ว (cb68382) แต่ SDK ฝั่ง handler ยังหมุนเอง — `_useSession` สั่ง refresh เมื่อ token เหลือ < EXPIRY_MARGIN_MS 90 วิ **ไม่ขึ้นกับ autoRefreshToken** (GoTrueClient.ts:3068/3113) → rotation กลางขาในสำเร็จ server-side แต่ Set-Cookie โดน RSC loader ทิ้ง = race PB-1 ยังไม่ตาย · แก้ที่ factory เดียว `ssr.ts`: `clientOptionsForLeg()` ตรวจ `x-ltc-bff-internal` ผ่าน `headers()` (**dynamic import ใน try/catch** — vi.mock เดิมของ next/headers export แค่ cookies · static binding พังทุก suite) → client ของขาในได้ `global: { fetch }` ที่ตอบ POST /auth/v1/token ด้วย 400 `{error_code:"bff_internal_leg"}` non-retryable (ไม่โดน backoff 5xx) + นอก allowlist ตายจริง → auth-js preserve session ที่ยังไม่หมดอายุ (handler ทำงานต่อ token เดิม ไม่มี rotation ไม่มีการลบ) / token ตายจริง → การลบโดน setAll policy กลืน (deathConfirmed=false ตาม doctrine r13) · regression 3 tests ผ่าน GET handler จริง + @supabase/ssr + supabase-js จริง: (1) ขาใน + token เหลือ 45 วิ → 200 ด้วย token เดิม (Authorization บน /rest/v1) + 0 call ถึง /token + jar เป๊ะ (2) positive control ไม่มี header → rotation ถึง transport + jar จริง (3) ขาใน + ตายจริง → credential อยู่ครบให้รอบถัดไปตัดสิน · **MINOR-1** (PB-11): subquery completed ของ v_enrollment_progress ไม่ผูก `m.course_id = e.course_id` (total มี) → ย้ายบทเรียนข้ามหลักสูตร (lessons_update 0010:355 อนุญาต staff:content — WITH CHECK ตรวจเฉพาะปลายทาง) ทำให้ completed>total → pct>100 · แก่นแก้: migration **0018** เติมเงื่อนไขเดียว + integration ย้ายบทเรียนจริงผ่าน claims path ของ staff:content (จัดการ **uq_lessons_sort** — unique (module_id, sort_order): ย้าย sort_order ไปช่องว่าง max+100 แล้วคืนค่าเดิมตอน restore) · **MINOR-2**: afterAll ของ suite ใหม่ 2 ไฟล์กลืน error ด้วย .catch(()=>{}) → เปลี่ยนเป็น restore-แบบตรวจ (re-select status/deleted_at + throw) + ลบผู้ใช้ทดสอบใน finally · **โบนัสจากการเขียน test จริง**: (1) keyset repeated-or ของ route (`enrolled_at.lt.TS,and(enrolled_at.eq.TS,id.lt.ID)`) ทำงานบน RPC ผ่าน PostgREST จริง — ก่อนหน้านี้พิสูจน์แค่ฝั่ง unit ที่ mock SDK (2) จับข้อจำกัด **PostgREST function-inline pruning**: คอลัมน์ที่ or=/order= อ้างต้องอยู่ใน select= ด้วย ไม่งั้น 42703 "column ... does not exist" (route จริงใส่ครบอยู่แล้ว) (3) timestamp `+00:00` ใน query string ต้อง encode (supabase-js ทำให้ route อยู่แล้ว — test เขียนเองต้อง encodeURIComponent) · **gates ท้าย round: unit 530/530 · integration 37/37 (7 ไฟล์) · tsc 0 · eslint 0 · build exit 0 (Middleware 121 kB) · gitleaks FS + diff e773ebe..HEAD = 0 leaks** · ต่อไป: codex gate r3 (diff e773ebe..3741f0e) → PASS = merge --no-ff develop + post-merge gates → PB-10/C-10 Playwright ปิด Phase 1
- 2026-09-11 · **codex gate cleanup r3 = PASS (0 BLOCKER / 0 MAJOR / 1 MINOR) → CTO APPROVE + MINOR แก้พร้อม approve (`2c4506f`) + merge --no-ff feat/phase1-cleanup → develop · บันทึก D33** — codex ยืนยันจาก source ที่ติดตั้งจริง: M1-residual ปิด (options.global.fetch ไหลถึง GoTrueClient._refreshAccessToken · 400 non-retryable · preserve path · การลบไม่ definitive โดน policy กลืน · header spoof benign — CSRF/middleware:75 + handler authz ไม่ถูกข้าม) · 0018 ปิด (completed ⊆ total ครบ · GRANT คงอยู่ตาม CREATE OR REPLACE) · verified-restore ปิด (vitest failTask จับ throw ของ afterAll) + ขอจำกัด claim ของ keyset proof ไว้ที่ filter-accept + cursor-cut · **MINOR สุดท้าย**: การคืนบทเรียน seed อยู่หลัง assertions → assertion พังกลางทาง = lesson ค้าง LTC-101 โดย afterAll ไม่เห็น → แก้: restore ใน finally (idempotent + verified re-select) + cleanup progress แม้ restore ล้ม + rethrow · **negative control: สำเนา test ที่ toBe(999) fail จริง แต่ seed intact (LTC-102=2 คืนครบ · LTC-101=3 · published/null)** · gates ท้าย fix: unit 530/530 · integration 37/37 · tsc 0 · eslint 0 · วงจร cleanup 3 รอบจบ: r1 0B/3M/0m → r2 0B/1M/2m → r3 PASS · ต่อไป: post-merge gates บน develop → PB-10/C-10 Playwright ปิด Phase 1
- 2026-09-11 · **merge --no-ff `a161d93` feat/phase1-cleanup → develop + post-merge gates เขียวครบ** — develop อยู่ merge-base e773ebe พอดี → tree หลัง merge = tree ที่ gate ผ่านเป๊ะ (git diff develop feat/phase1-cleanup = ว่าง) · unit 530/530 (45 ไฟล์) · integration 37/37 (7 ไฟล์) · tsc 0 · eslint 0 · build exit 0 (17/17 static · Middleware 121 kB) · gitleaks FS + diff e773ebe..develop = 0 leaks · **Phase 1 cleanup ปิดสมบูรณ์ (PB-1..PB-8 + PB-11; เหลือ PB-9 doc + PB-10 E2E)** · ต่อไป: PB-10/C-10 Playwright smoke (E2E-04/05/06 + logout E2E) ปิดท้าย Phase 1
- 2026-09-11 · **C-10/PB-10 แล้วเสร็จ — merge --no-ff `da7434b` feat/c10-playwright-smoke → develop · Wave C Phase 1 ปิดสมบูรณ์ (D34)** — worker-e2e (flash): 10 ไฟล์ใหม่ 898 บรรทัด (playwright.config + 4 specs + helpers 6) · lead verify อ่านทุกไฟล์ + รันเอง e2e **7/7 (7.6m exit 0)** · คุณสมบัติความมั่นคงปลอดภัย: เฉลย quiz ฝั่ง harness เท่านั้น (psql) · login ฟอร์ม /login จริง · learner mutation ผ่าน browser-session fetch (ผ่าน CSRF จริง) · service-role ไม่ถูกใช้สร้าง learner state (ลบ SERVICE_KEY dead export) · cleanup FK-ordered · sweep สะอาด (ไม่มี skip/only/TODO/secret literal) · gates เต็ม: unit 530/530 · integration 37/37 · tsc 0 · eslint 0 · build 17/17 · gitleaks FS 0 + diff 0 · post-merge develop เขียวซ้ำ + tree identical · เก็บกวาด DB: e2e users ค้าง 0 · ลบ probe-pb6* 5 ตัว · **gaps จริง → PB-12** (ไม่มีปุ่ม enroll · VideoPlayer src=null · DocumentViewer paragraphs=[]) · **สถานะ Wave C: C-0..C-11 ✅ + PB-1..PB-8/PB-10/PB-11 ✅ (เหลือ PB-9 doc + PB-12 โจทย์ Wave ถัดไป)** · ต่อไป: วางแผน Wave D (Assessment + Certificates)
- 2026-09-11 · **PB-9 แล้วเสร็จ — merge --no-ff `18faaa1` feat/pb9-cursor-hmac-provisioning → develop · Phase 1 + cleanup backlog ปิดครบทุกรายการ (D35)** — branch 4 commits (`6aa9c40` feat config+doc → `15a4134` gate r1 → `bd8c3b9` gate r2 → `64dc8af` r3 MINOR) · สาระ: superRefine prod fail-closed + instrumentation.ts boot check + /api/health 503 readiness + `server-only` ที่ config.ts + SECRETS-PROVISIONING.md 1.0.0 + .env.example (stream 2 ค่าตาม schema) · codex gate 3 รอบ: r1 0B/4M/1m → r2 0B/2M/1m → r3 **PASS** 0B/0M/1m (verdicts /tmp/codex-gate-pb9{,-r2,-r3}.txt) · tests ใหม่ 10 เคส (config 3 + instrumentation 4 + health 3) ผ่าน shuffle 42/7 · post-merge develop: unit 540/540 (47 ไฟล์) · tsc 0 · eslint 0 · build exit 0 · gitleaks FS 156 MB = 0 + diff 4 commits = 0 · develop อยู่ merge-base `64b87a6` พอดี → `git diff develop feat/pb9-cursor-hmac-provisioning` ว่างหลัง merge (tree identity) · ต่อไป: Wave D planning doc-first (Assessment + Certificates) — RPC พร้อมใน 0011 (start_attempt L411 / save_answer L677 / submit_attempt L755 / record_quiz_attempt L210) · PB-12 wire แหล่งเนื้อหาเก็บเข้า Wave D
- 2026-09-11 · **Wave D kickoff (D36)** — worker flash เขียน draft `.omc/plans/wave-d-plan.md` (122 บรรทัด · lanes D-0..D-10 · ข้อสังเกต O-1..O-9 + O-8b พร้อมอ้างอิงไฟล์/บรรทัดจริง) → lead spot-check ข้อเท็จจริงทุกข้อ (submit_attempt L881-884 synchronous/idempotent · learner_attempt_view NB baseline · rate patterns · ERR-CERT retired · RBAC perms ครบ) → ตัดสินครบทุกข้อ + **DCR-6 (API-SPEC 1.0.3)** + lead pre-work: rate-limit.ts pdf rule (tests 24/24) · npm i pdf-lib qrcode @types/qrcode (advisory 4 = ของเดิม vitest/next) · Sarabun TTF+OFL commit พร้อม · wave board D-0..D-10 ลง PROJECT-STATE · Phase 0 spawn 5 workers ขนาน (D-0..D-4 flash)
- 2026-09-11 · **Wave D Phase 0 spawned จริง — branch `feat/wave-d-phase0` @ `b76e6a3`** (develop `3a6865f` + pre-work 9 ไฟล์ +453/−12 · gates ก่อน branch: vitest 542/542 / tsc 0 / eslint 0 / build 0) · lead verify contract ครบก่อน spawn จาก 0011: start_attempt ไม่รับ session param (คืน attempt_id+session_id+expires_at+question_count · takeover บวก takeover:true) · **BFF decode session_id จาก access_token payload ใน session store เท่านั้น (GoTrue ฝัง native) ห้ามรับจาก body** · submit idempotent early-return มาก่อน session check · grace 5 นาที · save_answer triple-check · 5 workers (flash): d0 PB-12 · d1 exam BFF · d2 cert public/owner · d3 admin assessments · d4 admin certs + `src/lib/certificates/**` (จุดเดียวของ service_role) — ทุก lane ห้าม git · ไฟล์ disjoint · isolated tsconfig (D26) · ERR-CERT-001/002 retired · evidence-required
- 2026-09-11 · **D-0 ปิด — commit `09d22a2`** (8 ไฟล์ +662/−18) · lead re-verify ครบวง: isolated tsc 0 (8 ไฟล์ incl. tests — D26 doctrine) · eslint 0 · vitest 57/57 (learning.server 17 ใหม่) · อ่านไฟล์เสี่ยงสุดทั้งไฟล์ (learning.server.ts 403 บรรทัด + enroll-button.tsx + CTA 3-way + learn page) · ธงทั้ง 5 ตัดสิน: ไฟล์ใหม่ enroll-button.tsx อยู่ใน ownership (L40 sole-owner rule) · CourseViewerGate fail-open not_enrolled ตั้งใจ · getMyEnrollments 401=guest · resolveLessonMediaUrl null-on-failure → placeholder ไทย (ห้าม URL ปลอม) · **PB-14 ขยายครอบ enrolled-learner (lesson video ติด media_read เหมือน cert PDF)** · e2e-05 ยัง assert disabled จริง — D-9 เปลี่ยน UI-driven ภายหลัง · secret-grep สะอาด
- 2026-09-11 · **D-1 ปิด (lane เสี่ยงสุดของ Phase 0) — commit `a88c2d2`** (14 ไฟล์ +3,376) · lead re-verify: isolated tsc 0 · eslint 0 · vitest 114/114 (7 ไฟล์) · อ่าน exam.ts (523 บรรทัด) + 6 routes ทั้งไฟล์ · **ตัดสิน 6 ธงของ worker ด้วยหลักฐานจาก migrations จริง:** (1) attempt:start/view = doc-faithful (RBAC §2.3) (2) pass_pct 0010 hide เสียเปรียบ 0012 course_exam_summary public → lead batch grant+wire (3) **PB-16** learner_attempt_paper_view (0009 null โจทย์จน after_final_attempt — กลางสอบไม่มีแหล่ง user-JWT) (4) uniform NF-001 = privacy-safe (5) **PB-15** submit_attempt 'question_count' = sum(points) จริง (0011 L829/L884) (6) **lead edit ใน lane (D18-style)**: §4 #8 body strict-parse — `session_id` ใน body → ERR-VAL-001 400 fail ชัดแทนการเพิกเฉย + ยอมรับ body ว่าง {} + tests 5 เคสใหม่ (รวม zod v4 unrecognized_key → fallback field "body") · readJwtSessionClaim ตรง D20-B5 (store ไม่ใช่ header · RPC เทียบ claim อีกชั้น) · whitelist 4 ฟิลด์ toExamQuestion ไม่มีทางรั่วเฉลย · me/attempts keyset (started_at,id) desc + owner .eq ซ้ำ RLS · secret-grep สะอาด
- 2026-09-11 · **D-2 ปิด — commit `d1083f8`** (8 ไฟล์ +1,488) · lead re-verify: isolated tsc 0 · eslint 0 · vitest 43/43 + อ่าน 3 routes ด้วยตา (verify 200-always 4 ฟิลด์ไม่มีชื่อเจ้าของ + zod-parse กัน PII + ip_hash ไม่เก็บ IP ตรง + keyset desc) · คำตัดสิน: registrar ไม่มี certificate:view = ตรง RBAC-DESIGN §2 L83 · /me/certificates คง snake_case ตาม CertificatePublicView (§4 #13 exception เดียว) · **PB-13** ip_hash salt = anon key (dev-grade — lead แก้หลัง Phase 0) · media_read ไม่ครอบเจ้าของใบ → PB-14 lead migration
- 2026-09-11 · **D-3 ปิด — commit `2156c3c`** (8 ไฟล์ +2,278) · lead re-verify: isolated tsc 0 · eslint 0 · vitest 49/49 + อ่าน PATCH route ทั้งไฟล์ · คำตัดสิน version mechanism: bump ในแถวเดิม ตรง DD §3.4 L391 (used_at ไม่มีใน schema — สมมติฐาน spawn prompt ผิดเอง) · active+instructor → 403 pre-DB สะท้อน guard · is_correct ตัดตั้งแต่ select · ธง D-7: rules ตอบได้ไม่รวม pass_pct/selection (column grant 0010:708-714) — UI ห้ามสัญญาแสดงค่านี้จนกว่า grant ของ lead batch ลง
- 2026-09-11 · **D-4 ปิด — Wave D Phase 0 execution ครบ 5/5 lanes — commit `306c399`** (18 ไฟล์ +3,158 + fontkit) · lead re-verify: isolated tsc 0 · eslint 0 · vitest **72/72** (9 ไฟล์ — pdf.test render จริงด้วย Sarabun TTF + QR จริง) + อ่าน source ทั้ง 9 ไฟล์ · **ตัดสิน 7 ธง:** #1 fontkit → lead ติดตั้ง `@pdf-lib/fontkit@^1.1.1` · #2 bucket → PB-14 · #3 credit_snapshot null = SAFE (0006:13) · #4 **audit gap = จริง** (0008:457-461 service_role allowlist AUTH_* 12 เท่านั้น → CERT_* โดน 42501 — D-4 เป็น lane แรกที่มี mutation events ฝั่ง service_role จึงมองเห็น) → **0019 ขยาย allowlist** · #5 PDF fail คงใบ (D36-O6) · #6 compensating write = D36-O3 inherent · #7 CERT_PUBLIC_BASE_URL optional → fail-closed · **lead edits 4 จุด (D18-style): context.user_id actor attribution ครบทุก audit call** — 0008:476-486 derive v_actor จาก p_context.user_id แล้ว strip ก่อน strict-keys → ไม่ใส่ = actor null ในแถว audit · gates เขียวซ้ำหลัง edits · ยอมรับ: eligible filter-after-pagination (Wave E /bulk) · reissue ใช้ certificate:revoke (ไม่มี certificate:reissue ใน matrix — ชุด registrar/sa เดียวกัน) · secret-grep สะอาด · **ถัดไป: lead migration batch 0019 — อ่าน 0008:540-619 hash-chain tail ก่อนเขียน CREATE OR REPLACE append_audit_event**
- 2026-09-11 · **lead migration 0019 ลงจริง — commit `f9222fa` + state `df6be02`** (อ่าน hash-chain tail ของ 0008 ก่อนเขียน CREATE OR REPLACE append_audit_event ตามแผน D-4) — (a) `learner_attempt_paper_view` (b) submit_attempt คืน qc=count(*) + tp=sum(points) ทุกทาง (c) media_read recreate (cert owner + enrolled-learner) + private buckets media/certificates + storage policy objects_via_media_assets (d) grant select (pass_pct) (e) audit service_role allowlist +CERT_*/PII_ACCESS + actor lift/strip · vanilla reset-db → migrate 20 ไฟล์ rc=0 · **probes 25/25** (struct + RLS deny/allow + allowlist deny/allow + storage policy ผ่าน claims path จริง) · **integration 37/37 rc=0** (regression ชุดเดิม 7 ไฟล์) · gitleaks FS 157MB = 0 leaks
- 2026-09-11 · **BFF wire edits ครบ 6 ข้อ — commit `4fd51d3`** (13 ไฟล์ +333/−83) — exam.ts: ExamPaperJsonb strict + toExamPaperQuestion (แทน toExamQuestion) + SubmitAttemptResult บังคับ qc/tp + passPct ใน rulesView/Row · attempts route select จาก paper view + content ใน questions · submit/me-attempts/result/admin-assessments routes + schemas ตาม contract ใหม่ · certificates/shared.ts entityId string|null + doc allowlist 0019 · **eligible route bug-fix**: entityId `?? request.url` → `?? null` (เดิม fail uuid cast → RPC ปฏิเสธ → PII_ACCESS หายเงียบตอนคิวว่าง) · tests 5 ไฟล์ขยาย (paper strict matrix + qc/tp safeParse matrix + passPct asserts + banned-columns รวม question_paper) · **gates: eslint 15 ไฟล์ 0 · isolated tsc 0 · affected vitest 125 tests · full vitest 846/846 (72 ไฟล์) · typecheck 0 · build 0 · secret-grep diff 0** · บทเรียน: zod v4 array ไม่มี `.strict()` · block comment ไทยห้ามมี `*/` ในเนื้อความ
- 2026-09-11 · **PROJECT-STATE บน branch + บันทึก D37 — Wave D Phase 0 ปิดครบทุกส่วน** · PB-12/14/15/16 ✅ (เหลือ PB-13 ถัดไปหลัง merge) · **ถัดไป: codex gate feat/wave-d-phase0** (แตะ auth/security/data: audit allowlist + service_role cert writes + exam RPC contract + RLS media_read + BFF) → PASS = merge --no-ff → develop + post-merge gates → PB-13 → Phase 1 spawn D-5/D-6/D-7 (flash ตาม D9 · ไฟล์ใหม่ล้วน · D-5 รับธง reload gap D37-6 · D-7 รับ pass_pct แต่ selection ยังห้าม)
- 2026-09-11 · **codex gate Wave D Phase 0 r1 = FAIL (4B/4M) → แก้ครบ + probes จับ B9/B10/B11 ใน 0019 + บันทึก D38** — lead ตรวจยืนยันทุก finding จริงกับ source ก่อนแก้ (#35-#41): B1 media bucket กักสาขา + storage mirror inline · B2/B7 RPCs issue/revoke/reissue TX เดียว · B3/B5 verify RPC ใต้ anon/auth + route ผ่าน user client · B4 zod ทุกทางออก · B6 admin_update_question · B8 eligible filter ก่อน cut · **probes ใหม่ R1-R11 วิ่งบน DB จริงจับ 3 bugs ที่ unit-mock มองไม่เห็น**: B9 app_owner ไม่มี ACL UPDATE/policy → permission denied ทั้ง RPC ชุดใหม่ · B10 pgcrypto/extensions USAGE ไม่ถึง app_owner · B11 `o.value` scalar → missing FROM-clause · วงจร probe 5 รัน 13→18→20→24→**28/28** (full replay reset→migrate→seed→probes ทุกรัน) · gates: vitest **883/883** · integration **37/37** (re-seed หลัง probes) · lint 0 · tsc 0 · build 0 · gitleaks FS = 0 · บทเรียน: probes ทิ้ง state ใน DB → integration ต้องรันบน seed สด · subquery ใต้ set local role anon โดน RLS → NULL (ใช้ \gset ก่อนสลับ role) · **ถัดไป: commit 23 ไฟล์ → codex gate r2 (develop..HEAD) → PASS = merge --no-ff + post-merge gates → PB-13 → Phase 1 spawn**
- 2026-09-11 · **PB-13 ปิดสมบูรณ์ — codex gate r1 = PASS 0 findings → merge --no-ff → develop `b883ece` (#52) — บันทึก D50** — codex ตรวจ 6 invariants + รันอิสระ **987/987 (shuffle seed 1352)** · tsc 0 · eslint repo 0 · tree identity 0 ไบต์ · post-merge: vitest **987/987** · tsc/eslint/build 0 · gitleaks 0 (branch diff 451 บรรทัด) · **PB backlog ปิดครบทั้งหมดแล้ว (PB-1..PB-13 ✅ — ไม่มีรายการค้าง)** — ถัดไป: PROJECT-STATE + commit [#53] → **Phase 1 spawn D-5/D-6/D-7 (haiku ตาม D9)**
- 2026-09-11 · **merge Wave D Phase 0 → develop `b40c240`** (merge --no-ff ของ `874ea38` [#51] — Q1 fix รวมอยู่ในรอบ) — tree identity ยืนยัน `git diff develop feat/wave-d-phase0` = **0 ไบต์** · post-merge gates บน develop: vitest **976/976** (72 ไฟล์) · tsc 0 · eslint 0 (repo ทั้งหมด `npx eslint .`) · build 0 · gitleaks 0 บน full-branch diff 15,534 บรรทัด · แตก branch **`feat/pb13-ip-hash-salt`** จาก develop ทันทีเพื่อ PB-13
- 2026-09-11 · **PB-13 (IP_HASH_SALT) ครบโค้ด+tests+docs บน `feat/pb13-ip-hash-salt` — บันทึก D49** — config.ts mirror PB-9 ทุกจุด (envSchema · superRefine prod fail-closed · AppConfig.ipHashSalt · toConfig) · certificates/[code]: ipHashOf+userAgentHashOf ใช้ `ipHashSalt ?? supabaseAnonKey` (prod โดน boot กั้นอยู่แล้ว — ครอบ hash ทั้งสองตาม D46-O1) · tests: config 6 เคสใหม่ + **route.hash-salt.test ไฟล์ใหม่ 4 เคส** (แยกไฟล์เพราะ getConfig singleton — ตั้ง salt ก่อน import ใน worker ของตัวเอง) + instrumentation boot-reject PB-13 + PB-9 prod-pass ใส่ salt ครบ · .env.example + SECRETS-PROVISIONING **1.1.0** (§2 แถว+ผลข้างเคียงหมุน · §4 provisioning · §5 กลไก) · affected **60/60** (5 ไฟล์) · full **987/987** (73 ไฟล์) · tsc/eslint/build 0 · gitleaks round 412 บรรทัด = 0 · **ถัดไป: PROJECT-STATE + commit [#52] → codex gate → PASS = merge --no-ff + post-merge gates → Phase 1 spawn D-5/D-6/D-7**
- 2026-09-11 · **gate r11 = PASS (0B/0M/1m) → CTO APPROVE + Q1 แก้พร้อม approve — บันทึก D48 (#51)** — ทุก prior findings RESOLVED (H2 ปิดผ่าน P2 · H3 upheld รอบที่ 8) · codex รันอิสระ 976/976 + tsc 0 · Q1: 201 ตอบ questionCount stale 0 ข้างข้อที่สร้างจริง → `questionCount: createdQuestions.length` (ทับ stale ใน spread) + ขยาย assertion 2 เคส (mock embed บอก 5 → ต้องเป็น 1 · bank เปล่า → 0) · affected 15/15 · full **976/976** · tsc/eslint/build 0 · gitleaks 0 · **ถัดไป: commit → merge --no-ff → develop + post-merge gates → PB-13 → Phase 1 spawn D-5/D-6/D-7**
- 2026-09-11 · **gate r10 = FAIL (0B/2M) → แก้ครบ P1/P2 — บันทึก D47 (#50)** — lead ยืนยันทั้ง 2 ข้อกับ source ก่อนแก้ + probe PostgREST จริงด้วย fixture ชั่วคราว (ธนาคารเปล่า → `questions(count)` = `[{"count":0}]` aggregate row เดียงเสมอ · to-many embed = `[]` ไม่เคย null) แล้วลบ fixture (DELETE 1×2) — ออกแบบ exact-one/non-nullable บนฐานจริง กัน 503 storm · P1 pdf route cast ล้วน 2 จุด → CertPdfRowSchema (1 คีย์ exact · pdf_media_id nullable → 404 คงเดิม) + MediaAssetRowSchema (3 คีย์ exact · mime_type "" → application/pdf คงเดิม) ผ่าน parseInboundRow (`cert_pdf_row_drift`/`cert_pdf_media_row_drift` 503 ก่อน download/header) · P2 exact-one `questions.min(1).max(1)` + ตัด `.nullable()` ของ assessment_rules/question_options + interface/mapper ตาม — **H2 ของ r4 ปิดสมบูรณ์** (NOT-RESOLVED ของ r10 = gaps ชุดนี้) · tests +14 → **976/976** (72 ไฟล์) · โค้ดล้วนไม่แตะ migration · fresh reset+seed integration **37/37** · tsc/eslint/build 0 · gitleaks 389+15,522 บรรทัด 0 · **ถัดไป: commit → codex gate r11 → PASS = merge --no-ff + post-merge gates → PB-13 → Phase 1 spawn**
- 2026-09-11 · **gate r9 = FAIL (0B/3M) → แก้ครบ O1/O2/O3 — บันทึก D46** — lead ยืนยันทั้ง 3 ข้อกับ source ก่อนแก้ (#49): O1 UA ข้อความดิบ `left(...,256)` ลง certificate_verifications append-only = ช่องฉีด PII อิสระของ anon → คอลัมน์ user_agent_hash + CHECK hex64 + RPC gate user_agent_hash_invalid + BFF userAgentHashOf sha256(salt เดียวกับ ip_hash) — PB-13 ต้องคลุม hash ทั้งสอง · audit_logs.user_agent (0008) ไม่แตะ (BFF ส่ง null · ไม่ anon-reachable) · docs ตาม (DD+SDS) · O2 attempts `(rows ?? [])` cast → Array.isArray + AttemptPaperRowSchema 5 คีย์ .strict() ตาม select จริง (view 13 คอลัมน์) → 503 drift ไม่ใช่ TypeError 500/strip เงียบ 201 — D19-B1 leak test เขียนใหม่เป็น 503 · O3 attach `data[0]` + ตรวจ string เดียว → PdfAttachRowSchema {pdf_media_id, attached} .strict() หลัง unwrapScalarRow · drift = WARN+null คงใบ (D36-O6) · tests +17 → **962/962** (72 ไฟล์) · migration แตะ → full replay: probes r1 **48/48 effective** (R7i/R7i2 ใหม่ · R7j assertion นับผิด basis — แก้แล้ว verify แบบ SELECT บน DB เดิม · โค้ดถูกมาตลอด) · r2 **25/25** · reset+seed ใหม่ integration **37/37** · tsc/eslint/build 0 · gitleaks 15,308 บรรทัด 0 · **ถัดไป: commit → codex gate r10 → PASS = merge --no-ff + post-merge gates → PB-13 → Phase 1 spawn**
- 2026-09-11 · **gate r8 = FAIL (0B/2M/1m) → แก้ครบ N1/N2/n3 — บันทึก D45** — lead ยืนยันทั้ง 3 ข้อกับ source ก่อนแก้ (#48): N1 แถว success-but-null/bad-shape รั่วผ่าน cast+`?? []` (container null → 200 ว่าง · คีย์ขาด/เกิน → strip เงียบ) → inbound strict row schemas 4 ตัวใน exam.ts (18/11/8/13 คีย์ exact ตาม select จริง · question_snapshot required+nullable แยก missing จาก null) + parseInboundRow ครบ 7 เส้น exam routes · N2 wrapper scalar `data[0]` กลืนแถวที่สอง → `unwrapScalarRow` unwrap เฉพาะ array ยาวเป๊ะ 1 (ยาว 0/≥2 คง array ไปตายที่ schema) ใช้ครบ issue/revoke/reissue/verify · n3 btrim ใน eligible RPC ต่าง .trim() ของ JS → holder_name_trim parity ทั้งคิว+issuance+holderNameOf · tests +20 → **945/945** (72 ไฟล์ — เจอ fixture เดิม 2 คลาส: uuid non-hex ตายที่ z.string().uuid() · question-banks test เดิมยืนบน `?? []` กลืน null เป็น assert เปล่า → เติมคิวจริง) · migration แตะ → full replay: probes r1 **45/45** (+R15e/R15f · fixture -006) · r2 **25/25** · reset+seed ใหม่ integration **37/37** · tsc/eslint/build 0 · gitleaks 15,066 บรรทัด 0 · **ถัดไป: commit → codex gate r9 → PASS = merge --no-ff + post-merge gates → PB-13 → Phase 1 spawn**
- 2026-09-11 · **gate r7 = FAIL (0B/2M/1m) → แก้ครบ M1/M2/m3 — บันทึก D44** — lead ยืนยันทั้ง 3 ข้อกับ source ก่อนแก้ (#47): M1 holder-name constraint สองทาง (eligible page 503 ทั้งหน้าเพราะ learner ชื่อว่าง 1 คน · issue commit ใบ+audit แล้ว 503 ขาออก → retry โดน valid_certificate_exists) → prerequisite ใน cert_issue_core ก่อน mutation (22023 holder_name_missing/course_title_missing) + EligibleAttemptResource.holderName → z.string() (คิวยังเห็น) · M2 inbound drift หายเงียบทั้ง 4 cert RPC paths (container `?? []` กลืนเป็น 200 ว่าง · missing score_pct → fabricated null · คีย์เกิน strip) → row schemas exact-key 4 ตัว + container check + reason taxonomy ใหม่ 5 ตัว (cert_core_row_drift · cert_eligible_rows_not_array · eligible_row_drift · revoked_row_drift · reissue_row_drift) · m3 btrim ไม่ตัด tab/newline ต่าง JS .trim() → helper holder_name_trim ใช้ทั้งสอง derivation sites (issue core + eligible queue) · tests +16 → **925/925** (72 ไฟล์) · migration แตะ → full replay: probes r1 **43/43** (+R15a-d) · r2 **25/25** (แก้ cleanup ครอบใบ enrollment -02/-03 ของ r1) · หลัง probes reset+seed ใหม่ integration **37/37** · tsc/eslint/build 0 · gitleaks 793KB patch 0 · **ถัดไป: commit → codex gate r8 → PASS = merge --no-ff + post-merge gates → PB-13 → Phase 1 spawn**
- 2026-09-11 · **gate r6 = FAIL (0B/1M/0m) → แก้ครบ L1 (strict ครบทุกชั้นนอก admin-question + cert outbound) — บันทึก D43** — lead ยืนยัน L1 กับ source ก่อนแก้ (#46): exam.ts ขาออกหลายตัวยังไม่ .strict() (ExamPaperContent/AttemptQuestionView/AttemptStartView/AttemptSubmitView/MyAttemptView/AssessmentRules(Detail)View/AttemptQuestionSnapshot [jsonb จาก DB]/AttemptResultView ชุด) + admin certificates ส่ง resource โดยไม่ตรวจขาออก → `.strict()` ทุกชั้น (parent ไม่ cascade ใน zod v4 — ทุก nested ใส่เอง) + resource ใหม่ 4 ตัวใน certificate.ts + route 4 ตัวครอบ parseOutgoingView (drift → 503 ไม่ strip เงียบ) + AnswerSaveRequest ขาเข้า strict ด้วย (session_id ปลอม = 400 ไม่ใช่ strip เงียบ) · tests +11 → **909/909** (อัปเดต test เก่าผูก "strip เงียบ" 2 จุดเป็น reject 400 — AnswerSaveRequest + answers route D20-B5 ไม่ถึง RPC เลย) · ไม่แตะ migration → ไม่ต้อง replay: affected 54/54 · tsc/eslint/build 0 · **reset-db + seed ใหม่ + integration 37/37** (ตรวจ false-positive ของ strict ใหม่) · **ถัดไป: commit → codex gate r7 → PASS = merge --no-ff + post-merge gates → PB-13 → Phase 1 spawn**
- 2026-09-11 · **gate r5 = FAIL (0B/1M/1m) → แก้ครบ K1/K2 · H3 adjudication upheld — บันทึก D42** — lead ยืนยันทั้ง 2 findings กับ source ก่อนแก้: K1 (MAJOR) resource ขาออกฝั่ง question 4 ตัวไม่ .strict() (parent ไม่ครอบ nested ใน zod v4) → parseOutgoingView strip เงียบแล้ว 200 แทน 503 → `.strict()` ครบ + QuestionBankCreateResult ชัดเจนหลัง extend · K2 (MINOR) admin_update_question ไม่มี row lock + audit/return อ้าง v_version+1 (snapshot เก่าต่อ TX ที่ชนะ UPDATE อนุกรม) → `for update of q` + `returning q.version into v_version` (audit/return = เลขจริงจาก RETURNING) · r5 ยืนยัน H1/H2b/c/H4/H5/H6 RESOLVED + **H3 adjudication upheld โดย codex เอง** (0009_views.sql require submission + final allowed attempt ก่อนปล่อยเฉลย) · tests +4 → **898/898** · แก้ R8i probe เองด้วย (assertion เดิมเทียบ event ประวัติทุกแถวกับ version ปัจจุบัน = เท็จลบ; ที่ถูก = per-question max(audit version) = questions.version · K2 signature จริง = ซ้ำต่อข้อ) → probes r1 **37/37** r2 **25/25** · replay + seed สด integration **37/37** · tsc/eslint/build 0 · gitleaks worktree 0 · **ถัดไป: commit → codex gate r6 → PASS = merge --no-ff + post-merge gates → PB-13 → Phase 1 spawn**
- 2026-09-11 · **gate r4 = FAIL (0B/3M/3m) → แก้ครบ H1/H2a-c/H4/H5/H6 + H3 adjudicated NOT-A-DEFECT — บันทึก D41** — lead ยืนยันทุก finding กับ source ก่อนแก้ (#46): H1 QB_QUESTION_UPDATE audit ใน TX เดียวกับ admin_update_question (actor derive · signature คงเดิม) · H2a row-schema 3 ตัวขาเข้า + parseOutgoingView ขาออก · H2b PATCH parse-before-map + reload miss 500→503 · H2c verify exact-4-keys + safeParse → 503 ทั้งสองทิศ · H4 options ก่อน questions UPDATE (type-change IMMEDIATE เห็น options ใหม่ · deferred อ่าน type ปลายทาง) · H5 request_id v4+variant เท่านั้น · H6 holderNameOf trim ต่อ component · **H3 ไม่แก้**: result ปล่อยเฉลยหลัง after_final_attempt = สัญญา DD §3.4/D19-B1/DCR-6 — codex ยอมรับเองว่าขัด prompt ไม่ใช่โค้ด → แก้ prompt r5 invariant 3 แทน · tests **+9 → 894/894** (พบ test เก่าผูก contract เก่า 2 จุด: cert foundRow ฝัง PII จงใจ + PATCH reload-miss คาด 500 — อัปเดตตาม contract ใหม่ทั้งคู่) · probes r1 **36/36** (+R7h2/h3/R8f/R8g/R8h) r2 **25/25** · full replay + dry-run rc=0 · seed สด integration **37/37** · tsc/eslint/build 0 · gitleaks diff = 0 · **ถัดไป: commit → codex gate r5 (invariant 3 reword) → PASS = merge --no-ff + post-merge gates → PB-13 → Phase 1 spawn**
- 2026-09-11 · **gate r3 = FAIL (0B/3M/1m) → แก้ครบ G1-G4 — บันทึก D40** — lead ยืนยันทุก finding กับ source ก่อนแก้ (#45): G1 verify-RPC format gates ip_hash `^[0-9a-f]{64}$` + request_id uuid (ผิดรูป 22023 ไม่ persist ไม่ audit — กัน PII อิสระเข้า log append-only ผ่าน parameter ของ anon RPC) · G2 `select ... for update` ใน admin_attach_certificate_pdf (concurrent retry idempotent + ปิด revoke race) · G3 parseAdminAssessmentRow(created) ขาเข้าก่อนหยิบ id + reload fail → ERR-SYS-002 503 · G4 nested course `.strict()` · tests +3 (15/15 ไฟล์ · full **886/886** = HEAD 883+3 ตรวจด้วย git stash round-trip) · probes r1 **31/31** (+R7g/R7h · fixture 64-hex จริง) r2 **25/25** (+R12g for update) · replay + dry-run + seed สด integration **37/37** · tsc/eslint/build 0 · gitleaks diff 720KB = 0 (report 442 hits ทั้งหมดในไฟล์ /tmp อื่น — สแกน /tmp ทั้งก้อนเพราะ --no-git ที่ patch file) · บทเรียน: gitleaks image entrypoint คือ gitleaks เอง (ส่ง `detect` ตรง) + ห้ามแสกน /tmp wholesale · **ถัดไป: commit 5 ไฟล์ → codex gate r4 (develop..HEAD) → PASS = merge --no-ff + post-merge gates → PB-13 → Phase 1 spawn**
- 2026-09-11 · **gate r2 = FAIL (0B/6M/1m) → แก้ครบ F1-F7 + full replay จับ B12/B13/B14 ใน 0019 + probe r2 24/24 — บันทึก D39** — lead ยืนยันทุก finding กับ source ก่อนแก้ (#43-#44): F1 bucket กักสาขาบทเรียนทั้ง media_read+storage mirror · F2 RPC admin_attach_certificate_pdf (TX เดียว + idempotent) + BFF attachCertificatePdf กลาง + reissue requestId · F3 wrapper เหลือ AUDIT_READ/RATE_LIMIT_HIT/PII_ACCESS · F4 format gate ไม่ persist ไม่ audit · F5 zod row-validation ขาเข้า (*_row_drift) · F6 RPC admin_eligible_certificates (anti-join+keyset ใน SQL) · F7 tags 3 จุด · **B12 (replay)** policy app_owner_insert_media_assets ซ้ำ blanket 0010 → 0019 rollback ทั้งไฟล์ (r1 probes พังยับ 24 FAIL จนจับสาเหตุ) — เหลือ grant insert เฉพาะ · **B13 (replay)** วงเล็บ holder_name เพี้ยน → syntax error → dry-run loop (begin/rollback บน DB 0018) หาเร็ว · **B14 (runtime)** uuid→text cast ที่ CERT_PDF_ATTACH call · hardening: drop-if-exists ×3 + create or replace view · **สังเกตไว้ตัดสินรอบหน้า**: 0010 ให้เห็นบทเรียนเฉพาะ enrollment 'active' (completed เสียสิทธิ์อ่าน — media_read 'completed' = dead branch) + uq_enrollments 1 แถว/คน/หลักสูตรทุก status · probe r2 24 แถวใหม่ (R1c/R2r/R7f×4/R8f×3/R12a-f/R13a-e/R14a-b/R11r) วงจร 17→13→**24/24** + r1 ยัง **28/28** บน replay เดียวกัน (reset→migrate rc=0→seed→probes) · gates: vitest **883/883** · tsc 0 · eslint ทั้ง repo 0 · build 0 · integration **37/37** seed สด · gitleaks FS 157MB = 0 · บทเรียน: แก้ migration แล้วต้อง full replay ทุกครั้ง (unit-mock มองไม่เห็น DDL) · dry-run begin/rollback = วงจรแก้เร็วไม่ต้อง reset volume · **ถัดไป: commit r2 fixes → codex gate r3 (develop..HEAD) → PASS = merge --no-ff + post-merge gates → PB-13 → Phase 1 spawn**
- 2026-09-11 · **D-5 ปิด — commit `202197a` [#56]** (17 ไฟล์ +3,389) · lead re-verify: อ่าน source ทั้ง 11 ไฟล์ · eslint 0 (17 ไฟล์) · vitest **57/57** (6 ไฟล์) · **tsc 0 ทั้งโปรเจกต์** · client grep เฉลย = exit 1 · storage grep = เฉพาะ comment+guard-test · ธง D37-6 ก/ข/ค ครบในโค้ด (tab-memory state + warning + fail-closed reload contact-staff panel ไม่มี self-resume) · timer anchor serverTime/deadlineAt เท่านั้น · ERR-ASM-005=สำเร็จ · 004=no-retry · takeover=server-sanctioned banner · autosave 10 วิ/ข้อ (worker จับ bug lastAttemptMs เอง) · ตัดสินธง 6 ข้อ (path/checkbox→PB-18/entry→PB-17/attemptsUsed/ASM-004-005/takeover) — **Wave D Phase 1 execution ครบ 3/3 lanes**
- 2026-09-11 · **lead batch Phase 1** — AdminSidebar: ย้าย "ข้อสอบและการประเมิน"+"ประกาศนียบัตร" ออกจาก SOON → READY (+`/admin/question-banks`) + icon map 5 ตัว (แทน ternary 2 ตัว) · **PB-17..PB-20 เปิด** (exam discovery / selection marker / transport รวมศูนย์ / cert list endpoint — ทั้งหมด Wave E) · full-repo gates: vitest **1159/1159 (88 ไฟล์)** = 987+172 พอดี (D-5 57 + D-6 35 + D-7 80) · tsc 0 · eslint 0 · build 0 (**29/29 static** จาก 17) · **ถัดไป: gitleaks + codex gate ชุด Phase 1 UI (b883ece..HEAD) → PASS = Phase 2 (D-8/D-9)**
- 2026-09-11 · **codex gate ชุด Phase 1 UI (b883ece..HEAD) r1→r5 = PASS — บันทึก D51** — r1 FAIL (6 MAJOR) → lead ยืนยันทุกข้อกับ source ก่อนแก้ `6af895f` [#58] · r2 FAIL (1M/1m) → `62f6a49` [#59] · r3 FAIL (1M: terminal ERR-ASM-004/005 จาก save ถูกกลืนเป็น dirty วน retry ไม่จบ) → `6997ea7` [#60] · r4 FAIL (1M: ทางบันทึกรั่วหลัง terminal — timer ค้างยังยิง + toggle หลังเกิดเหตุยังตั้งรอ + flushAll early-return ไม่เคลียร์ timer) → `56acd84` [#61] · **r5 = PASS 0 findings** (codex รันเอง vitest 1,168/1,168 · tsc 0 · eslint 0) · สถาปัตยกรรม terminal ใหม่: `ExamFlushOutcome {ok:true / ok:false+terminalCode}` — exam-room: 004 จาก flush = แผงหมดเวลา canRetry:false (ข้อความเดียวกับ submit path) · 005 = ถือว่าสำเร็จ clearExamPaper → หน้าผล · ปิดทุกสายบันทึกหลัง terminal (guard ที่ปาก attemptSave + ยกเลิก timer/dirty ของทุกข้อทันทีที่เจอ + toggle หลัง terminal อัปเดตในหน้าได้แต่ห้ามตั้งรอบันทึก) · vitest 1159 → **1,168/1,168** (+9 regression) · gitleaks 0 ทุกรอบ
- 2026-09-11 · **GitHub backup ครบทุกสาขา (D24/D26)** — remote `origin` = github.com/narongsak-sukma/E-Leaning-LTC · โทเคนแรก (D24) ไม่มี scope `workflow` → 11 สาขาที่มี `.github/workflows/ci.yml` ถูกปฏิเสธฝั่ง GitHub · โทเคนใหม่ (D26) push ได้ครบ: docs/* 16 + feat/* 11 + develop · โทเคนอยู่ใน `.env` เท่านั้น (gitignored · ไม่เคย echo/commit) · แก้ branch upstream ที่ติด token-URL จาก `push -u` ย้อนกลับเป็น origin ครบทุกสาขา (ตรวจ grep แล้ว = 0)
- 2026-09-11 · **cron สำรองรายชั่วโมง `15279ead` (D25)** — durable · `13 * * * *` · push --all + tags ทุกชั่วโมง · working tree สกปรก → `git stash push -u` snapshot ไป `auto/wip` (force เฉพาะสาขานี้) แล้ว pop คืน (pop พัง = หยุด+รายงาน) · อ่านโทเคนจาก .env โดยไม่พิมพ์ค่า · อายุ 7 วัน — **lead ต้องสร้างใหม่ทุกสัปดาห์**
- 2026-09-11 · **Phase 2 spawn: D-8 (haiku)** — integration tests บน DB จริง: exam RPC ผ่าน claims path + session binding D20-B5 + cert path จริง (issue/revoke/reissue/verify 4 ฟิลด์) + fast assessment ใน beforeAll (time_limit=5, cooldown=0, max_attempts=2) + FK-ordered cleanup · ไฟล์ใหม่ใน tests/integration/ เท่านั้น · เป้า: เดิม 37/37 + ใหม่ · ถัดไป: D-9 E2E-07..11
- 2026-09-11 · **D-8 ปิด — commit [#63]** (31 tests ใหม่ · ชุดรวม 68/68 rc=0) — lead re-verify ครบ: อ่าน source ทั้งสองไฟล์ · รัน test:integration/tsc-isolated/eslint/gitleaks เองทั้งหมดเขียว · ตรวจ DB หลังรันด้วยมือ = สะอาด · ผลพลอยได้สำคัญ: **seedFastExams() (FAST-A/B) + วิดีโอจริงใน bucket media พร้อมให้ D-9 ใช้ทันที** · ธง 3 ข้อ (course 3 ต้อง role lawyer · storage ไม่รองรับ x-upsert · audit append-only จัดการ assert แบบ uuid-ใหม่-ต่อรอบ) · **ถัดไป: spawn D-9 (haiku) E2E-07..11 + e2e-05 UI-driven + ธง D-7 ข้อ 3 (ทางเข้า instructor สู่ admin shell)**
- 2026-09-11 · **D-9 ปิด + ตรวจรับโดย lead — commit [#64]** (7 ไฟล์ใหม่ + e2e-05 UI-driven · playwright **19/19 (9.0m) rc=0**) — lead re-verify ครบวงตาม H3: อ่านทุกไฟล์ทั้งไฟล์ (จุด security: เฉลยเดินทางผ่าน psql harness-only · error paths ไม่มี token · cleanup FK-ordered + media anti-join 6 ตาราง · audit append-only คงไว้) · รันเอง: playwright 19/19 · tsc isolated 0 · eslint 0 · gitleaks 96KB patch 0 · ตรวจ DB หลังรัน: d9 users/certs/attempts=0 · media_assets=5 (baseline) · คอนเทนเนอร์ 8/8 · ไม่มีล้ำเขตไฟล์ (git status ตรง grant เป๊ะ) · ธง 5 ข้อรับไว้: MFA UI = Wave F · **dev-app memory-threshold restart = ธงระยะยาว dev stack** · **instructor↔admin-shell ตัดสินนโยบายค้าง Wave E** (e2e-12 บันทึกพฤติกรรม redirect /login ไว้แล้ว) · ปุ่ม logout หน้า "/" · toast ออกใบ unmount เร็ว (แก้ด้วย MutationObserver แล้ว) · lead รีสตาร์ท ltc-dev-app 1 ครั้งก่อนรอบ verify (บรรเทาธง memory ตามที่ worker แนะ) · **ถัดไป: D-10 (migration 0020 auto-submit + UI copy + doc) จาก design สำเร็จใน /tmp/d10-plan.md**
- 2026-09-11 · **D-10 ปิด + ตรวจรับโดย lead — migration 0020 + UI copy + doc (D53 ตัดสิน 6 ข้อ · lead เป็นเจ้าของ migration เองตามแผน)** — **สถาปัตยกรรม**: `submit_attempt_core(p_attempt, p_late, p_auto)` = หางกราด 0019 (PB-15) ทุกไบต์ (พิสูจน์ parity ด้วย normalized diff) · `submit_attempt` เก็บ guards ทุกตัวเดิม (auth · ownership FOR UPDATE · early-return contract 0019 · session binding 2 ชั้น · status · deadline+grace) แล้วเรียก core(auto=false) · `auto_close_expired_attempts(batch 200)` SECURITY DEFINER + FOR UPDATE recheck (แข่งกับ submit มือ) · grants: core → app_owner/service_role · scheduler → app_owner/service_role/postgres · **pg_cron `* * * * *` job `ltc-auto-close-attempts`** (close เฉพาะพ้น grace 5 นาที = เส้นเดียวกับ ERR-ASM-004 · ปิด = ตรวจคะแนนจากคำตอบที่บันทึกไว้ late=true → passed/failed + credit ตามปกติ — ไม่มี state ใหม่) · audit = EXAM_SUBMIT แถวเดียว `auto=true` + `late_seconds` (รวม EXAM_TIME_LIMIT_EXCEED ของ doc เดิม · doc แก้ 2 แถว + alert rule 1 แถว) · UI copy exam-room.tsx 2 จุด ("สรุปผลการสอบจากคำตอบที่ระบบได้บันทึกไว้แล้ว" — ตรง semantics ใหม่ · ไม่มี test ผูก string เดิม) · **trial-before-apply จับ bug จริง 2 ตัว**: `%rowtype` ห้ามอยู่ใน signature พารามิเตอร์ (ใช้ได้เฉพาะ DECLARE) · pg_cron non-relocatable บน image นี้ — วัตถุอยู่ schema `cron` เสมอ ห้ามอ้างชื่อ 3-part (พิสูจน์เชิงประจักษ์ด้วย information_schema) · **replay เต็ม**: reset-db รอบแรกชน storage-api init race ที่ 0019 (`storage.buckets` ยังไม่มีคอลัมน์ `public`) → re-run one-shot ครบ 0001..0020 rc=0 · tracker ✓ · cron.job live active · seed 18 INSERT/4 profiles · **probes r1 48/48 + r2 25/25 (เดิมทั้งชุดเขียวบน DB ที่มี 0020 = parity เชิงพฤติกรรม)** + **P-probes ใหม่ 21/21**: P1 ปิด+ตรวจจริง (failed 25 ตามคำตอบ) + audit auto=true/late_seconds>300/actor ว่าง/idempotency_key · P1c ไม่ผ่านเกณฑ์ → ไม่มี credit · P2 in-grace ยัง in_progress · P3 active ไม่ถูกแตะ · P4 ส่งมือแล้ว scheduler ไม่ซ้ำ · P5 รอบสอง closed=0 · P6a/P6b authenticated เรียกตรง 42501 ทั้ง scheduler และ core · P7 auto-pass → credit.accrual แถวเดียว + rule snapshot CR-LTC-103 · P8a/P8b manual parity (return q_count=2/total_points=4/ไม่มี auto · audit actor=ผู้ส่งเอง ไม่มี auto marker — internal fn derive จาก auth.uid() ตาม 0019) · P9 cron ปิด e9 เองใน ~1 นาทีโดยไม่มี manual call + job_run_details succeeded · **gates: vitest 1168/1168 · tsc 0 · eslint 0 · build 0 · integration seed สด 68/68 (18.8s) กับ cron รันสด** · ธงรับไว้: fresh-volume reset-db↔storage-init race (mitigation re-run one-shot · tracker idempotent) · cron dev รันสดตลอดจะเก็บ attempt ค้างของ suite เก่า (ถูกต้องตามดีไซน์) · audit actor สองทางตาม parity · **ถัดไป: commit [#65] → codex gate ชุด Phase 2 (D-8+D-9+D-10) → PASS = ปิด Wave D**
- 2026-09-11 · **codex gate ชุด Phase 2 (D-8+D-9+D-10 · diff 52056bf..HEAD) — r1 FAIL → แก้ MAJOR-1 → r2 = PASS → CTO APPROVE (lead ตามอำนาจ D22) → Wave D ปิดครบทุก lane (D-0..D-10)** — r1 findings: **MAJOR-1 เดียว** (d9-helpers.ts auth/MFA error paths ใส่ response body ติด exception — enroll body มี TOTP secret · verify body มี access_token หลุดได้ในกรณี malformed-success · codex พิสูจน์ด้วยข้อมูลสังเคราะห์) + ยืนยัน parity 0020↔0019 ครบ (guards · grading · credit snapshot · return เทียบเท่า) · SQL security/scheduler/docs = ไม่มี finding · แก้ทั้ง 4 จุด (error = ขั้นตอน+status เท่านั้น) → commit `1320d2f` [#66] → **playwright รันใหม่ทั้งชุด 19/19 (9.0m) rc=0** (restart ltc-dev-app ก่อนรัน) · DB หลังรัน: d9 users=0 · attempts=0 · media=5 · grep text.slice ใน d9-helpers = 0 · r2: codex รันอิสระ **vitest 1168/1168 · tsc 0 · eslint 0 · D-9 isolated tsc 0** → **FINDINGS: none · VERDICT: PASS** · ธงจาก r1: codex sandbox ไม่มี Docker socket (ตรวจ DB สดไม่ได้ — ยืนยัน grants/cron จาก migration + lead probes แทน) · **ถัดไป: Wave E kickoff (PB-17 exam discovery · PB-18 selection marker · PB-19 transport รวมศูนย์ · PB-20 cert list endpoint · ตัดสิน instructor↔admin-shell · D39 ค้าง)**
- 2026-09-11 · **Wave E kickoff — แผน `.omc/plans/wave-e-plan.md` APPROVED + ตัดสิน D55 (10 ข้อ)** — worker flash (haiku ตาม D9) เขียน draft 11 lanes + ข้อสังเกต O-1..O-10 (ทุกข้ออ้าง file:line) → **lead spot-check ทุกข้อกับ source จริง** (อ่านรวม ~15 ไฟล์: 0012/0011/0019/0009/0010/0004 · certificates/learning/exam-api/rbac/admin.ts · (admin)/layout.tsx ทั้งไฟล์ · e2e-12 ทั้งไฟล์ · API-SPEC/SRS/RBAC-DESIGN/DD ช่วงที่เกี่ยว) — ผล verify: **ถูก 9/10 ข้อ · จับเพิ่ม 2 จุด**: (1) path ของ admin guard ผิดทั้งใน draft และ comment e2e-12 — จริง = `src/app/(admin)/layout.tsx:50-59` (route group เดียว ไม่มี admin/layout.tsx) (2) ทางเลือก O-2(ข) "derive จาก assessment_rules.selection" ใช้ไม่ได้จริง (selection = bank_ids เท่านั้น ไม่มี per-question type) → ตัดสิน D55-1 เป็น snapshot+default-multiple_choice แทน · ปรับแผน: E-7b ยุบรวม E-1/E-3 (D55-9) · E-7 scope เล็กลงเหลือ ADR+spec comment (D55-3 คง fail-closed) · 3 Phases: **Phase 0 = lead DCR ชุดเอกสาร + migration 0021..0024 ก่อน spawn** → E-0/E-2 · Phase 1 = E-1/E-3/E-5 · Phase 2 = E-4/E-6/E-7/E-9 · board Wave E ลง PROJECT-STATE แล้ว
- 2026-09-12 · **Wave E Phase 0 ปิดครบ — DCR-7 เอกสาร + migration 0021..0024 (lead-owned) → commit [#69]** — (1) spawn DCR worker (flash/haiku ตาม D9) เขียน 3 ไฟล์: API-SPECIFICATION.md v1.0.4 (PB-17 assessmentId บน GET /courses/{id} · PB-20 GET /admin/certificates endpoint ใหม่ staff:registrar+super_admin + audit PII_ACCESS · bulk row อัปเดต job/RPC batch 200/TX-per-cert คงประวัติ DCR-6) · DATA-DICTIONARY.md v1.1.1 (41 ตาราง · course_exam_summary +assessment_id คอลัมน์ท้ายสุด D55-10 · question_snapshot +type · cm_read/lessons_read D39 · §3.7 cert_bulk_jobs) · RBAC-DESIGN.md (§2.5 ADR admin shell fail-closed D55-3) — lead verify ทุกไฟล์กับ source จริง + จับ drift DD↔0023 (DD สั่ง total_attempts/pending/staff-read RLS · 0023 v1 เขียน total_candidates/no-pending/fail-closed) → reconcile สองทิศ: 0023 แก้ตาม DD canonical คอลัมน์/สถานะ/2 index · DD L725 แก้ RLS line ตาม implementation ที่เข้มกว่า (fail-closed ทุก JWT path — สอดคล้อง D55-2 role-gate-at-BFF) (2) migration lead-owned เขียนเอง 4 ตัว: 0021 (view +assessment_id — copy 0012 §5 เป๊ะ + ACL re-assert) · 0022 (start_attempt copy 0011:411-672 byte-verified โดย diff + แก้ 2 จุด: ord CTE เก็บ q.type + jsonb ใส่ 'type' · paper view default 'multiple_choice' สำหรับ snapshot เก่า + ACL) · 0023 (admin_list_certificates keyset-in-SQL อ่าน snapshots ไม่ live-join + certificates_issued_keyset_idx + cert_bulk_jobs + admin_cert_bulk_pick cap 200 WHERE เดียวกับ eligible · ทุก RPC owner app_owner + EXECUTE service_role เท่านั้น) · 0024 (cm_read ฐาน 0010 + lessons_read ฐาน **0015** เปลี่ยนเฉพาะ enrollment → in ('active','completed')) — (3) หลักฐานเต็ม: dry-run begin/rollback 4/4 exit=0 · in-place apply rc=0 · **fresh replay `down -v`→`up -d` 24/24 rc=0 สองรอบ** (รอบสองหลังแก้ 0024) · seed ok · P-probes ผ่าน (P1a view cols assessment_id ท้ายสุด/ACL · P2 fn type markers · P3 view default · P4 cert_bulk_jobs rls + 0 policies fail-closed · P5 pick parity · P6 D39 quals) — probe false 4 จุดพิสูจน์เป็น artifact ของ PG normalization (= ANY(ARRAY[...]) · uid() แทน auth.uid() · IS NULL พิมพ์ใหญ่ · pg_get_functiondef ต้อง ::regprocedure) **ไม่ใช่ defect จริง** · **integration 68/68 (18.86s)** (4) **บทเรียนสำคัญ (0024)**: policy เป็น object ต่อตาราง — การ drop+create ต้องตั้งต้นจาก **migration รุ่นล่าสุด**ที่นิยาม policy นั้น: 0024 v1 สร้าง lessons_read จากฐาน 0010 จนทับ guards 0013 (โซ่มีชีวิต)/0014 (self)/0015 (admin-restore) → full replay แรก integration จับได้ 6 fails (lesson-parent-softdelete) → เขียนใหม่จากฐาน 0015 เปลี่ยนเฉพาะ enrollment subquery → replay สอง integration กลับมา 68/68 (5) ค้นพบ: ธง D-4 เก่า (eligible filter-after-pagination) **ปิดแล้วโดย 0019-r2 F6** — BFF src/lib/certificates/issue.ts:266 เรียก RPC admin_eligible_certificates (anti-join ใน SQL) — 0023 จึงไม่แตะ eligible · สิ่งใหม่ = picker cap 200 + cert_bulk_jobs (6) ถัดไป: spawn E-0 (transport รวมศูนย์ PB-19) + E-2 (exam marker PB-18) — worker flash
- 2026-09-12 · **Wave E Phase 0 workers ปิดครบ + merge → develop `4659ef0` — PB-19/PB-18 ✅ (gate r1 FAIL → MAJOR-1 แก้จริง + MINOR-1 adjudicated D56 → r2 PASS)** — (1) spawn E-0+E-2 (flash/haiku ตาม D9 · ไฟล์ disjoint) — E-0: `src/lib/api/transport.ts` ใหม่ (163 บรรทัด · fetchJson superset GET/POST · 204 short-circuit · headers merge-last · cookie forward · x-ltc-bff-internal เฉพาะ server · credentials same-origin + cache no-store · error envelope §1.3 → ApiError · network fail ERR-SYS-001 status 0) + delegate 3 module (learning คง re-export ApiError+alias FetchCallOptions · certificates ตัด cross-lane · exam-api examRequest map → ExamApiError) + test 23 เคส · E-2: exam-api parse content.type (hand validator ไม่ดึง schema เฉลยเข้า bundle · default multiple_choice จุดเดียว) + exam-room radio/checkbox ตาม type + name ต่อข้อ + คำแนะนำ "เลือกได้ข้อเดียว" + test ใหม่ · E-2 ชัดธง schema gap: **ExamPaperJsonb ไม่มี type → paper path จะ fail ERR-SYS-002 บน DB จริง** (view 0022 coalesce เสมอ) — lead แก้เองตาม D23 (เกิน ownership worker): type enum เข้า ExamPaperContent+ExamPaperJsonb + mapper passthrough + 4 จุด fixture/assert (2) **gate r1 = FAIL**: MAJOR-1 (exam-room.tsx:123 selectSingleChoice ใช้ toggle(B)→toggle(A-ถอด) — toggle(B) ยิง autosave [A,B] ก่อน · หมดเวลาก่อนรอบถัดไป = cron 0020 ปิดตาม expires_at ตรวจจาก [A,B] ผิด · codex พิสูจน์ด้วย helper+saver จริง) + MINOR-1 (ข้อความ TransportCallOptions — **D56 NOT-A-DEFECT**) · แก้ MAJOR-1: `setSingle(questionId, choiceId)` ในเอนจิ้น — state = [choiceId] mutation เดียว → markDirtyAndSchedule ครั้งเดียว (guard ชุดเดียวกับ toggle: disposed · no-op ค่าเดิม · terminal อัปเดตในหน้าเท่านั้น) + exam-room เรียกตรง + ลบ selectSingleChoice (grep เหลือ 0) + เทสต์ใหม่ 6 เคสย้ำ **ทุก payload** ตามคำสั่ง gate (throttle-timeout ก่อนหมดเวลา · inflight · terminal · seed takeover หลายตัวยุบเหลือตัวเดียว) → `c4a2acb` (3) **gate r2 = PASS 0 MAJOR** (codex รันเอง: tsc 0 · vitest **1209/1209** · eslint 14 ไฟล์ 0 · runtime probe ไม่เขียนไฟล์ 42 assertions + ApiError identity) · minor 1 ไม่ขวาง: exam-room.test เรียก setSingle ตรง — interaction ผ่าน radio จริง → **backlog E-9** (4) merge --no-ff → **develop `4659ef0`** (tree: 14 ไฟล์ +1,031/−238) · post-merge: vitest 1209/1209 · tsc 0 · eslint repo 0 · build 0 · gitleaks 151 commits **no leaks** · push origin (URL ผิดรอบแรก — เจ้าของ repo จริง `narongsak-sukma` แก้แล้ว) · **ธง dev-stack**: คอนเทนเนอร์ dev 8/8 Up อยู่ระหว่างทาง (5) ถัดไป: **spawn Phase 1 — E-1 (assessmentId สาย BFF→UI) · E-3 (GET /admin/certificates) · E-5 (reports 5 เส้น อ่าน views ผ่าน user-JWT client)** ตาม D55 · prompts พร้อม /tmp/wave-e-e{1,3,5}-prompt.md
- 2026-09-12 · **Wave E Phase 1 ปิดครบ 3/3 lanes — gate r1→r2→r3 (PASS) → merge --no-ff develop `dd4d805` → push — PB-17/20 ปิด (รวม PB-18/19 จาก Phase 0 = backlog Wave E ครบทั้ง 4)** — (1) **E-1** `3085309` [#72]: GET /courses/{id} เผย `assessmentId` ข้อสอบปลายหลักสูตร (view 0021) + การ์ดสอบลิงก์ "เข้าสอบ" หน้าหลักสูตร (enrolled) · (2) **E-3** `c2fa545` [#73]: BFF GET /api/v1/admin/certificates — role-gate staff:registrar+super_admin (D55-2) · ค้นหา status/holder/course + keyset (after_issued_at+after_id คู่) อ่านผ่าน RPC admin_list_certificates (0023 snapshots ไม่ live-join) · ตารางทะเบียนใบ + ปุ่มเพิกถอน/ออกใบแทนต่อแถว (PB-20) · (3) **E-5** `675e16c` [#74]: /admin/reports/{enrollments,assessments,credits} + /admin/exams/{monitoring,statistics} — อ่าน views 0009/0016 ผ่าน user-JWT SSR client (RLS staff 4 บทบาท) · aggregate ทั้งหมดเป็น SQL ฝั่ง DB (id.count()/score_pct.avg()) · export CSV/JSON + report_exports audit ผ่าน 0025 (D55-6 scope ตาม DCR-6) · (4) **lead 0025** `2a7167d`: append_audit_event เปิด ADMIN_EXPORT ใน service_role allowlist + v_keys arm report_type/row_count/filters (actor จาก context.user_id) · (5) **gate r1 = FAIL (2 MAJOR + 5 MINOR)**: MAJOR-1 compose ไม่เปิด aggregates (live-proof 400 PGRST123 → 200 หลังเปิด) · MAJOR-2 PGRST_API_MAX_ROWS ตัดเงียบ → `e8c9ada`: docker-compose PGRST_DB_AGGREGATES_ENABLED + views.ts readCappedRows batch loop (หน้า 1,000 · sentinel limit+1 · order deterministic ครบ grain) · MINOR-3..5: container Array.isArray + strict ทุกแถวก่อน slice · INSERT uuid + OUTBOUND_SCHEMAS ตรวจขาออก 3 type · CSV FORMULA_PREFIXES · MINOR-6..7 `9855fe1`: refreshOnClose useRef (refresh ตอนปิดโมดัล ไม่ปลด button กลางอากาศ) · XOR half-pair after_issued_at/after_id → 400 · (6) **gate r2 = FAIL (1 MAJOR + 1 MINOR)**: MAJOR = monitoring.ts aggregate อ่านครั้งเดียว กลุ่ม >1,000 โดน cap เงียบ → `61587ac`: readAllAggRows หน้า 1,000 จนหมดจริง + lookup `.in()` chunk 200 (URL ~8KB) + container drift 3 จุด · MINOR = ปิดโมดัลกลาง submitting ได้ → `ca438eb`: modalCloseBlocked + handleClose early-return + reducer CLOSE guard ซ้ำชั้น · (7) **gate r3 = PASS (0 MAJOR · 1 MINOR test-coverage)** — codex รันเอง tsc 0 · vitest 1378/1378 · eslint 0 · ตรวจ A-E ครบ (codex ยืนยัน PostgREST ทำ GROUP BY ก่อน LIMIT/OFFSET — offset pagination ของเราถูก) → ปิด MINOR `27bfac6` (mock .in() กรองจริง · assert 6 ชุดต่อเนื่อง · avg 201 id ข้าม 2 ชุด) — **D57** · (8) full gates: vitest **1379/1379** (103 ไฟล์) · tsc 0 · eslint 0 · build 0 · gitleaks no-git 33 findings ทั้งหมดใน .env/.omc (gitignored) = **0 committable** · (9) merge --no-ff → **develop `dd4d805`** (tree: 42 ไฟล์ +5,691/−389) · post-merge: vitest 1379/1379 · tsc 0 · build 0 · push origin แล้ว (develop bf13c02..dd4d805 + branch feat/wave-e-phase1 ใหม่) · (10) **ถัดไป: spawn Phase 2 — E-4 (bulk issue BFF) · E-6 (CRT-008 async job flag off) · E-7 (ADR shell+e2e-12 comment) · E-9 (integration/e2e บน object ใหม่ + radio interaction test ค้างจาก Phase 0 minor)** ตาม D55 · **prod-checklist (D57)**: hosted Supabase เปิด PGRST_DB_AGGREGATES_ENABLED ตอนวาง prod

- 2026-09-12 · **Wave E Phase 2 เริ่ม — lead ส่งมอบ infrastructure ก่อน spawn: migration 0026 + docs + E-7** — (1) **0026** `ee260ed` [#75]: feature_flags (RLS fail-closed + policy app_owner · seed cert_auto_issue=false) · cert_issue_core v2 เพิ่ม p_mode manual/bulk/auto (fail-closed ERR-VAL-001|invalid_issue_mode · core body byte-verbatim จาก 0019 — difflib 3 hunks) · admin_cert_bulk_issue_run (FOR UPDATE + guard จบแล้ว + วน admin_cert_bulk_pick 200/รอบ + savepoint ต่อใบ + ความคืบหน้าทุกรอบ) · cert_auto_issue_tick (flag → advisory lock → ≤5 รอบ × 200 · actor ระบบ 00000000-0000-4000-8000-00000000a170 mode=auto) + cron ltc-cert-auto-issue */2 · (2) **บั๊กจาก probe (D58)**: GRANT เพิ่มให้ app_owner ไม่พอ — RLS กรองแถวเพราะ app_owner ไม่มี BYPASSRLS → เพิ่ม policy per-command 3 ตัว (แบบแผน 0019) ทั้งไฟล์ 0026 และ dev DB แบบ lockstep · (3) **evidence**: P1-P4 probes ผ่านครบ (รายละเอียดใน header) · integration 68/68 rc=0 (signature change ของ core ไม่ทำอะไรพัง) · fresh replay down -v → up → 26/26 rc=0 + seed 7 หลักสูตร rc=0 · (4) **docs lockstep**: DD 1.1.2 (feature_flags §3.7 + RPC + policy) · API-SPEC bulk row ปรับ + เพิ่ม GET /admin/certificates/bulk/{jobId} (E-4) · AUDIT-DESIGN CERT_ISSUE context เพิ่ม mode + actor ระบบ · (5) **E-7 lead ปิดเอง** `dcb93b3` [#77]: comment e2e-12 ชี้ path จริง src/app/(admin)/layout.tsx:50-59 + ลบวลีรอการตัดสิน (D55-3 เป็นนโยบายทางการแล้ว) — เจอบั๊ก block comment `staff-*/...` ตัด comment กลางคัน → eslint จับ → แก้ + amend ก่อน push · (6) **spawn workers flash 3 ตัว**: E-4 (bulk BFF POST/GET + lib + tests) · E-6 (config.ts getFeatureFlagEnabled fail-closed false) · E-9 (integration dcr8 bulk+cron + ตรวจ radio backlog) — ไฟล์ไม่ซ้อนกัน · ถัดไป: รวมผล worker → gate codex ชุด Phase 2 → merge

- 2026-09-12 · **Phase 2 workers ปิดครบ → gate r1 FAIL (3M+2m) → 0027 worker redesign แก้ครบ + seed อัตโนมัติ** — (1) **workers 3/3**: E-4 `1e8cc85` [#78] (BFF bulk POST/GET + lib) · E-6 `bfb2531` (getFeatureFlagEnabled fail-closed + boot-fail) · E-9 `7d7bfed` [#79] (integration ใหม่) + lead `ea38a01` (D59 sqlerrm→ตัวแปร) (2) **gate r1 (codex) = FAIL**: MAJOR-1 head-of-line blocking — picker หยิบ 200 แถวแรกซ้ำทุกรอบ + `exit when v_round_issued=0` ทิ้งแถวออกได้ที่อยู่หลังชุดล้มทั้งหมด (ขัด SRS AC ≤5 นาที) · MAJOR-2 ทั้ง job ใน TX เดียวของ RPC — savepoint ≠ TX ต่อใบ, abort = rollback ทั้ง job, ความคืบหน้าไม่ durable · MAJOR-3 เทส auto เปิด flag แบบ global กวาดแถวนอก fixture ใน dev DB ร่วม · m1 API-SPEC แถว bulk ค้างรุ่น RPC · m2 clamp ≥102 แถว/prefix/keyset-tie ไม่มีเทสจริง · **C2 import cycle `config.ts↔supabase/server.ts` — codex ยอมรับตามที่ lead เสนอ (เรียกกันเฉพาะใน function body = lazy จริง · tsc/vitest ผ่าน)** (3) **0027 worker redesign** (แกน D60): `admin_cert_bulk_issue_step` procedure **commit ต่อใบ** (ใบ + audit CERT_ISSUE mode='bulk' + counts ของ job = TX เดียวต่อใบ) + picker v2 cursor `(submitted_at, attempt_id)` **เดินผ่านแถวล้ม** + job 'running' กลางคันโดน resume อัตโนมัติรอบถัดไป (anti-join กันซ้ำ) + `cert_auto_issue_tick(p_course, ...)` scoped ตามหลักสูตร + pg_cron `ltc-cert-bulk-step` ทุกนาที + สัญญา jsonb 5 คีย์/idle/locked + เพดาน 100k → 'failed' + **PG≥15: procedure ต้อง INVOKER เปล่า** (0B000 พิสูจน์แล้ว) · 0026 บางส่วน superseded (header บอกชัด: เหลือ canonical = a170 + cert_issue_core v2 + flag + policy D58) (4) **BFF สอดคล้อง**: createBulkJob = insert-only ตอบ 202 (worker หยิบ ≤1 นาที · ตามผลด้วย GET) + route.test/bulk.test เขียนใหม่ (22+12 เคส) (5) **tests**: dcr8 สร้างใหม่ทั้งไฟล์ 11 เคส — พัก cron จริงทั้งสองตัวใน beforeAll + job ไล่ created_at ให้ `step(null)` deterministic + M1 bulk (200 ชื่อว่างหน้าคิว + คนมีชื่อท้ายคิวได้ใบจริง 1/200/201) + M1 auto (3+1) + M2 stop/resume (เพดาน 2 → running + counts/ใบค้างจริงมองจาก session อื่น → resume completed 3 ไม่ซ้ำ) + M3 (scoped tick ไม่แตะนอก fixture) · dcr7 +2 ปิด m2 (clamp 102 ใบ → 101 · prefix · keyset tie issued_at เดียวกัน) — **11/11 · 7/7** (6) **docs lockstep**: DD 1.1.3 · API-SPEC 1.0.5 (POST insert-only + GET สัญญาเต็ม) · AUDIT-LOG-DESIGN (request_id null สำหรับ mode bulk/auto — เกิดใน worker ไม่มี request) · 0026 header superseded-note (7) **gates**: vitest **1434/1434** (106 ไฟล์) rc=0 · tsc 0 · eslint 0 · build 0 · gitleaks (docker zricethezav/gitleaks v8.18.4 --redact) 55 findings ทั้งหมด benign (.env gitignored ✓ · key ทดสอบใน tests/docker-compose tracked โดยเจตนา · .next/.omc artifacts) = **0 committable** (8) **fresh replay**: down -v → up → **27/27 fresh** (ยืนยัน applied_at จริง 0027 @ 04:41 -local) · cron ×3 ถูก · flag=false · a170 · **พบ seed หายเพราะเป็น manual step ตาม header — แก้ราก: ผูก seed เข้า db-migrate ขั้น 4** (mount /seed.sql + psql แบบไม่ -1 เพราะไฟล์ begin/commit เอง · idempotent INSERT 0 0) + dcr8 post-replay **11/11** · ถัดไป: commit [#80] → push → **gate r2** (ไม่มี PASS = ไม่ merge)
- 2026-09-12 · **gate r2 = FAIL (3M+3m) → 0028 cursor durable แก้ครบ + dcr8 15/15** — (1) **พบ (ตรวจยืนยันทุกข้อกับ 0027 จริงก่อนแก้)**: MAJOR-1 cursor ในหน่วยความจำ procedure — ข้าม CALL หาย → เดินแถวล้มซ้ำจากหัวคิว (M1 กลับมาเมื่อข้าม CALL) · MAJOR-2 เพดาน 100k อยู่หลัง branch ชน v_max (p_max_certs=1 ไม่ตรวจเลย · job สะสมเต็มแล้วยังเดินแถวเพิ่ม — คนได้ใบจาก job ที่ตาย) · MAJOR-3 step(null) หยิบ job นอก fixture บน DB ร่วม + cleanup ล้มไม่คืน cron · MINOR-1 หลักฐาน M3 เฉพาะคนชื่อว่าง (ออกไม่ได้อยู่แล้ว — เพิ่มคนออกได้จริง) · MINOR-2 seed activate กรอง bank_id → รันซ้ำปลุกข้อของ developer ได้ · MINOR-3 builtin ไม่ qualify pg_catalog (2) **0028**: cert_bulk_jobs + cursor_submitted_at/cursor_attempt_id · ตาราง cert_auto_cursor (owner supabase_admin · RLS fail-closed · revoke รวม service_role · policy app_owner สามคำสั่งตาม D58) · step/tick แทนที่ทั้งตัว — clamp ด้วย CASE ล้วน (เลี่ยง ambiguous least/greatest) · pre-loop cap check · per-row cap check ก่อน branch · lock ต่อ scope · pg_catalog ครบ (ยกเว้น coalesce/nullif = special syntax ของ parser) · INVOKER เปล่าตาม D60 — ตรวจหลัง apply: proconfig=null · prosecdef=false (3) **dcr8 เขียนใหม่ 15 เคส**: cross-CALL bulk (job8 ชนเพดาน 120 ×2 → completed 0/200/200 — แบบเดิม running 0/240 จับได้) · cross-tick auto (เพดาน 1 ×2 → รอบสาม failed=0 — แบบเดิม = 2) · reset proof สองฝั่ง (แถวเก่าสุด seed ตอนหลัง กลับมาถูกเห็นหลัง sweep จบ) · เพดาน 100k สองเคส (สะสม 99,999 + เดิน 1 = ปิดพอดี · สะสม 100,000 อยู่แล้ว = ไม่เดินแถวใด) · assertNoForeignJobs ก่อน step(null) ทุกจุด · afterAll try/finally · MINOR-1 คนนอก scope ที่ออกได้จริง (0 ใบ + a170 ไม่เคยออกใบบนหลักสูตรนั้น) (4) **docs lockstep**: DD 1.1.4 (คอลัมน์ใหม่ + ตาราง cert_auto_cursor §3.7 + worker section 0028 canonical) · API-SPEC 1.0.6 (cursor ข้าม CALL · failed@100k ทันที) · 0027 header superseded-note (canonical เหลือ picker v2/ตาราง/cron/grants) · seed.sql activate เป็น id-list 5 ข้อ (no-op จริงบน DB ที่โตแล้ว) (5) **gates**: tsc 0 · vitest **1434/1434** (106 ไฟล์) · integration **90/90** (10 ไฟล์ · dcr8 15/15) · gitleaks (docker v8.18.4 --redact): findings ทั้งหมดใน .env (gitignored ✓ D24/D26) และ .omc/state/** (gitignored · 0 tracked) = **0 committable** · ถัดไป: commit [#81] → push → **gate r3** (ไม่มี PASS = ไม่ merge)
- 2026-09-12 · **gate r3 = FAIL (2M+3m → adjudicated real) → 0029 arrival-first two-lane tick แก้ครบ + dcr8 15/15 (two-lane + iso DB)** — (1) **พบ (ตรวจยืนยันทุกข้อกับ 0028 จริงก่อนแก้ /tmp/codex-gate-e-p2-r3.txt)**: MAJOR-1 แถวใหม่กว่า sweep_top ล่องหนจน sweep เก่าจบ (นาที 8 — ขัด AC ≤5 นาที) · MAJOR-2 assertNoForeignJobs TOCTOU · MINOR-1 DD cursor rows ผิดที่ + API-SPEC lastError · MINOR-2 cursor asserts อ่อน (ผ่านแม้ไม่มีแถว) · MINOR-3 seed id-list (แก้รอบก่อน) — (2) **0029** `supabase/migrations/0029_cert_auto_arrival_first.sql` (applied dev DB + smoke): §1 cert_auto_cursor += sweep_top คู่ขอบบน · §2 admin_cert_auto_fresh_pick (sql stable security definer app_owner · asc · > p_top · null-guard · revoke รวม service_role) · §3 cert_auto_issue_tick แทนที่ (INVOKER เปล่า D60 · pg_catalog ครบ · advisory lock ต่อ scope): fresh lane ก่อน → backlog — งบ v_max เดียว · แถวแรกของ sweep ใหม่ตั้งขอบบน · capped → probe · คิวหมด → reset สองคู่ — (3) **dcr8 surgery** (`/tmp/dcr8_surgery_r4.py`): ลบ step(null) สองเคสจาก DB ร่วม + ลบ assertNoForeignJobs + เพิ่มคน 18 (ผู้มาใหม่กลาง sweep) + course7 five-tick two-lane (t3 = ตัวจับ MAJOR-1: p18 ได้ใบ person12 ยัง 0) + course5 exact-cap reset + **iso describe ใหม่** (DB แยก ltc_e9_iso_dcr8: stub auth.* → pg_dump -s clone → seed → insurance a170/flag → fixture) — (4) **docs**: DD 1.1.5 (sweep_top cols · fresh_pick · tick สองเฟส · cursor rows ย้ายเข้า cert_bulk_jobs/cert_auto_cursor · ลบออกจาก event_outbox) · API-SPEC 1.0.7 (lastError coalesce) · 0028 header SUPERSEDED ส่วน tick (`/tmp/docs_surgery_r4.py` — Edit tool พังกับ Thai combining marks ใช้ python line-surgery) — (5) **gates**: tsc 0 · vitest 1434/1434 (106 ไฟล์) · integration 90/90 (10 ไฟล์ · dcr8 15) · lint/build/gitleaks รอบ commit — บันทึก D62 — ถัดไป: commit [#82] → push → **gate r4**
- 2026-09-12 · **gate r4 = FAIL (2M+2m → adjudicated real) → 0030 walked-markers แก้ครบ + dcr8 17/17 (held-TX late-commit + 0028-cursor normalize)** — (1) **พบ (ตรวจยืนยันทุกข้อกับ 0029/0020 จริงก่อนแก้ /tmp/codex-gate-e-p2-r4.txt)**: MAJOR-1 แถว commit ช้า (submitted_at = now() ตอนเริ่ม TX ของ 0020) ตกช่อง (cursor, sweep_top) ของ 0029 · MAJOR-2 สถานะ 0028 ค้างไม่ถูก normalize ตอน upgrade · MINOR-1 dcr8 evidence ไม่ครบสองคู่ + ไม่มี toHaveLength · MINOR-2 API-SPEC lastError ขัดแย้งในแถวเดียว — (2) **0030** `supabase/migrations/0030_cert_auto_walked_markers.sql` (15/15 เดิมผ่านครบก่อนแก้เทส = semantics ถูกอนุรักษ์): §1 cert_auto_cursor += sweep_epoch · §2 ตาราง cert_auto_walked (RLS fail-closed + policy app_owner select/insert/delete · FK cascade ผ่าน scope_key) · §3 admin_cert_auto_unmarked_pick (sql stable security definer app_owner · anti-join marker · desc · drop admin_cert_auto_fresh_pick) · §4 tick แทนที่ (INVOKER เปล่า D60): normalize XOR-null → เดิน unmarked desc ทีละแถว (marker+watermark+ใบ+audit TX เดียว) → drain probe ด้วย picker → epoch+1 + reset — p_result เท่าเดิมทุกประการ — (3) **dcr8 surgery** (`/tmp/dcr8_surgery_r5.py` + r5b): evidence E7/E8/E9/E10 (cursor ครบสองคู่ + toHaveLength + top=head-unchanged หลัง tick3) + เคสใหม่ 2: หลักสูตร 9 held psql session ถือ TX ค้าง (sentinel `held-ready` บน stdout · finally rollback) → tick2 ออกใบคน commit ช้าทันที · หลักสูตร 10 seed cursor แบบ 0028 → tick เดียวออกใบผู้มาใหม่+เก่า + epoch ขยับสองครั้ง + marker ล้าง — (4) **docs**: DD 1.1.6 (walked § + unmarked_pick + tick marker-based + sweep_epoch + watermark informational) · API-SPEC 1.0.8 (lastError qualifier) · 0029 header SUPERSEDED — บันทึก D63 — ถัดไป: commit [#83] → push → **gate r5**
- 2026-09-12 · **gate r5 = FAIL (0M+3m → adjudicated ทั้งสาม REAL) → r6 แก้ครบ + dcr8 18/18** — (1) **adjudicate**: MINOR-1 จริง (on-close หลังเหตุการณ์ = ค้าง · EPIPE) · MINOR-2 จริง (cursor ค้าง tick2 ก็ผ่าน non-null) · MINOR-3 จริง (DD ภาษา 0029 + pipe ไม่ escape) — (2) **dcr8 surgery** (L-list เท่านั้น): `startHeldSession(openSql, sentinel)` — exitCode promise ตั้งแต่ spawn · waitForSentinel reject ทันทีเมื่อ early close + ถอด timer/listener · end() กลืน stdin error + race timeout 10 วินาที · เคส MAJOR-1 (r4) ใช้ helper + `held = null` หลัง commit สำเร็จ · regression ใหม่ (เคส 18): SQL ผิด → `rejects.toThrow(/closed before sentinel/)` <15 วินาที + end() คืน exit ≠ 0 · cur3 เสริม: `aid` = คน 18 เป๊ะ + `ts > topTs` — (3) **docs**: DD 1.1.7 (sweep_top_* = หัวของรอบตาม 0030 · 1.1.5 กำกับข้ออ้างที่ถูกหักล้าง) · API-SPEC 1.0.9 (escape `ERR-SYS-002\|bulk_row_limit` ครบ 3 จุด) — หลักฐาน: tsc 0 · eslint 0 · dcr8 **18/18 ×3 รอบติด** — บันทึก D64 — ถัดไป: commit [#84] → push → **gate r6**
- 2026-09-12 · **gate r6 = FAIL (0M+1m → REAL) → r7 แก้จุดเดียว + audit ตาราง** — MINOR: `/admin/exams/monitoring|statistics` (ประวัติ 1.0.3 ของแถวเวอร์ชัน API-SPEC) pipe เปล่า = แถวแตก 4 เซลล์ · แก้: escape + audit ใหม่ (นับ unescaped pipe ทุกแถวตาราง DD+API-SPEC เทียบ header — 0 mismatch ทั้งคู่) · r6 ส่วนอื่นผ่านครบ (A1-A6 · B1-B3 · C1-C2/C4 · D — codex รัน probe early-close 18ms เอง) — ถัดไป: commit [#85] → push → **gate r7**
- 2026-09-12 · **gate r7 = FAIL (0M+1m → REAL) → r8 ซ่อมตาราง PROJECT-STATE ครบทั้งไฟล์** — MINOR: แถว D65 อ้าง `monitoring\|statistics` ไม่ escape ในแถวตาราง 4 คอลัมน์ = แถวแตก 5 เซลล์ (โรคเดียวกับที่ r7 เพิ่งแก้ใน API-SPEC) · แก้: escape D65 + audit นับ unescaped pipe ทุกแถวของ PROJECT-STATE → เพิ่ม escape 16 แถวรวม D65 (อีก 15 แถว legacy: 15 แถว escape-only + แถวหัวเพิ่ม escape พร้อมอัปเดตสถานะ · ยอด diff +19/-17 รวมแถวว่าง 185 re-pair — แยกยอด diff ออกจากจำนวนแถวซ่อม) · audit สามไฟล์ (DD · API-SPEC · PROJECT-STATE) = 0 mismatch — ถัดไป: commit [#86] → push → **gate r8**
- 2026-09-12 · **gate r8 = FAIL (0M+1m → REAL) → r9 ปรับเลขหลักฐานสามจุดให้ตรง diff จริง** — MINOR: header/D66/event เขียน 'legacy 16 · escape-only 17 (+17/-17)' แต่จริงคือเพิ่ม escape 16 แถวรวม D65 (15 แถว escape-only + แถวหัวพร้อมอัปเดตสถานะ) และ +19/-17 รวมแถวว่าง 185 re-pair · ส่วนอื่นของ r8 ผ่านครบ (D65/D66 = 5 pipes · audit สามไฟล์ 0 mismatch · escape-only ยืนยัน · invariants ไม่เปลี่ยน) · แก้: นับด้วยสคริปต์จาก diff จริง → ปรับสามจุด + D67 + event นี้ — ถัดไป: commit [#87] → push → **gate r9**
- 2026-09-12 · **Wave E Phase 2 ปิด — gate r9 = PASS (0M+0m) → fresh-volume replay → merge --no-ff develop `0ab99b7` → post-merge gates ผ่านครบ → push** — (1) **gate r9**: แก้ r8 MINOR (เลขหลักฐาน) ผ่านทุก checkpoint (นับเอง 16 แถวรวม D65 · audit สามไฟล์ 0 mismatch · D65-D67 = 5 pipes · invariants คงเดิม) (2) **fresh-volume replay**: make reset-db (down -v → up) → _dev.migrations **30/30** rc=0 + seed (a170 · crons 2/2) + ตรวจ objects 0030 (cert_auto_walked+RLS · sweep_epoch · picker · tick) + dcr8 **18/18** บน volume ใหม่ (07:13) (3) **merge** `0ab99b7` (24 ไฟล์ +5372/-36) → post-merge บน develop: unit **1434/1434** · integration **93/93** · tsc 0 · eslint 0 · build 0 · gitleaks 10 placeholder (0 committable) → push develop `ddf02c9..0ab99b7` — Phase 2 lanes ปิดครบ (E-4/E-6/E-9 + docs · migration 0026-0030 บน develop)
- 2026-09-12 · **Wave E Phase 3 (CRB Credit Bank) เปิด — แผน `.omc/plans/wave-e-p3-plan.md` + ตัดสิน D68 สิบข้อ** — หลักฐานเปิด: RTM must-have CRB-001/002/003/005/006/007 ไม่มีในชั้นแอป (grep ยืนยัน: ตาราง+RLS พร้อมตั้งแต่ 0006/0010 · event_outbox 0007 ออกแบบไว้เป็น outbox ของ credit แต่ไม่เคยมีผู้เขียน · submit_attempt_core ไม่จับกฎ · issue.ts:26 ยอมรับเองว่าอ่านยอด credit ไม่ได้ · ไม่มี route/หน้า UI ของ credit เลย) · gap sweep รอบทั้งระบบ (RTM ↔ find src/app/api): CRB ทั้งโดเมน + NTF (ทั้งโดเมน) + IDENT license/PDPA + ADM dashboard/users/audit ยังขาด → roadmap: P3 CRB → P4 NTF → P5 IDENT/PDPA+ADM → Wave F · ถัดไป: DCR-9 (API-SPEC 1.1.0 · DD 1.2.0 · AUDIT events) + migration 0031 (lead-owned)
- 2026-09-12 · **Phase 3 Phase 0 ปิด — DCR-9 + migration 0031 (lead-owned · DOC-FIRST)** — docs: API-SPECIFICATION 1.1.0 (§3.7 admin/credit-rules + admin/credits · /me/transcript param format) · DATA-DICTIONARY 1.2.0 (§3.5 functions 6 ตัว + reversal semantics + credit_cycle_defaults) · AUDIT-LOG-DESIGN 1.0.1 (CREDIT_ACCRUAL จุดเขียนจริงโดย consumer + CREDIT_REVERSAL · รวม 53 events) — pipe-audit 0 mismatch · migration `supabase/migrations/0031_credit_bank.sql` (10 หมวด: credit_cycle_defaults canonical Q1 · ensure_renewal_cycle lazy anchor ใบอนุญาต→role · credit_accrual_tick consumer advisory-lock ≤5×200 commit-ต่อ-event backoff→failed · submit_attempt_core v3 snapshot +required_credits_per_cycle · admin_revoke_certificate v2 reversal ใน TX · admin_credit_adjust · my_credit_summary · my_credit_transcript · grants/policies app_owner · cron ltc-credit-accrual 1 นาที) — dry-run BEGIN/ROLLBACK exit 0 · applied in-place แล้ว (สองรอบ — รอบสองหลังแก้ bug) · **probes 21/21** `.omc/artifacts/probe-0031.sql` (BEGIN→ROLLBACK ไม่ทิ้ง residue บน dev DB): P1 lazy cycle จาก role 400 วัน (starts_on = grant+1ปี leap-safe) · P2 anchor license decided_at · P3 citizen null · P4 tick processed=1 + ledger 3.50 + audit actor ระบบ · P5 ส่งซ้ำ already_accrued · P6 backoff + attempts≥5 failed terminal · P7 reversal 1 แถว −3.50 + audit CERT_REVOKE/CREDIT_REVERSAL คู่ · P7b revoke ซ้ำ ERR-VAL-001 · P8 adjust ผ่าน JWT + ERR-CRD-002 + ERR-RBAC-001 · P9 summary 3.50−3.50−1.25 = −1.25 missing 13.25 · P10 transcript net 0.00 + cert revoked · **probe จับ bug จริงก่อน ship: my_credit_transcript outer jsonb_object_agg เหนือ UNION ALL คีย์ซ้ำเก็บค่าหลังสุดโดยไม่ sum (−3.50 ทับ 3.50) → แก้เป็น sum ก่อนแล้วค่อย object_agg + re-apply 0031 (idempotent) + 21/21** · integration fresh replay **10 ไฟล์/93 tests ผ่าน** (iso-DB รวม 0031 — v3/v2 แทนที่ไม่ถอยหลัง) · gitleaks (docker zricethezav/gitleaks:v8.24.3 · ไฟล์เปลี่ยน 4) = `[]` exit 0 (full-repo 55 findings = baseline เดิมทั้งหมด ไม่มีในไฟล์ DCR-9 — `.omc/artifacts/gitleaks-dcr9.json`) · task ref `[#88]` (0f4f252 kickoff) · ถัดไป: commit + push → **Phase 1 spawn E-10/E-11/E-12 (flash)** + lead AdminSidebar
- 2026-09-12 · **Phase 3 Phase 1 ปิด — 3 flash workers + lead follow-ups (D9/D16/D22)** — **E-10** ผิวผู้เรียน: `api/v1/me/credits` (RPC my_credit_summary) + `me/transcript` (json/CSV UTF-8 BOM + formula-escape/PDF BFF) + `lib/api/credits.ts` (zod strict สองทาง — drift → ERR-SYS-002 fail-closed) + หน้า `(learner)/my/credits` + `my/transcript` + components/credit 5 ตัว — **71/71** (9+11+24+7+4+6+5+5) · **E-11** ผิวหลังบ้าน: `admin/credit-rules` GET/POST + `[id]` PATCH lifecycle-only (ตรวจ transition ฝั่ง BFF ก่อนถึง trigger 0010) + `admin/credits/adjustments` (RPC admin_credit_adjust 0031 ทาง user-JWT — C-9) + `admin/credits/[userId]` ledger keyset + `lib/api/admin-credit.ts` + หน้า admin 2 หน้า + components/admin/credit 4 ตัว — **126/126** (26+18+21+17+28+16) · **E-12** เทส: `dcr9-credit.test.ts` 1 describe/8 it (fixture cleanup TX + cron unschedule/reschedule afterAll) + `e2e-13-credit-after-pass.spec.ts` 2/2 — **follow-up lead**: (1) **migration `0032_credit_rule_audit_allowlist.sql`** (lead-owned D36) เปิด CREDIT_RULE_CREATE/UPDATE ทาง service_role (strict v_keys 5+4 คีย์ · actor lift+strip ตาม 0008/0019/0025 · class ข ไม่เปิด · การตัดสิน lead บันทึกในหัวไฟล์: NOTICE config-data best-effort ตามแบบแผน ADMIN_EXPORT/0025 · ทางเลือก atomic RPC สงวนไว้) — **probes 5/5** (accept 2 · strict reject 22023 · CREDIT_ADJUST 42501 · lift+strip ยืนยันใน audit_logs) + applied in-place + `dcr9-audit-allowlist.test.ts` 4 เคส (E-12 — BEGIN/ROLLBACK ไม่ทิ้ง residue · แก้ scheme-id ชน d8 เป็นเนมสเปซ `f3f3f3f3-` เอง) (2) **API-SPECIFICATION 1.1.1** — แก้พาธ §3.7 สองแถวตรง implementation ตามแผน C-7 (`GET /admin/credits/{userId}` · `POST /admin/credits/adjustments` — doc 1.1.0 เขียนคลาด) + หมายเหตุ audit CREDIT_RULE_*/CREDIT_ADJUST/PII_ACCESS (3) **AUDIT-LOG-DESIGN 1.0.2** — §2.5 กำกับจุดเขียน CREDIT_RULE_* + context keys strict (4) **E-11 follow-up**: PII_ACCESS best-effort บน ledger GET (วางหลัง strict parse ขาออก — drift 503 = ไม่มี disclosure = ไม่เขียน audit) + comments "allowlist ยังไม่เปิด" แก้ตรงจริง 0032 · **AdminSidebar** (lead): +กฎหน่วยกิต (sliders) +หน่วยกิต Credit Bank (coins) −SOON · **เกท lead เต็ม**: vitest **1631/1631** (120 ไฟล์ = 1434 ฐาน + 71 E-10 + 126 E-11 เป๊ะ) · integration fresh-replay **105/105** (12 ไฟล์ บน volume ใหม่ `make reset-db` **32/32 migrations**) · typecheck 0 · eslint ไฟล์เปลี่ยน 0 · build 0 · gitleaks staged 43 ไฟล์ 9,354 แถว = `[]` exit 0 (`.omc/artifacts/gitleaks-wave-e-p1.json`) · สแกน no-fake-completion ไฟล์ใหม่สะอาด (เจอแค่ `skipIf(!DB_URL)` มาตรฐาน) · task ref `[#88]` · ถัดไป: commit + push → codex CTO gate วนจน PASS → merge --no-ff develop → post-merge gates
- 2026-09-12 · **Phase 3 gate r1 = FAIL (8 BLOCKER / 4 MINOR) — ตรวจยืนยันทุกข้อกับโค้ดจริงก่อนแก้ → แก้ครบทุกข้อ (atomic RPC pivot + B1 schema + docs 1.1.2/1.0.3)** — (1) **adjudication ใหญ่**: แบบ allowlist best-effort ของ 0032 ถูก codex **ปฏิเสธ** (BLOCKER-2) — AUDIT §1.5 บังคับ event ที่คู่ mutation เขียนใน TX เดียว · ADMIN_EXPORT เป็น read-side disclosure ใช้ตั้งแบบแผนไม่ได้ · lead ยอมรับและ implement ทางเลือก atomic RPC ที่สงวนไว้ · ตรวจยืนยัน 8 BLOCKER กับโค้ดจริงทั้งหมดก่อนแก้ (B1 strict RevokedRowSchema 3 คีย์ vs RPC v2 5 คีย์ → 503 ทุกครั้งเพิกถอน · B3 ไม่มี aal check ใน migration ใด · B5 floor(365/365.25)=0 วันครบรอบพอดี · B6 z.enum json/csv ขาด pdf · B7 fail-open audit · B4 tick ไม่เช็ค cert status · B8 fixture scoping) (2) **migration 0031 แก้ in-place** (branch ยังไม่ merge · แบบแผน Phase 0): B5 ensure_renewal_cycle = walk-back→forward บน year-lattice + invariant `p_on_date between v_start and v_end` fail-closed (probe จริง: ครบรอบ 2026-09-12 → [2026-09-12, 2027-09-11] · event เก่ามาช้า 2026-09-11 → รอบข้างเคียง [2025-09-12, 2026-09-11] — เจอและแก้ edge เพิ่มจาก probe ของ fix เอง) · B4 credit_accrual_tick ข้าม accrual เมื่อ enrollment มี cert revoked (event processed + last_error='skipped: certificate revoked' + counter revoked_skipped) · B3 admin_credit_adjust + RPC ใหม่บังคับ aal2 (ERR-AUTH-004\|mfa_required) · MINOR-4 reversal audit original_entry_ids = id แถว accrual ต้นทาง (returning original_entry_id) (3) **0032 เขียนใหม่เป็น `0032_credit_rule_atomic_rpc.sql`** (ไฟล์เก่าลบ): `admin_create_credit_rule` (13 params · validation คู่ BFF · INSERT+audit ใน TX เดียวผ่าน append_audit_event_internal) + `admin_update_credit_rule_status` (lifecycle + audit TX เดียว) + `admin_credit_rule_staff_guard` (roles+aal2 กลาง) + wrapper กลับสถานะ 0025 (ตัด CREDIT_RULE_* จาก allowlist ทั้งสอง class) + **REVOKE INSERT/UPDATE credit_rules จาก authenticated+service_role** (path เดียว = RPC) — probes 7/7 บน dev DB (wrapper ปฏิเสธ service_role+authenticated · ADMIN_EXPORT ยังผ่าน lift+strip · guard 42501 · direct INSERT 42501 · B5 สองทิศ) (4) **B1**: RevokedRowSchema → 5 คีย์ + revoke.ts map + RevokedCertificateResource additive (creditReversedRows/Total) + route/component/lib tests — 50/50 (5) **docs**: API-SPEC **1.1.2** (audit กฎเครดิต = atomic RPC · ledger PII_ACCESS fail-closed · revoke response additive · B6 pdf ตรงสัญญา) · AUDIT-LOG-DESIGN **1.0.3** (§2.5 จุดเขียน = atomic RPC class ก · 1.0.2 best-effort ถูกปฏิเสธ) (6) **dispatch**: E-11 resume (switch credit-rules POST/PATCH → RPC + เอา audit call ออก + PII retry-once→503) · E-12 resume (flip allowlist เทสเป็น 42501 + TOTP aal2 helper จริง + เคสใหม่ B3/B4/B5/M4 + per-run scoping) · E-10 spawn ใหม่ (transcript PDF pdf-lib + strict query) · MINOR-1 (closed select list โดยการสร้าง) + MINOR-3 (skipIf harness มาตรฐาน 0 skipped) = จะโต้แย้งใน gate r2 — ถัดไป: รวมผล workers → เกทเต็ม → commit [#88] → **gate r2**
- 2026-09-12 · **r1-fix wave: workers E-10/E-11 ปิด + lead ปิด 2 blocker ที่ E-12 พบจาก integration จริง (สิทธิ์ definer 0032 · endpoint MFA ของ GoTrue)** — (1) **E-11 ปิด**: credit-rules POST/PATCH → RPC `admin_create_credit_rule`/`admin_update_credit_rule_status` (unwrap [row] · mapRpcError · drift 503) + ledger PII_ACCESS retry-once→**503 fail-closed** (ERR-SYS-002\|credit_ledger_pii_audit_unavailable · ไม่มี data ใน body) — **134/134** · tsc 0 · eslint 0 (2) **E-10 ปิด**: `lib/credits/transcript-pdf.ts` (pdf-lib+fontkit Sarabun subset:false · auto page-break + repeated header + page-number footer · fontkit หาย = ERR-SYS-002 fail-closed) + /me/transcript format=pdf จริง (content-disposition attachment) + strict query schemas — **30/30** + builder 6 · tsc 0 · eslint 0 (3) **blocker ใหม่ A — สิทธิ์ definer (E-12 พบผ่าน PostgREST จริง: `42501 permission denied for table credit_rules` แม้ผ่าน guard+validation)**: 0006/0010 ไม่เคย GRANT INSERT/UPDATE `credit_rules` ให้ app_owner (เดิมเส้นเขียน = BFF service_role เขียนตรง) — SECURITY DEFINER RPC ใหม่รันในนาม app_owner จึงเขียนตารางตัวเองไม่ได้ · แก่รอบ fix: 0032 §5 เพิ่ม `GRANT INSERT, UPDATE ON public.credit_rules TO app_owner` (ตามแบบแผน 0010 §6 — grant ให้เจ้าของ definer เฉพาะที่ RPC ต้องใช้) · re-apply idempotent + วัดซ้ำ `has_table_privilege(app_owner, credit_rules, INSERT/UPDATE)` = t/t — E-12 เคส RPC accept รอ re-run · **(3b) fix รอบสองจาก lead probe e2e จริง (signup→confirm→TOTP aal2 จริง→RPC ผ่าน Kong/PostgREST)**: UPDATE ของ `admin_update_credit_rule_status` **เงียบ 0 แถว** (RLS บังคับแม้ app_owner — 0010 มี policy app_owner เฉพาะ SELECT/INSERT ไม่มี UPDATE → UPDATE match 0 ไม่ error) = คืน 200 แถว null ทั้งหมด + audit entity_id ว่าง (**สำเร็จปลอม** — create ไม่เจอเพราะมี policy INSERT อยู่แล้ว) · แก่ 2 จุดใน 0032: `create policy app_owner_update_credit_rules for update to app_owner using(true) with check(true)` (แบบแผน 0031 §9) + **fail-closed** `if r.id is null then raise ERR-SYS-002|rule_update_noop` หลัง UPDATE · **lead probe e2e หลักฐานครบ**: aal1 → 42501 ERR-AUTH-004 · aal2 create → 200 + audit CREDIT_RULE_CREATE (entity_id/request_id ถูก) · draft→active 200 · active→retired 200 · retired→active → 400 ERR-VAL-001|invalid_transition · audit 3 แถวครบ status_from/to · TOTP flow จริง (enroll→challenge→RFC 6238 verify→access_token aal=aal2) ผ่าน GoTrue `/auth/v1/factors*` บน dev stack (4) **blocker ใหม่ B — MFA 404 คลี่ด้วยซอร์ส (ไม่ใช่ config)**: GoTrue v2.164.0 mount MFA ที่ **`/auth/v1/factors*`** (`POST /factors` · `/factors/{id}/challenge` · `/factors/{id}/verify` · `DELETE /factors/{id}` — internal/api/api.go) **ไม่มี route /mfa/* เลย** (probe จริง: /mfa/enroll=404 จากตัว GoTrue · /factors=401 รอ Bearer = route มีจริง) · TOTP enroll/verify **default true** (`internal/conf TOTPFactorTypeConfiguration` — WebAuthn/Phone เท่านั้นที่ default false) ไม่ต้องตั้ง env ใด · verifyTOTPFactor สำเร็จ → `updateMFASessionAndClaims` = **access_token ใหม่ aal=aal2** (อ่านซอร์สยืนยัน) → mintAal2Token ของ E-12 ใช้ TOTP จริงได้ (enroll→challenge→RFC 6238 node:crypto→verify) · `GOTRUE_API_MFA_ENABLED` ที่เคยเพิ่มใน compose = inert → **ถอด** เหลือ comment กำกับ endpoint จริง + probe evidence (5) r2 prompt อัปเดตตาม: B2 เพิ่ม grant app_owner · B3 endpoint /factors* — ถัดไป: รอ E-12 (เคส RPC + aal2 สองทาง + B4/B5/M4 + B8) → เกทเต็ม (reset-db 32/32 · unit · integration 0 skipped · tsc/eslint/build · gitleaks) → commit [#88] → **gate r2**
- 2026-09-12 · **r1-fix wave ปิดครบ + full gate battery บน fresh volume + commit `8610691` → gate r2 เปิด** — (1) **E-12 ปิด**: `dcr9-credit.test.ts` **10/10** (เพิ่มเคส 9 revoke-before-tick: event processed + last_error breadcrumb + revoked_skipped · เคส 10 anniversary lattice สองทิศ idempotent · เคส 6 ลำดับ guard: ERR-RBAC-001 ก่อน ERR-AUTH-004 ก่อน ERR-CRD-002 · MINOR-4 assert original_entry_ids = id แถว accrual ต้นทาง) + `dcr9-audit-allowlist.test.ts` **7/7** (แก้ harness ของตัวเอง: jsonb_object_keys lateral alias `kk(k)` + sorted-set assertion กัน collation) + **`tests/integration/helpers-aal2.ts`** mintAal2Token TOTP จริง (password grant → POST /auth/v1/factors → RFC 6238 node:crypto SHA1 30s/6หลัก → challenge → verify → aal2) — mint สำเร็จ 5 ครั้ง 0 window rollover + e2e-13 header note (2) **full gate battery บน fresh volume** (`make reset-db` → RESET_EXIT=0 · containers healthy): migrations **32/32** (0032 = ฉบับ atomic) · unit **121 ไฟล์/1650 เทส** EXIT=0 · integration **12 ไฟล์/110 เทส** EXIT=0 (0 skipped) · tsc 0 · eslint 45 ไฟล์ 0 · build 0 · gitleaks staged 27 ไฟล์ (+2,468/−855) = `[]` exit 0 (`.omc/artifacts/gitleaks-p3-fix.json`) (3) **commit `8610691`** `fix(credit): resolve gate r1 B1-B8+MINORs` [#88] 27 ไฟล์ (0031 in-place B3/B4/B5/M4 · 0032 atomic + GRANT app_owner + policy UPDATE + fail-closed noop · B1 schema chain · B6 transcript-pdf ใหม่ · B7/E-11 RPC+PII fail-closed · E-12 เทส+helpers-aal2 · docs 1.1.2/1.0.3 · docker-compose MFA comment · PROJECT-STATE) → push masked 92e9d57..8610691 (4) **gate r2 prompt** เติมเลขจริงครบ (121/1650 · 12/110 · 8610691) + โต้แย้ง MINOR-1/MINOR-3 → **codex gate r2 รันแล้ว** (`.omc/artifacts/gate-p3-r2-output.md`) — ถัดไป: verdict → PASS = merge --no-ff develop → post-merge gates → close-out / FAIL = แก้ตาม findings ต่อ
- 2026-09-12 · **codex gate p3-r2 = FAIL (4 BLOCKER/1 MINOR ทั้งหมดข้อใหม่) → แก้ครบ + พิสูจน์ด้วย probe + regression ใหม่ 2 เคส** — การ adjudicate: MINOR เก่า r1 สองข้อ codex ยืนยันเจตนาเดิม (map-then-parse ไม่ใช่ strip — mapper ทิ้ง unknown keys ก่อน zod `.strict()` จึงถอนข้อ · skipIf เป็นมาตรฐาน harness 0 skipped ใน evidence) · **B1 sign**: `credit_reversed_total` จาก RPC เป็นลบจริง (0031 เขียน reversal เป็น −amount — ตรง "ผลรวมติดลบ" AUDIT §2.5) แต่ zod `.min(0)` + fixture +12.5 ทำให้ทุก revoke ที่มี credit โดน 503 — แก้ schema เป็น `[-999_999_999, 0]` + fixture ติดลบทั้ง lib/route/API-SPEC 1.1.3 · **B2 tick↔revoke race**: เพิ่ม `pg_advisory_xact_lock(hashtext('ltc:credit:enr:'||enrollment))` ใน `admin_revoke_certificate` (ก่อน UPDATE) และ `credit_accrual_tick` ราย event (ก่อนอ่านสถานะ cert) — ลำดับ lock deadlock-free (tick: global→per-enr · revoke: per-enr) · พิสูจน์ด้วย `.omc/artifacts/probe-r2-lock.sh` สองเซสชัน: revoke บล็อก→lock timeout ใบยัง valid · tick บล็อก→breadcrumb ใน `last_error` + ledger 0 → **B3 anchor drift**: `(c)` เดิมต่อ lattice จาก `v_last_end+1` (ค่า clip) ทำให้ sequence 29 ก.พ. ของ codex สร้างหน้าต่างซ้อน→ชน EXCLUDE→event ตาย retry จน failed — เขียนใหม่เป็น fixed-anchor lattice L(k)=anchor+k ปี จาก anchor ตรง ๆ (license `decided_at` ล่าสุด ไม่มี→role `granted_at` เก่าสุด) หน้าต่างต่อกัน by construction · probe `.omc/artifacts/probe-r2.sql` 6/6 (รวม contiguity + idempotent + forward) · **B4**: เข็มขัด cleanup ของ dcr9-audit ลบรหัส `CR-LTC-7xx` โดยไม่กรองเวลา อาจโดนกฎจริงของ dev DB — เพิ่ม `created_at >= SUITE_STARTED_AT` + เดินเลี่ยงรหัสที่มีอยู่ก่อนในเคส 6 (กัน assert count-0 เจอกฎคนอื่น) · **MINOR-1**: `admin_update_credit_rule_status` เพิ่ม `for update` ตอนอ่านสถานะ — PATCH สองคำขอพร้อมกันอ่านค่าหลังคนแรก commit → invalid_transition ไม่ซ้ำ audit · regression ใหม่: **เคส 11** (Feb-29 lattice ครบ 4 จังหวะ ตาม sequence ของ codex ตรง ๆ) + **เคส 12** (สองเซสชันจริงใน vitest — holder ถือ advisory lock: revoke `rejects /lock timeout/` + ใบ valid · tick breadcrumb + ledger 0 · ปล่อย lock → rewind backoff → tick accrual 3.50 · revoke → `credit_reversed_total = −3.50` = B1 end-to-end ชั้น RPC) · แบตเตอรี่หลังแก้: vitest **121 ไฟล์/1,650 เคส** ✓ · integration **12 ไฟล์/112 เคส** (110→112) ✓ · tsc 0 · eslint 0 · build 0 · gitleaks staged `no leaks` (`.omc/artifacts/gitleaks-p3-r2fix.json`) · migrations แก้ in-place + re-apply exit 0 ทั้ง 0031/0032 (branch ยังไม่ merge — แบบแผนเดิม)
