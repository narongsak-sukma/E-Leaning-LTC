-- seed.sql — LTC E-Learning
-- DD v1.0.0 ไม่ระบุ reference data ใด (notification_templates ไม่มีรายการ key ที่กำหนด)
-- และ super_admin จำลองไม่ได้ (RBAC §4.1: ต้องมี >= 2 บัญชีจริงเห็นชอบนอกระบบ)
-- จึงไม่ seed แถวใด — เพิ่มภายหลังเมื่อ DD ระบุ reference data ผ่าน DCR
select 1;
